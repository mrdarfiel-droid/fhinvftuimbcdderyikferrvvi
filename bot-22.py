# -*- coding: utf-8 -*-
"""
============================================================
        БОТ КЛАНА LINKOR  (в стиле Iris) — версия 3
============================================================
Новое в этой версии:
  • Бот работает ТОЛЬКО в одном указанном чате (см. ALLOWED_CHAT_ID),
    из остальных групп выходит сам.
  • Система LP (Linkor Points) — валюта клана, отображается в профиле.
  • Точная статистика по МОСКОВСКОМУ времени:
    сообщений всего / сегодня / за неделю (Пн 00:00 — Вс 23:59 МСК).
  • Стартовые балансы LP загружаются из списка SEED_LP по @username.

Запуск:
  1) pip install python-telegram-bot --upgrade
  2) Вставь токен в TOKEN.
  3) python bot.py
  4) Добавь бота в чат, сделай админом (банить + ограничивать).
  5) Напиши в чате /id — бот покажет ID чата.
  6) Впиши этот ID в ALLOWED_CHAT_ID и перезапусти бота.
============================================================
"""

import html
import hashlib
import os
import re
import json
import random
import sqlite3
import time
import asyncio
import httpx
from types import SimpleNamespace
from datetime import datetime, timedelta, timezone

from telegram import Update, ChatPermissions, InlineKeyboardButton, InlineKeyboardMarkup
from telegram.constants import ChatMemberStatus, ParseMode
from telegram.ext import (
    Application,
    ApplicationHandlerStop,
    CallbackQueryHandler,
    CommandHandler,
    MessageHandler,
    ContextTypes,
    ChatMemberHandler,
    filters,
)

# ============================================================
#                      НАСТРОЙКИ
# ============================================================

def _find_bot_token():
    """Находит токен бота среди переменных окружения под любым именем."""
    pattern = re.compile(r"^\d{5,}:[A-Za-z0-9_-]{20,}$")
    for name in ("TOKEN", "BOT_TOKEN", "TELEGRAM_BOT_TOKEN", "BOT_API_TOKEN",
                 "BOT_IP_TOKEN", "API_TOKEN", "TG_TOKEN"):
        v = (os.environ.get(name) or "").strip()
        if pattern.match(v):
            return v
    for v in os.environ.values():                 # запас: любая переменная, похожая на токен
        v = (v or "").strip()
        if pattern.match(v):
            return v
    return "СЮДА_ВСТАВЬ_ТОКЕН"


TOKEN = _find_bot_token()

# ID чата, в котором работает бот. 0 = пока не задан (бот работает везде,
# чтобы ты мог узнать ID командой /id). После — впиши сюда число и перезапусти.
ALLOWED_CHAT_ID = -1003256279775

# Папка для данных (база переживает редеплой только в постоянном хранилище bothost).
# Приоритет: DATA_DIR → SHARED_DIR → /app/shared (общее хранилище bothost) → /app/data → текущая папка.
DATA_DIR = os.getenv("DATA_DIR") or os.getenv("SHARED_DIR") or ""
if not DATA_DIR:
    for _p in ("/app/shared", "/app/data"):
        if os.path.isdir(_p):
            DATA_DIR = _p
            break
    else:
        DATA_DIR = "."
try:
    os.makedirs(DATA_DIR, exist_ok=True)
except Exception:
    DATA_DIR = "."
DB_FILE = os.path.join(DATA_DIR, "linkor.db")
print(f"[DB] база данных: {DB_FILE}", flush=True)

WARN_LIFETIME_DAYS = 7
MAX_WARNS = 3

LP_MAX = 10000                     # потолок LP (правила STORE)
LP_MIN = -10000                    # минимальный баланс (штрафы могут уводить в минус)
LP_WEEKLY_CAP = 150                # 5.7: недельный лимит наград за нормы (кубки/ранг/квесты/викторина — общий пул)
LP_PER_MESSAGE = 0.02              # 6.0: LP за сообщение (надёжная база активности)
MSG_DAILY_CAP = 500                # 6.0: макс. сообщений/день с LP. 600→500 (анти-спам) → потолок 10 LP/день
SELF_SPAM_LIMIT = 8                # 5.7: сообщений подряд без ответа других, ещё дающих LP. Было 10
WEEKLY_MSG_NORM = 450              # клановая норма сообщений/нед (для регламента)
SUPER_NORM_MSGS = 1250             # порог «сверх-активности» (только для отметки в нормах)
WARN_LP_PENALTY = 100              # сколько LP сжигать на стадии варна
STARTING_LP = 10                   # стартовый баланс нового участника (5.4: было 50 — снижено по решению владельца)

# Ежедневный стрик: награда не каждый день (чтобы не было инфляции), а за «вехи» серии.
# {день_серии: бонус_LP}. Экономику держим скромной — основной заработок всё равно от активности.
STREAK_MILESTONES = {3: 3, 7: 8, 14: 15, 30: 40, 60: 70, 100: 120}   # 5.7: снижено (было 5/15/30/75/120/250)

# ===== 5.7 «КАЗНА»: игровые стоки LP (подъём актива + сжигание лишних LP) =====
PASS_PER_MONTH_LIMIT = 1           # 5.7: пассов на человека в календарный месяц
LOTTERY_TICKET = 20               # цена билета лотереи (LP)
LOTTERY_MIN_PLAYERS = 3           # минимум участников, иначе возврат билетов
DUEL_MIN, DUEL_MAX = 5, 50        # пределы ставки в дуэли (LP)
DUEL_TIMEOUT_MIN = 5              # сколько минут ждать принятия дуэли
DUEL_COOLDOWN_MIN = 180          # 5.7 анти-слив: пауза между дуэлями для одного игрока (мин)
DUEL_PAIR_COOLDOWN_MIN = 720     # 5.7 анти-слив: та же ПАРА может дуэлиться не чаще раза в N мин (мин)
DUEL_DAILY_LIMIT = 5             # 5.7 анти-слив: максимум дуэлей на игрока в день
DUEL_RAKE_PCT = 10               # 5.7 анти-слив: % ставки сгорает (комиссия в «казну») — слив другу убыточен
TITLE_TEMP_COST = 100            # временный титул в профиле (LP)
TITLE_TEMP_DAYS = 7             # на сколько дней даётся временный титул
TITLE_MAX_LEN = 24             # максимум символов в титуле
TITLE_AUCTION_MIN_STEP = 10    # минимальный шаг ставки на аукционе титула недели (LP)

# ===== 5.8 «АРСЕНАЛ»: квесты, ачивки, викторина =====
QUEST_REWARD = 0               # 5.8→5.9: квесты больше НЕ дают LP — награда теперь престижные ТИТУЛЫ (см. QUEST_TITLES)
QUIZ_REWARD = 10              # 5.9: больше НЕ используется для LP — викторина даёт ТИТУЛЫ (см. QUIZ_TITLES)
# 5.9: за победы в викторине — престижные титулы (как у квестов). LP не начисляются.
QUIZ_TITLES = [
    (3, "🧠 Знаток"),
    (10, "📚 Эрудит"),
    (25, "🎓 Гений клана"),
    (50, "🏆 Чемпион викторин"),
]
# 5.9: за КОЛИЧЕСТВО выполненных квестов выдаётся крутой титул в профиль (престиж вместо инфляции LP).
QUEST_TITLES = [
    (3, "🌱 Активист"),
    (10, "⚡ Энтузиаст"),
    (25, "🔥 Мастер квестов"),
    (50, "💎 Ветеран Линкора"),
    (100, "👑 Легенда Линкора"),
]
QUIZ_TIMEOUT_SEC = 60        # сколько секунд на ответ в викторине
QUIZ_COOLDOWN_MIN = 90       # 5.9: пауза между викторинами в чате (90 мин — событие, не гринд-ферма)
QUIZ_DAILY_LIMIT = 8         # 5.9: максимум викторин в чате за день (страховка от гринда)
_quiz_day_count = {}         # "chat:day" -> сколько викторин сегодня
_quiz_last = {}              # chat_id -> время последней викторины (для КД)
# Каталог ежедневных квестов: (код, описание, цель). Прогресс считается из реальных событий дня.
DAILY_QUESTS = [
    ("msg30",  "Напиши 30 сообщений в чате", 30),
    ("msg60",  "Напиши 60 сообщений в чате", 60),
    ("msg100", "Будь активным: 100 сообщений за день", 100),
    ("rep1",   "Получи 1 респект от соклановца", 1),
    ("duel1",  "Сыграй 1 дуэль (победа не обязательна)", 1),
    ("streak", "Просто загляни и отметься (серия дня)", 1),
]
DAILY_QUESTS_BY_CODE = {q[0]: q for q in DAILY_QUESTS}
# Каталог ачивок-значков: (код, эмодзи, название, описание). Выдаются навсегда.
ACHIEVEMENTS = [
    ("week_streak", "🔥", "Завсегдатай", "7 дней подряд в чате"),
    ("month_streak", "💎", "Преданный", "30 дней подряд"),
    ("chatty",      "💬", "Болтун", "5000 сообщений всего"),
    ("veteran",     "🎖", "Ветеран", "20000 сообщений всего"),
    ("legend_rank", "👑", "Легенда", "достигнут ранг Легенда+"),
    ("respected",   "⭐", "Уважаемый", "50 репутации"),
    ("duelist",     "⚔️", "Дуэлянт", "10 побед в дуэлях"),
    ("lucky",       "🍀", "Счастливчик", "победа в лотерее"),
]
ACHIEVEMENTS_BY_CODE = {a[0]: a for a in ACHIEVEMENTS}

# Сезоны: топ активности за календарный месяц (МСК). В конце месяца топ-3 получают LP и попадают
# в зал славы, счёт сезона обнуляется (новый месяц считается заново). {место: бонус_LP}.
SEASON_REWARDS = {1: 200, 2: 120, 3: 60}
# 6.0 Сезоны 2.0: чемпионы сезона получают особый престиж-титул в профиль (не только LP).
SEASON_TITLES = {1: "🏆 Чемпион сезона", 2: "🥈 Призёр сезона", 3: "🥉 Призёр сезона"}
SEASON_CHAMPION_TITLE_DAYS = 31   # сколько дней держится титул чемпиона (до конца следующего сезона)

# ===== 6.0 «ИМПЕРИЯ»: ивент-модуль (временные множители LP, запускает владелец) =====
EVENT_TYPES = {
    "double":  ("🔥 Двойные LP", 2.0),
    "triple":  ("⚡ Тройные LP", 3.0),
    "happy":   ("🎉 Счастливый час", 2.0),
}
EVENT_MAX_HOURS = 72            # максимальная длительность ивента (часы)


def event_active():
    """Возвращает (название, множитель) активного ивента или (None, 1.0)."""
    until = meta_get("event_until")
    if not until:
        return None, 1.0
    try:
        if datetime.fromisoformat(until) <= msk_now():
            return None, 1.0
    except Exception:
        return None, 1.0
    name = meta_get("event_name") or "Ивент"
    try:
        mult = float(meta_get("event_mult") or "1")
    except Exception:
        mult = 1.0
    return name, mult


def event_multiplier():
    """Текущий множитель LP за сообщения (1.0 если ивента нет)."""
    return event_active()[1]


def event_status_text():
    name, mult = event_active()
    if not name:
        return f"{pe('note')} Сейчас активных ивентов нет."
    until = meta_get("event_until")
    try:
        left = datetime.fromisoformat(until) - msk_now()
        hrs = int(left.total_seconds() // 3600)
        mins = int((left.total_seconds() % 3600) // 60)
        left_s = f"{hrs} ч {mins} мин" if hrs else f"{mins} мин"
    except Exception:
        left_s = "—"
    return (f"{pe('mega')} <b>АКТИВЕН ИВЕНТ: {name}</b>\n{CLAN_LINE}\n"
            f"{pe('lp')} Множитель LP за сообщения: <b>x{mult:g}</b>\n"
            f"{pe('clock')} Осталось: <b>{left_s}</b>")



# Анти-рейд: если за RAID_WINDOW секунд войдёт RAID_JOIN_COUNT+ человек — включается защита
# на RAID_LOCKDOWN_MIN минут (новые входящие в это время получают мут до проверки админом).
RAID_JOIN_COUNT = 5
RAID_WINDOW = 60
RAID_LOCKDOWN_MIN = 10

# Префикс команд: в ЧАТЕ бот реагирует ТОЛЬКО на сообщения, начинающиеся с этого символа
# (.help, .profile, .Linkor ...). Обычные сообщения без точки командой не считаются — так бот
# не сработает случайно (например, от слова «linkor» в обычной речи). В личке точка необязательна.
CMD_PREFIX = "."

# Команды участников, недоступные без принятого соглашения (faq/terms/help оставляем свободными).
GATED_COMMANDS = {
    "profile", "prof", "proff", "top", "shop", "store", "norms", "rules", "docs",
    "tag", "brawl", "untag", "streak", "season", "hof", "halloffame", "hall", "menu",
    "lp", "joke", "fact", "toast", "ai", "linkor",
    "dice", "darts", "basket", "football", "bowling", "casino", "coin", "8ball",
    "rps", "pick", "number", "duel", "roll", "guess",
    "лотерея", "лото", "lottery", "дуэль", "дуель", "титул", "title",
    "квест", "quest", "задание", "ачивки", "achievements", "значки", "достижения",
    "викторина", "quiz", "квиз", "ответ", "answer",
    "ивент", "event", "ивенты", "events",
    "карточка", "card", "карта",
}

# Управляющие команды владельца — работают только в личке с ботом (в группе не выполняются).
ADMIN_DM_COMMANDS = {
    "bonusall", "lpall", "грантвсем", "всемлп", "backup", "бэкап", "бекап",
    "modlog", "log", "журнал", "analytics", "dashboard", "аналитика", "статклан",
    "checknorms", "проверканорм", "snapshotnorms", "снимокнорм", "месяцнормы", "monthnorms",
    "пранкменю", "prankmenu",
    "отчётнеделя", "отчетнеделя", "weeklyreport", "недельныйотчет",
    "отчётмесяц", "отчетмесяц", "monthlyreport", "месячныйотчет",
    "дайджест", "digest", "сводка",
}

# 5.7: штабные команды, чьё сообщение бот удаляет сразу после вызова (чат остаётся чистым,
# ответ бот даёт отдельным сообщением). Команды, которые САМИ удаляют своё сообщение
# (announce/summon/poll), сюда НЕ входят — иначе двойное удаление.
STAFF_AUTODELETE_COMMANDS = {
    "ban", "unban", "kick", "mute", "unmute", "warn", "unwarn", "снятьварн",
    "lp", "лп", "rep", "реп", "репутация",
    "warns", "варны", "нормаварн", "normwarn", "normwarns",
    "удалить", "remove", "delete", "del", "kickdb",
    "чистка", "cleanup", "prune", "ошибки", "errors", "журналошибок",
    "отчет", "отчёт", "report", "актив", "activity", "статдни",
    "утро", "доброеутро", "morning", "ночь", "ночнойрежим", "night",
    "добавитьгиф", "addgif", "гиф+", "гифы", "gifs", "гифки", "очиститьгифы", "cleargifs",
    "отпуск", "vacation", "отпускдо", "vac",
    "tag", "тег", "brawl", "untag", "untie", "отвязать", "отвязка",
    "пранк", "prank", "задать", "set", "выдать", "титул", "title",
    "форсквест", "forcequest", "квестдать", "завершитьквест",
}

LEADER_ID = 1644726751             # лидер клана (максимальный баланс)
EXCLUDED_IDS = {485558746, 44125359}        # админы вне экономики: без LP и конкурсов
CONTEST_CREATORS = {1644726751, 485558746}  # кто создаёт конкурсы (в личке)

# --- Оформление клана ---
CLAN_NAME = "LINKOR"
CLAN_LINE = "━━━━━━━━━━━━━━━"   # разделитель для официальных сообщений

# Премиум-эмодзи (custom emoji). Формат: имя -> (id, запасной обычный смайлик).
# id пустой "" -> покажется только запасной смайлик. Рендерятся, т.к. владелец бота с Premium.
PREMIUM_EMOJI_ENABLED = True   # премиум-эмодзи включены (анимированные). Все сообщения с ними идут с parse_mode=HTML
PREMIUM_EMOJI = {
    "lp":          ("5411441682363202957", "🪙"),   # значок LP
    "shop":        ("5213211117538521843", "🛒"),   # магазин
    "mega":        ("5213363464323479192", "📢"),   # мегафон (объявления/сбор)
    "excl":        ("5220226053263342894", "❗"),   # восклицательный знак (нормы)
    "bp":          ("5213473832098084979", "🎫"),   # Brawl Pass
    "bpplus":      ("5427033788037217281", "🎟"),   # Brawl Pass Plus
    "bpup":        ("5212955451020293672", "🔼"),   # улучшение до BP+
    "pro":         ("5328039924350348810", "💎"),   # Pro Pass
    "cup":         ("5213326931331659859", "🏆"),   # кубок
    "catlove":     ("5274108397971592115", "😻"),   # кошкодевочка с сердечками (приветствие)
    "catneutral":  ("5273934086723884809", "😺"),   # кошкодевочка нейтральная
    "fire":        ("4956499161319998529", "🔥"),   # огонь (активность/хайп)
    "crown":       ("4958725487682650920", "👑"),   # корона (лидер/иерархия)
    "shield":      ("4958900559139570572", "🛡"),   # щит (модерация)
    "star":        ("5438496463044752972", "⭐"),   # звезда (репутация)
    "robot":       ("5332814197111663865", "🤖"),   # робот/ИИ
    "game":        ("5467774597971588287", "🎮"),   # геймпад (игры)
    "calendar":    ("5413879192267805083", "📅"),   # календарь (дата)
    "lock":        ("5210939187213121918", "🔒"),   # замок (закрытый магазин)
    "scales":      ("5400250414929041085", "⚖️"),   # весы (правила)
    # --- пачка 4.6 ---
    "msg":         ("4956475826762679249", "💬"),   # сообщение/активность
    "stats":       ("5231200819986047254", "📊"),   # статистика
    "gold":        ("5440539497383087970", "🥇"),   # медаль 1 место
    "silver":      ("5447203607294265305", "🥈"),   # медаль 2 место
    "bronze":      ("5453902265922376865", "🥉"),   # медаль 3 место
    "check":       ("4956721670690702265", "✅"),   # галочка
    "cross":       ("4958526153955476488", "❌"),   # крестик
    "warn":        ("5447644880824181073", "⚠️"),   # предупреждение (варн)
    "mute":        ("6030467682283818947", "🔇"),   # мут
    "unmute":      ("5222171647678630073", "🔊"),   # размут
    "ban":         ("5210939187213121918", "🚫"),   # бан (тот же значок-замок)
    "unban":       ("5220151819048597492", "🔓"),   # разбан
    "hug":         ("5375069356680033632", "🤝"),   # обнимашки/рукопожатие
    "contest":     ("4956596167451346576", "🎉"),   # конкурс
    "pin":         ("5397782960512444700", "📌"),   # булавка (закрепить/итоги)
    "rate":        ("5402186569006210455", "💱"),   # курс
    "growth":      ("5429651785352501917", "📈"),   # рост
    "clock":       ("5902331842723319210", "🕙"),   # часы
    "role":        ("4958621433509970793", "🏷"),   # роль
    "envelope":    ("5253742260054409879", "📨"),   # конверт/обжалование
    "folder":      ("5433653135799228968", "📑"),   # папка (документация)
    "note":        ("6032746961298264832", "📝"),   # блокнот с карандашом (сводка)
    "scroll":      ("5217939515754174728", "📜"),   # свиток (правила/кодекс)
    "person":      ("5296665364346727584", "👤"),   # человек (профиль)
    "bulb":        ("5422439311196834318", "💡"),   # лампочка (идея)
}

# --- Топики (форумные темы). 0 = не задано. ID темы узнай командой "айди" в нужном топике ---
NO_MODERATION_TOPIC_ID = 6770  # топик "cherdak linkor" — там автомодерация НЕ работает
ANNOUNCE_TOPIC_ID = 0          # топик для объявлений (куда бот пишет по команде "объявление")

# --- Магазин: часы работы по МСК ---
STORE_OPEN_HOUR = 10
STORE_CLOSE_HOUR = 22
STORE_MANAGER = "@VoxTecn"

# --- Награды за игровые нормы (начисляет только админ секретными командами) ---
NORM_RANK_LP = 70              # норма ранга (Легендарный) — было 220
NORM_TROPHY_LP = 70            # норма кубков — было 200; теперь копится и выдаётся раз в МЕСЯЦ (за каждую закрытую неделю)
NORM_MONTH_PER_WEEK = 95       # 6.0: LP за каждую закрытую неделю нормы кубков (главный стабильный доход; платится помесячно)
NORM_MSG_WEEK_LP = 35          # 6.0: LP за закрытую норму СООБЩЕНИЙ за неделю (стабильный доход для всех активных, платится помесячно)

# --- Система норм-кубков (Этап 5) ---
WEEKLY_TROPHY_NORM = 1450      # сколько кубков надо набрать за неделю (Пн→Вс МСК)
NORMS_START_DATE = "2026-06-22"  # нормы начинают считаться с этого понедельника (раньше — ничего)
NORM_FAIL_PENALTY = 50         # штраф LP за невыполнение нормы кубков
NORM_WARN_LIMIT = 3            # столько норм-варнов за NORM_WARN_DAYS → авто-исключение
NORM_WARN_DAYS = 30            # срок жизни норм-варна (месяц)
SOFT_START_WEEKS = 2           # «мягкий старт»: первые N недель нормы считаются, но без штрафов

# Разовый бонус всем участникам за запуск «Цитадели» и терпение во время техработ (срабатывает 1 раз).
CITADEL_BONUS_LP = 150

# --- Документация и версия бота ---
# Имя бота — всегда LINKOR BOSS. UPDATE_NAME — название большого обновления (5.x = «Цитадель»).
# Версия (5.2) — это патч в рамках этого обновления.
BOT_FULL_NAME = "LINKOR BOSS"
BOT_VERSION = "6.0"
PATCH_CODE = "LB-6.0"
UPDATE_NAME = "Цитадель"        # название большого обновления (вся ветка 5.x)
PATCH_NAME = UPDATE_NAME        # для совместимости со старым кодом
PATCH_TYPE = "обновление"
BOT_OWNER = "@yaukoshi_san"    # владелец и разработчик бота (ты)
BOT_ADMIN = "@VoxTecn"         # админ бота / владелец чата
RELEASE_DATE = "13.06.2026"

# История изменений ДЛЯ УЧАСТНИКОВ по версиям (новые сверху). Без админских/секретных команд.
# Формат: (версия, тип, [пункты])
CHANGELOG_HISTORY = [
    ("6.0", "Империя", [
        "🎉 Ивенты! Администрация может запускать особые события: <b>x2/x3 LP за сообщения</b> на время. Когда идёт ивент — самое время быть активным. Проверить: <code>.event</code>.",
        "🏆 Сезоны 2.0: чемпионы месяца теперь получают не только LP, но и <b>титул-регалию в профиль</b> — 🏆 Чемпион / 🥈🥉 Призёр сезона на целый месяц. Борись за топ: <code>.season</code>.",
        "💰 Экономика обновлена: теперь LP дают ОБЕ нормы — за кубки (95/нед) и за сообщения (35/нед), выплата 1-го числа за каждую закрытую неделю. Закрывай нормы — двигайся к призам заметно быстрее. Активность важна как никогда!",
        "🖼️ Карточка профиля картинкой: <code>.card</code> — красивая карточка в фирменном тёмно-золотом стиле клана с твоими статами, титулом и рангом. Можно сохранить и показать!",
        "✨ Профиль 2.0: добавлены место в текущем сезоне, прогресс квестов и побед в викторине.",
        "✨ Финальное большое обновление Линкора — дальше только полировка и развитие.",
    ]),
    ("5.9", "Регалии", [
        "👑 Титулы вместо роли: у участников в профиле вместо «Участник» теперь красуется заработанный титул. Качай престиж — выделяйся!",
        "❓ Викторина теперь на ТИТУЛЫ, а не LP: за победы открываются 🧠 Знаток (3), 📚 Эрудит (10), 🎓 Гений клана (25), 🏆 Чемпион викторин (50).",
        "🎯 Квесты дают титулы: 🌱 Активист (3), ⚡ Энтузиаст (10), 🔥 Мастер квестов (25), 💎 Ветеран (50), 👑 Легенда Линкора (100).",
        "🏆 Витрина титулов <code>.title list</code> — все награды за квесты и викторину, видно сколько до каждой.",
        "⚖️ Экономика спокойнее: викторина и квесты больше не печатают LP — только престиж. Твой баланс копится честно, призы клана не обесцениваются.",
    ]),
    ("5.8", "Арсенал", [
        "🎯 Квесты дня: у каждого своё задание (написать N сообщений, получить респект, сыграть дуэль…). Награда — престижные ТИТУЛЫ в профиль: 3 квеста → 🌱 Активист, 10 → ⚡ Энтузиаст, 25 → 🔥 Мастер квестов, 50 → 💎 Ветеран, 100 → 👑 Легенда Линкора. Смотри своё: <code>.quest</code>.",
        "🏅 Значки-ачивки: за серии, активность, ранг, репутацию и победы. Постоянные награды в профиле — смотри <code>.achievements</code>. Открываются автоматически.",
        "❓ Викторина по Brawl Stars: <code>.quiz</code> запускает вопрос, первый верный <code>.answer N</code> забирает банк LP.",
        "📅 Нормы кубков: проверка по-прежнему еженедельная, но LP начисляются раз в месяц «большой проверкой» 1-го числа — за каждую закрытую неделю. Отыграть нормы только в конце месяца не выйдет: считается каждая неделя отдельно.",
        "✨ Профиль теперь показывает значки игрока в ряд под рангом.",
        "⚖️ Экономика поджата: LP за сообщение и дневной потолок снижены. Награды квеста и викторины идут через общий недельный лимит вместе с нормами.",
    ]),
    ("5.7", "Казна", [
        "🎰 Лотерея: купи билет за 20 LP (<code>.lottery ticket</code>), банк растёт, в конце — розыгрыш, победитель забирает всё.",
        "⚔️ Дуэли на LP: ответь сопернику <code>.duel 50</code>, он принимает — победитель (50/50) забирает ставку. Ставка 5–50 LP, комиссия казны 10%.",
        "⭐ Титулы: свой титул в профиле на 7 дней за 100 LP (<code>.title buy ТЕКСТ</code>).",
        "🎫 Лимит магазина: 1 пасс на человека в календарный месяц.",
        "💰 Экономика занёрфлена: LP за сообщение и дневной потолок снижены, серии и потолок наград за нормы тоже.",
        "📜 <code>.docs</code> стал персональным — листает только тот, кто открыл.",
    ]),
    ("5.6", "Разведка", [
        "🏅 Ранги Brawl Stars: «ранг» — твой ранг, «топ ранг» — топ клана, ранг виден и в профиле.",
        "🎁 Авто-награды за ранги: первый в жизни выход на ступень — Алмаз +20, Мифик +40, Легенда +70, Мастер +100, Про +150 LP. Бот замечает сам и поздравляет в чате.",
        "🔭 Ежедневно бот сам обновляет кубки, ники и ранги всех привязанных — «обновлять тег» вручную больше не нужно.",
        "💎 Кубки считаются надёжно: точка отсчёта недели фиксируется автоматически, привязал тег среди недели — отсчёт честно идёт с этого момента.",
        "➕ Полировка: понятные имена в нормах, у проваливших видна и выполненная часть.",
    ]),
    ("5.5", "Устав", [
        "📋 Команда «нормы» — личный прогресс недели: сообщения, прирост кубков (живые данные из игры), отпуск, сроки проверки.",
        "📣 Суббота, 12:00 — бот сам напоминает тем, кто ещё не закрыл норму сообщений: впереди последний день.",
        "⭐ «Первый писавший» отмечается только утром (08:00–12:00).",
    ]),
    ("5.4", "Фундамент", [
        "➕ Репутация — без точки: просто ответь на сообщение «+» или «респект» (а «-» или «дизреспект» — понизить).",
        "🔥 Серии активности: за 3/7/14/30/60/100 дней подряд — бонусы LP, рекорд серии виден в профиле.",
        "🎬 Гифки приветствий стали надёжнее: бот берёт лёгкий формат, который Telegram доставляет всегда.",
        "⚖️ Баланс: владелец скрыт из всех публичных топов — борьба за №1 настоящая.",
    ]),
    ("5.3", "обновление", [
        "🌅 Авто-приветствия по Москве: в 8:00 бот желает доброго утра (с датой, тёплым текстом и мини-итогом вчерашнего дня), а в 22:00 — спокойной ночи и мягкий ночной режим.",
        "☀️ Тексты приветствий зависят от дня недели: в пятницу про выходные, в понедельник про новую неделю и т.д. — живее и без шаблона.",
        "⭐ Первого, кто написал утром, бот отмечает в чате — небольшое соревнование «кто раньше» подстёгивает актив.",
        "😻 Авто-гифки в приветствиях: утром бодрые котики, ночью — спокойные. Бот подбирает их сам. А свои избранные гифки можно положить в копилку командой <code>.добавитьгиф</code> — они в приоритете.",
        "📅 Листание прошлых топов: команда «топ день» показывает топ активности за сегодня, а стрелками можно листать прошлые дни (до недели назад).",
        "📖 Раздел «docs» стал листаемым: главная информация всегда на месте, а изменения версий перелистываются стрелками.",
    ]),
    ("5.2", "обновление", [
        "🛒 Магазин стал кнопочным: короткое сообщение и разделы (как получать LP, за рубли, правила) открываются прямо в нём же — а товары всегда под рукой одной кнопкой.",
        "🧹 Топы теперь чистятся сами: как только участник выходит из клана, он автоматически убирается из всех топов, LP и статистики — в рейтингах только нынешние бойцы. Вышедших ранее можно убрать разом (у администрации есть команда).",
        "📊 Ежедневный официальный отчёт по чату — владельцу в личку, только для администрации: единый Excel-файл со сводкой, активностью участников, встроенными диаграммами (активность за неделю + доли сообщений) и журналом модерации. Сразу видно, кто тянет чат и растёт ли актив.",
    ]),
    ("5.1", "обновление", [
        "📲 Меню стало аккуратным: кнопки работают только у того, кто открыл меню, а разделы открываются прямо в том же сообщении — чат больше не засоряется.",
        "⭐ Репутацию теперь можно не только повышать, но и понижать: ответь на сообщение знаком «-» (или «-rep»). Репутация может уходить в минус.",
        "🤖 Помощник: если AI не ответил (лимит/связь), повтор разрешён уже через несколько секунд, а не через полминуты. Команды «факт/шутка/тост» стали легче.",
        "📑 История изменений теперь в сворачиваемых цитатах — открывается по тапу, не засоряя чат.",
        "📜 Соглашение: теперь принимается один раз — кнопки исчезают после выбора, повторно «принять/отказать» по кругу больше нельзя.",
        "🔇 Длительность мута можно писать удобно: «30м» (минуты), «12ч» (часы), «7д» (дни) — не только в минутах.",
        "🎮 Привязка тега стала защищённой: тег должен состоять в клубе LINKOR, а игровой ник — совпадать с твоей припиской «LT | ник» в чате. Чужой профиль привязать больше нельзя.",
        "👤 В профиле вместо неточной даты вступления теперь рекорд серии дней.",
        "👑 Зал славы прокачан: теперь это топы по LP, сообщениям, репутации и кубкам — соревнуйтесь за место среди легенд клана.",
        "📋 До старта норм (22 июня) нормы больше не показываются в профиле и не начисляют LP — всё включится одновременно.",
        "❓ Справка (help) стала кнопочным меню: разделы (профиль, клан, игры, AI, админ) открываются прямо в том же сообщении и сворачиваются — больше никакой стены текста на пол-чата.",
        "⌨️ Команды теперь с точкой в начале: <code>.help</code>, <code>.profile</code>, <code>.Linkor вопрос</code>. Слово без точки командой не считается — бот больше не срабатывает случайно (например, от «linkor» в обычной речи). В личке точка необязательна.",
    ]),
    ("5.0", "глобальное обновление", [
        "🎉 В честь запуска «Цитадели» и за терпение во время техработ — всем участникам разовый бонус +150 LP!",
        "🏰 Стартовало большое обновление «Цитадель» — выходит этапами, фичи будут добавляться.",
        "🔥 Ежедневный стрик: пиши в чат каждый день — за серии (3, 7, 14, 30… дней подряд) капают бонусные LP. Смотри командой «streak».",
        "🏆 Топ по кубкам Brawl Stars — команда «top trophies» (нужен привязанный тег).",
        "🗓 Сезоны: каждый месяц — отдельный топ активности. В конце месяца топ-3 получают LP и попадают в зал славы, топ обнуляется. Команды «season» и «hof».",
        "📲 Кнопочное меню: команда «menu» — открывает разделы (профиль, магазин, топы, нормы…) кнопками, без ввода команд.",
        "🛒 Магазин по кнопке: в «shop» теперь можно нажать на товар — оформится заявка, LP зарезервируются, а менеджер выдаст товар (или вернёт LP при отклонении).",
        "🛡 Управление: журнал модерации («modlog»), опросы («poll»), сводная аналитика клана («analytics»), бэкап базы («backup»), кнопки «Одобрить/Отклонить» в обжалованиях и авто-защита от рейдов.",
        "🏆 Нормы кубков: бот сам делает снимок кубков в понедельник и проверяет в воскресенье. Набрал +1450 за неделю → +70 LP; не набрал → −50 LP и норм-варн (3 за месяц = исключение). Первые 2 недели — мягкий старт без штрафов. Уехал отдыхать — админ ставит «отпуск».",
        "📜 Согласие с правилами: новичок принимает соглашение в личке боту по кнопке. Без согласия бот недоступен (кроме владельца и админов).",
        "🏷 Привязка тега обязательна для новичков: не привязал за 3 дня — авто-исключение. Старичков это правило не трогает.",
        "🗓 Нормы кубков и активности начинают считаться с 22 июня 2026 — чтобы все спокойно вошли в ритм (особенно у кого экзамены).",
        "💬 Норма активности (450 сообщений/нед) теперь тоже с проверкой: не выполнил — −50 LP и норм-варн (максимум один в неделю; 3 за месяц = исключение).",
    ]),
    ("4.9", "финальный патч перед 5.0", [
        "🤖 Все команды бота теперь на английском (profile, lp, top, shop, norms…) — бот больше не срабатывает на обычные русские фразы в чате.",
        "🛡 При снятии варна списанные за него LP теперь возвращаются игроку обратно.",
        "📜 Появилось соглашение и FAQ — читается в личке боту командой «faq».",
        "🪙 Баланс стал надёжнее: начисление LP больше не может «потеряться» технически.",
        "💾 Все данные (баланс, репутация, привязанный тег, топы) сохраняются между обновлениями.",
        "🎨 Финальная вычитка оформления перед большим релизом 5.0.",
    ]),
    ("4.8", "исправления оформления", [
        "📅 В профиле убраны повторяющиеся значки: сообщения, день, неделя и стаж теперь разные иконки.",
        "🛒 Порядок товаров в магазине исправлен: Brawl Pass → улучшение → Brawl Pass Plus → Pro Pass.",
        "📊 Прогресс-бар теперь ведёт сначала к Brawl Pass (а не к улучшению, которое без BP не купить).",
    ]),
    ("4.7", "исправления", [
        "🐞 Исправлена путаница в чатах с темами: бот больше не принимает тему за «ответ», поэтому LP начисляются и показываются правильному человеку.",
        "🪙 Команда «lp» всегда показывает ТВОЙ баланс; чужой — по «lp @юзер».",
        "👤 «profile» по умолчанию открывает твой профиль; чужой — по «profile @юзер».",
        "🔧 Повышена точность и стабильность баланса, топов и репутации.",
    ]),
    ("4.6", "финальная полировка", [
        "🎨 Полностью обновлённое премиум-оформление: все значки анимированы, единый дорогой стиль.",
        "🎮 Привязка игрового тега Brawl Stars: бот видит твои кубки и трофейный рекорд (команда «tag»).",
        "📅 Воскресный авто-отчёт по клану: топ активных за неделю и кто не дотянул норму.",
        "🏅 Топы, профиль и модерация получили новые анимированные эмодзи.",
    ]),
    ("4.5", "крупная доработка", [
        "📑 Документация хранит полную историю изменений по версиям.",
        "🚫 Анти-спам: за длинную серию сообщений подряд LP больше не начисляются.",
        "👤 Личный кабинет в ЛС: напиши боту «profile» — баланс, статистика и прогресс.",
        "📊 В профиле появился прогресс-бар до следующей награды.",
    ]),
    ("4.1", "минорное обновление", [
        "💬 Дневной потолок активности, баланс пересчитан — быть активным каждый день выгоднее.",
        "⚖️ Обжалование наказаний: напиши боту в личку «appeal».",
        "🛡 При снятии варна/мута и разбане можно указывать причину (например, амнистия).",
        "📊 Топы и баланс LP сохраняются между обновлениями бота.",
        "🤖 Помощник Linkor запоминает контекст диалога; факты и сводка лучше.",
    ]),
    ("4.0", "глобальное обновление", [
        "💬 Новая система LP: баллы капают за каждое сообщение — чем активнее, тем больше.",
        "🪙 LP стали дробными и начисляются плавно.",
        "🎨 Полностью обновлённое премиум-оформление: анимированные эмодзи и новый стиль.",
        "🛒 Переработан магазин: понятнее, как получать LP и что купить.",
        "🧮 Исправлены ошибки в балансе и топах.",
    ]),
]

# --- Автомодерация (прогрессивно, по кодексу) ---
# Лестница: 1) устный выговор → 2) мут → 3) варн → 3 варна = исключение.
DISCIPLINE_DECAY_DAYS = 7          # через сколько дней «чистого» поведения стадия сбрасывается
MUTE_HOURS_INSULT = 6              # мут за оскорбления/мат (кодекс 1.1: 1–12 ч)
MUTE_HOURS_CAPS = 2                # мут за КАПС (кодекс 1.10: 1–3 ч)
MUTE_HOURS_FLOOD = 1               # мут за флуд (кодекс 1.8: 1–6 ч)

FLOOD_LIMIT = 5                    # сообщений
FLOOD_WINDOW = 7                   # за столько секунд
CAPS_MIN_LEN = 12                  # длина, с которой проверяем КАПС
CAPS_RATIO = 0.7
REP_COOLDOWN_MIN = 60


def is_owner(user_id):
    """5.9: владелец бота — без КД и лимитов вообще. Единая проверка для всех ограничений."""
    return user_id == LEADER_ID
# 5.4: реакции-на-ответ работают БЕЗ точки — если сообщение целиком равно слову и является ответом.
REACTION_PLUS = {"+", "+rep", "+реп", "респект", "respect"}
REACTION_MINUS = {"-", "−", "-rep", "-реп", "дизреспект", "disrespect"}

# Мат и крепкие словечки в чате РАЗРЕШЕНЫ — за обычную брань бот не наказывает.
# Этот список по умолчанию ПУСТ. Сюда можно добавить слова, которые ты хочешь
# блокировать ВСЕГДА (мгновенно, без AI), даже если это просто выражение.
BANNED_WORDS = [
]

# ---------- AI (Cerebras — бесплатно, без карты, 1 млн токенов/день) ----------
# Регистрация: cloud.cerebras.ai (email, без карты и без листа ожидания, ~5 минут).
# Ключ создаётся в разделе API Keys. Лимиты бесплатно: 1 000 000 токенов/день,
# 30 запросов/мин, 14 400 запросов/день, контекст до 8192 токенов. Очень быстро (2600+ ток/сек).
# ВАЖНО: зарубежный сервис — запросы лучше с зарубежного IP (твой VPS); с ПК в РФ может нужен VPN.
AI_API_KEY = os.environ.get("AI_API_KEY", "")   # из переменной окружения (bothost) или впиши в кавычки
AI_BASE_URL = "https://api.cerebras.ai/v1"
AI_MODEL = "gpt-oss-120b"          # сильная бесплатная модель; запасные: llama-3.3-70b
                                   # актуальный список — в консоли cloud.cerebras.ai
AI_MODERATION = False              # ВЫКЛ: проверка КАЖДОГО сообщения превышает лимит Cerebras (ошибки 429)
                                   # и мешает ассистенту Linkor. Включай, только если будет платный/больший лимит.
AI_MOD_AUTOPUNISH = True           # если AI_MODERATION включишь — нарушения пойдут по лестнице наказаний
AI_COOLDOWN_SEC = 30               # пауза между AI-запросами (для команды Linkor)
AI_RETRY_SEC = 8                   # если AI не ответил — даём повтор уже через столько секунд (не штрафуем полным кулдауном)

# --- Brawl Stars API (привязка игрового тега) ---
# Ключ создаётся на developer.brawlstars.com. На хостинге с «плавающим» IP используем
# прокси RoyaleAPI: в кабинете Brawl впиши в whitelist IP 45.79.218.79, а ключ положи
# в переменную окружения BRAWL_API_KEY. Без ключа функция тега просто отключена.
BRAWL_API_KEY = os.environ.get("BRAWL_API_KEY", "")
BRAWL_API_BASE = "https://bsproxy.royaleapi.dev/v1"   # прокси RoyaleAPI (фиксированный IP)
BRAWL_CACHE_TTL = 600              # сколько секунд держать данные игрока в памяти
LINKOR_CLUB_TAG = "2U80CYP9J"      # тег клуба LINKOR (без #): тег можно привязать только если игрок состоит в клубе
TAG_VERIFY_BY_TITLE = True         # дополнительно сверять игровой ник с припиской «LT | ник» в чате
# Примечание: Brawl-API не отдаёт ранговую лигу (Ranked) — её норму («Легендарная») проверяют вручную
# командой «норма ранг». Бот автоматически следит только за приростом кубков (норма кубков).

# Московское время (UTC+3, без перевода часов)
MSK = timezone(timedelta(hours=3))

# ------------------------------------------------------------
# СТАРТОВЫЕ БАЛАНСЫ LP (из твоего списка).
# Ключ — @username БЕЗ собачки, в любом регистре. LP начислятся,
# как только человек напишет в чат. Можешь свободно редактировать.
# ⚠️ Записи без @username (например "❄️Рейна ашот🧊" и "?") добавь
#    вручную: ответь на сообщение человека и напиши "лп =140".
# ------------------------------------------------------------
# Старые начисления по @username больше не используются — у всех старт 100 LP.
SEED_LP = {}

# ------------------------------------------------------------
# СТАРТОВЫЕ БАЛАНСЫ LP ПО ID (надёжнее, чем по @username).
# Подходит для людей без юзернейма. Формат:  user_id: количество_LP
# Балансы появятся сразу при запуске, даже до первого сообщения.
# Существующие балансы НЕ перезаписываются (защита от случайной потери).
# ------------------------------------------------------------
SEED_LP_BY_ID = {
    # Когда пришлёшь список "очки↔айди", проставь LP напротив нужных ID
    # и убери # в начале строки. Пример:  1644726751: 500,
    #
    # --- Список ID участников чата (заготовка) ---
    # 1644726751: 0,   # ты (Лидер)
    # 5563767723: 0,
    # 6381412181: 0,
    # 485558746: 0,    # Админ
    # 6978315935: 0,
    # 44125359: 0,     # Админ2
    # 6467563944: 0,
    # 6556750104: 0,
    # 5873356357: 0,
    # 1601449534: 0,
    # 8053010448: 0,
    # 6354139019: 0,
    # 6188088077: 0,
    # 6662198472: 0,
    # 7217527932: 0,
    # 6127748766: 0,
    # 7680788701: 0,
    # 5377783977: 0,
    # 1156680025: 0,
    # 5226704708: 0,
    # 8377662046: 0,
    # 1582472561: 0,
    # 5193879335: 0,
    # 1652428291: 0,
    # 8227895272: 0,
    # 5800132769: 0,
    # 8772020145: 0,
    # 5228906058: 0,
    # 2038946511: 0,
    # 5639130526: 0,
    # 2006050254: 0,
    # 7732769013: 0,
    # 7721750865: 0,
}

# ============================================================
#                   РОЛИ И ПРАВА
# ============================================================
MEMBER, MANAGER, MODER, ADMIN, LEADER = 0, 1, 2, 3, 4
ROLE_NAMES = {MEMBER: "Участник", MANAGER: "Менеджер", MODER: "Модератор",
              ADMIN: "Администрация", LEADER: "Лидер"}
ROLE_WORDS = {"участник": MEMBER, "юзер": MEMBER, "менеджер": MANAGER,
              "модер": MODER, "модератор": MODER, "админ": ADMIN,
              "администратор": ADMIN, "администрация": ADMIN, "лидер": LEADER}

# Роли, выдаваемые автоматически при запуске (по ID). Удобно для админов,
# которые не являются админами Telegram. Не понижает уже выданные роли.
SEED_ROLES_BY_ID = {
    1644726751: LEADER,    # ты — лидер клана
    485558746: ADMIN,      # владелец чата (резерв на случай ЧП) + админ
    44125359: ADMIN,       # Админ2
}

# ============================================================
#                   БАЗА ДАННЫХ
# ============================================================
conn = sqlite3.connect(DB_FILE, check_same_thread=False)
conn.row_factory = sqlite3.Row
# Надёжность базы: WAL переживает внезапные перезагрузки без потери данных,
# foreign_keys и busy_timeout снижают риск ошибок при «занятой» базе.
try:
    conn.execute("PRAGMA journal_mode=WAL")
    conn.execute("PRAGMA synchronous=NORMAL")
    conn.execute("PRAGMA busy_timeout=5000")
    conn.commit()
except Exception as e:
    print(f"[DB] не удалось включить WAL: {e}", flush=True)


def msk_now():
    return datetime.now(MSK)


def _add_col(table, col, decl):
    cols = [r["name"] for r in conn.execute(f"PRAGMA table_info({table})").fetchall()]
    if col not in cols:
        conn.execute(f"ALTER TABLE {table} ADD COLUMN {col} {decl}")


def init_db():
    conn.execute("""CREATE TABLE IF NOT EXISTS greeting_gifs (
        id INTEGER PRIMARY KEY AUTOINCREMENT, kind TEXT, file_id TEXT, ts TEXT)""")
    conn.execute("""CREATE TABLE IF NOT EXISTS users (
        chat_id INTEGER, user_id INTEGER, username TEXT, name TEXT,
        gems INTEGER DEFAULT 0, reputation INTEGER DEFAULT 0,
        mutes INTEGER DEFAULT 0, role INTEGER DEFAULT 0,
        joined TEXT, last_rep TEXT, lp INTEGER DEFAULT 0,
        PRIMARY KEY (chat_id, user_id))""")
    conn.execute("""CREATE TABLE IF NOT EXISTS warns (
        chat_id INTEGER, user_id INTEGER, ts TEXT)""")
    conn.execute("""CREATE TABLE IF NOT EXISTS stats (
        chat_id INTEGER, user_id INTEGER, day TEXT, cnt INTEGER DEFAULT 0,
        PRIMARY KEY (chat_id, user_id, day))""")
    conn.execute("""CREATE TABLE IF NOT EXISTS lp_log (
        chat_id INTEGER, user_id INTEGER, day TEXT, delta INTEGER)""")
    conn.execute("""CREATE TABLE IF NOT EXISTS pending_lp (
        username TEXT PRIMARY KEY, amount INTEGER)""")
    conn.execute("""CREATE TABLE IF NOT EXISTS meta (key TEXT PRIMARY KEY, value TEXT)""")
    conn.execute("""CREATE TABLE IF NOT EXISTS hall_of_fame (
        chat_id INTEGER, season TEXT, rank INTEGER, user_id INTEGER,
        name TEXT, score INTEGER, ts TEXT)""")
    conn.execute("""CREATE TABLE IF NOT EXISTS orders (
        id INTEGER PRIMARY KEY AUTOINCREMENT, chat_id INTEGER, user_id INTEGER,
        name TEXT, item TEXT, price INTEGER, status TEXT DEFAULT 'pending', ts TEXT)""")
    conn.execute("""CREATE TABLE IF NOT EXISTS modlog (
        id INTEGER PRIMARY KEY AUTOINCREMENT, chat_id INTEGER, admin_id INTEGER, admin_name TEXT,
        target_id INTEGER, target_name TEXT, action TEXT, reason TEXT, ts TEXT)""")
    _add_col("users", "lp", "INTEGER DEFAULT 0")
    _add_col("users", "norm_week", "TEXT")
    _add_col("users", "super_week", "TEXT")
    _add_col("users", "earn_week", "TEXT")
    _add_col("users", "earn_amt", "INTEGER DEFAULT 0")
    _add_col("users", "disc_stage", "INTEGER DEFAULT 0")
    _add_col("users", "disc_last", "TEXT")
    _add_col("users", "brawl_tag", "TEXT")
    _add_col("users", "brawl_trophies", "INTEGER")     # последние известные кубки (для топа)
    _add_col("users", "brawl_name", "TEXT")            # имя в Brawl Stars
    _add_col("users", "streak", "INTEGER DEFAULT 0")   # текущая серия дней подряд
    _add_col("users", "streak_last", "TEXT")           # дата последнего чек-ина (МСК, YYYY-MM-DD)
    _add_col("users", "streak_best", "INTEGER DEFAULT 0")  # рекорд серии
    _add_col("users", "trophy_base", "INTEGER")            # кубки на начало недели (снимок)
    _add_col("users", "trophy_base_week", "TEXT")          # понедельник недели снимка (YYYY-MM-DD)
    _add_col("users", "vacation_until", "TEXT")            # «отпуск» от норм до этой даты (YYYY-MM-DD)
    _add_col("users", "norm_anchor_week", "TEXT")          # понедельник недели, на начало которой участник был в клане
    _add_col("users", "agreed_faq", "INTEGER DEFAULT 0")   # принял ли соглашение (по кнопке в ЛС)
    _add_col("users", "tag_gate", "INTEGER DEFAULT 0")     # 1 = новичок, обязан привязать тег
    _add_col("users", "tag_warns", "INTEGER DEFAULT 0")    # счётчик тег-варнов (нет тега)
    _add_col("users", "tag_warn_last", "TEXT")             # дата последнего тег-варна (YYYY-MM-DD)
    _add_col("users", "gone", "INTEGER DEFAULT 0")         # 5.4: 1 = вышел из чата (история сохраняется)
    _add_col("users", "rank_best", "INTEGER")              # 5.6: лучшее замеченное «сырое» значение ранга
    _add_col("users", "rank_seen", "TEXT")                 # 5.6: дата, когда лучший ранг был замечен
    _add_col("users", "rank_paid_tier", "INTEGER DEFAULT -1")  # 5.6: высшая ступень, за которую уже выдана награда
    _add_col("users", "title_text", "TEXT")               # 5.7: личный титул в профиле
    _add_col("users", "title_until", "TEXT")              # 5.7: дата окончания временного титула (YYYY-MM-DD), NULL = бессрочно
    _add_col("users", "prank", "TEXT")                    # 5.9: JSON-overlay фейкового профиля для пранков (визуал, реальные данные не трогаются)
    _add_col("users", "earned_title", "TEXT")             # 5.9: престижный титул, заработанный за квесты/викторину
    _add_col("users", "quiz_wins", "INTEGER DEFAULT 0")   # 5.9: всего побед в викторине (для титулов)
    conn.execute("""CREATE TABLE IF NOT EXISTS lottery (
        chat_id INTEGER, user_id INTEGER, name TEXT, ts TEXT,
        PRIMARY KEY (chat_id, user_id))""")
    conn.execute("""CREATE TABLE IF NOT EXISTS duels (
        chat_id INTEGER, a INTEGER, b INTEGER, bet INTEGER, winner INTEGER, ts TEXT)""")
    conn.execute("""CREATE TABLE IF NOT EXISTS quest_done (
        chat_id INTEGER, user_id INTEGER, day TEXT, code TEXT,
        PRIMARY KEY (chat_id, user_id, day))""")
    conn.execute("""CREATE TABLE IF NOT EXISTS achievements (
        chat_id INTEGER, user_id INTEGER, code TEXT, ts TEXT,
        PRIMARY KEY (chat_id, user_id, code))""")
    conn.execute("""CREATE TABLE IF NOT EXISTS rep_log (
        chat_id INTEGER, from_id INTEGER, to_id INTEGER, day TEXT, delta INTEGER)""")
    conn.execute("""CREATE TABLE IF NOT EXISTS norm_weeks (
        chat_id INTEGER, user_id INTEGER, week TEXT, cups_ok INTEGER DEFAULT 0, msg_ok INTEGER DEFAULT 0,
        PRIMARY KEY (chat_id, user_id, week))""")
    conn.execute("""CREATE TABLE IF NOT EXISTS errors (
        id INTEGER PRIMARY KEY AUTOINCREMENT, ts TEXT, place TEXT, text TEXT)""")
    conn.execute("""CREATE TABLE IF NOT EXISTS norm_warns (
        chat_id INTEGER, user_id INTEGER, ts TEXT)""")
    conn.commit()
    load_seed()
    load_seed_by_id()
    load_roles_by_id()
    reset_balances()


def reset_balances():
    """Разовый сброс: всем по STARTING_LP, лидеру максимум, исключённым админам 0."""
    if not ALLOWED_CHAT_ID:
        return
    if conn.execute("SELECT 1 FROM meta WHERE key='reset_done'").fetchone():
        return
    conn.execute("UPDATE users SET lp=? WHERE chat_id=?", (STARTING_LP, ALLOWED_CHAT_ID))
    conn.execute("UPDATE users SET lp=? WHERE chat_id=? AND user_id=?", (LP_MAX, ALLOWED_CHAT_ID, LEADER_ID))
    for uid in EXCLUDED_IDS:
        conn.execute("UPDATE users SET lp=0 WHERE chat_id=? AND user_id=?", (ALLOWED_CHAT_ID, uid))
    conn.execute("DELETE FROM pending_lp")
    conn.execute("INSERT OR REPLACE INTO meta (key, value) VALUES ('reset_done','1')")
    conn.commit()


def load_roles_by_id():
    """Назначает роли по ID при запуске. Не понижает уже выданные роли."""
    if not ALLOWED_CHAT_ID:
        return
    for uid, role in SEED_ROLES_BY_ID.items():
        row = get_user(ALLOWED_CHAT_ID, uid)
        if row is None:
            conn.execute(
                "INSERT OR IGNORE INTO users (chat_id,user_id,username,name,joined,lp,role) VALUES (?,?,?,?,?,0,?)",
                (ALLOWED_CHAT_ID, uid, "", "", msk_now().isoformat(), role))
        elif row["role"] < role:
            conn.execute("UPDATE users SET role=? WHERE chat_id=? AND user_id=?",
                         (role, ALLOWED_CHAT_ID, uid))
    conn.commit()


def load_seed_by_id():
    """Заносит стартовые LP по ID. Не трогает уже существующие записи."""
    if not ALLOWED_CHAT_ID:
        return
    for uid, amt in SEED_LP_BY_ID.items():
        amt = max(0, min(LP_MAX, int(amt)))
        conn.execute(
            "INSERT OR IGNORE INTO users (chat_id,user_id,username,name,joined,lp) VALUES (?,?,?,?,?,?)",
            (ALLOWED_CHAT_ID, uid, "", "", msk_now().isoformat(), amt))
    conn.commit()


def load_seed():
    if conn.execute("SELECT 1 FROM meta WHERE key='seed_done'").fetchone():
        return
    for uname, amt in SEED_LP.items():
        conn.execute("INSERT OR IGNORE INTO pending_lp (username, amount) VALUES (?,?)",
                     (uname.lower(), amt))
    conn.execute("INSERT OR REPLACE INTO meta (key, value) VALUES ('seed_done','1')")
    conn.commit()


def ensure_user(chat_id, user):
    row = conn.execute("SELECT 1 FROM users WHERE chat_id=? AND user_id=?",
                       (chat_id, user.id)).fetchone()
    if row is None:
        start_lp = 0 if user.id in EXCLUDED_IDS else STARTING_LP
        conn.execute(
            "INSERT INTO users (chat_id,user_id,username,name,joined,lp) VALUES (?,?,?,?,?,?)",
            (chat_id, user.id, user.username or "", user.full_name, msk_now().isoformat(), start_lp))
    else:
        conn.execute("UPDATE users SET username=?, name=?, gone=0 WHERE chat_id=? AND user_id=?",
                     (user.username or "", user.full_name, chat_id, user.id))
    conn.commit()
    credit_pending(chat_id, user)


def credit_pending(chat_id, user):
    if not user.username:
        return
    row = conn.execute("SELECT amount FROM pending_lp WHERE username=?",
                       (user.username.lower(),)).fetchone()
    if row:
        add_lp(chat_id, user.id, row[0])
        conn.execute("DELETE FROM pending_lp WHERE username=?", (user.username.lower(),))
        conn.commit()


def get_user(chat_id, user_id):
    return conn.execute("SELECT * FROM users WHERE chat_id=? AND user_id=?",
                        (chat_id, user_id)).fetchone()


def set_lp(chat_id, user_id, value):
    if user_id in EXCLUDED_IDS:                      # админы вне экономики: всегда 0
        conn.execute("INSERT INTO users (chat_id,user_id,lp) VALUES (?,?,0) "
                     "ON CONFLICT(chat_id,user_id) DO UPDATE SET lp=0", (chat_id, user_id))
        conn.commit()
        return 0
    value = max(LP_MIN, min(LP_MAX, round(float(value), 2)))   # от -10000 до +10000, дробные
    # UPSERT: если строки игрока ещё нет — создаём, чтобы начисление никогда не терялось
    conn.execute("INSERT INTO users (chat_id,user_id,lp) VALUES (?,?,?) "
                 "ON CONFLICT(chat_id,user_id) DO UPDATE SET lp=excluded.lp", (chat_id, user_id, value))
    conn.commit()
    return value


def add_lp(chat_id, user_id, delta):
    row = get_user(chat_id, user_id)
    cur = row["lp"] if row else 0
    new = set_lp(chat_id, user_id, cur + delta)
    applied = new - cur
    if abs(delta) >= 1:   # ручные начисления логируем (мелкие +0.05 за сообщения — нет, чтобы не засорять)
        print(f"[LP] chat={chat_id} user={user_id}: {fmt_lp(cur)} {'+' if delta>=0 else ''}{delta} -> {fmt_lp(new)}", flush=True)
    if applied != 0:
        conn.execute("INSERT INTO lp_log (chat_id,user_id,day,delta) VALUES (?,?,?,?)",
                     (chat_id, user_id, today_str(), applied))
        conn.commit()
    return new


def lp_earned(chat_id, user_id, period):
    """Сколько LP получено за 'day' или 'week'."""
    if period == "day":
        return conn.execute("SELECT COALESCE(SUM(delta),0) FROM lp_log WHERE chat_id=? AND user_id=? AND day=?",
                            (chat_id, user_id, today_str())).fetchone()[0]
    mon, sun = week_bounds()
    return conn.execute("SELECT COALESCE(SUM(delta),0) FROM lp_log WHERE chat_id=? AND user_id=? AND day BETWEEN ? AND ?",
                        (chat_id, user_id, mon, sun)).fetchone()[0]


def award_lp_capped(chat_id, user_id, amount):
    """Начисляет LP с учётом НЕДЕЛЬНОГО ЛИМИТА заработка. Возвращает (сколько_выдано, баланс).
    Ручные начисления админа (команда «lp») идут мимо лимита."""
    if LP_WEEKLY_CAP <= 0:
        return amount, add_lp(chat_id, user_id, amount)
    mon, _ = week_bounds()
    row = get_user(chat_id, user_id)
    earned = (row["earn_amt"] if (row and row["earn_week"] == mon) else 0) or 0
    room = max(0, LP_WEEKLY_CAP - earned)
    give = round(min(amount, room), 2)
    if give <= 0:
        return 0, (row["lp"] if row else 0)
    bal = add_lp(chat_id, user_id, give)
    conn.execute("UPDATE users SET earn_week=?, earn_amt=? WHERE chat_id=? AND user_id=?",
                 (mon, round(earned + give, 2), chat_id, user_id))
    conn.commit()
    return give, bal


def get_role(chat_id, user_id):
    row = get_user(chat_id, user_id)
    return row["role"] if row else MEMBER


def set_role(chat_id, user_id, role):
    conn.execute("UPDATE users SET role=? WHERE chat_id=? AND user_id=?", (role, chat_id, user_id))
    conn.commit()


def inc_mutes(chat_id, user_id):
    conn.execute("UPDATE users SET mutes=mutes+1 WHERE chat_id=? AND user_id=?", (chat_id, user_id))
    conn.commit()


def _prune_warns(chat_id, user_id):
    limit = (msk_now() - timedelta(days=WARN_LIFETIME_DAYS)).isoformat()
    conn.execute("DELETE FROM warns WHERE chat_id=? AND user_id=? AND ts<?",
                 (chat_id, user_id, limit))
    conn.commit()


def add_warn(chat_id, user_id):
    _prune_warns(chat_id, user_id)
    conn.execute("INSERT INTO warns (chat_id,user_id,ts) VALUES (?,?,?)",
                 (chat_id, user_id, msk_now().isoformat()))
    conn.commit()
    return count_warns(chat_id, user_id)


def count_norm_warns(chat_id, user_id):
    """Активные норм-варны за последние NORM_WARN_DAYS дней."""
    limit = (msk_now() - timedelta(days=NORM_WARN_DAYS)).isoformat()
    conn.execute("DELETE FROM norm_warns WHERE chat_id=? AND user_id=? AND ts<?", (chat_id, user_id, limit))
    conn.commit()
    return conn.execute("SELECT COUNT(*) FROM norm_warns WHERE chat_id=? AND user_id=?", (chat_id, user_id)).fetchone()[0]


def add_norm_warn(chat_id, user_id):
    conn.execute("INSERT INTO norm_warns (chat_id,user_id,ts) VALUES (?,?,?)", (chat_id, user_id, msk_now().isoformat()))
    conn.commit()
    return count_norm_warns(chat_id, user_id)


def on_vacation(row):
    """True, если у участника действует «отпуск» от норм."""
    try:
        vu = row["vacation_until"] if row and "vacation_until" in row.keys() else None
        return bool(vu) and vu >= today_str()
    except Exception:
        return False


def norms_launch_monday():
    """Понедельник, с которого начинают считаться нормы (фиксированная дата старта)."""
    return NORMS_START_DATE


def soft_start_active():
    """True в первые SOFT_START_WEEKS недель после старта норм — штрафы не применяются."""
    try:
        launch = datetime.fromisoformat(norms_launch_monday()).date()
        return msk_now().date() < launch + timedelta(weeks=SOFT_START_WEEKS)
    except Exception:
        return False


def count_warns(chat_id, user_id):
    _prune_warns(chat_id, user_id)
    return conn.execute("SELECT COUNT(*) FROM warns WHERE chat_id=? AND user_id=?",
                        (chat_id, user_id)).fetchone()[0]


def remove_one_warn(chat_id, user_id):
    row = conn.execute("SELECT rowid FROM warns WHERE chat_id=? AND user_id=? ORDER BY ts DESC LIMIT 1",
                       (chat_id, user_id)).fetchone()
    if row:
        conn.execute("DELETE FROM warns WHERE rowid=?", (row[0],))
        conn.commit()
    return count_warns(chat_id, user_id)


def clear_warns(chat_id, user_id):
    conn.execute("DELETE FROM warns WHERE chat_id=? AND user_id=?", (chat_id, user_id))
    conn.commit()


def today_str():
    return msk_now().strftime("%Y-%m-%d")


def week_bounds():
    now = msk_now()
    monday = (now - timedelta(days=now.weekday())).date()
    sunday = monday + timedelta(days=6)
    return monday.isoformat(), sunday.isoformat()


def touch_streak(chat_id, user):
    """Отмечает ежедневный чек-ин при ПЕРВОМ сообщении за день (МСК).
    Возвращает (новая_серия, бонус_LP) если сегодня отметился впервые, иначе None."""
    row = get_user(chat_id, user.id)
    if not row:
        return None
    today = msk_now().date()
    last = row["streak_last"] if "streak_last" in row.keys() else None
    if last == today.isoformat():
        return None                                  # уже отмечался сегодня
    yesterday = (today - timedelta(days=1)).isoformat()
    new_streak = (row["streak"] or 0) + 1 if last == yesterday else 1
    best = max(row["streak_best"] or 0, new_streak)
    conn.execute("UPDATE users SET streak=?, streak_last=?, streak_best=? WHERE chat_id=? AND user_id=?",
                 (new_streak, today.isoformat(), best, chat_id, user.id))
    conn.commit()
    bonus = STREAK_MILESTONES.get(new_streak, 0)
    if bonus and user.id not in EXCLUDED_IDS:
        add_lp(chat_id, user.id, bonus)
    return new_streak, bonus


def meta_get(key):
    r = conn.execute("SELECT value FROM meta WHERE key=?", (key,)).fetchone()
    return r[0] if r else None


def meta_set(key, value):
    conn.execute("INSERT OR REPLACE INTO meta (key, value) VALUES (?, ?)", (key, str(value)))
    conn.commit()


MONTHS_RU = ["", "Январь", "Февраль", "Март", "Апрель", "Май", "Июнь",
             "Июль", "Август", "Сентябрь", "Октябрь", "Ноябрь", "Декабрь"]


def current_season():
    return msk_now().strftime("%Y-%m")


def season_title(season):
    """'2026-06' -> 'Июнь 2026'."""
    try:
        y, m = season.split("-")
        return f"{MONTHS_RU[int(m)]} {y}"
    except Exception:
        return season


def season_days_left():
    now = msk_now()
    nxt = now.replace(year=now.year + 1, month=1, day=1) if now.month == 12 else now.replace(month=now.month + 1, day=1)
    return (nxt.date() - now.date()).days


def log_error(place, err):
    """5.4: журнал ошибок бота — пишем в БД, чтобы баги не терялись. Смотреть: команда .ошибки"""
    try:
        conn.execute("INSERT INTO errors (ts, place, text) VALUES (?,?,?)",
                     (msk_now().isoformat(), str(place)[:60], str(err)[:500]))
        conn.commit()
    except Exception:
        pass
    print(f"[ОШИБКА] {place}: {err}", flush=True)


def errors_on(day):
    try:
        return conn.execute("SELECT COUNT(*) AS c FROM errors WHERE ts LIKE ?",
                            (str(day) + "%",)).fetchone()["c"]
    except Exception:
        return 0


def errors_last(n=10):
    try:
        return conn.execute("SELECT ts, place, text FROM errors ORDER BY id DESC LIMIT ?", (n,)).fetchall()
    except Exception:
        return []


def lp_day_flows(chat_id, day):
    """Экономика дня: (выпущено LP, сожжено LP) по журналу начислений."""
    row = conn.execute(
        "SELECT COALESCE(SUM(CASE WHEN delta>0 THEN delta END),0) AS pos, "
        "COALESCE(SUM(CASE WHEN delta<0 THEN delta END),0) AS neg "
        "FROM lp_log WHERE chat_id=? AND day=?", (chat_id, day)).fetchone()
    return round(row["pos"], 2), round(abs(row["neg"]), 2)


def disp_name(row):
    """5.6: имя для вывода — name → @username → ID. Спасает от пустых/битых имён Telegram («?», пустые строки)."""
    try:
        n = (row["name"] or "").strip()
        if n and n != "?":
            return n
        un = row["username"] if "username" in row.keys() else None
        if un:
            return "@" + un
        return f"ID:{row['user_id']}"
    except Exception:
        return "?"


def season_top(chat_id, season, limit=10):
    """Топ участников по числу сообщений за месяц season ('YYYY-MM'). Без админов вне экономики,
    владельца (5.4) и ушедших."""
    excl = tuple(EXCLUDED_IDS | {LEADER_ID}) or (0,)
    ph = ",".join("?" * len(excl))
    return conn.execute(
        f"SELECT u.name, u.user_id, COALESCE(SUM(s.cnt),0) AS val FROM users u "
        f"LEFT JOIN stats s ON s.chat_id=u.chat_id AND s.user_id=u.user_id AND s.day LIKE ? "
        f"WHERE u.chat_id=? AND COALESCE(u.gone,0)=0 AND u.user_id NOT IN ({ph}) "
        f"GROUP BY u.user_id ORDER BY val DESC LIMIT ?",
        (f"{season}-%", chat_id, *excl, limit)).fetchall()


def stat_add(chat_id, user_id):
    conn.execute(
        "INSERT INTO stats (chat_id,user_id,day,cnt) VALUES (?,?,?,1) "
        "ON CONFLICT(chat_id,user_id,day) DO UPDATE SET cnt=cnt+1",
        (chat_id, user_id, today_str()))
    conn.commit()


def stat_today(chat_id, user_id):
    r = conn.execute("SELECT cnt FROM stats WHERE chat_id=? AND user_id=? AND day=?",
                     (chat_id, user_id, today_str())).fetchone()
    return r[0] if r else 0


def stat_week(chat_id, user_id):
    mon, sun = week_bounds()
    return conn.execute("SELECT COALESCE(SUM(cnt),0) FROM stats WHERE chat_id=? AND user_id=? AND day BETWEEN ? AND ?",
                        (chat_id, user_id, mon, sun)).fetchone()[0]


def stat_total(chat_id, user_id):
    return conn.execute("SELECT COALESCE(SUM(cnt),0) FROM stats WHERE chat_id=? AND user_id=?",
                        (chat_id, user_id)).fetchone()[0]


def norm_reward_due(chat_id, user_id):
    """True, если норма выполнена и LP за неё на этой неделе ещё не выдавались."""
    if WEEKLY_MSG_NORM <= 0 or user_id in EXCLUDED_IDS:
        return False
    if stat_week(chat_id, user_id) < WEEKLY_MSG_NORM:
        return False
    mon, _ = week_bounds()
    row = get_user(chat_id, user_id)
    return not (row and row["norm_week"] == mon)


def mark_norm(chat_id, user_id):
    mon, _ = week_bounds()
    conn.execute("UPDATE users SET norm_week=? WHERE chat_id=? AND user_id=?", (mon, chat_id, user_id))
    conn.commit()


def super_reward_due(chat_id, user_id):
    """True, если набран сверх-порог сообщений и бонус на этой неделе ещё не выдан."""
    if SUPER_NORM_MSGS <= 0 or user_id in EXCLUDED_IDS:
        return False
    if stat_week(chat_id, user_id) < SUPER_NORM_MSGS:
        return False
    mon, _ = week_bounds()
    row = get_user(chat_id, user_id)
    return not (row and row["super_week"] == mon)


def mark_super(chat_id, user_id):
    mon, _ = week_bounds()
    conn.execute("UPDATE users SET super_week=? WHERE chat_id=? AND user_id=?", (mon, chat_id, user_id))
    conn.commit()


# ============================================================
#                   ВСПОМОГАТЕЛЬНОЕ
# ============================================================
def esc(s):
    return html.escape(str(s or ""))


def pe(name):
    """Премиум-эмодзи по имени из PREMIUM_EMOJI. Возвращает HTML <tg-emoji> или запасной смайлик."""
    item = PREMIUM_EMOJI.get(name)
    if not item:
        return ""
    eid, fb = item
    if PREMIUM_EMOJI_ENABLED and eid:
        return f'<tg-emoji emoji-id="{eid}">{fb}</tg-emoji>'
    return fb


# Обычные эмодзи -> имя премиум-эмодзи (для авто-замены в истории изменений и т.п.)
_PLAIN_TO_PREMIUM = {
    "🎨": "star", "🎮": "game", "📅": "calendar", "🏅": "cup", "📑": "folder",
    "🚫": "cross", "👤": "person", "📊": "stats", "💬": "msg", "⚖️": "scales",
    "⚖": "scales", "🛡": "shield", "🤖": "robot", "🪙": "lp", "🛒": "shop",
    "🧮": "stats", "🐞": "check", "💾": "folder", "📜": "scroll", "🔧": "shield",
    "🚀": "growth", "🔥": "fire", "⭐": "star", "🏆": "cup", "📌": "pin",
}


def premiumize(text):
    """Заменяет обычные эмодзи в тексте на премиум-версии (где есть соответствие)."""
    for plain, nm in _PLAIN_TO_PREMIUM.items():
        if plain in text:
            text = text.replace(plain, pe(nm))
    return text


def fmt_lp(v):
    """Красиво показывает LP: целые без точки (120), дробные с нужными знаками (12.4)."""
    try:
        v = round(float(v or 0), 2)
    except (TypeError, ValueError):
        return "0"
    if v == int(v):
        return str(int(v))
    return f"{v:.2f}".rstrip("0").rstrip(".")


def fmt_num(n):
    """Число с разделителями тысяч: 45000 -> 45 000."""
    try:
        return f"{int(n):,}".replace(",", " ")
    except (TypeError, ValueError):
        return str(n)


def _days_word(n):
    """Склонение: 1 день, 2 дня, 5 дней."""
    n = abs(int(n))
    if 11 <= n % 100 <= 14:
        return "дней"
    d = n % 10
    if d == 1:
        return "день"
    if 2 <= d <= 4:
        return "дня"
    return "дней"


# Вехи прогресс-бара (по логике покупки, а не по цене): сначала Brawl Pass.
# «Улучшение до BP+» не веха — это докупка к уже имеющемуся Brawl Pass.
STORE_REWARDS = [(850, "Brawl Pass"), (1650, "Brawl Pass Plus"), (2800, "Pro Pass")]

# Товары для покупки по кнопке: (ключ, название, цена в LP, имя премиум-эмодзи).
STORE_ITEMS = [
    ("bp", "Brawl Pass", 850, "bp"),
    ("bpup", "Улучшение до Brawl Pass Plus", 750, "bpup"),
    ("bpplus", "Brawl Pass Plus", 1650, "bpplus"),
    ("pro", "Pro Pass", 2800, "pro"),
]
STORE_ITEMS_BY_KEY = {k: (k, n, p, e) for (k, n, p, e) in STORE_ITEMS}
# Кто принимает/выдаёт заказы (получает заявки в ЛС): владелец + менеджер магазина.
STORE_ADMINS = {LEADER_ID, 485558746}


def next_reward(lp):
    """Ближайшая награда, до которой ещё не хватает LP. None — если на всё уже хватает."""
    for cost, nm in STORE_REWARDS:
        if lp < cost:
            return cost, nm
    return None


def progress_bar(current, target, width=10):
    """Текстовый прогресс-бар: ▰▰▰▱▱▱▱▱▱▱"""
    if target <= 0:
        target = 1
    filled = max(0, min(width, round(current / target * width)))
    return "▰" * filled + "▱" * (width - filled)


def progress_line(lp):
    """Строка прогресса до ближайшей награды (или сообщение, что на всё хватает)."""
    lp = max(0, lp)
    nr = next_reward(lp)
    if not nr:
        return f"{pe('cup')} На все товары STORE уже хватает!"
    cost, nm = nr
    pct = int(min(100, lp / cost * 100))
    return (f"{pe('star')} До «{nm}»: <b>{fmt_lp(max(0, cost - lp))}</b> LP\n"
            f"{progress_bar(lp, cost)} {pct}%")


# ============================================================
#                   BRAWL STARS API (привязка тега)
# ============================================================
_brawl_cache = {}                  # TAG -> (данные, время)
BRAWL_TAG_RE = re.compile(r"[0289PYLQGRJCUV]{3,12}")


async def brawl_fetch(tag):
    """Запрос данных игрока Brawl Stars (через прокси RoyaleAPI). None при ошибке/без ключа."""
    if not BRAWL_API_KEY:
        return None
    t = (tag or "").strip().upper().lstrip("#")
    url = f"{BRAWL_API_BASE}/players/%23{t}"
    headers = {"Authorization": f"Bearer {BRAWL_API_KEY}", "Accept": "application/json"}
    try:
        async with httpx.AsyncClient(timeout=20) as client:
            r = await client.get(url, headers=headers)
        if r.status_code == 200:
            data = r.json()
            _brawl_cache[t] = (data, time.time())
            return data
        print(f"[BRAWL] HTTP {r.status_code} для тега #{t}", flush=True)
    except Exception as e:
        print(f"[BRAWL] ошибка запроса: {e}", flush=True)
    return None


# ===== 5.6 «РАЗВЕДКА»: ранговый режим Brawl Stars (ОФИЦИАЛЬНО — карта подтверждена боем 12.06.2026) =====
# Официальный API ранг игрока не отдаёт. Но в журнале боёв ранговые матчи помечены типом
# "soloRanked", и поле trophies игрока в ТАКОМ бою кодирует ступень ранга, а не кубки.
# Карта ступеней ниже — предположительная (так делают сторонние трекеры); сверяется живыми
# боями через команду «ранг тест». При расхождении правим ТОЛЬКО списки/формулу здесь.
_RANK_TIERS = ["Бронза", "Серебро", "Золото", "Алмаз", "Мифик", "Легенда", "Мастер", "Про"]
_ROMAN = {1: "I", 2: "II", 3: "III"}
LEGEND_VALUE_MIN = 16   # «Легенда I» по предположительной карте (для будущей нормы)


def rank_name(v):
    """Название ступени ранга по «сырому» значению из журнала боёв."""
    try:
        v = int(v)
    except Exception:
        return "?"
    if v <= 0:
        return "без ранга"
    tier, step = (v - 1) // 3, (v - 1) % 3 + 1
    if tier < len(_RANK_TIERS):
        return f"{_RANK_TIERS[tier]} {_ROMAN[step]}"
    return f"ступень {v}"


async def brawl_battlelog(tag):
    """Журнал последних боёв игрока (до 25). None при ошибке/без ключа."""
    if not BRAWL_API_KEY:
        return None
    t = (tag or "").strip().upper().lstrip("#")
    url = f"{BRAWL_API_BASE}/players/%23{t}/battlelog"
    headers = {"Authorization": f"Bearer {BRAWL_API_KEY}", "Accept": "application/json"}
    try:
        async with httpx.AsyncClient(timeout=20) as client:
            r = await client.get(url, headers=headers)
        if r.status_code == 200:
            return r.json()
        print(f"[BRAWL] battlelog HTTP {r.status_code} для #{t}", flush=True)
    except Exception as e:
        log_error("battlelog", e)
    return None


def extract_ranked_entries(data, tag):
    """Достаёт из журнала боёв ранговые матчи (soloRanked) и «сырое» значение ранга игрока.
    Возвращает список (новые сверху): {ts, mode, map, result, value}."""
    t = "#" + (tag or "").strip().upper().lstrip("#")
    out = []
    for it in (data or {}).get("items", []) or []:
        b = it.get("battle") or {}
        if b.get("type") != "soloRanked":
            continue
        val = None
        groups = b.get("teams") or ([b.get("players")] if b.get("players") else [])
        for team in groups:
            for p in team or []:
                if (p.get("tag") or "").upper() == t:
                    val = (p.get("brawler") or {}).get("trophies")
        if val is None:
            continue
        ev = it.get("event") or {}
        out.append({"ts": it.get("battleTime", ""), "mode": ev.get("mode", "?"),
                    "map": ev.get("map", "?"), "result": b.get("result", "?"), "value": int(val)})
    return out


def _bs_dt(ts):
    """'20260611T120000.000Z' → '11.06 12:00' (UTC)."""
    try:
        return f"{ts[6:8]}.{ts[4:6]} {ts[9:11]}:{ts[11:13]}"
    except Exception:
        return "?"


RANK_TIER_REWARDS = {3: 20, 4: 40, 5: 70, 6: 100, 7: 150}   # Алмаз/Мифик/Легенда/Мастер/Про — LP за ПЕРВЫЙ в жизни выход


def rank_tier(v):
    try:
        v = int(v)
    except Exception:
        return -1
    return (v - 1) // 3 if v > 0 else -1


def apply_rank_progress(chat_id, user_id, value):
    """5.6: обновляет лучший ранг и выдаёт разовые награды за новые ступени (с Алмаза).
    Возвращает (лучшее_значение, [(ступень, lp), ...]). Награды не выдаются повторно даже после смены тега."""
    row = conn.execute("SELECT rank_best, COALESCE(rank_paid_tier,-1) AS paid FROM users WHERE chat_id=? AND user_id=?",
                       (chat_id, user_id)).fetchone()
    if not row:
        return None, []
    best = row["rank_best"]
    if best is None or value > best:
        best = value
        conn.execute("UPDATE users SET rank_best=?, rank_seen=? WHERE chat_id=? AND user_id=?",
                     (best, today_str(), chat_id, user_id))
    paid, new_tier, awards = row["paid"], rank_tier(best), []
    for t in range(max(paid + 1, min(RANK_TIER_REWARDS)), new_tier + 1):
        if t in RANK_TIER_REWARDS:
            add_lp(chat_id, user_id, RANK_TIER_REWARDS[t])
            awards.append((_RANK_TIERS[t], RANK_TIER_REWARDS[t]))
    if new_tier > paid:
        conn.execute("UPDATE users SET rank_paid_tier=? WHERE chat_id=? AND user_id=?", (new_tier, chat_id, user_id))
    conn.commit()
    return best, awards


def brawl_cached(tag):
    item = _brawl_cache.get((tag or "").strip().upper().lstrip("#"))
    if item and (time.time() - item[1] < BRAWL_CACHE_TTL):
        return item[0]
    return None


def brawl_profile_line(chat_id, user_id):
    """Строка с тегом для профиля (кэш или последние сохранённые кубки)."""
    row = get_user(chat_id, user_id)
    tag = (row["brawl_tag"] if row and "brawl_tag" in row.keys() else None)
    if not tag:
        return ""
    data = brawl_cached(tag)
    trophies = data.get("trophies", 0) if data else (row["brawl_trophies"] if (row and "brawl_trophies" in row.keys()) else 0)
    highest = data.get("highestTrophies", 0) if data else 0
    line = (f"\n\n<blockquote>{pe('game')} <b>Brawl Stars</b> · #{esc(tag)}\n"
            f"{pe('cup')} Кубки: <b>{fmt_num(trophies)}</b>")
    if highest:
        line += f" · рекорд <b>{fmt_num(highest)}</b>"
    line += "</blockquote>"
    return line


def brawl_card(data, tag, bound=False):
    name = esc(data.get("name", "?"))
    club = data.get("club") or {}
    club_name = esc(club.get("name")) if club.get("name") else "—"
    head = f"{pe('check')} <b>Тег привязан!</b>" if bound else f"{pe('game')} <b>ПРОФИЛЬ BRAWL STARS</b>"
    norm_note = (f"\n<i>Следи за нормой клана: прирост +{WEEKLY_TROPHY_NORM} кубков в неделю.</i>"
                 if today_str() >= NORMS_START_DATE else "")
    return (f"{head}\n{CLAN_LINE}\n"
            f"{pe('game')} <b>{name}</b> · #{esc(tag)}\n"
            f"<blockquote>{pe('cup')} Кубки: <b>{fmt_num(data.get('trophies', 0))}</b>\n"
            f"{pe('star')} Рекорд кубков: <b>{fmt_num(data.get('highestTrophies', 0))}</b>\n"
            f"{pe('crown')} Уровень: <b>{data.get('expLevel', '?')}</b>\n"
            f"{pe('shield')} Клуб: <b>{club_name}</b></blockquote>{norm_note}")


def _norm_nick(s):
    """Нормализует ник для сравнения: нижний регистр, только буквы/цифры (срезает пробелы, эмодзи, знаки)."""
    return re.sub(r"[^0-9a-zа-яё]+", "", (s or "").lower())


def _nick_from_title(title):
    """Достаёт игровой ник из приписки в чате: 'LT | babah' -> 'babah'."""
    if not title:
        return ""
    return re.split(r"[|:/\\]", title)[-1].strip()


async def member_custom_title(context, user_id):
    """Кастомный титул (приписка) участника в чате клана — есть только у админов. '' если нет/ошибка."""
    try:
        m = await context.bot.get_chat_member(ALLOWED_CHAT_ID, user_id)
        return getattr(m, "custom_title", "") or ""
    except Exception:
        return ""


async def cmd_brawl(update, context, parts):
    user = update.effective_user
    chat_id = update.effective_chat.id
    if not BRAWL_API_KEY:
        await update.message.reply_text(
            f"{pe('game')} Привязка тега скоро заработает — функция ещё настраивается администрацией.",
            parse_mode=ParseMode.HTML)
        return True
    # с аргументом → привязать тег
    if len(parts) > 1:
        tag = parts[1].strip().upper().lstrip("#")
        if not BRAWL_TAG_RE.fullmatch(tag):
            await update.message.reply_text(
                "Похоже, тег неверный. Пример: <code>.tag #2PP0LC</code> — тег виден в профиле игры под именем.",
                parse_mode=ParseMode.HTML)
            return True
        await context.bot.send_chat_action(chat_id, "typing")
        data = await brawl_fetch(tag)
        if not data:
            await update.message.reply_text(
                "Не нашёл такого игрока 😕 Проверь тег (он в профиле Brawl Stars, начинается с #).")
            return True
        ensure_user(chat_id, user)
        # --- Проверки владения тегом (владелец клана — без проверок) ---
        if user.id != LEADER_ID:
            # 1) тег должен состоять в клубе LINKOR (отсекает чужие/про-аккаунты)
            club_tag = ((data.get("club") or {}).get("tag") or "").upper().lstrip("#")
            if LINKOR_CLUB_TAG and club_tag != LINKOR_CLUB_TAG:
                await update.message.reply_text(
                    f"{pe('cross')} Этот тег <b>не состоит в клубе {CLAN_NAME}</b> в игре. "
                    f"Привязать можно только свой аккаунт, который сейчас в клубе — вступи в клуб и попробуй снова.",
                    parse_mode=ParseMode.HTML)
                return True
            # 2) игровой ник должен совпадать с твоей припиской «LT | ник» в чате (отсекает чужой тег своего клана)
            if TAG_VERIFY_BY_TITLE:
                nick = _nick_from_title(await member_custom_title(context, user.id))
                gname = data.get("name") or ""
                if nick and _norm_nick(nick) != _norm_nick(gname):
                    await update.message.reply_text(
                        f"{pe('cross')} Игровой ник аккаунта (<b>{esc(gname)}</b>) не совпадает с твоей "
                        f"припиской в чате (<b>{esc(nick)}</b>). Привязать можно только <b>свой</b> профиль.\n"
                        f"Если ник в игре изменился — попроси администратора обновить твою приписку.",
                        parse_mode=ParseMode.HTML)
                    return True
        prev = conn.execute("SELECT brawl_tag FROM users WHERE chat_id=? AND user_id=?",
                            (chat_id, user.id)).fetchone()
        prev_tag = ((prev["brawl_tag"] or "") if prev else "").upper().lstrip("#")
        tro = int(data.get("trophies", 0))
        conn.execute("UPDATE users SET brawl_tag=?, brawl_trophies=?, brawl_name=?, tag_warns=0, tag_warn_last=NULL WHERE chat_id=? AND user_id=?",
                     (tag, tro, data.get("name"), chat_id, user.id))
        mon_w = week_bounds()[0]
        if prev_tag and prev_tag != tag:
            # 5.6: сменился аккаунт — старая база кубков бессмысленна; ранг обнуляется (награды повторно НЕ выдаются)
            conn.execute("UPDATE users SET trophy_base=?, trophy_base_week=?, rank_best=NULL, rank_seen=NULL "
                         "WHERE chat_id=? AND user_id=?", (tro, mon_w, chat_id, user.id))
        else:
            # 5.6: КРИТФИКС — перепривязка того же тега базу недели НЕ трогает (раньше прирост кубков терялся);
            # если базы этой недели ещё нет — отсчёт честно начинается с этого момента
            conn.execute("UPDATE users SET trophy_base=?, trophy_base_week=? WHERE chat_id=? AND user_id=? "
                         "AND (trophy_base_week IS NULL OR trophy_base_week<>?)",
                         (tro, mon_w, chat_id, user.id, mon_w))
        conn.commit()
        await update.message.reply_text(brawl_card(data, tag, bound=True), parse_mode=ParseMode.HTML)
        return True
    # без аргумента → показать свой
    row = get_user(chat_id, user.id)
    tag = row["brawl_tag"] if (row and "brawl_tag" in row.keys()) else None
    if not tag:
        await update.message.reply_text(
            f"{pe('game')} У тебя не привязан игровой тег. Привяжи: <code>.tag #ТВОЙТЕГ</code> "
            f"(тег виден в профиле Brawl Stars под именем).", parse_mode=ParseMode.HTML)
        return True
    await context.bot.send_chat_action(chat_id, "typing")
    data = await brawl_fetch(tag) or brawl_cached(tag)
    if not data:
        await update.message.reply_text("Не получилось получить данные сейчас, попробуй позже.")
        return True
    conn.execute("UPDATE users SET brawl_trophies=?, brawl_name=? WHERE chat_id=? AND user_id=?",
                 (int(data.get("trophies", 0)), data.get("name"), chat_id, user.id))
    conn.commit()
    await update.message.reply_text(brawl_card(data, tag, bound=False), parse_mode=ParseMode.HTML)
    return True


def mention(user):
    """HTML-упоминание, которое пингует участника (работает даже без @юзернейма)."""
    name = esc(getattr(user, "full_name", None) or getattr(user, "first_name", None) or "участник")
    return f'<a href="tg://user?id={user.id}">{name}</a>'


# Подмена латиницы/цифр на похожие кириллические буквы (обход фильтра вида "пид0р", "xуй")
_LEET = str.maketrans({
    "a": "а", "e": "е", "o": "о", "p": "р", "c": "с", "y": "у", "x": "х", "k": "к",
    "b": "б", "m": "м", "h": "н", "t": "т", "0": "о", "3": "з", "4": "ч", "6": "б",
    "@": "а", "$": "с", "1": "и",
})


def normalize_text(t):
    """Приводит к нижнему регистру, заменяет латиницу/цифры и убирает все не-буквы."""
    t = t.lower().translate(_LEET)
    return "".join(ch for ch in t if ch.isalpha())


def has_banned(text):
    norm = normalize_text(text)
    return any(w in norm for w in BANNED_WORDS)


def real_reply(msg):
    """Возвращает сообщение-ответ ТОЛЬКО если это настоящий ответ пользователя.
    В форум-топиках «ответом» считается корневое сообщение темы (message_thread_id),
    хотя человек никому не отвечал — такой авто-ответ игнорируем, иначе бот путает,
    кому начислять LP и чей баланс показывать."""
    if not msg:
        return None
    r = getattr(msg, "reply_to_message", None)
    if not r:
        return None
    tid = getattr(msg, "message_thread_id", None)
    if tid and getattr(r, "message_id", None) == tid:
        return None                      # это привязка к корню темы, а не ответ
    if getattr(r, "forum_topic_created", None):
        return None                      # ответ на служебное сообщение создания темы
    return r


def target_user(update):
    r = real_reply(update.message)
    return r.from_user if r else None


def resolve_target(update, parts):
    """Находит участника: по НАСТОЯЩЕМУ ответу, по @юзернейму или по ID (из базы бота)."""
    msg = update.message
    rr = real_reply(msg)
    if rr:
        return rr.from_user
    chat_id = update.effective_chat.id
    for p in parts[1:]:
        if p.startswith("@") and len(p) > 1:
            row = conn.execute("SELECT user_id, username, name FROM users WHERE chat_id=? AND lower(username)=?",
                               (chat_id, p[1:].lower())).fetchone()
            if row:
                return SimpleNamespace(id=row["user_id"], username=row["username"],
                                       full_name=row["name"] or f"@{row['username']}")
            return None
        if p.isdigit():
            row = conn.execute("SELECT user_id, username, name FROM users WHERE chat_id=? AND user_id=?",
                               (chat_id, int(p))).fetchone()
            if row:
                return SimpleNamespace(id=row["user_id"], username=row["username"],
                                       full_name=row["name"] or str(row["user_id"]))
    return None


def target_specified(parts):
    """Указал ли пользователь @юзернейм или ID (даже если не нашёлся)."""
    return any(p.startswith("@") or p.isdigit() for p in parts[1:])


async def read_target(update, context, parts):
    """Кого показывать в ЧИТАЮЩИХ командах (лп, профиль).
    По умолчанию — сам отправитель. По @юзеру/ID — указанного (любой может посмотреть).
    По НАСТОЯЩЕМУ ответу — только если отправитель модератор+ (чтобы участники не путались)."""
    sender = update.effective_user
    if target_specified(parts):
        return resolve_target(update, parts) or False     # False = указан, но не найден
    if real_reply(update.message):
        if await eff_role(update, context) >= MODER:
            return real_reply(update.message).from_user
    return sender


async def eff_role(update, context, user_id=None):
    chat_id = update.effective_chat.id
    uid = user_id or update.effective_user.id
    db_role = get_role(chat_id, uid)
    try:
        m = await context.bot.get_chat_member(chat_id, uid)
        # Владелец/админ чата получают максимум ADMIN. Роль ЛИДЕР выдаётся
        # только явно (по ID в SEED_ROLES_BY_ID), чтобы владелец чата
        # не становился автоматически лидером клана.
        tg = ADMIN if m.status in (ChatMemberStatus.OWNER, ChatMemberStatus.ADMINISTRATOR) else MEMBER
    except Exception:
        tg = MEMBER
    return max(db_role, tg)


async def require(update, context, level):
    if await eff_role(update, context) < level:
        await update.message.reply_text("⛔ У тебя недостаточно прав для этого.")
        return False
    return True


def parse_duration(s):
    m = re.fullmatch(r"(\d+)\s*([мmчhдd]?)", s.strip().lower())
    if not m:
        return None
    val, unit = int(m.group(1)), m.group(2)
    if unit in ("ч", "h"):
        return val * 60
    if unit in ("д", "d"):
        return val * 60 * 24
    return val


def fmt_duration(minutes):
    """Красивый вывод длительности: 30 мин · 12 ч · 7 дн (без дробей, где не нужно)."""
    minutes = int(minutes)
    if minutes % (60 * 24) == 0 and minutes >= 60 * 24:
        return f"{minutes // (60 * 24)} дн"
    if minutes % 60 == 0 and minutes >= 60:
        return f"{minutes // 60} ч"
    if minutes >= 60:
        return f"{round(minutes / 60, 1)} ч"
    return f"{minutes} мин"


FULL_PERMS = ChatPermissions(
    can_send_messages=True, can_send_polls=True, can_send_other_messages=True,
    can_add_web_page_previews=True, can_send_audios=True, can_send_documents=True,
    can_send_photos=True, can_send_videos=True, can_send_video_notes=True,
    can_send_voice_notes=True)


async def do_mute(context, chat_id, user_id, minutes):
    until = msk_now() + timedelta(minutes=minutes)
    await context.bot.restrict_chat_member(
        chat_id, user_id, permissions=ChatPermissions(can_send_messages=False), until_date=until)
    inc_mutes(chat_id, user_id)


async def do_exclude(context, chat_id, user_id):
    await context.bot.ban_chat_member(chat_id, user_id)
    await context.bot.unban_chat_member(chat_id, user_id)


# --- Анти-рейд ---
_recent_joins = {}   # chat_id -> [timestamps]
_raid_until = {}     # chat_id -> unix-время до конца локдауна


async def check_raid(context, chat_id, member):
    """Фиксирует вход; при наплыве включает защиту. Возвращает True, если действует локдаун
    (входящего стоит ограничить и не приветствовать как обычно)."""
    now = time.time()
    js = [t for t in _recent_joins.get(chat_id, []) if now - t < RAID_WINDOW]
    js.append(now)
    _recent_joins[chat_id] = js
    locked = now < _raid_until.get(chat_id, 0)
    if not locked and len(js) >= RAID_JOIN_COUNT:
        _raid_until[chat_id] = now + RAID_LOCKDOWN_MIN * 60
        locked = True
        try:
            await context.bot.send_message(
                chat_id,
                f"{pe('shield')} <b>Анти-рейд: наплыв входов</b> ({len(js)} за минуту).\n"
                f"Новые участники на {RAID_LOCKDOWN_MIN} мин в режиме «только чтение» до проверки админом. "
                f"Снять ограничение: <code>unmute</code> ответом на сообщение участника.",
                parse_mode=ParseMode.HTML)
        except Exception:
            pass
        for admin_id in ({LEADER_ID} | CONTEST_CREATORS):
            try:
                await context.bot.send_message(
                    admin_id, f"{pe('warn')} Возможный рейд: {len(js)} входов за минуту. "
                    f"Защита включена на {RAID_LOCKDOWN_MIN} мин — новые ограничены.", parse_mode=ParseMode.HTML)
            except Exception:
                pass
    if locked:
        try:
            await context.bot.restrict_chat_member(chat_id, member.id, permissions=ChatPermissions(can_send_messages=False))
        except Exception:
            pass
    return locked


async def check_tag_gate(update, context, user):
    """Тег-гейт для новичков: нет тега → каждый день тег-варн; 3 дня без тега → исключение."""
    chat_id = update.effective_chat.id
    if user.id == LEADER_ID or user.id in CONTEST_CREATORS or user.id in EXCLUDED_IDS:
        return
    row = get_user(chat_id, user.id)
    if not row or not row["tag_gate"]:
        return                                           # не новичок (старичков гейт не трогает)
    if row["brawl_tag"]:
        # тег есть — снимаем тег-варны, если оставались
        if row["tag_warns"]:
            conn.execute("UPDATE users SET tag_warns=0, tag_warn_last=NULL WHERE chat_id=? AND user_id=?", (chat_id, user.id))
            conn.commit()
        return
    today = today_str()
    if row["tag_warn_last"] == today:
        return                                           # сегодня уже предупреждали
    n = (row["tag_warns"] or 0) + 1
    conn.execute("UPDATE users SET tag_warns=?, tag_warn_last=? WHERE chat_id=? AND user_id=?", (n, today, chat_id, user.id))
    conn.commit()
    if n >= 3:
        try:
            await do_exclude(context, chat_id, user.id)
        except Exception:
            pass
        conn.execute("UPDATE users SET tag_warns=0, tag_warn_last=NULL, tag_gate=0 WHERE chat_id=? AND user_id=?", (chat_id, user.id))
        conn.commit()
        try:
            await update.message.reply_text(
                f"{pe('ban')} <b>{esc(user.full_name)}</b> исключён: не привязал игровой тег за 3 дня (обязательное правило).",
                parse_mode=ParseMode.HTML)
        except Exception:
            pass
    else:
        try:
            await update.message.reply_text(
                f"{pe('warn')} {mention(user)}, привяжи игровой тег: <code>.tag #ТВОЙ_ТЕГ</code>. "
                f"Это обязательно. Тег-варн <b>{n}/3</b> — на 3-й день без тега будет авто-исключение.",
                parse_mode=ParseMode.HTML)
        except Exception:
            pass


def fmt_date(iso):
    try:
        return datetime.fromisoformat(iso).strftime("%d.%m.%Y")
    except Exception:
        return "—"


# ============================================================
#                   ДЕЙСТВИЯ МОДЕРАЦИИ
# ============================================================
def log_action(update, target, action, reason=""):
    """Записывает действие модерации в журнал (modlog)."""
    try:
        admin = update.effective_user
        conn.execute(
            "INSERT INTO modlog (chat_id, admin_id, admin_name, target_id, target_name, action, reason, ts) "
            "VALUES (?,?,?,?,?,?,?,?)",
            (update.effective_chat.id, admin.id, getattr(admin, "full_name", ""),
             getattr(target, "id", None), getattr(target, "full_name", ""), action, reason or "", msk_now().isoformat()))
        conn.commit()
    except Exception:
        pass


async def act_warn(update, context, t, reason):
    chat_id = update.effective_chat.id
    ensure_user(chat_id, t)
    log_action(update, t, "варн", reason)
    n = add_warn(chat_id, t.id)
    bal = add_lp(chat_id, t.id, -WARN_LP_PENALTY)
    pen = f"\n{pe('lp')} Списано {WARN_LP_PENALTY} LP (баланс: {fmt_lp(bal)})"
    if n >= MAX_WARNS:
        clear_warns(chat_id, t.id)
        await do_exclude(context, chat_id, t.id)
        await update.message.reply_text(
            f"{pe('ban')} <b>{esc(t.full_name)}</b> получил 3-й варн и исключён (кодекс 4.3).\nПричина: {esc(reason)}{pen}",
            parse_mode=ParseMode.HTML)
    else:
        await update.message.reply_text(
            f"{pe('warn')} <b>{esc(t.full_name)}</b> — варн {n}/{MAX_WARNS}.\nПричина: {esc(reason)}{pen}\n"
            f"Снимется через {WARN_LIFETIME_DAYS} дней.", parse_mode=ParseMode.HTML)


async def act_unwarn(update, context, t, reason=""):
    chat_id = update.effective_chat.id
    ensure_user(chat_id, t)
    log_action(update, t, "снят варн", reason)
    before = count_warns(chat_id, t.id)
    n = remove_one_warn(chat_id, t.id)
    extra = f"\nПричина: {esc(reason)}" if reason else ""
    if before > 0:
        # варн реально сняли — возвращаем списанные за него LP
        bal = add_lp(chat_id, t.id, WARN_LP_PENALTY)
        ref = f"\n{pe('lp')} Возвращено {WARN_LP_PENALTY} LP (баланс: {fmt_lp(bal)})"
        await update.message.reply_text(
            f"{pe('check')} Снят варн у <b>{esc(t.full_name)}</b>. Осталось: {n}/{MAX_WARNS}.{ref}{extra}",
            parse_mode=ParseMode.HTML)
    else:
        await update.message.reply_text(
            f"{pe('catneutral')} У <b>{esc(t.full_name)}</b> нет активных варнов.", parse_mode=ParseMode.HTML)


async def act_mute(update, context, t, minutes, reason):
    log_action(update, t, f"мут {fmt_duration(minutes)}", reason)
    await do_mute(context, update.effective_chat.id, t.id, minutes)
    await update.message.reply_text(
        f"{pe('mute')} <b>{esc(t.full_name)}</b> получил мут на {fmt_duration(minutes)}.\nПричина: {esc(reason)}", parse_mode=ParseMode.HTML)


async def act_unmute(update, context, t, reason=""):
    log_action(update, t, "снят мут", reason)
    await context.bot.restrict_chat_member(update.effective_chat.id, t.id, permissions=FULL_PERMS)
    extra = f"\nПричина: {esc(reason)}" if reason else ""
    await update.message.reply_text(f"{pe('unmute')} С <b>{esc(t.full_name)}</b> снят мут.{extra}", parse_mode=ParseMode.HTML)


async def act_ban(update, context, t, reason):
    log_action(update, t, "бан", reason)
    await context.bot.ban_chat_member(update.effective_chat.id, t.id)
    await update.message.reply_text(
        f"{pe('ban')} <b>{esc(t.full_name)}</b> забанен.\nПричина: {esc(reason)}", parse_mode=ParseMode.HTML)


async def act_unban(update, context, t, reason=""):
    log_action(update, t, "разбан", reason)
    await context.bot.unban_chat_member(update.effective_chat.id, t.id)
    extra = f"\nПричина: {esc(reason)}" if reason else ""
    await update.message.reply_text(f"{pe('unban')} <b>{esc(t.full_name)}</b> разбанен.{extra}", parse_mode=ParseMode.HTML)


async def act_kick(update, context, t):
    log_action(update, t, "кик", "")
    await do_exclude(context, update.effective_chat.id, t.id)
    await update.message.reply_text(f"{pe('unban')} <b>{esc(t.full_name)}</b> исключён (без бана).", parse_mode=ParseMode.HTML)


# ============================================================
#                   ПРОФИЛЬ / LP / РЕПУТАЦИЯ / ТОП
# ============================================================
_CARD_CYR_FONT = None        # путь к найденному шрифту с кириллицей (или None)
_CARD_FONT_CHECKED = False

_TRANSLIT = {
    'а': 'a', 'б': 'b', 'в': 'v', 'г': 'g', 'д': 'd', 'е': 'e', 'ё': 'e', 'ж': 'zh',
    'з': 'z', 'и': 'i', 'й': 'y', 'к': 'k', 'л': 'l', 'м': 'm', 'н': 'n', 'о': 'o',
    'п': 'p', 'р': 'r', 'с': 's', 'т': 't', 'у': 'u', 'ф': 'f', 'х': 'h', 'ц': 'ts',
    'ч': 'ch', 'ш': 'sh', 'щ': 'sch', 'ъ': '', 'ы': 'y', 'ь': '', 'э': 'e', 'ю': 'yu', 'я': 'ya',
}


def _translit(s):
    """Кириллица → латиница (для карты, если на хосте нет русского шрифта)."""
    out = []
    for ch in (s or ""):
        low = ch.lower()
        if low in _TRANSLIT:
            r = _TRANSLIT[low]
            out.append(r.upper() if ch.isupper() else r)
        else:
            out.append(ch)
    return "".join(out)


def _find_cyr_font():
    """Ищет TTF с кириллицей: рядом с ботом, затем в системе. Кэширует путь. None если не найден."""
    global _CARD_CYR_FONT, _CARD_FONT_CHECKED
    if _CARD_FONT_CHECKED:
        return _CARD_CYR_FONT
    _CARD_FONT_CHECKED = True
    here = os.path.dirname(os.path.abspath(__file__))
    candidates = [
        os.path.join(here, "DejaVuSans.ttf"), os.path.join(here, "font.ttf"),
        os.path.join(here, "arial.ttf"), os.path.join(here, "Roboto-Regular.ttf"),
        "/usr/share/fonts/truetype/dejavu/DejaVuSans.ttf",
        "/usr/share/fonts/truetype/liberation/LiberationSans-Regular.ttf",
        "/usr/share/fonts/truetype/freefont/FreeSans.ttf",
    ]
    from PIL import ImageFont
    for p in candidates:
        try:
            if os.path.exists(p):
                f = ImageFont.truetype(p, 24)
                # проверяем, что в шрифте есть кириллица (буква «А»)
                if f.getmask("А").getbbox() is not None:
                    _CARD_CYR_FONT = p
                    return p
        except Exception:
            continue
    return None


def _card_font(size, bold=False):
    """Шрифт для карты. Если есть кириллический TTF — берём его (и Bold-вариант рядом), иначе дефолт PIL."""
    from PIL import ImageFont
    base = _find_cyr_font()
    if base:
        path = base
        if bold:
            cand = base.replace("DejaVuSans.ttf", "DejaVuSans-Bold.ttf").replace("-Regular", "-Bold").replace("FreeSans", "FreeSansBold")
            if os.path.exists(cand):
                path = cand
        try:
            return ImageFont.truetype(path, size)
        except Exception:
            pass
    # система: классические жирные пути
    paths = (["/usr/share/fonts/truetype/dejavu/DejaVuSans-Bold.ttf"] if bold
             else ["/usr/share/fonts/truetype/dejavu/DejaVuSans.ttf"])
    for p in paths:
        try:
            return ImageFont.truetype(p, size)
        except Exception:
            continue
    try:
        return ImageFont.load_default(size)
    except Exception:
        return ImageFont.load_default()


def _ct(s):
    """Текст для карты: оставляем кириллицу если шрифт её поддерживает, иначе транслитерируем."""
    return s if _find_cyr_font() else _translit(s)


def render_profile_card(chat_id, user_id, display_name):
    """6.0: рисует карточку профиля (тёмно-золотой стиль клана) и возвращает BytesIO PNG. None при сбое."""
    try:
        from PIL import Image, ImageDraw
        import io
        row = get_user(chat_id, user_id)
        if not row:
            return None
        W, H = 900, 520
        bg = (16, 18, 24)
        gold = (212, 175, 55)
        white = (235, 238, 245)
        grey = (150, 156, 168)
        img = Image.new("RGB", (W, H), bg)
        d = ImageDraw.Draw(img)
        # рамка-золото
        d.rectangle([6, 6, W - 7, H - 7], outline=gold, width=3)
        d.rectangle([14, 14, W - 15, H - 15], outline=(60, 55, 30), width=1)
        # шапка
        d.text((40, 32), CLAN_NAME.upper(), font=_card_font(36, True), fill=gold)
        d.text((42, 82), "PROFILE CARD", font=_card_font(17, False), fill=grey)
        d.line([40, 116, W - 40, 116], fill=(60, 55, 30), width=2)
        # имя + титул (транслит если нет кириллического шрифта)
        d.text((40, 132), _ct(display_name)[:26], font=_card_font(40, True), fill=white)
        et = active_title(row) or (row["earned_title"] if "earned_title" in row.keys() else None)
        if et:
            # на карте эмодзи может не отрисоваться дефолтным шрифтом — чистим до букв/цифр
            et_clean = _ct(re.sub(r"[^\w\s\-!?.]", "", str(et), flags=re.UNICODE)).strip()
            if et_clean:
                d.text((42, 188), et_clean[:34], font=_card_font(22, False), fill=gold)
        # статы — две колонки
        lp = row["lp"]
        rep = row["reputation"] or 0
        streak = row["streak"] or 0
        total = stat_total(chat_id, user_id)
        rb = row["rank_best"] if "rank_best" in row.keys() else None
        rankname = _ct(rank_name(rb)) if rb else "—"
        y0 = 244
        col1 = [("LP BALANCE", fmt_lp(lp)), ("REPUTATION", str(rep)), ("DAY STREAK", str(streak))]
        col2 = [("MESSAGES", str(total)), ("RANK", rankname), ("QUESTS", str(quests_total(chat_id, user_id)))]
        for i, (label, val) in enumerate(col1):
            yy = y0 + i * 72
            d.text((48, yy), label, font=_card_font(16, False), fill=gold)
            d.text((48, yy + 24), val, font=_card_font(30, True), fill=white)
        for i, (label, val) in enumerate(col2):
            yy = y0 + i * 72
            d.text((480, yy), label, font=_card_font(16, False), fill=gold)
            d.text((480, yy + 24), val, font=_card_font(30, True), fill=white)
        # разделитель колонок
        d.line([450, y0, 450, y0 + 210], fill=(60, 55, 30), width=1)
        # подвал
        d.text((40, H - 50), "BRAWL STARS CLAN", font=_card_font(15, False), fill=grey)
        d.text((W - 210, H - 50), today_str(), font=_card_font(15, False), fill=grey)
        buf = io.BytesIO()
        img.save(buf, format="PNG")
        buf.seek(0)
        buf.name = "profile_card.png"
        return buf
    except Exception as e:
        log_error("карточка профиля", e)
        return None


async def show_profile(update, context, user):
    chat_id = update.effective_chat.id
    ensure_user(chat_id, user)
    row = get_user(chat_id, user.id)
    warns = count_warns(chat_id, user.id)
    role = max(row["role"], await eff_role(update, context, user.id))
    # 5.9: пранк-overlay — подменяем ТОЛЬКО отображаемые значения, реальные данные/экономика не трогаются
    pr = _prank_get(row)
    disp_name_full = pr.get("name", user.full_name)
    disp_lp = pr.get("lp", row["lp"])
    disp_rep = pr.get("rep", row["reputation"])
    disp_rank = pr.get("rank", None)
    disp_streak = pr.get("streak", row["streak"] or 0)
    disp_total = pr.get("msg", stat_total(chat_id, user.id))
    uname = f" (@{esc(row['username'])})" if row["username"] else ""
    _title = pr.get("title", active_title(row))
    title_line = f"\n{pe('star')} «<b>{esc(_title)}</b>»" if _title else ""
    # 5.9: у рядовых участников вместо «Участник» — заработанный титул (квесты/викторина). Штаб показывает должность.
    _earned = pr.get("rank_role") or (row["earned_title"] if "earned_title" in row.keys() else None)
    if role >= MODER:
        role_disp = ROLE_NAMES.get(role, "Участник")
    else:
        role_disp = _earned or ROLE_NAMES.get(role, "Участник")
    week_line = f"{pe('stats')} За неделю: <b>{stat_week(chat_id, user.id)}</b>"
    if WEEKLY_MSG_NORM > 0 and today_str() >= NORMS_START_DATE:
        wk = stat_week(chat_id, user.id)
        week_line += f" / норма {WEEKLY_MSG_NORM} {pe('check') if wk >= WEEKLY_MSG_NORM else pe('cross')}"
    text = (
        f"{pe('person')} <b>ПРОФИЛЬ {CLAN_NAME}</b>\n{CLAN_LINE}\n"
        f"{esc(disp_name_full)}{uname}{title_line}\n\n"
        f"<blockquote>{pe('role')} Роль: <b>{role_disp}</b>\n"
        f"{pe('lp')} LP: <b>{fmt_lp(disp_lp)}</b> / {LP_MAX}\n"
        f"{pe('star')} Репутация: <b>{disp_rep}</b></blockquote>\n"
        f"<blockquote>{pe('msg')} Всего сообщений: <b>{disp_total}</b>\n"
        f"{pe('calendar')} Сегодня: <b>{stat_today(chat_id, user.id)}</b>\n"
        f"{week_line}</blockquote>\n"
        f"{pe('fire')} Серия: <b>{disp_streak} {_days_word(disp_streak)} подряд</b>"
        f" · рекорд <b>{(row['streak_best'] if 'streak_best' in row.keys() else 0) or 0}</b>"
    )
    # тег Brawl Stars (если привязан) — при пранке ранга строку тега не выводим, чтобы не палиться
    if not (disp_rank is not None):
        text += brawl_profile_line(chat_id, user.id)
    rb = row["rank_best"] if "rank_best" in row.keys() else None
    if disp_rank is not None:
        text += f"\n{pe('cup')} Ранг: <b>{esc(str(disp_rank))}</b>"
    elif rb:
        text += f"\n{pe('cup')} Ранг: <b>{rank_name(rb)}</b>"
    # 5.8: значки-ачивки (эмодзи в ряд)
    _badges = user_achievements(chat_id, user.id)
    if _badges:
        icons = " ".join(ACHIEVEMENTS_BY_CODE[c][1] for c in _badges if c in ACHIEVEMENTS_BY_CODE)
        text += f"\n{pe('star')} Значки: {icons}"
    # 6.0 Профиль 2.0: место в текущем сезоне + прогресс титулов (квесты/викторина)
    if user.id not in EXCLUDED_IDS and disp_rank is None:
        try:
            srows = [r for r in season_top(chat_id, current_season(), 50) if r["val"] > 0]
            pos = next((i + 1 for i, r in enumerate(srows) if r["user_id"] == user.id), None)
            if pos:
                medal = {1: "🥇", 2: "🥈", 3: "🥉"}.get(pos, f"#{pos}")
                text += f"\n{pe('cup')} Место в сезоне: <b>{medal}</b> из {len(srows)}"
        except Exception:
            pass
        qd = quests_total(chat_id, user.id)
        qw = quiz_wins_total(chat_id, user.id)
        if qd or qw:
            text += f"\n{pe('game')} Квестов: <b>{qd}</b> · Побед в викторине: <b>{qw}</b>"
    # прогресс до награды — всем, кто в экономике (кроме «замороженных» админов)
    if user.id not in EXCLUDED_IDS:
        text += f"\n\n<blockquote>{progress_line(row['lp'])}</blockquote>"
    # дисциплина (варны, муты, стадия) — видно ТОЛЬКО модераторам и выше
    if await eff_role(update, context) >= MODER:
        stages = {0: "чисто", 1: "был устный выговор", 2: "был мут"}
        nw = count_norm_warns(chat_id, user.id)
        vac = (f" · в отпуске до {row['vacation_until']}"
               if (row["vacation_until"] and row["vacation_until"] >= today_str()) else "")
        text += (f"\n\n{pe('shield')} <i>Для модерации:</i>\n"
                 f"<blockquote>{pe('warn')} Варны: <b>{warns}/{MAX_WARNS}</b>\n"
                 f"{pe('cup')} Норм-варны: <b>{nw}/{NORM_WARN_LIMIT}</b>{vac}\n"
                 f"{pe('mute')} Получено мутов: <b>{row['mutes']}</b>\n"
                 f"{pe('stats')} Стадия: <b>{stages.get(row['disc_stage'], 'чисто')}</b></blockquote>")
    await update.message.reply_text(text, parse_mode=ParseMode.HTML)


async def give_rep(update, context, t, delta=1):
    chat_id = update.effective_chat.id
    giver = update.effective_user
    if t.id == giver.id:
        return await update.message.reply_text("Нельзя менять репутацию самому себе 🙂")
    grow = get_user(chat_id, giver.id)
    if grow and grow["last_rep"] and not is_owner(giver.id):   # 5.9: владелец без КД
        try:
            last = datetime.fromisoformat(grow["last_rep"])
            if msk_now() - last < timedelta(minutes=REP_COOLDOWN_MIN):
                left = REP_COOLDOWN_MIN - int((msk_now()-last).total_seconds()//60)
                return await update.message.reply_text(
                    f"{pe('clock')} Репутацию можно менять раз в {REP_COOLDOWN_MIN} мин. Подожди ещё {left} мин.",
                    parse_mode=ParseMode.HTML)
        except Exception:
            pass
    ensure_user(chat_id, t)
    conn.execute("UPDATE users SET reputation=reputation+? WHERE chat_id=? AND user_id=?", (delta, chat_id, t.id))
    conn.execute("UPDATE users SET last_rep=? WHERE chat_id=? AND user_id=?", (msk_now().isoformat(), chat_id, giver.id))
    conn.execute("INSERT INTO rep_log (chat_id, from_id, to_id, day, delta) VALUES (?,?,?,?,?)",
                 (chat_id, giver.id, t.id, today_str(), delta))   # 5.8: для квеста «получи респект»
    conn.commit()
    row = get_user(chat_id, t.id)
    newrep = row["reputation"] if row else 0
    if delta >= 0:
        await update.message.reply_text(
            f"{pe('star')} <b>{esc(t.full_name)}</b> получает +1 к репутации! Сейчас: <b>{newrep}</b>",
            parse_mode=ParseMode.HTML)
        await try_complete_quest(context, chat_id, t.id, t.full_name)   # 5.8
        await check_achievements(context, chat_id, t.id, t.full_name)
    else:
        await update.message.reply_text(
            f"{pe('warn')} <b>{esc(t.full_name)}</b> теряет 1 репутацию. Сейчас: <b>{newrep}</b>",
            parse_mode=ParseMode.HTML)


async def show_top(update, context, args):
    """args — список слов после 'топ'. Категория: актив/очки/реп. Период: день/неделя/(пусто=всё)."""
    chat_id = update.effective_chat.id
    words = [w.lower() for w in args]
    # период
    if any(w in ("день", "сегодня", "day", "today") for w in words):
        period = "day"
    elif any(w in ("неделя", "нед", "week") for w in words):
        period = "week"
    else:
        period = "all"
    # категория
    if any(w in ("очки", "лп", "lp", "points") for w in words):
        cat = "lp"
    elif any(w in ("реп", "репутация", "rep") for w in words):
        cat = "rep"
    elif any(w in ("trophies", "trophy", "кубки", "cups", "cup") for w in words):
        cat = "trophies"
    else:
        cat = "msg"     # «актив» и значение по умолчанию

    medals = {0: pe("gold"), 1: pe("silver"), 2: pe("bronze")}
    unit = ""

    if cat == "rep":
        rows = conn.execute("SELECT name, reputation AS val FROM users WHERE chat_id=? "
                            "AND COALESCE(gone,0)=0 AND user_id<>? ORDER BY val DESC LIMIT 10",
                            (chat_id, LEADER_ID)).fetchall()
        label = "по репутации"
    elif cat == "trophies":
        rows = conn.execute(
            "SELECT COALESCE(brawl_name, name) AS name, brawl_trophies AS val FROM users "
            "WHERE chat_id=? AND brawl_tag IS NOT NULL AND brawl_trophies IS NOT NULL "
            "AND COALESCE(gone,0)=0 AND user_id<>? "
            "ORDER BY val DESC LIMIT 10", (chat_id, LEADER_ID)).fetchall()
        label = "по кубкам Brawl Stars"
    elif cat == "lp":
        if period == "all":
            # за всё время топ очков не ведём (есть максимальные балансы) — показываем текущий баланс
            rows = conn.execute("SELECT name, lp AS val FROM users WHERE chat_id=? "
                                "AND COALESCE(gone,0)=0 AND user_id<>? ORDER BY val DESC LIMIT 10",
                                (chat_id, LEADER_ID)).fetchall()
            label = "по балансу LP (сейчас)"
            unit = " LP"
        else:
            day_filter = "day=?" if period == "day" else "day BETWEEN ? AND ?"
            params = (today_str(),) if period == "day" else week_bounds()
            rows = conn.execute(
                f"SELECT u.name, COALESCE(SUM(l.delta),0) AS val FROM users u "
                f"LEFT JOIN lp_log l ON l.chat_id=u.chat_id AND l.user_id=u.user_id AND l.{day_filter} "
                f"WHERE u.chat_id=? AND COALESCE(u.gone,0)=0 AND u.user_id<>? "
                f"GROUP BY u.user_id ORDER BY val DESC LIMIT 10",
                (*params, chat_id, LEADER_ID)).fetchall()
            label = "по заработанным LP за " + ("сегодня" if period == "day" else "неделю")
            unit = " LP"
    else:   # сообщения / актив
        if period == "day":
            rows = conn.execute("SELECT u.name, s.cnt AS val FROM stats s JOIN users u USING(chat_id,user_id) "
                                "WHERE s.chat_id=? AND s.day=? AND COALESCE(u.gone,0)=0 AND u.user_id<>? "
                                "ORDER BY val DESC LIMIT 10", (chat_id, today_str(), LEADER_ID)).fetchall()
            label = "по активности за сегодня"
        elif period == "week":
            mon, sun = week_bounds()
            rows = conn.execute("SELECT u.name, COALESCE(SUM(s.cnt),0) AS val FROM users u "
                                "LEFT JOIN stats s ON s.chat_id=u.chat_id AND s.user_id=u.user_id AND s.day BETWEEN ? AND ? "
                                "WHERE u.chat_id=? AND COALESCE(u.gone,0)=0 AND u.user_id<>? "
                                "GROUP BY u.user_id ORDER BY val DESC LIMIT 10",
                                (mon, sun, chat_id, LEADER_ID)).fetchall()
            label = "по активности за неделю (Пн–Вс, МСК)"
        else:
            rows = conn.execute("SELECT u.name, COALESCE(SUM(s.cnt),0) AS val FROM users u "
                                "LEFT JOIN stats s ON s.chat_id=u.chat_id AND s.user_id=u.user_id "
                                "WHERE u.chat_id=? AND COALESCE(u.gone,0)=0 AND u.user_id<>? "
                                "GROUP BY u.user_id ORDER BY val DESC LIMIT 10",
                                (chat_id, LEADER_ID)).fetchall()
            label = "по активности за всё время"

    rows = [r for r in rows if r["val"]]
    if not rows:
        if cat == "trophies":
            return await update.message.reply_text(
                f"{pe('game')} Пока ни у кого не привязан тег или нет данных по кубкам. Привяжите тег: <code>.tag #ТЕГ</code>.",
                parse_mode=ParseMode.HTML)
        return await update.message.reply_text("Пока нет данных для этого топа.")
    lines = [f"{pe('cup')} <b>ТОП {CLAN_NAME} — {label}</b>\n"]
    for i, r in enumerate(rows):
        if cat == "lp":
            val = fmt_lp(r["val"])
        elif cat == "trophies":
            val = fmt_num(r["val"])
        else:
            val = int(r["val"])
        marker = medals.get(i) or f"{pe('star')}"
        lines.append(f"<b>{i+1}.</b> {marker} {esc(r['name'])} — <b>{val}</b>{unit}")
    if cat == "trophies":
        lines.append(f"\n<i>Кубки обновляются, когда игрок привязывает тег командой</i> <code>.tag #ТЕГ</code>")
    await update.message.reply_text("\n".join(lines), parse_mode=ParseMode.HTML)


async def show_season(update, context):
    chat_id = update.effective_chat.id
    season = current_season()
    rows = [r for r in season_top(chat_id, season, 10) if r["val"] > 0]
    left = season_days_left()
    head = (f"{pe('cup')} <b>СЕЗОН «{season_title(season)}» — {CLAN_NAME}</b>\n{CLAN_LINE}\n"
            f"{pe('clock')} До конца сезона: <b>{left} {_days_word(left)}</b>\n\n")
    if not rows:
        return await update.message.reply_text(
            head + "Пока никто не активничал в этом сезоне — будь первым!", parse_mode=ParseMode.HTML)
    medals = {0: pe("gold"), 1: pe("silver"), 2: pe("bronze")}
    lines = [f"{pe('growth')} <b>Топ активных в сезоне:</b>"]
    for i, r in enumerate(rows):
        marker = medals.get(i) or pe("star")
        lines.append(f"<b>{i+1}.</b> {marker} {esc(r['name'])} — <b>{r['val']}</b>")
    tail = (f"\n{pe('crown')} В конце сезона топ-3 получат LP "
            f"(+{SEASON_REWARDS[1]} / +{SEASON_REWARDS[2]} / +{SEASON_REWARDS[3]}), "
            f"<b>титул-регалию в профиль</b> (🏆 Чемпион / 🥈🥉 Призёр) на месяц и место в зале славы (<code>hof</code>).")
    await update.message.reply_text(head + "\n".join(lines) + tail, parse_mode=ParseMode.HTML)


def _hof_leaders(chat_id, kind, limit=3):
    """Топ-N участников по категории для Зала Славы."""
    if kind == "lp":
        return conn.execute("SELECT name, lp AS val FROM users WHERE chat_id=? ORDER BY val DESC LIMIT ?",
                            (chat_id, limit)).fetchall()
    if kind == "rep":
        return conn.execute("SELECT name, reputation AS val FROM users WHERE chat_id=? ORDER BY val DESC LIMIT ?",
                            (chat_id, limit)).fetchall()
    if kind == "trophies":
        return conn.execute(
            "SELECT COALESCE(brawl_name, name) AS name, brawl_trophies AS val FROM users "
            "WHERE chat_id=? AND brawl_tag IS NOT NULL AND brawl_trophies IS NOT NULL "
            "ORDER BY val DESC LIMIT ?", (chat_id, limit)).fetchall()
    return conn.execute(
        "SELECT u.name, COALESCE(SUM(s.cnt),0) AS val FROM users u "
        "LEFT JOIN stats s ON s.chat_id=u.chat_id AND s.user_id=u.user_id "
        "WHERE u.chat_id=? GROUP BY u.user_id ORDER BY val DESC LIMIT ?", (chat_id, limit)).fetchall()


async def show_hall_of_fame(update, context):
    chat_id = update.effective_chat.id
    medals = {0: pe("gold"), 1: pe("silver"), 2: pe("bronze")}

    def fmt_top(kind, unit="", is_lp=False, is_tr=False):
        rows = [r for r in _hof_leaders(chat_id, kind, 3) if r["val"]]
        if not rows:
            return None
        parts = []
        for i, r in enumerate(rows):
            v = fmt_lp(r["val"]) if is_lp else (fmt_num(r["val"]) if is_tr else int(r["val"]))
            parts.append(f"{medals.get(i, pe('star'))} {esc(r['name'])} — <b>{v}</b>{unit}")
        return "\n".join(parts)

    sections = []
    for kind, title, kw in (("lp", "По LP", dict(unit=" LP", is_lp=True)),
                            ("msg", "По сообщениям", {}),
                            ("rep", "По репутации", {}),
                            ("trophies", "По кубкам Brawl Stars", dict(is_tr=True))):
        t = fmt_top(kind, **kw)
        if t:
            icon = {"lp": pe("lp"), "msg": pe("msg"), "rep": pe("star"), "trophies": pe("cup")}[kind]
            sections.append(f"{icon} <b>{title}</b>\n{t}")

    # победители сезонов (если есть)
    rows = conn.execute(
        "SELECT season, rank, name, score FROM hall_of_fame WHERE chat_id=? ORDER BY season DESC, rank ASC",
        (chat_id,)).fetchall()
    season_block = ""
    if rows:
        seasons = {}
        for r in rows:
            seasons.setdefault(r["season"], []).append(r)
        blocks = []
        for season, items in list(seasons.items())[:6]:
            winners = " · ".join(f"{medals.get(it['rank'] - 1, '')} {esc(it['name'])}"
                                 for it in sorted(items, key=lambda x: x["rank"]))
            blocks.append(f"<b>{season_title(season)}</b>\n{winners}")
        season_block = f"\n\n{pe('calendar')} <b>Победители сезонов</b>\n" + "\n\n".join(blocks)

    if not sections and not season_block:
        return await update.message.reply_text(
            f"{pe('cup')} Зал славы пока пуст — статистика появится, когда участники начнут писать.",
            parse_mode=ParseMode.HTML)
    body = "\n\n".join(sections) + season_block
    await update.message.reply_text(
        f"{pe('cup')} <b>ЗАЛ СЛАВЫ — {CLAN_NAME}</b>\n{CLAN_LINE}\n\n{body}", parse_mode=ParseMode.HTML)


# ===================== КНОПОЧНОЕ МЕНЮ И МАГАЗИН =====================
def main_menu_keyboard(owner_id):
    """Главное меню (инлайн-кнопки). owner_id — кто открыл меню (жать может только он).
    В подписях кнопок только обычные эмодзи (премиум там не работают)."""
    u = owner_id
    return InlineKeyboardMarkup([
        [InlineKeyboardButton("👤 Профиль", callback_data=f"menu:profile:{u}"),
         InlineKeyboardButton("🛒 Магазин", callback_data=f"menu:shop:{u}")],
        [InlineKeyboardButton("🏆 Топы", callback_data=f"menu:top:{u}"),
         InlineKeyboardButton("📅 Сезон", callback_data=f"menu:season:{u}")],
        [InlineKeyboardButton("🔥 Стрик", callback_data=f"menu:streak:{u}"),
         InlineKeyboardButton("👑 Зал славы", callback_data=f"menu:hof:{u}")],
        [InlineKeyboardButton("📋 Нормы", callback_data=f"menu:norms:{u}"),
         InlineKeyboardButton("📜 Правила", callback_data=f"menu:rules:{u}")],
        [InlineKeyboardButton("❓ Помощь", callback_data=f"help:menu:{u}")],
    ])


def store_keyboard():
    """Кнопки покупки товаров за LP."""
    rows = []
    for key, name, price, _e in STORE_ITEMS:
        rows.append([InlineKeyboardButton(f"{name} — {price} LP", callback_data=f"buy:{key}")])
    return InlineKeyboardMarkup(rows)


def store_menu_keyboard(owner_id):
    """Кнопочное меню магазина: разделы-инфо (сворачиваемые) + кнопки покупки товаров."""
    u = owner_id
    rows = [
        [InlineKeyboardButton("💰 Как получать LP", callback_data=f"store:earn:{u}"),
         InlineKeyboardButton("💎 За рубли", callback_data=f"store:rubles:{u}")],
        [InlineKeyboardButton("📜 Правила STORE", callback_data=f"store:rules:{u}")],
    ]
    for key, name, price, _e in STORE_ITEMS:
        rows.append([InlineKeyboardButton(f"{name} — {price} LP", callback_data=f"buy:{key}")])
    return InlineKeyboardMarkup(rows)


def _store_intro():
    return (f"{pe('shop')} <b>{CLAN_NAME} STORE</b>\n{CLAN_LINE}\n"
            f"<i>Внутренний магазин клана. LP зарабатываются только активностью.</i>\n"
            f"{pe('lp')} Баланс до {LP_MAX} LP · нажми товар — оформим заявку, LP зарезервируются.\n"
            f"<i>Разделы ниже — нажми, чтобы узнать подробнее.</i>")


def _store_section(key):
    """Раздел магазина (в сворачиваемой цитате)."""
    if key == "earn":
        return (f"{pe('growth')} <b>Как получать LP</b>\n<blockquote expandable>"
                f"{pe('msg')} За каждое сообщение в чате — будь активен каждый день, LP копятся "
                f"(есть честный дневной потолок от спама)\n"
                f"{pe('cup')} Норма кубков — +{NORM_MONTH_PER_WEEK} LP за неделю (выплата раз в месяц) · {pe('star')} Норма ранга (Легендарный) — +{NORM_RANK_LP} LP\n"
                f"{pe('hug')} Помощь, медийность, сложные задачи — на усмотрение администрации\n"
                f"<i>LP нельзя купить, подарить или вывести — только заработать. В играх (лотерея, дуэли) "
                f"LP не передаются напрямую, а часть всегда сгорает в казну.</i></blockquote>")
    if key == "rubles":
        return (f"{pe('crown')} <b>За рубли</b>\n<blockquote expandable>"
                f"Паки и гемы — цены по курсу, уточняй у менеджера {STORE_MANAGER}. "
                f"Покупка и выдача товара — через менеджера.\n"
                f"{pe('rate')} Курс: 1 LP = 0.5 ₽ скидки при покупке за рубли.</blockquote>")
    if key == "rules":
        return (f"{pe('scales')} <b>Правила STORE</b>\n<blockquote expandable>"
                f"Накрутка и передача LP запрещены, фейковые оплаты — блокировка. "
                f"При ошибке начисления по вине STORE — компенсируем.\n"
                f"Улучшение до BP Plus покупается, только если у тебя уже есть Brawl Pass.\n"
                f"{pe('clock')} Работаем ежедневно {STORE_OPEN_HOUR}:00–{STORE_CLOSE_HOUR}:00 МСК.</blockquote>")
    return _store_intro()


def _proxy_update(update):
    """Обёртка для колбэков: даёт .message (сообщение под кнопкой), .effective_chat/.effective_user."""
    cq = update.callback_query
    return SimpleNamespace(message=cq.message, effective_chat=cq.message.chat,
                           effective_user=cq.from_user, callback_query=cq)


async def menu_cmd(update, context):
    kb = main_menu_keyboard(update.effective_user.id)
    try:
        await update.message.reply_text(
            f"{pe('crown')} <b>МЕНЮ {CLAN_NAME}</b>\n{CLAN_LINE}\n"
            f"<i>Выбери раздел кнопкой ниже.</i>", parse_mode=ParseMode.HTML,
            reply_markup=kb)
    except Exception as e:
        print(f"[MENU] ошибка отправки меню (HTML): {e}", flush=True)
        try:
            await update.message.reply_text(f"МЕНЮ {CLAN_NAME}\nВыбери раздел кнопкой ниже.", reply_markup=kb)
        except Exception as e2:
            print(f"[MENU] ошибка отправки меню (plain): {e2}", flush=True)


async def store_info(update, context):
    hour = msk_now().hour
    if not (STORE_OPEN_HOUR <= hour < STORE_CLOSE_HOUR):
        return await update.message.reply_text(
            f"{pe('lock')} <b>{CLAN_NAME} STORE — закрыт</b>\n{CLAN_LINE}\n"
            f"Магазин работает ежедневно с <b>{STORE_OPEN_HOUR}:00</b> до <b>{STORE_CLOSE_HOUR}:00</b> по МСК.\n"
            f"Загляни в рабочее время {pe('catneutral')}", parse_mode=ParseMode.HTML)
    kb = store_menu_keyboard(update.effective_user.id)
    try:
        await update.message.reply_text(_store_intro(), parse_mode=ParseMode.HTML, reply_markup=kb)
    except Exception as e:
        print(f"[STORE] ошибка отправки магазина: {e}", flush=True)
        try:
            await update.message.reply_text(f"{CLAN_NAME} STORE. Выбери раздел или товар кнопкой.", reply_markup=kb)
        except Exception:
            pass


async def norms_info(update, context):
    await update.message.reply_text(
        f"{pe('excl')} <b>ОБЯЗАТЕЛЬНЫЕ НОРМЫ КЛАНА {CLAN_NAME}</b>\n{CLAN_LINE}\n"
        f"<i>Регламент действует для каждого участника без исключений.</i>\n\n"
        f"<blockquote>{pe('star')} Прирост кубков: минимум +{WEEKLY_TROPHY_NORM} в неделю\n"
        f"{pe('msg')} Активность: минимум {WEEKLY_MSG_NORM} сообщений в неделю\n"
        f"{pe('cup')} Ранг: достичь Легендарного (Legendary) за сезон\n"
        f"{pe('warn')} Отсутствие более 3 дней без предупреждения — исключение</blockquote>\n"
        f"{pe('calendar')} <b>Нормы кубков и активности считаются с 22 июня 2026</b> "
        f"(первые 2 недели — мягкий старт без штрафов).\n"
        f"{pe('shield')} <b>Наказания за невыполнение</b>\n"
        f"<blockquote>За каждую невыполненную норму (кубки или активность) — −{NORM_FAIL_PENALTY} LP. "
        f"Плюс норм-варн (максимум один в неделю): 3 норм-варна за месяц — исключение. "
        f"Кто едет отдыхать/на учёбе — админ ставит «отпуск», нормы на паузе.</blockquote>\n"
        f"{pe('fire')} <b>Выполнение норм — не просьба, а обязательное условие нахождения в клане.</b> "
        f"Не тянешь — освобождаешь место тем, кто готов играть и развиваться.",
        parse_mode=ParseMode.HTML)


def _docs_header():
    return (
        f"{pe('folder')} <b>ДОКУМЕНТАЦИЯ — {BOT_FULL_NAME}</b>\n{CLAN_LINE}\n\n"
        f"{pe('robot')} Бот: <b>{BOT_FULL_NAME}</b> · версия <b>{BOT_VERSION}</b> ({PATCH_CODE})\n"
        f"{pe('shield')} Обновление: <b>«{UPDATE_NAME}»</b> · тип: {PATCH_TYPE}\n"
        f"{pe('calendar')} Релиз: <b>{RELEASE_DATE}</b>\n\n"
        f"{pe('crown')} <b>Разработка</b>\n"
        f"<blockquote>Владелец и разработчик бота: {BOT_OWNER}\n"
        f"Администратор бота / владелец чата: {BOT_ADMIN}</blockquote>\n"
        f"{pe('scroll')} <b>О боте</b>\n"
        f"<blockquote>Бот клана {CLAN_NAME}: модерация по кодексу, система LP, статистика "
        f"активности, конкурсы, магазин и AI-помощник.</blockquote>\n"
        f"{pe('bulb')} Нашёл баг или есть идея — пиши {BOT_ADMIN}.")


_DOCS_VER_NAMES = {"5": "Цитадель", "4": "Адмирал", "3": "Флагман"}


def _docs_ver_label(ver):
    vn = _DOCS_VER_NAMES.get(ver.split(".")[0], "")
    return f"{ver} «{vn}»" if vn else ver


def _docs_screen(i):
    """Экран docs: шапка (остаётся всегда) + изменения одной версии."""
    ver, kind, items = CHANGELOG_HISTORY[i]
    body = premiumize("\n".join(items))
    pos = f"{i + 1}/{len(CHANGELOG_HISTORY)}"
    return (_docs_header() +
            f"\n\n{pe('star')} <b>Изменения · {_docs_ver_label(ver)} — {kind}</b>  <i>({pos})</i>\n"
            f"<blockquote expandable>{body}</blockquote>\n"
            f"<i>Листай версии стрелками ниже ⤵️</i>")


def _docs_arrows_kb(i, owner_id):
    # 5.7: кнопки персональные — листать может только тот, кто открыл (как в help)
    row = []
    if i < len(CHANGELOG_HISTORY) - 1:   # есть версия старее
        row.append(InlineKeyboardButton(f"← {CHANGELOG_HISTORY[i + 1][0]}", callback_data=f"docs:v:{i + 1}:{owner_id}"))
    if i > 0:                            # есть версия новее
        row.append(InlineKeyboardButton(f"{CHANGELOG_HISTORY[i - 1][0]} →", callback_data=f"docs:v:{i - 1}:{owner_id}"))
    return InlineKeyboardMarkup([row]) if row else None


async def docs_info(update, context):
    uid = update.effective_user.id
    await update.message.reply_text(_docs_screen(0), reply_markup=_docs_arrows_kb(0, uid),
                                    parse_mode=ParseMode.HTML)


def prank_menu_kb():
    """5.9: клавиатура пранк-меню (только владелец, в личке)."""
    rows = []
    codes = list(PRANK_SCENARIOS.items())
    for i in range(0, len(codes), 2):
        row = [InlineKeyboardButton(codes[i][1][0], callback_data=f"pm:{codes[i][0]}")]
        if i + 1 < len(codes):
            row.append(InlineKeyboardButton(codes[i + 1][1][0], callback_data=f"pm:{codes[i + 1][0]}"))
        rows.append(row)
    rows.append([InlineKeyboardButton("👤 Пранк профиля (как)", callback_data="pm:howprofile")])
    rows.append([InlineKeyboardButton("❌ Закрыть", callback_data="pm:close")])
    return InlineKeyboardMarkup(rows)


def prank_menu_text():
    return (f"{pe('game')} <b>ПРАНК-МЕНЮ ВЛАДЕЛЬЦА</b>\n{CLAN_LINE}\n"
            f"Жми кнопку — прикол запустится <b>в чате клана</b> с живым таймером "
            f"({PRANK_COUNTDOWN_SEC} сек). В конце сообщение <b>само исчезнет</b> — никто не догадается, что это розыгрыш 😎\n\n"
            f"<i>Реальная экономика, баны и муты НЕ затрагиваются.</i>")


async def _run_countdown_prank(context, code):
    """5.9: фейк-пранк с обратным отсчётом в чате клана. В конце сообщение САМО УДАЛЯЕТСЯ —
    разоблачения нет, чтобы никто не догадался, что это розыгрыш."""
    sc = PRANK_SCENARIOS.get(code)
    if not sc or not ALLOWED_CHAT_ID:
        return
    cid = ALLOWED_CHAT_ID
    if cid in _prank_running:
        return
    _prank_running.add(cid)
    try:
        buildup = sc[1]
        try:
            msg = await context.bot.send_message(
                cid, f"{buildup}\n\n⏳ <b>{PRANK_COUNTDOWN_SEC}</b> сек", parse_mode=ParseMode.HTML)
        except Exception as e:
            log_error("пранк-старт", e)
            return
        for s in range(PRANK_COUNTDOWN_SEC - 1, -1, -1):
            await asyncio.sleep(1)
            try:
                await context.bot.edit_message_text(
                    f"{buildup}\n\n⏳ <b>{s}</b> сек",
                    chat_id=cid, message_id=msg.message_id, parse_mode=ParseMode.HTML)
            except Exception:
                pass   # пропуск кадра при рейт-лимите — не критично
        await asyncio.sleep(0.6)
        # финал: тихо удаляем сообщение — пранк не палится, остаётся интрига
        try:
            await context.bot.delete_message(chat_id=cid, message_id=msg.message_id)
        except Exception as e:
            log_error("пранк-удаление", e)
    finally:
        _prank_running.discard(cid)


async def on_prankmenu_callback(update, context):
    cq = update.callback_query
    if not is_owner(cq.from_user.id):
        return await cq.answer("Это меню только для владельца.", show_alert=True)
    data = (cq.data or "pm:close").split(":", 1)[1]
    if data == "close":
        try:
            await cq.edit_message_text(f"{pe('check')} Пранк-меню закрыто.", parse_mode=ParseMode.HTML)
        except Exception:
            pass
        return await cq.answer()
    if data == "howprofile":
        await cq.answer()
        try:
            await cq.edit_message_text(
                f"{pe('game')} <b>ПРАНК ПРОФИЛЯ</b>\n{CLAN_LINE}\n"
                f"В чате ответь на сообщение игрока:\n"
                f"<blockquote><code>.пранк @ник лп 999999 реп -50</code>\n"
                f"<code>.пранк @ник ник Лохматый ранг Бронза I</code>\n"
                f"<code>.пранк стоп</code> — вернуть · <code>.пранк список</code></blockquote>\n"
                f"Фейк виден всем в его профиле, экономика не трогается.",
                reply_markup=InlineKeyboardMarkup([[InlineKeyboardButton("← Назад", callback_data="pm:back")]]),
                parse_mode=ParseMode.HTML)
        except Exception:
            pass
        return
    if data == "back":
        await cq.answer()
        try:
            await cq.edit_message_text(prank_menu_text(), reply_markup=prank_menu_kb(), parse_mode=ParseMode.HTML)
        except Exception:
            pass
        return
    if data in PRANK_SCENARIOS:
        if ALLOWED_CHAT_ID in _prank_running:
            return await cq.answer("В чате уже идёт пранк — дождись конца.", show_alert=True)
        await cq.answer("Запускаю в чате! 😈", show_alert=False)
        asyncio.create_task(_run_countdown_prank(context, data))
        try:
            await cq.edit_message_text(
                f"{pe('check')} Пранк «{PRANK_SCENARIOS[data][0]}» запущен в чате! "
                f"Открой чат и наблюдай 😎",
                reply_markup=InlineKeyboardMarkup([[InlineKeyboardButton("← В меню", callback_data="pm:back")]]),
                parse_mode=ParseMode.HTML)
        except Exception:
            pass
        return
    await cq.answer()


async def on_docs_callback(update, context):
    cq = update.callback_query
    data = cq.data or ""
    parts = data.split(":")
    owner_id = int(parts[3]) if len(parts) > 3 and parts[3].lstrip("-").isdigit() else cq.from_user.id
    if cq.from_user.id != owner_id:
        return await cq.answer("Этот список изменений открыл другой игрок. Открой свой — напиши: .docs", show_alert=True)
    try:
        i = int(parts[2]) if len(parts) > 2 and parts[2].lstrip("-").isdigit() else 0
        i = max(0, min(len(CHANGELOG_HISTORY) - 1, i))
        await cq.edit_message_text(_docs_screen(i), reply_markup=_docs_arrows_kb(i, owner_id), parse_mode=ParseMode.HTML)
    except Exception as e:
        log_error("docs-кнопки", e)
    try:
        await cq.answer()
    except Exception:
        pass


def _day_top_text(chat_id, offset):
    """Топ активности (по сообщениям) за день: offset 0=сегодня, 1=вчера и т.д."""
    d = msk_now().date() - timedelta(days=offset)
    rows = conn.execute(
        "SELECT COALESCE(u.name,'?') AS name, s.cnt AS cnt FROM stats s "
        "JOIN users u ON u.chat_id=s.chat_id AND u.user_id=s.user_id "
        "WHERE s.chat_id=? AND s.day=? AND s.cnt>0 AND u.user_id<>? ORDER BY s.cnt DESC LIMIT 10",
        (chat_id, d.isoformat(), LEADER_ID)).fetchall()
    when = "сегодня" if offset == 0 else ("вчера" if offset == 1 else _ru_date(d))
    head = f"{pe('stats')} <b>Топ активности — {when}</b>\n<i>{d.strftime('%d.%m.%Y')} · по сообщениям</i>"
    if not rows:
        return head + "\n\n<i>За этот день активности не было.</i>"
    medals = ["🥇", "🥈", "🥉"]
    lines = [f"{(medals[i] if i < 3 else str(i + 1) + '.')} {esc(r['name'])} — <b>{r['cnt']}</b> сообщ."
             for i, r in enumerate(rows)]
    return head + "\n\n" + "\n".join(lines)


def _day_top_kb(offset, owner_id):
    # 5.7: персональные кнопки листания (как help/docs)
    row = []
    if offset < 6:   # глубже недели не уходим
        row.append(InlineKeyboardButton("← раньше", callback_data=f"td:{offset + 1}:{owner_id}"))
    if offset > 0:   # вперёд — только до сегодня
        row.append(InlineKeyboardButton("позже →", callback_data=f"td:{offset - 1}:{owner_id}"))
    return InlineKeyboardMarkup([row]) if row else None


async def on_dailytop_callback(update, context):
    cq = update.callback_query
    parts = (cq.data or "td:0").split(":")
    owner_id = int(parts[2]) if len(parts) > 2 and parts[2].lstrip("-").isdigit() else cq.from_user.id
    if cq.from_user.id != owner_id:
        return await cq.answer("Этот топ открыл другой игрок. Открой свой — напиши: .топ день", show_alert=True)
    try:
        off = int(parts[1]) if len(parts) > 1 and parts[1].lstrip("-").isdigit() else 0
        off = max(0, min(6, off))
        await cq.edit_message_text(_day_top_text(cq.message.chat.id, off),
                                   reply_markup=_day_top_kb(off, owner_id), parse_mode=ParseMode.HTML)
    except Exception as e:
        log_error("топ-день кнопки", e)
    try:
        await cq.answer()
    except Exception:
        pass


def _faq_text():
    """Текст соглашения/FAQ (используется и для показа, и для согласия по кнопке)."""
    return (
        f"{pe('scroll')} <b>СОГЛАШЕНИЕ И FAQ — {CLAN_NAME}</b>\n{CLAN_LINE}\n"
        f"<i>Действует для каждого, кто состоит в клане, пользуется ботом, магазином (STORE) "
        f"или обращается в поддержку.</i>\n\n"

        f"{pe('check')} <b>1. Принятие соглашения</b>\n"
        f"<blockquote>Используя бота, STORE или находясь в клане {CLAN_NAME}, ты "
        f"<b>автоматически и полностью соглашаешься</b> с этим соглашением — даже если не читал его. "
        f"Не согласен хотя бы с одним пунктом — прекрати пользоваться ботом и покинь клан.</blockquote>\n"

        f"{pe('lp')} <b>2. LP и STORE</b>\n"
        f"<blockquote>LP — внутренние клановые баллы. Это <b>не деньги</b>: они не имеют реальной "
        f"стоимости, не покупаются напрямую, не передаются между участниками, не обмениваются на валюту и "
        f"не подлежат возврату. Награды и товары в STORE, а также <b>призы за конкурсы и ивенты</b> "
        f"выдаются <b>на усмотрение администрации</b> и не гарантируются — их наличие, сроки и условия "
        f"могут измениться. Ничто в этом соглашении не является трудовым договором или обещанием выплаты.</blockquote>\n"

        f"{pe('rate')} <b>3. Покупки за рубли</b>\n"
        f"<blockquote>Любые покупки за деньги оформляются индивидуально через менеджера {STORE_MANAGER}. "
        f"Все условия (цена, сроки, порядок выдачи, возврат) согласовываются <b>до оплаты</b>. "
        f"Совершая оплату, ты подтверждаешь согласие с этими условиями. Администрация не несёт "
        f"ответственности за договорённости, совершённые в обход официального менеджера.</blockquote>\n"

        f"{pe('shield')} <b>4. Решения администрации</b>\n"
        f"<blockquote>Наказания (предупреждение, мут, бан, исключение) и начисление/списание LP — "
        f"на усмотрение администрации в рамках кодекса. Решения администрации <b>окончательны</b>. "
        f"Не согласен с наказанием — есть цивилизованная процедура: написать боту в ЛС «appeal». "
        f"Оскорбления и давление на администрацию обжалование не ускоряют.</blockquote>\n"

        f"{pe('folder')} <b>5. Данные</b>\n"
        f"<blockquote>Для работы функций бот хранит: твой ID, @юзернейм, имя, активность, баланс LP, "
        f"репутацию и привязанный игровой тег. Пользуясь ботом, ты соглашаешься на их обработку. "
        f"Эти данные используются только внутри клана и не передаются третьим лицам.</blockquote>\n"

        f"{pe('warn')} <b>6. Ответственность</b>\n"
        f"<blockquote>Бот и клан предоставляются «как есть». Администрация не отвечает за технические "
        f"сбои, временную недоступность, потерю LP по причинам вне её контроля, а также за действия "
        f"Supercell, Telegram или третьих лиц. Участие в клане и использование бота — добровольные.</blockquote>\n"

        f"{pe('growth')} <b>7. Изменения</b>\n"
        f"<blockquote>Правила, нормы, курс LP, ассортимент STORE и условия этого соглашения могут "
        f"меняться в любой момент без отдельного уведомления. Продолжая пользоваться ботом и оставаясь "
        f"в клане, ты соглашаешься с актуальной версией.</blockquote>\n"

        f"{pe('person')} <b>8. Несовершеннолетним</b>\n"
        f"<blockquote>Если тебе нет 18 лет, участвуй только с ведома родителей или опекунов, и любые "
        f"вопросы, связанные с деньгами, решай вместе с ними. Помни: ничто здесь не является трудовым "
        f"договором и не гарантирует выплат.</blockquote>\n"

        f"{pe('scales')} <b>9. Споры</b>\n"
        f"<blockquote>Все спорные ситуации решаются администрацией клана {CLAN_NAME}, и её решение "
        f"является окончательным.</blockquote>\n"

        f"{pe('catneutral')} Остались вопросы — пиши {BOT_ADMIN}. Удачи в боях!"
    )


async def faq_info(update, context):
    """Пользовательское соглашение / FAQ (показ в личке)."""
    await update.message.reply_text(_faq_text(), parse_mode=ParseMode.HTML)


def faq_deeplink(context):
    """Ссылка, открывающая бота в ЛС с запуском соглашения."""
    uname = getattr(context.bot, "username", None) or "bot"
    return f"https://t.me/{uname}?start=agree"


async def send_faq_agreement(update, context):
    """Показывает соглашение с кнопками Да/Нет (в личке)."""
    kb = InlineKeyboardMarkup([[
        InlineKeyboardButton("✅ Да, я согласен", callback_data="faq:yes"),
        InlineKeyboardButton("❌ Нет, не согласен", callback_data="faq:no")]])
    await update.message.reply_text(
        _faq_text() + f"\n\n{pe('excl')} <b>Чтобы пользоваться ботом и состоять в клане, прими соглашение кнопкой ниже.</b>",
        parse_mode=ParseMode.HTML, reply_markup=kb)


def has_agreed(user_id):
    if user_id == LEADER_ID or user_id in CONTEST_CREATORS or user_id in EXCLUDED_IDS:
        return True
    row = conn.execute("SELECT agreed_faq FROM users WHERE chat_id=? AND user_id=?",
                       (ALLOWED_CHAT_ID, user_id)).fetchone()
    return bool(row and row["agreed_faq"])


# Статусы, означающие, что человек ещё в чате (всё остальное = вышел/исключён/забанен).
ACTIVE_MEMBER_STATUSES = (ChatMemberStatus.MEMBER, ChatMemberStatus.ADMINISTRATOR,
                          ChatMemberStatus.OWNER, ChatMemberStatus.RESTRICTED)

# Авто-чистка: True — как только участник вышел/исключён из чата, бот сразу удаляет его из системы
# (топы, LP, статистика). Полностью без команд. False — чистить только вручную командой .чистка.
AUTO_PURGE_ON_LEAVE = True   # 5.4: ушедшие не стираются, а помечаются — история сохраняется, повторный вход не даёт бонусов


def mark_left(chat_id, user_id):
    """5.4: помечает участника как вышедшего. Из живых топов он исчезает, но LP, варны и история
    сохраняются — эксплойт «вышел-зашёл» (сброс варнов + новые стартовые LP) закрыт."""
    conn.execute("UPDATE users SET gone=1 WHERE chat_id=? AND user_id=?", (chat_id, user_id))
    conn.commit()


def purge_user(chat_id, user_id):
    """Полностью удаляет участника из системы: пропадает из всех топов, LP, статистики и сбора."""
    for tbl in ("users", "warns", "stats", "lp_log", "pending_lp", "norm_warns"):
        try:
            conn.execute(f"DELETE FROM {tbl} WHERE chat_id=? AND user_id=?", (chat_id, user_id))
        except Exception:
            pass
    conn.commit()


async def cleanup_left_members(update, context):
    """Проходит по участникам из базы и помечает тех, кто уже вышел/исключён из чата.
    Из живых топов они исчезают, но история и LP сохраняются (5.4) — на случай возврата."""
    chat_id = update.effective_chat.id
    rows = conn.execute("SELECT user_id, name FROM users WHERE chat_id=? AND COALESCE(gone,0)=0",
                        (chat_id,)).fetchall()
    total = len(rows)
    if not total:
        return await update.message.reply_text("В базе пока нет участников для проверки.")
    status_msg = await update.message.reply_text(
        f"{pe('clock')} Проверяю {total} участников — кто ещё в чате… Немного подожди.",
        parse_mode=ParseMode.HTML)
    protected = {LEADER_ID} | EXCLUDED_IDS | CONTEST_CREATORS | STORE_ADMINS
    removed, checked = [], 0
    for r in rows:
        uid = r["user_id"]
        checked += 1
        if uid in protected:
            continue
        try:
            m = await context.bot.get_chat_member(chat_id, uid)
            if m.status not in ACTIVE_MEMBER_STATUSES:
                mark_left(chat_id, uid)
                removed.append(r["name"] or str(uid))
        except Exception:
            pass   # не удалось узнать статус — на всякий случай оставляем
        if checked % 20 == 0:
            await asyncio.sleep(1)   # бережём лимиты Telegram
    if removed:
        names = ", ".join(esc(n) for n in removed[:30])
        extra = f" и ещё {len(removed) - 30}" if len(removed) > 30 else ""
        txt = (f"{pe('check')} <b>Чистка завершена!</b>\n"
               f"Помечено вышедших из клана: <b>{len(removed)}</b> из {total} проверенных.\n"
               f"<blockquote expandable>{names}{extra}</blockquote>\n"
               f"<i>Они исчезли из живых топов; история и LP сохранены на случай возврата.</i>")
    else:
        txt = (f"{pe('check')} Чистка завершена: все {total} участников ещё в чате — удалять некого. "
               f"Чисто! {pe('star')}")
    try:
        await status_msg.edit_text(txt, parse_mode=ParseMode.HTML)
    except Exception:
        await update.message.reply_text(txt, parse_mode=ParseMode.HTML)


async def summon_all(update, context, text):
    """Сбор клана: упоминает (пингует) всех известных участников. Только админ."""
    chat_id = update.effective_chat.id
    note = text.split(maxsplit=1)[1].strip() if len(text.split()) > 1 else ""
    rows = conn.execute("SELECT user_id, name FROM users WHERE chat_id=?", (chat_id,)).fetchall()
    if not rows:
        return await update.message.reply_text("Пока некого созывать — участники появятся, когда напишут в чат.")
    try:
        await update.message.delete()
    except Exception:
        pass
    header = f"{pe('mega')} <b>СБОР КЛАНА {CLAN_NAME}!</b>"
    if note:
        header += f"\n<blockquote>{esc(note)}</blockquote>"
    mentions = [f'<a href="tg://user?id={r["user_id"]}">{esc(r["name"] or "боец")}</a>' for r in rows]
    BATCH = 40
    sent = False
    for i in range(0, len(mentions), BATCH):
        chunk = " ".join(mentions[i:i + BATCH])
        body = (header + "\n\n" + chunk) if not sent else chunk
        await context.bot.send_message(chat_id, body, parse_mode=ParseMode.HTML)
        sent = True


# ============================================================
#               КОНКУРСЫ (как @BestRandom_bot)
# ============================================================
active_giveaways = {}   # chat_id -> {msg_id, count, desc, parts:{user_id:name}}


def _giveaway_text(gw):
    title = esc(gw.get("name", "КОНКУРС")) or "КОНКУРС"
    body = esc(gw.get("desc", "")) or "Жми кнопку, чтобы участвовать!"
    tail = f"\n{pe('clock')} Длительность: {gw['minutes']} мин" if gw.get("minutes") else ""
    return (f"{pe('contest')} <b>{title}</b>\n\n{body}\n\n"
            f"{pe('cup')} Победителей: {gw['count']}\n{pe('hug')} Участников: {len(gw['parts'])}{tail}")


async def publish_giveaway(context, data):
    """Публикует конкурс в чат клана и ставит таймер авто-итогов."""
    chat_id = ALLOWED_CHAT_ID
    gw = {"name": data["name"], "count": data["count"], "desc": data["desc"],
          "minutes": data["minutes"], "parts": {}}
    kb = InlineKeyboardMarkup([[InlineKeyboardButton("🎉 Участвовать", callback_data="gw_join")]])
    msg = await context.bot.send_message(chat_id, _giveaway_text(gw),
                                         parse_mode=ParseMode.HTML, reply_markup=kb)
    gw["id"] = msg.message_id
    gw["msg_id"] = msg.message_id
    active_giveaways[chat_id] = gw
    asyncio.create_task(_schedule_end(context, chat_id, gw["minutes"], gw["id"]))


async def _schedule_end(context, chat_id, minutes, gw_id):
    try:
        await asyncio.sleep(minutes * 60)
        gw = active_giveaways.get(chat_id)
        if gw and gw.get("id") == gw_id:
            await finalize_giveaway(context, chat_id)
    except Exception:
        pass


async def giveaway_join(update, context):
    q = update.callback_query
    chat_id = q.message.chat.id
    if ALLOWED_CHAT_ID and chat_id != ALLOWED_CHAT_ID:
        return await q.answer()
    gw = active_giveaways.get(chat_id)
    if not gw or q.data != "gw_join":
        return await q.answer("Конкурс уже завершён.")
    u = q.from_user
    if u.id in EXCLUDED_IDS:
        return await q.answer("🛡️ Администраторы не участвуют в конкурсах.")
    if u.id in gw["parts"]:
        return await q.answer("Ты уже участвуешь ✅")
    gw["parts"][u.id] = u.full_name
    await q.answer("✅ Ты в игре! Удачи!")
    kb = InlineKeyboardMarkup([[InlineKeyboardButton("🎉 Участвовать", callback_data="gw_join")]])
    try:
        await context.bot.edit_message_text(_giveaway_text(gw), chat_id=chat_id,
                                             message_id=gw["msg_id"], parse_mode=ParseMode.HTML,
                                             reply_markup=kb)
    except Exception:
        pass


async def on_action_callback(update, context):
    """Обрабатывает нажатия инлайн-кнопок: меню (menu:), покупка (buy:), решение по заказу (order:)."""
    cq = update.callback_query
    data = cq.data or ""
    user = cq.from_user
    print(f"[CB] нажатие кнопки: {data} от {user.id}", flush=True)

    # ----- СОГЛАСИЕ С FAQ (кнопки в ЛС) -----
    if data.startswith("faq:"):
        choice = data.split(":", 1)[1]
        ensure_user(ALLOWED_CHAT_ID, user)
        already = has_agreed(user.id)
        if choice == "yes":
            if already:
                await cq.answer("Ты уже принял соглашение ✅")
            else:
                conn.execute("UPDATE users SET agreed_faq=1 WHERE chat_id=? AND user_id=?", (ALLOWED_CHAT_ID, user.id))
                conn.commit()
                await cq.answer("Спасибо! Соглашение принято.")
            try:
                await cq.edit_message_text(
                    f"{pe('check')} <b>Соглашение принято.</b> Теперь бот тебе полностью доступен.\n"
                    f"Не забудь привязать игровой тег: <code>.tag #ТВОЙ_ТЕГ</code>. Удачи в клане {CLAN_NAME}! {pe('fire')}",
                    parse_mode=ParseMode.HTML)
            except Exception:
                try:
                    await cq.edit_message_reply_markup(reply_markup=None)
                except Exception:
                    pass
        else:  # отказ — объясняем, выбор за участником; кнопки убираем
            await cq.answer()
            try:
                await cq.edit_message_text(
                    f"{pe('catneutral')} Понятно. Без согласия с соглашением пользоваться ботом и состоять в клане "
                    f"{CLAN_NAME} нельзя. Решение за тобой: можешь покинуть клан или передумать в любой момент — "
                    f"просто нажми /start и прими соглашение. Если останешься без согласия, администрация может тебя исключить.",
                    parse_mode=ParseMode.HTML)
            except Exception:
                try:
                    await cq.edit_message_reply_markup(reply_markup=None)
                except Exception:
                    pass
        return

    # ----- РЕШЕНИЕ ПО ОБЖАЛОВАНИЮ (только владелец/со-создатели) -----
    if data.startswith("appeal:"):
        if user.id not in ({LEADER_ID} | CONTEST_CREATORS):
            return await cq.answer("Только администрация может это делать.", show_alert=True)
        try:
            _, action, sid = data.split(":", 2)
            target_id = int(sid)
        except ValueError:
            return await cq.answer()
        approved = (action == "approve")
        await cq.answer("Решение принято.")
        verdict = "ОДОБРЕНО ✅" if approved else "ОТКЛОНЕНО ❌"
        try:
            await cq.edit_message_text(
                (cq.message.text_html if hasattr(cq.message, "text_html") else "") +
                f"\n\n<b>Решение: {verdict}</b> (by {esc(user.full_name)})", parse_mode=ParseMode.HTML)
        except Exception:
            try:
                await cq.message.reply_text(f"Решение по обжалованию: <b>{verdict}</b>", parse_mode=ParseMode.HTML)
            except Exception:
                pass
        try:
            if approved:
                await context.bot.send_message(
                    target_id,
                    f"{pe('check')} Твоё обжалование <b>одобрено</b>. Администрация снимет наказание/восстановит доступ. Спасибо за терпение!",
                    parse_mode=ParseMode.HTML)
            else:
                await context.bot.send_message(
                    target_id,
                    f"{pe('cross')} Твоё обжалование рассмотрено и <b>отклонено</b>. Решение администрации окончательно.",
                    parse_mode=ParseMode.HTML)
        except Exception:
            pass
        return

    # ----- МЕНЮ (жать может только открывший; раздел открывается в этом же сообщении) -----
    if data.startswith("menu:"):
        parts = data.split(":")
        action = parts[1] if len(parts) > 1 else ""
        owner_id = int(parts[2]) if len(parts) > 2 and parts[2].lstrip("-").isdigit() else user.id
        if user.id != owner_id:
            return await cq.answer("Это меню открыл другой игрок. Открой своё — напиши: menu", show_alert=True)
        await cq.answer()
        back_btn = InlineKeyboardButton("⬅️ Меню", callback_data=f"menu:back:{owner_id}")
        if action == "back":
            try:
                await cq.edit_message_text(
                    f"{pe('crown')} <b>МЕНЮ {CLAN_NAME}</b>\n{CLAN_LINE}\n"
                    f"<i>Выбери раздел кнопкой ниже.</i>", parse_mode=ParseMode.HTML,
                    reply_markup=main_menu_keyboard(owner_id))
            except Exception:
                pass
            return
        # Захватываем текст раздела, чтобы показать его прямо в этом сообщении (без новых сообщений в чат)
        cap = {"text": None, "markup": None}
        async def _cap(text="", **kw):
            cap["text"] = text
            if kw.get("reply_markup") is not None:
                cap["markup"] = kw["reply_markup"]
        fake_msg = SimpleNamespace(reply_text=_cap, chat=cq.message.chat,
                                   message_thread_id=getattr(cq.message, "message_thread_id", None))
        fake = SimpleNamespace(message=fake_msg, effective_chat=cq.message.chat,
                               effective_user=user, callback_query=cq)
        try:
            if action == "profile":
                await show_profile(fake, context, user)
            elif action == "shop":
                await store_info(fake, context)
            elif action == "top":
                await show_top(fake, context, ["active"])
            elif action == "season":
                await show_season(fake, context)
            elif action == "hof":
                await show_hall_of_fame(fake, context)
            elif action == "norms":
                await norms_info(fake, context)
            elif action == "rules":
                await rules_cmd(fake, context)
            elif action == "streak":
                row = get_user(cq.message.chat.id, user.id)
                cur = (row["streak"] or 0) if row else 0
                best = (row["streak_best"] or 0) if row else 0
                cap["text"] = (f"{pe('fire')} Твоя серия: <b>{cur} {_days_word(cur)} подряд</b>\n"
                               f"{pe('star')} Рекорд: <b>{best} {_days_word(best)}</b>")
            elif action == "help":
                cap["text"] = _help_intro()
        except Exception as e:
            print(f"[MENU] ошибка раздела {action}: {e}", flush=True)
        if cap["markup"] is not None:
            try:
                kb = InlineKeyboardMarkup(list(cap["markup"].inline_keyboard) + [[back_btn]])
            except Exception:
                kb = InlineKeyboardMarkup([[back_btn]])
        else:
            kb = InlineKeyboardMarkup([[back_btn]])
        try:
            await cq.edit_message_text(cap["text"] or "—", parse_mode=ParseMode.HTML, reply_markup=kb)
        except Exception as e:
            print(f"[MENU] не удалось показать раздел {action}: {e}", flush=True)
        return

    # ----- КНОПОЧНАЯ СПРАВКА (help:) -----
    if data.startswith("help:"):
        parts = data.split(":")
        action = parts[1] if len(parts) > 1 else ""
        owner_id = int(parts[2]) if len(parts) > 2 and parts[2].lstrip("-").isdigit() else user.id
        if user.id != owner_id:
            return await cq.answer("Эту справку открыл другой игрок. Открой свою — напиши: .help", show_alert=True)
        staff = await can_use_admin(context, user.id)
        if action == "admin" and not staff:
            return await cq.answer("Этот раздел только для администрации.", show_alert=True)
        await cq.answer()
        if action in ("menu", "back"):
            try:
                await cq.edit_message_text(_help_intro(), parse_mode=ParseMode.HTML,
                                           reply_markup=help_menu_keyboard(owner_id, staff))
            except Exception:
                pass
            return
        back = InlineKeyboardMarkup([[InlineKeyboardButton("⬅️ Назад", callback_data=f"help:menu:{owner_id}")]])
        try:
            await cq.edit_message_text(_help_section(action), parse_mode=ParseMode.HTML, reply_markup=back)
        except Exception as e:
            print(f"[HELP] не удалось показать раздел {action}: {e}", flush=True)
        return

    # ----- КНОПОЧНЫЙ МАГАЗИН (store:) -----
    if data.startswith("store:"):
        parts = data.split(":")
        action = parts[1] if len(parts) > 1 else ""
        owner_id = int(parts[2]) if len(parts) > 2 and parts[2].lstrip("-").isdigit() else user.id
        if user.id != owner_id:
            return await cq.answer("Этот магазин открыл другой игрок. Открой свой — напиши: .shop", show_alert=True)
        await cq.answer()
        if action in ("menu", "back"):
            try:
                await cq.edit_message_text(_store_intro(), parse_mode=ParseMode.HTML,
                                           reply_markup=store_menu_keyboard(owner_id))
            except Exception:
                pass
            return
        back = InlineKeyboardMarkup([[InlineKeyboardButton("⬅️ Назад", callback_data=f"store:menu:{owner_id}")]])
        try:
            await cq.edit_message_text(_store_section(action), parse_mode=ParseMode.HTML, reply_markup=back)
        except Exception as e:
            print(f"[STORE] не удалось показать раздел {action}: {e}", flush=True)
        return

    # ----- ПОКУПКА (резерв LP + заявка менеджеру) -----
    if data.startswith("buy:"):
        item = STORE_ITEMS_BY_KEY.get(data.split(":", 1)[1])
        if not item:
            return await cq.answer("Товар не найден.")
        _k, name, price, _e = item
        chat_id = cq.message.chat.id
        if ALLOWED_CHAT_ID and chat_id != ALLOWED_CHAT_ID:
            return await cq.answer()
        if user.id in EXCLUDED_IDS:
            return await cq.answer("🛡️ Администраторы вне экономики не покупают.", show_alert=True)
        ensure_user(chat_id, user)
        row = get_user(chat_id, user.id)
        bal = row["lp"] if row else 0
        if bal < price:
            return await cq.answer(f"Недостаточно LP. Нужно ещё {fmt_lp(price - bal)}.", show_alert=True)
        # 5.7: лимит — 1 пасс на человека в календарный месяц (защита бюджета клана)
        month = msk_now().strftime("%Y-%m")
        taken = conn.execute(
            "SELECT COUNT(*) AS c FROM orders WHERE chat_id=? AND user_id=? AND status IN ('pending','done') "
            "AND ts LIKE ?", (chat_id, user.id, month + "%")).fetchone()["c"]
        if taken >= PASS_PER_MONTH_LIMIT and not is_owner(user.id):   # 5.9: владелец без лимита
            return await cq.answer(
                f"Лимit: {PASS_PER_MONTH_LIMIT} пасс в месяц. В этом месяце ты уже оформлял пасс — "
                f"следующий можно будет в начале следующего месяца.", show_alert=True)
        new_bal = add_lp(chat_id, user.id, -price)            # резерв (вернём при отклонении)
        cur = conn.execute(
            "INSERT INTO orders (chat_id, user_id, name, item, price, status, ts) VALUES (?,?,?,?,?, 'pending', ?)",
            (chat_id, user.id, user.full_name, name, price, msk_now().isoformat()))
        conn.commit()
        oid = cur.lastrowid
        await cq.answer("Заявка отправлена! LP зарезервированы.", show_alert=True)
        await cq.message.reply_text(
            f"{pe('shop')} <b>{esc(user.full_name)}</b> оформил заявку: <b>{esc(name)}</b> за <b>{price} LP</b>.\n"
            f"{pe('lp')} LP зарезервированы (баланс: {fmt_lp(new_bal)}). Менеджер выдаст товар.",
            parse_mode=ParseMode.HTML)
        admin_kb = InlineKeyboardMarkup([[
            InlineKeyboardButton("✅ Выдано", callback_data=f"order:done:{oid}"),
            InlineKeyboardButton("❌ Отклонить", callback_data=f"order:reject:{oid}")]])
        uname = f" (@{user.username})" if user.username else ""
        for admin_id in STORE_ADMINS:
            try:
                await context.bot.send_message(
                    admin_id,
                    f"{pe('shop')} <b>Новый заказ #{oid}</b>\n"
                    f"Покупатель: <b>{esc(user.full_name)}</b>{uname} (<code>{user.id}</code>)\n"
                    f"Товар: <b>{esc(name)}</b> · Цена: <b>{price} LP</b> (зарезервировано)\n"
                    f"Выдай товар и нажми «Выдано», либо «Отклонить» — тогда LP вернутся.",
                    parse_mode=ParseMode.HTML, reply_markup=admin_kb)
            except Exception:
                pass
        return

    # ----- РЕШЕНИЕ ПО ЗАКАЗУ (только менеджер/владелец) -----
    if data.startswith("order:"):
        if user.id not in STORE_ADMINS:
            return await cq.answer("Только менеджер магазина может это делать.", show_alert=True)
        try:
            _, action, oid = data.split(":", 2)
            oid = int(oid)
        except ValueError:
            return await cq.answer()
        o = conn.execute("SELECT * FROM orders WHERE id=?", (oid,)).fetchone()
        if not o:
            return await cq.answer("Заказ не найден.")
        if o["status"] != "pending":
            return await cq.answer(f"Заказ уже обработан: {o['status']}.")
        if action == "done":
            conn.execute("UPDATE orders SET status='done' WHERE id=?", (oid,))
            conn.commit()
            await cq.answer("Готово: заказ выдан ✅")
            try:
                await cq.edit_message_text(
                    f"{pe('check')} Заказ #{oid} — <b>{esc(o['item'])}</b> для <b>{esc(o['name'])}</b>: ВЫДАНО ✅",
                    parse_mode=ParseMode.HTML)
            except Exception:
                pass
            try:
                await context.bot.send_message(
                    o["user_id"], f"{pe('check')} Твой заказ <b>{esc(o['item'])}</b> выдан! Спасибо, что с нами {pe('hug')}",
                    parse_mode=ParseMode.HTML)
            except Exception:
                pass
        elif action == "reject":
            conn.execute("UPDATE orders SET status='rejected' WHERE id=?", (oid,))
            conn.commit()
            add_lp(o["chat_id"], o["user_id"], o["price"])        # возврат LP
            await cq.answer("Заказ отклонён, LP возвращены.")
            try:
                await cq.edit_message_text(
                    f"{pe('cross')} Заказ #{oid} — <b>{esc(o['item'])}</b> для <b>{esc(o['name'])}</b>: ОТКЛОНЁН, LP возвращены.",
                    parse_mode=ParseMode.HTML)
            except Exception:
                pass
            try:
                await context.bot.send_message(
                    o["user_id"], f"{pe('cross')} Заказ <b>{esc(o['item'])}</b> отклонён. {pe('lp')} {o['price']} LP возвращены.",
                    parse_mode=ParseMode.HTML)
            except Exception:
                pass
        return


async def finalize_giveaway(context, chat_id):
    """Подводит итоги и публикует победителей в чат. Используется таймером и вручную."""
    gw = active_giveaways.pop(chat_id, None)
    if not gw:
        return
    title = esc(gw.get("name", "КОНКУРС"))
    parts = gw["parts"]
    if not parts:
        return await context.bot.send_message(chat_id, f"{pe('contest')} Конкурс «{title}» завершён — участников не было 😔",
                                              parse_mode=ParseMode.HTML)
    winners = random.sample(list(parts.keys()), min(gw["count"], len(parts)))
    lines = [f"{pe('contest')} <b>ИТОГИ: {title}</b>\n", f"{pe('cup')} Победител" + ("и" if len(winners) > 1 else "ь") + ":"]
    for w in winners:
        lines.append(f"• <a href=\"tg://user?id={w}\">{esc(parts[w])}</a>")
    lines.append(f"\nВсего участников: {len(parts)}")
    await context.bot.send_message(chat_id, "\n".join(lines), parse_mode=ParseMode.HTML)


async def end_giveaway(update, context):
    """Ручное завершение конкурса досрочно (для админов, в чате)."""
    if not await require(update, context, ADMIN):
        return
    chat_id = update.effective_chat.id
    if not active_giveaways.get(chat_id):
        return await update.message.reply_text("Сейчас нет активного конкурса.")
    await finalize_giveaway(context, chat_id)


# ============================================================
#               ПРИВАТНОЕ СОЗДАНИЕ КОНКУРСА (в личке)
# ============================================================
async def emoji_id_report(context, ids):
    """Формирует список ID премиум-эмодзи с их базовыми смайликами (для подсказки)."""
    info = {}
    try:
        stickers = await context.bot.get_custom_emoji_stickers(list(ids))
        info = {s.custom_emoji_id: (s.emoji or "") for s in stickers}
    except Exception:
        pass
    lines = []
    for i in ids:
        base = info.get(i, "")
        lines.append(f"{base + ' ' if base else ''}<code>{i}</code>")
    return "\n".join(lines)


async def is_staff(context, user_id):
    """Считается ли пользователь админом/модером (для помощи и обжалований в ЛС)."""
    if user_id in ({LEADER_ID} | EXCLUDED_IDS | CONTEST_CREATORS):
        return True
    try:
        if get_role(ALLOWED_CHAT_ID, user_id) >= MODER:
            return True
    except Exception:
        pass
    try:
        admins = await context.bot.get_chat_administrators(ALLOWED_CHAT_ID)
        return any(a.user.id == user_id for a in admins)
    except Exception:
        return False


def is_bot_staff(user_id):
    """Админ по ВНУТРЕННЕЙ роли бота (MODER+) или из штата. НЕ по телеграм-админке:
    в этом чате титулы у всех, значит почти все — телеграм-админы, и проверять по ней нельзя.
    Используется, чтобы прятать админ-команды от обычных участников."""
    if user_id in ({LEADER_ID} | CONTEST_CREATORS | STORE_ADMINS | EXCLUDED_IDS):
        return True
    try:
        return get_role(ALLOWED_CHAT_ID, user_id) >= MODER
    except Exception:
        return False


# Доступ к админ-командам бота (видимость в help + право вызова):
#   False — владелец + штат бота + администраторы клан-чата (как реальные права на выполнение)
#   True  — ТОЛЬКО владелец бота (самый строгий режим)
ADMIN_OWNER_ONLY = False


async def can_use_admin(context, user_id):
    """Единая проверка: кто видит и может вызывать админ-команды бота.
    По умолчанию совпадает с правами на ВЫПОЛНЕНИЕ (eff_role): владелец + штат + админы клан-чата.
    Если ADMIN_OWNER_ONLY=True — доступ только у владельца бота."""
    if user_id == LEADER_ID:
        return True
    if ADMIN_OWNER_ONLY:
        return False
    if user_id in (CONTEST_CREATORS | STORE_ADMINS | EXCLUDED_IDS):
        return True
    try:
        if get_role(ALLOWED_CHAT_ID, user_id) >= MODER:
            return True
    except Exception:
        pass
    try:
        m = await context.bot.get_chat_member(ALLOWED_CHAT_ID, user_id)
        return m.status in (ChatMemberStatus.OWNER, ChatMemberStatus.ADMINISTRATOR)
    except Exception:
        return False


async def can_appeal(context, user_id):
    """Может ли писать обжалование: он есть/был в чате клана (включая забаненных/в муте)."""
    if get_user(ALLOWED_CHAT_ID, user_id):
        return True
    try:
        m = await context.bot.get_chat_member(ALLOWED_CHAT_ID, user_id)
        return getattr(m, "status", "left") != "left"
    except Exception:
        return False


async def dm_profile(update, context):
    """Личный кабинет в ЛС: профиль, баланс и прогресс для участника клана."""
    user = update.effective_user
    row = get_user(ALLOWED_CHAT_ID, user.id)
    if not row:
        return await update.message.reply_text(
            f"{pe('catneutral')} Пока не вижу твою статистику — напиши хотя бы одно сообщение в чате клана, и она появится здесь.",
            parse_mode=ParseMode.HTML)
    uname = f" (@{esc(row['username'])})" if row["username"] else ""
    _drole = row["role"]
    _dearned = row["earned_title"] if "earned_title" in row.keys() else None
    _drole_disp = ROLE_NAMES.get(_drole, "Участник") if _drole >= MODER else (_dearned or ROLE_NAMES.get(_drole, "Участник"))
    text = (
        f"{pe('person')} <b>ЛИЧНЫЙ КАБИНЕТ {CLAN_NAME}</b>\n{CLAN_LINE}\n"
        f"{esc(row['name'])}{uname}\n\n"
        f"<blockquote>{pe('role')} Роль: <b>{_drole_disp}</b>\n"
        f"{pe('lp')} LP: <b>{fmt_lp(row['lp'])}</b> / {LP_MAX}\n"
        f"{pe('star')} Репутация: <b>{row['reputation']}</b></blockquote>\n"
        f"<blockquote>{pe('msg')} Всего сообщений: <b>{stat_total(ALLOWED_CHAT_ID, user.id)}</b>\n"
        f"{pe('calendar')} Сегодня: <b>{stat_today(ALLOWED_CHAT_ID, user.id)}</b>\n"
        f"{pe('stats')} За неделю: <b>{stat_week(ALLOWED_CHAT_ID, user.id)}</b></blockquote>\n"
        f"{pe('fire')} Серия: <b>{(row['streak'] or 0)} {_days_word(row['streak'] or 0)} подряд</b>\n"
    )
    text += brawl_profile_line(ALLOWED_CHAT_ID, user.id)
    if user.id not in EXCLUDED_IDS:
        text += f"\n<blockquote>{progress_line(row['lp'])}</blockquote>"
    await update.message.reply_text(text, parse_mode=ParseMode.HTML)


async def submit_appeal(update, context, text):
    """Пересылает обжалование владельцу (и со-создателям) в ЛС с кнопками решения."""
    user = update.effective_user
    uname = f" @{user.username}" if user.username else ""
    body = (f"📨 <b>НОВОЕ ОБЖАЛОВАНИЕ</b>\n{CLAN_LINE}\n"
            f"От: <b>{esc(user.full_name)}</b>{esc(uname)}\n"
            f"ID: <code>{user.id}</code>\n\n"
            f"<blockquote>{esc(text)}</blockquote>\n"
            f"<i>Реши кнопкой ниже. Снять наказание в чате (мут/бан) нужно вручную.</i>")
    kb = InlineKeyboardMarkup([[
        InlineKeyboardButton("✅ Одобрить", callback_data=f"appeal:approve:{user.id}"),
        InlineKeyboardButton("❌ Отклонить", callback_data=f"appeal:reject:{user.id}")]])
    delivered = False
    for admin_id in ({LEADER_ID} | CONTEST_CREATORS):
        try:
            await context.bot.send_message(admin_id, body, parse_mode=ParseMode.HTML, reply_markup=kb)
            delivered = True
        except Exception:
            pass
    if delivered:
        return await update.message.reply_text(
            "✅ Обжалование отправлено администрации на рассмотрение. С тобой свяжутся. Спасибо!")
    return await update.message.reply_text(
        "⚠️ Не удалось доставить обжалование автоматически. Напиши напрямую администратору клана.")


async def owner_command(update, context, first, parts):
    """Управляющие команды владельца — работают ВЕЗДЕ (в чате и в личке), только для владельца.
    Возвращает True, если команда обработана."""
    if update.effective_user.id != LEADER_ID:
        return False
    if first in ("пранкменю", "prankmenu"):
        await update.message.reply_text(prank_menu_text(), reply_markup=prank_menu_kb(), parse_mode=ParseMode.HTML)
        return True
    if first in ("отчётнеделя", "отчетнеделя", "weeklyreport", "недельныйотчет"):
        await update.message.reply_text(build_weekly_report(ALLOWED_CHAT_ID), parse_mode=ParseMode.HTML)
        return True
    if first in ("отчётмесяц", "отчетмесяц", "monthlyreport", "месячныйотчет"):
        now = msk_now()
        if any(p in ("текущий", "этот", "this") for p in parts[1:]):
            mkey = now.strftime("%Y-%m")
        else:
            mkey = (now.replace(day=1) - timedelta(days=1)).strftime("%Y-%m")
        await update.message.reply_text(build_monthly_report(ALLOWED_CHAT_ID, mkey), parse_mode=ParseMode.HTML)
        return True
    if first in ("дайджест", "digest", "сводка"):
        left = ai_cooldown_left(update.effective_user.id)
        # отдельный КД дайджеста через meta
        last = meta_get("digest_last_ts")
        if last:
            try:
                mins = (msk_now() - datetime.fromisoformat(last)).total_seconds() / 60
                if mins < DIGEST_COOLDOWN_MIN:
                    await update.message.reply_text(
                        f"{pe('clock')} Дайджест недавно делался. Следующий через <b>{int(DIGEST_COOLDOWN_MIN - mins) + 1} мин</b>.",
                        parse_mode=ParseMode.HTML)
                    return True
            except Exception:
                pass
        await update.message.reply_text(f"{pe('clock')} Собираю дайджест тем чата…", parse_mode=ParseMode.HTML)
        text, err = await build_ai_digest()
        if err:
            await update.message.reply_text(f"{pe('note')} {err}", parse_mode=ParseMode.HTML)
            return True
        meta_set("digest_last_ts", msk_now().isoformat())
        await update.message.reply_text(
            f"{pe('stats')} <b>AI-ДАЙДЖЕСТ ЧАТА</b>\n{CLAN_LINE}\n{text}\n\n"
            f"<i>🔒 Приватность: только темы, без имён и цитат.</i>", parse_mode=ParseMode.HTML)
        return True
    if first in ("backup", "бэкап", "бекап"):
        await send_backup(update, context); return True
    if first in ("modlog", "log", "журнал"):
        await show_modlog(update, context, parts); return True
    if first in ("analytics", "dashboard", "аналитика", "статклан"):
        await clan_analytics(update, context); return True
    if first in ("checknorms", "проверканорм"):
        await update.message.reply_text(f"{pe('clock')} <b>Проверяю нормы за неделю…</b>", parse_mode=ParseMode.HTML)
        d, f, e = await run_norm_check(context, ALLOWED_CHAT_ID, announce=True)
        if d == 0 and f == 0 and e == 0:
            await update.message.reply_text(
                f"{pe('bulb')} Некого проверять — нет снимков кубков этой недели или Brawl API недоступен.",
                parse_mode=ParseMode.HTML)
        return True
    if first in ("месяцнормы", "monthnorms"):
        # ручной запуск большой месячной проверки. По умолчанию — за прошлый месяц; «текущий»/«this» — за этот.
        now = msk_now()
        if any(p in ("текущий", "этот", "this", "now") for p in parts[1:]):
            mkey = now.strftime("%Y-%m")
        else:
            mkey = (now.replace(day=1) - timedelta(days=1)).strftime("%Y-%m")
        await update.message.reply_text(
            f"{pe('clock')} <b>Большая проверка норm за {mkey}…</b>", parse_mode=ParseMode.HTML)
        n = await run_monthly_norms(context, ALLOWED_CHAT_ID, mkey)
        if n == 0:
            await update.message.reply_text(
                f"{pe('bulb')} За {mkey} нет закрытых недель по кубкам — выдавать нечего.",
                parse_mode=ParseMode.HTML)
        return True
    if first in ("snapshotnorms", "снимокнорм"):
        force = any(p in ("заново", "force") for p in parts[1:])
        ok = await run_norm_snapshot(context, ALLOWED_CHAT_ID, force=force)
        if ok:
            await update.message.reply_text(
                f"{pe('check')} <b>Снимок кубков сделан.</b>\n"
                f"{pe('warn')} <i>Точка отсчёта сброшена на СЕЙЧАС — недельный прирост у всех пошёл заново.</i>",
                parse_mode=ParseMode.HTML)
        else:
            await update.message.reply_text(
                f"{pe('warn')} Снимок этой недели уже есть ({meta_get('trophy_snapshot_ts') or '—'} МСК). "
                f"Повтор <b>обнулит прирост кубков</b> у всех. Если точно нужно: <code>.снимокнорм заново</code>.",
                parse_mode=ParseMode.HTML)
        return True
    if first in ("bonusall", "lpall", "грантвсем", "всемлп"):
        try:
            amount = int(parts[1]) if len(parts) > 1 else 0
        except ValueError:
            amount = 0
        if amount == 0 or abs(amount) > LP_MAX:
            await update.message.reply_text(
                f"{pe('lp')} <b>Массовое начисление LP</b>\n{CLAN_LINE}\n"
                f"<blockquote>Формат:\n<code>bonusall 150</code> — начислить всем\n"
                f"<code>bonusall -50</code> — списать у всех</blockquote>\n"
                f"Сумма: от 1 до {LP_MAX}.", parse_mode=ParseMode.HTML)
            return True
        rows = conn.execute("SELECT user_id FROM users WHERE chat_id=?", (ALLOWED_CHAT_ID,)).fetchall()
        n = 0
        for r in rows:
            if r["user_id"] in EXCLUDED_IDS or r["user_id"] == LEADER_ID:
                continue
            add_lp(ALLOWED_CHAT_ID, r["user_id"], amount)
            n += 1
        if amount > 0:
            try:
                await context.bot.send_message(
                    ALLOWED_CHAT_ID,
                    f"{pe('crown')} <b>ПОДАРОК КЛАНУ!</b>\n{CLAN_LINE}\n\n"
                    f"<blockquote>{pe('lp')} Каждому участнику начислено <b>+{amount} LP</b></blockquote>\n"
                    f"Получили: <b>{n}</b> чел. Спасибо, что вы с нами! {pe('fire')}", parse_mode=ParseMode.HTML)
            except Exception:
                pass
            await update.message.reply_text(
                f"{pe('check')} <b>Готово.</b> Начислено <b>+{amount} LP</b> {n} участникам, объявление — в чате.",
                parse_mode=ParseMode.HTML)
        else:
            await update.message.reply_text(
                f"{pe('check')} <b>Готово.</b> Списано <b>{amount} LP</b> у {n} участников (без объявления в чат).",
                parse_mode=ParseMode.HTML)
        return True
    return False


async def private_router(update, context):
    user = update.effective_user
    if not user:
        return
    text = (update.message.text or "").strip()
    # В личке команды тоже с точкой. Запоминаем, была ли она, и срезаем.
    had_dot = bool(CMD_PREFIX) and len(text) > 1 and text.startswith(CMD_PREFIX) and not text.startswith(CMD_PREFIX * 2)
    if had_dot:
        text = text[len(CMD_PREFIX):].strip()
    low = text.lower()
    is_creator = user.id in CONTEST_CREATORS
    gw_state = context.user_data.get("gw_state")
    appeal_state = context.user_data.get("appeal_state")

    # --- Эмодзи-ID (инструмент только для админов/создателей) ---
    if is_creator and not gw_state and not appeal_state:
        ents = (update.message.entities or []) + (update.message.caption_entities or [])
        emoji_ids = [e.custom_emoji_id for e in ents if getattr(e, "custom_emoji_id", None)]
        if emoji_ids:
            report = await emoji_id_report(context, emoji_ids)
            return await update.message.reply_text(
                f"🆔 Нашёл премиум-эмодзи ({len(emoji_ids)} шт.) — рядом базовый смайлик:\n{report}\n\n"
                f"Скопируй весь список и пришли мне.", parse_mode=ParseMode.HTML)
        st = getattr(update.message, "sticker", None)
        if st is not None:
            cid = getattr(st, "custom_emoji_id", None)
            if cid:
                report = await emoji_id_report(context, [cid])
                return await update.message.reply_text(
                    f"🆔 ID этого премиум-эмодзи:\n{report}\n\nПришли мне его.", parse_mode=ParseMode.HTML)
            return await update.message.reply_text(
                "Это обычный <b>стикер</b>, а не премиум-эмодзи 🙂 Премиум-эмодзи вставляются прямо в текст "
                "через значок 😀 у поля ввода.", parse_mode=ParseMode.HTML)

    # --- Отмена любого процесса ---
    if low in ("cancel", "отмена", "/cancel", "стоп"):
        context.user_data.pop("gw_state", None)
        context.user_data.pop("gw_new", None)
        context.user_data.pop("appeal_state", None)
        return await update.message.reply_text("❌ Отменено.")

    # --- Приём текста обжалования ---
    if appeal_state == "await_text":
        if len(text) < 10:
            return await update.message.reply_text("Опиши обжалование подробнее (хотя бы пару предложений). Или напиши «отмена».")
        context.user_data.pop("appeal_state", None)
        return await submit_appeal(update, context, text)

    # --- В личке команды тоже только с точкой. Без точки и без активного процесса — это не команда. ---
    if not had_dot and not gw_state and not appeal_state:
        return await update.message.reply_text(
            f"{pe('person')} Команды пишутся с точкой в начале: <code>.help</code>, <code>.profile</code>, "
            f"<code>.appeal</code> …", parse_mode=ParseMode.HTML)

    # --- Старт обжалования (для участников клана) ---
    if low in ("appeal",):
        if not await can_appeal(context, user.id):
            return await update.message.reply_text("Обжалования принимаются только от участников клана LINKOR.")
        context.user_data["appeal_state"] = "await_text"
        return await update.message.reply_text(
            f"⚖️ <b>Обжалование наказания</b>\n{CLAN_LINE}\n"
            "Опиши в <b>одном сообщении</b> по форме:\n"
            "<blockquote>1) Какое наказание (мут / бан / варн)\n"
            "2) Когда примерно выдали\n"
            "3) Почему считаешь, что его стоит снять</blockquote>\n"
            "Напиши всё одним сообщением — я передам администрации. Для отмены — «отмена».",
            parse_mode=ParseMode.HTML)

    # --- Помощь в ЛС (админам — кнопка админ-команд показывается автоматически) ---
    if low in ("help", "commands", "menu"):
        return await help_cmd(update, context)

    # --- Личный кабинет в ЛС (профиль/баланс/прогресс) ---
    if low in ("profile", "p", "lp", "balance", "cabinet"):
        return await dm_profile(update, context)

    # --- FAQ / соглашение (только в ЛС, английский триггер) ---
    if low in ("faq", "terms"):
        return await faq_info(update, context)

    # --- Управляющие команды владельца (только в личке) ---
    _parts = low.split()
    if _parts and await owner_command(update, context, _parts[0], _parts):
        return

    # --- Создание конкурса (только создатели) ---
    if is_creator:
        if not gw_state:
            if low in ("конкурс", "новыйконкурс", "/конкурс"):
                context.user_data["gw_new"] = {}
                context.user_data["gw_state"] = "name"
                return await update.message.reply_text(
                    f"{pe('note')} Создаём конкурс.\n\nШаг 1/4 — введи <b>НАЗВАНИЕ</b> конкурса:\n"
                    "(в любой момент напиши «отмена»)", parse_mode=ParseMode.HTML)
            return await update.message.reply_text(
                "Доступно в личке: пришли <b>премиум-эмодзи</b> — верну ID · <b>конкурс</b> — создать конкурс · "
                "<b>help</b> — команды.", parse_mode=ParseMode.HTML)
        data = context.user_data["gw_new"]
        if gw_state == "name":
            data["name"] = text
            context.user_data["gw_state"] = "timer"
            return await update.message.reply_text(
                "Шаг 2/4 — на сколько <b>МИНУТ</b> запустить конкурс? (например 60)", parse_mode=ParseMode.HTML)
        if gw_state == "timer":
            try:
                data["minutes"] = max(1, int(text))
            except ValueError:
                return await update.message.reply_text("Введи число минут, например 60.")
            context.user_data["gw_state"] = "winners"
            return await update.message.reply_text("Шаг 3/4 — сколько <b>ПОБЕДИТЕЛЕЙ</b>?", parse_mode=ParseMode.HTML)
        if gw_state == "winners":
            try:
                data["count"] = max(1, int(text))
            except ValueError:
                return await update.message.reply_text("Введи число победителей, например 1.")
            context.user_data["gw_state"] = "desc"
            return await update.message.reply_text(
                "Шаг 4/4 — введи <b>ОПИСАНИЕ / УСЛОВИЯ</b> конкурса:", parse_mode=ParseMode.HTML)
        if gw_state == "desc":
            data["desc"] = text
            context.user_data.pop("gw_state", None)
            context.user_data.pop("gw_new", None)
            if not ALLOWED_CHAT_ID:
                return await update.message.reply_text("⚠️ Не задан ALLOWED_CHAT_ID — некуда публиковать.")
            await publish_giveaway(context, data)
            return await update.message.reply_text(
                f"✅ Конкурс «{esc(data['name'])}» опубликован в чате клана!\n"
                f"Итоги подведутся автоматически через {data['minutes']} мин.", parse_mode=ParseMode.HTML)
        return

    # --- Обычный участник в ЛС ---
    return await update.message.reply_text(
        f"{pe('catneutral')} Я бот клана {CLAN_NAME}. В личке доступно:\n"
        "<blockquote>👤 <b>profile</b> — твой баланс, статистика и прогресс\n"
        "⚖️ <b>appeal</b> — обжаловать наказание (мут/бан/варн)\n"
        "📜 <b>faq</b> — соглашение и ответы на вопросы\n"
        "📋 <b>help</b> — список команд клана</blockquote>", parse_mode=ParseMode.HTML)


# ============================================================
#               ТРИГГЕРЫ (как в Iris)
# ============================================================
# 5.9: прокси для штабных команд — ответ ОТДЕЛЬНЫМ сообщением вместо реплая на (уже удалённую) команду.
class _StandaloneMessage:
    """Обёртка над Message: reply_text шлёт самостоятельное сообщение в чат (без цитаты). Остальное — как у оригинала."""
    __slots__ = ("_real", "_bot", "_chat_id", "_thread")

    def __init__(self, real_msg, bot, chat_id, thread):
        object.__setattr__(self, "_real", real_msg)
        object.__setattr__(self, "_bot", bot)
        object.__setattr__(self, "_chat_id", chat_id)
        object.__setattr__(self, "_thread", thread)

    async def reply_text(self, text, **kwargs):
        kwargs.pop("reply_to_message_id", None)
        kwargs.pop("reply_to_message", None)
        if self._thread and "message_thread_id" not in kwargs:
            kwargs["message_thread_id"] = self._thread
        return await self._bot.send_message(chat_id=self._chat_id, text=text, **kwargs)

    # алиас на случай вызова reply_html и т.п. — направляем в standalone
    async def reply_html(self, text, **kwargs):
        kwargs["parse_mode"] = "HTML"
        return await self.reply_text(text, **kwargs)

    def __getattr__(self, name):
        return getattr(self._real, name)


class _StandaloneUpdate:
    """Обёртка над Update: подменяет message/effective_message на standalone-версию, остальное делегирует."""
    __slots__ = ("_real", "_msg")

    def __init__(self, real_update, bot, chat_id, thread):
        object.__setattr__(self, "_real", real_update)
        object.__setattr__(self, "_msg", _StandaloneMessage(real_update.message, bot, chat_id, thread))

    @property
    def message(self):
        return self._msg

    @property
    def effective_message(self):
        return self._msg

    def __getattr__(self, name):
        return getattr(self._real, name)


# ============================================================
async def handle_triggers(update, context, text, low, parts):
    first = parts[0]
    # 5.9: штабную команду удаляем СРАЗУ, а ответ бота шлём ОТДЕЛЬНЫМ сообщением (без цитаты на команду).
    # Объекты PTB неизменяемы, поэтому оборачиваем update в лёгкий прокси, где reply_text → standalone send.
    if (first in STAFF_AUTODELETE_COMMANDS and update.effective_chat
            and update.effective_chat.type in ("group", "supergroup")
            and update.message and await can_use_admin(context, update.effective_user.id)):
        _chat_id = update.effective_chat.id
        _thread = getattr(update.message, "message_thread_id", None)
        try:
            await update.message.delete()        # удаляем команду мгновенно — никто её не видит
        except Exception:
            pass
        update = _StandaloneUpdate(update, context.bot, _chat_id, _thread)
    # Управляющие команды владельца работают ВЕЗДЕ (и в чате, и в ЛС). Единый обработчик owner_command.
    if first in ADMIN_DM_COMMANDS:
        if await owner_command(update, context, first, parts):
            return True
        return False
    # Гейт соглашения: команды участников недоступны без принятого FAQ (владелец/админы — мимо).
    # Не блокируем чтение соглашения/справки и обычные сообщения (только известные команды).
    u = update.effective_user
    if (u and first in GATED_COMMANDS and not has_agreed(u.id)):
        kb = InlineKeyboardMarkup([[InlineKeyboardButton("✅ Принять соглашение", url=faq_deeplink(context))]])
        await update.message.reply_text(
            f"{pe('lock')} Чтобы пользоваться ботом клана, сначала прими соглашение — кнопка ниже (откроется бот в личке).",
            parse_mode=ParseMode.HTML, reply_markup=kb)
        return True
    t = resolve_target(update, parts)
    rest = " ".join(parts[1:]) if len(parts) > 1 else "без указания причины"

    if first in ("profile", "p"):
        rt = await read_target(update, context, parts)
        if rt is False:
            await update.message.reply_text("Не нашёл такого участника. Он должен был хотя бы раз написать в чат после запуска бота.")
        else:
            await show_profile(update, context, rt)
        return True
    if first in ("карточка", "card", "карта"):
        cid = ALLOWED_CHAT_ID if update.effective_chat.type == "private" else update.effective_chat.id
        tgt = update.effective_user
        rt = await read_target(update, context, parts)
        if rt not in (False, None):
            tgt = rt
        ensure_user(cid, tgt)
        await context.bot.send_chat_action(update.effective_chat.id, "upload_photo")
        buf = render_profile_card(cid, tgt.id, tgt.full_name)
        if buf is None:
            await update.message.reply_text(
                f"{pe('note')} Не удалось сделать карточку (рендер недоступен). Открой текстовый профиль: <code>.p</code>",
                parse_mode=ParseMode.HTML)
            return True
        try:
            await context.bot.send_photo(
                update.effective_chat.id, photo=buf,
                caption=f"{pe('person')} Карточка <b>{esc(tgt.full_name)}</b> · {CLAN_NAME}",
                parse_mode=ParseMode.HTML)
        except Exception as e:
            log_error("отправка карточки", e)
            await update.message.reply_text(f"{pe('cross')} Не удалось отправить карточку.", parse_mode=ParseMode.HTML)
        return True
    if first in ("топдень", "деньтоп", "topday", "daytop", "dailytop") or \
       (first in ("top", "топ") and len(parts) > 1 and parts[1] in ("день", "дня", "сутки", "day", "daily", "today")):
        await update.message.reply_text(_day_top_text(update.effective_chat.id, 0),
                                        reply_markup=_day_top_kb(0, update.effective_user.id), parse_mode=ParseMode.HTML)
        return True
    if first in ("top",):
        await show_top(update, context, parts[1:])
        return True
    if first in ("streak", "стрик", "чекин", "checkin"):
        rt = await read_target(update, context, parts)
        if rt is False:
            await update.message.reply_text("Не нашёл такого участника.")
            return True
        row = get_user(update.effective_chat.id, rt.id)
        cur = (row["streak"] or 0) if row else 0
        best = (row["streak_best"] or 0) if row else 0
        nxt = next((m for m in sorted(STREAK_MILESTONES) if m > cur), None)
        who = "Твоя серия" if rt.id == update.effective_user.id else f"Серия <b>{esc(rt.full_name)}</b>"
        txt = (f"{pe('fire')} {who}: <b>{cur} {_days_word(cur)} подряд</b>\n"
               f"{pe('star')} Рекорд: <b>{best} {_days_word(best)}</b>")
        if nxt:
            txt += f"\n{pe('lp')} Следующая награда — на <b>{nxt}-й день</b> (+{STREAK_MILESTONES[nxt]} LP)"
        txt += "\n<i>Серия растёт за каждый день, когда ты пишешь в чат. Пропустил день — обнуляется.</i>"
        await update.message.reply_text(txt, parse_mode=ParseMode.HTML)
        return True
    if first in ("season", "сезон"):
        await show_season(update, context); return True
    if first in ("hof", "halloffame", "hall"):
        await show_hall_of_fame(update, context); return True
    if first in ("menu", "меню"):
        await menu_cmd(update, context); return True
    if first in ("orders", "заказы"):
        if not await require(update, context, ADMIN):
            return True
        rows = conn.execute(
            "SELECT id, name, item, price, ts FROM orders WHERE chat_id=? AND status='pending' ORDER BY id DESC LIMIT 20",
            (update.effective_chat.id,)).fetchall()
        if not rows:
            await update.message.reply_text(f"{pe('check')} Активных заявок нет.", parse_mode=ParseMode.HTML)
            return True
        lines = [f"{pe('shop')} <b>Активные заявки магазина</b>"]
        for o in rows:
            lines.append(f"#{o['id']} — <b>{esc(o['name'])}</b>: {esc(o['item'])} ({o['price']} LP)")
        lines.append(f"\n<i>Заявки приходят менеджеру в ЛС с кнопками «Выдано/Отклонить».</i>")
        await update.message.reply_text("\n".join(lines), parse_mode=ParseMode.HTML)
        return True
    if first in ("poll", "опрос", "голосование"):
        if not await require(update, context, MODER):
            return True
        await make_poll(update, context, text); return True
    if first in ("shop", "store"):
        await store_info(update, context); return True
    if first in ("tag", "brawl"):
        await cmd_brawl(update, context, parts); return True
    if first in ("untag", "untie"):
        ensure_user(update.effective_chat.id, update.effective_user)
        conn.execute("UPDATE users SET brawl_tag=NULL WHERE chat_id=? AND user_id=?",
                     (update.effective_chat.id, update.effective_user.id))
        conn.commit()
        await update.message.reply_text(f"{pe('check')} Игровой тег отвязан.", parse_mode=ParseMode.HTML)
        return True
    if first == "norms":
        await norms_info(update, context); return True
    if first in ("docs", "info", "version", "about", "changelog"):
        await docs_info(update, context); return True
    if first in ("faq", "terms"):
        await update.message.reply_text(
            f"{pe('scroll')} Соглашение и FAQ доступны в личке боту. Напиши мне в ЛС <b>faq</b> — пришлю полный текст.",
            parse_mode=ParseMode.HTML)
        return True
    if first in ("announce",):
        if await require(update, context, ADMIN):
            ann = text.split(maxsplit=1)[1].strip() if len(text.split()) > 1 else ""
            if not ann:
                await update.message.reply_text("Напиши текст: объявление <текст>")
            else:
                try:
                    await update.message.delete()
                except Exception:
                    pass
                kwargs = {"parse_mode": ParseMode.HTML}
                if ANNOUNCE_TOPIC_ID:
                    kwargs["message_thread_id"] = ANNOUNCE_TOPIC_ID
                await context.bot.send_message(
                    update.effective_chat.id,
                    f"{pe('mega')} <b>ОБЪЯВЛЕНИЕ {CLAN_NAME}</b>\n{CLAN_LINE}\n\n{esc(ann)}", **kwargs)
        return True
    if first in ("summon", "call"):
        if await require(update, context, ADMIN):
            await summon_all(update, context, text)
        return True
    if first in ("удалить", "remove", "delete", "del", "kickdb"):
        if not await require(update, context, ADMIN):
            return True
        tgt = resolve_target(update, parts)
        if not tgt:
            await update.message.reply_text(
                f"{pe('person')} Кого удалить из системы? Ответь на его сообщение или укажи: "
                f"<code>.удалить @ник</code> или <code>.удалить ID</code>.", parse_mode=ParseMode.HTML)
            return True
        if tgt.id in ({LEADER_ID} | EXCLUDED_IDS):
            await update.message.reply_text("Этого пользователя удалять нельзя (владелец/штатный админ).")
            return True
        purge_user(update.effective_chat.id, tgt.id)
        await update.message.reply_text(
            f"{pe('check')} {esc(getattr(tgt, 'full_name', 'Боец'))} удалён из системы — "
            f"пропал из всех топов, LP и статистики.", parse_mode=ParseMode.HTML)
        return True
    if first in ("отвязать", "untag", "отвязка"):
        if not await require(update, context, MODER):
            return True
        tgt = resolve_target(update, parts)
        if not tgt:
            await update.message.reply_text(
                f"{pe('game')} У кого снять игровой тег? Ответь на сообщение или укажи: "
                f"<code>.отвязать @ник</code> или <code>.отвязать ID</code>.", parse_mode=ParseMode.HTML)
            return True
        conn.execute("UPDATE users SET brawl_tag=NULL, brawl_trophies=0 WHERE chat_id=? AND user_id=?",
                     (update.effective_chat.id, tgt.id))
        conn.commit()
        await update.message.reply_text(
            f"{pe('check')} Тег у {esc(getattr(tgt, 'full_name', 'бойца'))} отвязан — "
            f"он пропал из топа по кубкам. Может привязать свой: <code>.tag #ТЕГ</code>.",
            parse_mode=ParseMode.HTML)
        return True
    if first in ("чистка", "cleanup", "prune"):
        if await require(update, context, ADMIN):
            await cleanup_left_members(update, context)
        return True
    if first in ("актив", "activity", "статдни"):
        if not await require(update, context, MODER):
            return True
        cid = ALLOWED_CHAT_ID if update.effective_chat.type == "private" else update.effective_chat.id
        rt = await read_target(update, context, parts)
        t = rt if rt not in (False, None) else update.effective_user
        mon, sun = week_bounds()
        rows = conn.execute(
            "SELECT day, cnt FROM stats WHERE chat_id=? AND user_id=? AND day BETWEEN ? AND ? ORDER BY day",
            (cid, t.id, mon, sun)).fetchall()
        det = "\n".join(f"{r['day']}: <b>{r['cnt']}</b>" for r in rows) or "<i>записей за эту неделю нет</i>"
        await update.message.reply_text(
            f"{pe('stats')} <b>АКТИВ (сырые записи счётчика) — {esc(t.full_name)}</b>\n"
            f"<i>неделя {mon} → {sun} · ID {t.id}</i>\n"
            f"<blockquote>{det}</blockquote>\n"
            f"Сумма за неделю: <b>{stat_week(cid, t.id)}</b> · сегодня: <b>{stat_today(cid, t.id)}</b>",
            parse_mode=ParseMode.HTML)
        return True
    # ===== 6.0 «ИМПЕРИЯ»: ивенты =====
    if first in ("ивент", "event", "ивенты", "events"):
        sub = parts[1].lower() if len(parts) > 1 else ""
        # запуск/стоп — только владелец
        if sub in EVENT_TYPES or sub in ("стоп", "stop", "off"):
            if not is_owner(update.effective_user.id):
                await update.message.reply_text(event_status_text(), parse_mode=ParseMode.HTML)
                return True
            if sub in ("стоп", "stop", "off"):
                meta_set("event_until", "")
                await update.message.reply_text(f"{pe('check')} Ивент остановлен.", parse_mode=ParseMode.HTML)
                if ALLOWED_CHAT_ID:
                    try:
                        await context.bot.send_message(ALLOWED_CHAT_ID,
                            f"{pe('note')} Ивент завершён администрацией. Спасибо за актив! 💙", parse_mode=ParseMode.HTML)
                    except Exception:
                        pass
                return True
            label, mult = EVENT_TYPES[sub]
            hrs = next((int(p) for p in parts[2:] if p.isdigit()), 24)
            hrs = max(1, min(EVENT_MAX_HOURS, hrs))
            until = (msk_now() + timedelta(hours=hrs)).isoformat()
            meta_set("event_until", until)
            meta_set("event_name", label)
            meta_set("event_mult", str(mult))
            await update.message.reply_text(
                f"{pe('check')} Запущен ивент <b>{label}</b> на <b>{hrs} ч</b> (x{mult:g} LP).", parse_mode=ParseMode.HTML)
            if ALLOWED_CHAT_ID:
                try:
                    await context.bot.send_message(ALLOWED_CHAT_ID,
                        f"{pe('mega')} <b>ИВЕНТ: {label}!</b>\n{CLAN_LINE}\n"
                        f"{pe('lp')} LP за сообщения — <b>x{mult:g}</b> на ближайшие <b>{hrs} ч</b>!\n"
                        f"Пишите, общайтесь, зарабатывайте больше! {pe('fire')}", parse_mode=ParseMode.HTML)
                except Exception:
                    pass
            return True
        # статус (всем)
        if is_owner(update.effective_user.id) and sub in ("", "help", "помощь"):
            await update.message.reply_text(
                f"{pe('game')} <b>ИВЕНТ-МОДУЛЬ</b>\n{CLAN_LINE}\n"
                f"{event_status_text()}\n\n"
                f"<blockquote>Запуск (владелец):\n"
                f"<code>.event double 24</code> — x2 LP на 24 ч\n"
                f"<code>.event triple 6</code> — x3 LP на 6 ч\n"
                f"<code>.event happy 2</code> — счастливый час\n"
                f"<code>.event stop</code> — остановить</blockquote>",
                parse_mode=ParseMode.HTML)
            return True
        await update.message.reply_text(event_status_text(), parse_mode=ParseMode.HTML)
        return True

    # ===== 5.7 «КАЗНА» =====
    # ===== 5.9 РЕЖИМ ВЛАДЕЛЬЦА: пранки и прямые сеттеры =====
    if first in ("пранк", "prank"):
        if not is_owner(update.effective_user.id):
            return True   # тихо игнорируем для не-владельца
        cid = ALLOWED_CHAT_ID if update.effective_chat.type == "private" else update.effective_chat.id
        sub = parts[1].lower() if len(parts) > 1 else ""
        if sub in ("стоп", "off", "сброс", "clear"):
            tgt = _owner_target(update, parts)
            if tgt in (False, None):
                await update.message.reply_text(f"{pe('note')} Кому снять пранк? Ответь на сообщение или укажи @ник.", parse_mode=ParseMode.HTML)
                return True
            conn.execute("UPDATE users SET prank=NULL WHERE chat_id=? AND user_id=?", (cid, tgt.id))
            conn.commit()
            await update.message.reply_text(f"{pe('check')} Пранк снят с <b>{esc(tgt.full_name)}</b> — профиль настоящий.", parse_mode=ParseMode.HTML)
            return True
        if sub in ("список", "list", "все"):
            rows = conn.execute("SELECT user_id, name, prank FROM users WHERE chat_id=? AND prank IS NOT NULL AND prank<>''", (cid,)).fetchall()
            if not rows:
                await update.message.reply_text(f"{pe('check')} Активных пранков нет.", parse_mode=ParseMode.HTML)
                return True
            lines = [f"• <b>{esc(disp_name(r))}</b>: <code>{esc(r['prank'])}</code>" for r in rows]
            await update.message.reply_text(f"{pe('game')} <b>Активные пранки:</b>\n" + "\n".join(lines), parse_mode=ParseMode.HTML)
            return True
        # .пранк @кто поле значение [поле значение ...]
        tgt = _owner_target(update, parts)
        if tgt in (False, None):
            await update.message.reply_text(
                f"{pe('game')} <b>ПРАНК-РЕЖИМ</b> (видят все, экономика не трогается)\n"
                f"<blockquote>Поля: лп, реп, ник, ранг, серия, сообщения, титул\n"
                f"<code>.пранк @ник лп 999999 реп -50</code>\n"
                f"<code>.пранк @ник ник Лохматый ранг Бронза I</code>\n"
                f"<code>.пранк стоп @ник</code> · <code>.пранк список</code></blockquote>",
                parse_mode=ParseMode.HTML)
            return True
        # собрать пары поле-значение из «сырых» слов после цели
        raw = update.message.text.split()
        # выкинем команду и токен-цель (@ник или это reply)
        toks = raw[1:]
        if toks and toks[0].startswith("@"):
            toks = toks[1:]
        overlay = _prank_get(get_user(cid, tgt.id))
        i = 0
        applied = []
        while i < len(toks):
            key = toks[i].lower().lstrip(".")
            if key in _PRANK_FIELDS and i + 1 < len(toks):
                okey, otype = _PRANK_FIELDS[key]
                if otype == "int":
                    try:
                        overlay[okey] = int(toks[i + 1].replace("+", ""))
                    except ValueError:
                        overlay[okey] = 0
                    applied.append(f"{key}={overlay[okey]}")
                    i += 2
                else:
                    # строковое поле — берём остаток до следующего известного поля
                    j = i + 1
                    words = []
                    while j < len(toks) and toks[j].lower().lstrip(".") not in _PRANK_FIELDS:
                        words.append(toks[j]); j += 1
                    overlay[okey] = clean_title(" ".join(words))   # 5.9: чистим < > & во всех строковых полях
                    applied.append(f"{key}=«{overlay[okey]}»")
                    i = j
            else:
                i += 1
        if not applied:
            await update.message.reply_text(f"{pe('note')} Не понял поля. Пример: <code>.пранк @ник лп 999999</code>", parse_mode=ParseMode.HTML)
            return True
        ensure_user(cid, tgt)
        conn.execute("UPDATE users SET prank=? WHERE chat_id=? AND user_id=?", (json.dumps(overlay, ensure_ascii=False), cid, tgt.id))
        conn.commit()
        await update.message.reply_text(
            f"{pe('check')} Пранк на <b>{esc(tgt.full_name)}</b>: {', '.join(applied)}\n"
            f"<i>Открой его профиль — увидишь фейк. Снять: <code>.пранк стоп</code> ответом.</i>",
            parse_mode=ParseMode.HTML)
        return True

    if first in ("форсквест", "forcequest", "квестдать", "завершитьквест"):
        if not is_owner(update.effective_user.id):
            return True
        cid = ALLOWED_CHAT_ID if update.effective_chat.type == "private" else update.effective_chat.id
        tgt = _owner_target(update, parts)
        if tgt in (False, None):
            await update.message.reply_text(
                f"{pe('note')} Кому завершить квест дня? Ответь на сообщение игрока или укажи @ник.\n"
                f"<code>.forcequest @ник</code>", parse_mode=ParseMode.HTML)
            return True
        ensure_user(cid, tgt)
        if quest_claimed(cid, tgt.id):
            await update.message.reply_text(f"{pe('check')} У <b>{esc(tgt.full_name)}</b> квест дня уже выполнен.", parse_mode=ParseMode.HTML)
            return True
        code, desc, _goal = quest_of_day(cid, tgt.id)
        conn.execute("INSERT OR IGNORE INTO quest_done (chat_id, user_id, day, code) VALUES (?,?,?,?)",
                     (cid, tgt.id, today_str(), code))
        conn.commit()
        new_title, total = update_quest_title(cid, tgt.id)
        msg = f"{pe('check')} Квест дня <b>{esc(tgt.full_name)}</b> засчитан вручную (всего: {total})."
        if new_title:
            msg += f"\n{pe('star')} Открыт титул <b>{new_title}</b>!"
        await update.message.reply_text(msg, parse_mode=ParseMode.HTML)
        # анонс в чат, если открылся титул
        if new_title and ALLOWED_CHAT_ID:
            try:
                await context.bot.send_message(
                    ALLOWED_CHAT_ID,
                    f"{pe('star')} <b>{esc(tgt.full_name)}</b> получает титул <b>{new_title}</b> за квесты! {pe('fire')}",
                    parse_mode=ParseMode.HTML)
            except Exception as e:
                log_error("форсквест-анонс", e)
        return True

    if first in ("задать", "set", "выдать"):
        if not is_owner(update.effective_user.id):
            return True
        cid = ALLOWED_CHAT_ID if update.effective_chat.type == "private" else update.effective_chat.id
        field = parts[1].lower() if len(parts) > 1 else ""
        tgt = _owner_target(update, parts)
        val = next((p for p in parts[2:] if p.lstrip("-+").isdigit()), None)
        if tgt in (False, None) or field not in ("реп", "репутация", "rep", "лп", "lp", "кубки", "trophies", "ранг", "rank") or val is None:
            await update.message.reply_text(
                f"{pe('game')} <b>ПРЯМЫЕ СЕТТЕРЫ</b> (меняют РЕАЛЬНЫЕ данные)\n"
                f"<blockquote><code>.задать реп @ник 100</code> — точное значение репутации\n"
                f"<code>.задать лп @ник 5000</code> — точный баланс LP\n"
                f"<code>.задать кубки @ник 30000</code> — кубки в профиле\n"
                f"<code>.задать ранг @ник 19</code> — ранг (значение; 19=Мастер I)</blockquote>",
                parse_mode=ParseMode.HTML)
            return True
        n = int(val)
        ensure_user(cid, tgt)
        if field in ("реп", "репутация", "rep"):
            conn.execute("UPDATE users SET reputation=? WHERE chat_id=? AND user_id=?", (n, cid, tgt.id))
            done = f"репутация = <b>{n}</b>"
        elif field in ("лп", "lp"):
            conn.execute("UPDATE users SET lp=? WHERE chat_id=? AND user_id=?", (max(0, min(LP_MAX, n)), cid, tgt.id))
            done = f"баланс = <b>{fmt_lp(max(0, min(LP_MAX, n)))} LP</b>"
        elif field in ("кубки", "trophies"):
            conn.execute("UPDATE users SET brawl_trophies=? WHERE chat_id=? AND user_id=?", (n, cid, tgt.id))
            done = f"кубки = <b>{n}</b>"
        else:  # ранг
            conn.execute("UPDATE users SET rank_best=? WHERE chat_id=? AND user_id=?", (n, cid, tgt.id))
            done = f"ранг = <b>{rank_name(n)}</b> (значение {n})"
        conn.commit()
        await update.message.reply_text(f"{pe('check')} <b>{esc(tgt.full_name)}</b>: {done}. Изменения реальные.", parse_mode=ParseMode.HTML)
        return True


    # ===== 5.8 «АРСЕНАЛ» =====
    if first in ("квест", "quest", "задание"):
        cid = ALLOWED_CHAT_ID if update.effective_chat.type == "private" else update.effective_chat.id
        me = update.effective_user
        ensure_user(cid, me)
        if me.id in EXCLUDED_IDS or me.id == LEADER_ID:
            await update.message.reply_text(f"{pe('note')} Квесты — для участников клана.", parse_mode=ParseMode.HTML)
            return True
        # 5.9: если требование уже выполнено (дуэль сыграна, респект получен) — засчитываем прямо при просмотре
        await try_complete_quest(context, cid, me.id, me.full_name)
        code, desc, goal = quest_of_day(cid, me.id)
        base = code.rstrip("0123456789") or code  # 5.9 ФИКС: обрезаем цифру у ВСЕХ кодов (duel1→duel, rep1→rep), иначе квесты не засчитывались
        # прогресс к следующему престиж-титулу
        total = quests_total(cid, me.id)
        nxt = next(((need, tt) for need, tt in QUEST_TITLES if total < need), None)
        _urow = get_user(cid, me.id)
        cur_t = (_urow["earned_title"] if _urow and "earned_title" in _urow.keys() else None)
        title_line = (f"{pe('star')} Твой титул за квесты: <b>{cur_t}</b>\n" if cur_t else "")
        if nxt:
            title_line += f"{pe('cup')} До титула <b>{nxt[1]}</b>: ещё <b>{nxt[0] - total}</b> квестов (всего: {total})\n"
        else:
            title_line += f"{pe('cup')} Все титулы открыты! Всего квестов: <b>{total}</b> 👑\n"
        done = quest_claimed(cid, me.id)
        if done:
            await update.message.reply_text(
                f"{pe('check')} <b>Квест дня выполнен!</b>\n«{desc}»\n{title_line}"
                f"Новый квест — завтра в 00:00 МСК.",
                parse_mode=ParseMode.HTML)
            return True
        prog = quest_progress(cid, me.id, base)
        bar = progress_bar(min(prog, goal), goal)
        await update.message.reply_text(
            f"{pe('game')} <b>КВЕСТ ДНЯ — {esc(me.full_name)}</b>\n{CLAN_LINE}\n"
            f"«{desc}»\n{bar} <b>{min(prog, goal)}/{goal}</b>\n{title_line}"
            f"{pe('star')} Награда: <b>престижный титул в профиль</b> · выполни до конца дня (МСК)",
            parse_mode=ParseMode.HTML)
        return True

    if first in ("ачивки", "achievements", "значки", "достижения"):
        cid = ALLOWED_CHAT_ID if update.effective_chat.type == "private" else update.effective_chat.id
        t = update.effective_user
        if (len(parts) > 1 or update.message.reply_to_message) and await eff_role(update, context) >= MODER:
            rt = await read_target(update, context, parts)
            if rt not in (False, None):
                t = rt
        ensure_user(cid, t)
        await check_achievements(context, cid, t.id, t.full_name)   # подтянуть свежие
        have = set(user_achievements(cid, t.id))
        lines = []
        for code, emoji, title, descr in ACHIEVEMENTS:
            mark = emoji if code in have else "🔒"
            status = "" if code in have else f" <i>— {descr}</i>"
            lines.append(f"{mark} <b>{title}</b>{status}")
        got = len(have)
        await update.message.reply_text(
            f"{pe('cup')} <b>ЗНАЧКИ — {esc(t.full_name)}</b>\n{CLAN_LINE}\n"
            f"Открыто: <b>{got}/{len(ACHIEVEMENTS)}</b>\n\n" + "\n".join(lines),
            parse_mode=ParseMode.HTML)
        return True

    if first in ("викторина", "quiz", "квиз"):
        cid = update.effective_chat.id
        if update.effective_chat.type not in ("group", "supergroup"):
            await update.message.reply_text(f"{pe('note')} Викторина — только в чате клана.", parse_mode=ParseMode.HTML)
            return True
        if _quiz_state.get(cid):
            await update.message.reply_text(f"{pe('note')} Викторина уже идёт — отвечай на текущий вопрос!", parse_mode=ParseMode.HTML)
            return True
        # 5.9: КД между викторинами + дневной лимит (владелец — без ограничений)
        if not is_owner(update.effective_user.id):
            last = _quiz_last.get(cid, 0)
            left = QUIZ_COOLDOWN_MIN * 60 - (time.time() - last)
            if left > 0:
                await update.message.reply_text(
                    f"{pe('clock')} Викторина недавно была. Следующую можно через <b>{int(left // 60) + 1} мин</b>.",
                    parse_mode=ParseMode.HTML)
                return True
            dkey = f"{cid}:{today_str()}"
            if _quiz_day_count.get(dkey, 0) >= QUIZ_DAILY_LIMIT:
                await update.message.reply_text(
                    f"{pe('note')} На сегодня викторин хватит ({QUIZ_DAILY_LIMIT}/день). Возвращайтесь завтра!",
                    parse_mode=ParseMode.HTML)
                return True
            _quiz_day_count[dkey] = _quiz_day_count.get(dkey, 0) + 1
        _quiz_last[cid] = time.time()
        q = random.choice(QUIZ_QUESTIONS)
        opts = list(enumerate(q["options"], 1))
        _quiz_state[cid] = {"answer": q["answer"], "ts": time.time(), "options": q["options"]}
        opts_txt = "\n".join(f"<b>{i}.</b> {esc(o)}" for i, o in opts)
        await update.message.reply_text(
            f"{pe('game')} <b>ВИКТОРИНА BRAWL STARS</b>\n{CLAN_LINE}\n"
            f"{esc(q['q'])}\n\n{opts_txt}\n\n"
            f"{pe('star')} Награда: <b>престиж к титулу «Эрудит»</b> · ответь номером: <code>.answer N</code> · "
            f"{QUIZ_TIMEOUT_SEC} сек",
            parse_mode=ParseMode.HTML)
        return True

    if first in ("ответ", "answer", "otvet"):
        cid = update.effective_chat.id
        st = _quiz_state.get(cid)
        if not st:
            await update.message.reply_text(f"{pe('note')} Сейчас нет активной викторины. Запусти: <code>.quiz</code>", parse_mode=ParseMode.HTML)
            return True
        if (time.time() - st["ts"]) > QUIZ_TIMEOUT_SEC:
            ans = st["options"][st["answer"] - 1]
            _quiz_state.pop(cid, None)
            await update.message.reply_text(
                f"{pe('clock')} Время вышло! Правильный ответ был: <b>{esc(ans)}</b>.", parse_mode=ParseMode.HTML)
            return True
        pick = next((p for p in parts[1:] if p.isdigit()), None)
        if not pick:
            await update.message.reply_text(f"{pe('note')} Ответь номером: <code>.answer 2</code>", parse_mode=ParseMode.HTML)
            return True
        me = update.effective_user
        if int(pick) == st["answer"]:
            _quiz_state.pop(cid, None)
            ensure_user(cid, me)
            new_title = None
            if me.id not in EXCLUDED_IDS and me.id != LEADER_ID:
                conn.execute("UPDATE users SET quiz_wins=COALESCE(quiz_wins,0)+1 WHERE chat_id=? AND user_id=?",
                             (cid, me.id))
                conn.commit()
                new_title = refresh_earned_title(cid, me.id)
            wins = quiz_wins_total(cid, me.id)
            if new_title:
                await update.message.reply_text(
                    f"{pe('star')} <b>{esc(me.full_name)}</b> отвечает верно и получает титул <b>{new_title}</b>! "
                    f"(побед в викторине: {wins}) {pe('fire')}", parse_mode=ParseMode.HTML)
            else:
                await update.message.reply_text(
                    f"{pe('cup')} <b>{esc(me.full_name)}</b> отвечает верно! Побед в викторине: <b>{wins}</b> {pe('fire')}",
                    parse_mode=ParseMode.HTML)
        else:
            await update.message.reply_text(
                f"{pe('cross')} <b>{esc(me.full_name)}</b>, неверно. Попробуют другие — время ещё есть!",
                parse_mode=ParseMode.HTML)
        return True

    if first in ("лотерея", "лото", "lottery"):
        cid = ALLOWED_CHAT_ID if update.effective_chat.type == "private" else update.effective_chat.id
        sub = parts[1].lower() if len(parts) > 1 else ""
        # розыгрыш — только админ
        if sub in ("розыгрыш", "draw", "розыграть", "итог"):
            if not await require(update, context, ADMIN):
                return True
            players = conn.execute("SELECT user_id, name FROM lottery WHERE chat_id=?", (cid,)).fetchall()
            if len(players) < LOTTERY_MIN_PLAYERS:
                # возврат билетов
                for p in players:
                    add_lp(cid, p["user_id"], LOTTERY_TICKET)
                conn.execute("DELETE FROM lottery WHERE chat_id=?", (cid,))
                conn.commit()
                await update.message.reply_text(
                    f"{pe('note')} Лотерея отменена: участников меньше {LOTTERY_MIN_PLAYERS}. "
                    f"Билеты возвращены ({len(players)} чел).", parse_mode=ParseMode.HTML)
                return True
            bank = len(players) * LOTTERY_TICKET
            win = random.choice(players)
            add_lp(cid, win["user_id"], bank)
            conn.execute("DELETE FROM lottery WHERE chat_id=?", (cid,))
            conn.commit()
            grant_achievement(cid, win["user_id"], "lucky")   # 5.8: значок «Счастливчик»
            await update.message.reply_text(
                f"{pe('mega')} <b>РОЗЫГРЫШ ЛОТЕРЕИ!</b>\n{CLAN_LINE}\n"
                f"Участников: <b>{len(players)}</b> · банк: <b>{bank} LP</b>\n"
                f"{pe('cup')} Победитель — <b>{esc(win['name'])}</b>! Забирает весь банк 🎉",
                parse_mode=ParseMode.HTML)
            return True
        # статус
        if sub in ("статус", "info", ""):
            n = lottery_count(cid)
            mine = conn.execute("SELECT 1 FROM lottery WHERE chat_id=? AND user_id=?",
                                (cid, update.effective_user.id)).fetchone()
            await update.message.reply_text(
                f"{pe('game')} <b>ЛОТЕРЕЯ {CLAN_NAME}</b>\n{CLAN_LINE}\n"
                f"Билет: <b>{LOTTERY_TICKET} LP</b> · участников: <b>{n}</b> · банк: <b>{n * LOTTERY_TICKET} LP</b>\n"
                f"{'✅ Ты в игре!' if mine else 'Купить билет: <code>.lottery ticket</code>'}\n"
                f"<i>Розыгрыш объявит администрация. Минимум {LOTTERY_MIN_PLAYERS} участника.</i>",
                parse_mode=ParseMode.HTML)
            return True
        # купить билет
        if sub in ("билет", "buy", "ticket", "вступить"):
            if update.effective_user.id in EXCLUDED_IDS:
                await update.message.reply_text("🛡️ Администраторы вне экономики не играют.", parse_mode=ParseMode.HTML)
                return True
            ensure_user(cid, update.effective_user)
            if conn.execute("SELECT 1 FROM lottery WHERE chat_id=? AND user_id=?",
                            (cid, update.effective_user.id)).fetchone():
                await update.message.reply_text(f"{pe('check')} Ты уже в лотерее. Жди розыгрыша!", parse_mode=ParseMode.HTML)
                return True
            bal = (get_user(cid, update.effective_user.id) or {"lp": 0})["lp"]
            if bal < LOTTERY_TICKET:
                await update.message.reply_text(
                    f"{pe('lp')} Не хватает LP на билет (нужно {LOTTERY_TICKET}, у тебя {fmt_lp(bal)}).",
                    parse_mode=ParseMode.HTML)
                return True
            add_lp(cid, update.effective_user.id, -LOTTERY_TICKET)
            conn.execute("INSERT INTO lottery (chat_id, user_id, name, ts) VALUES (?,?,?,?)",
                         (cid, update.effective_user.id, update.effective_user.full_name, msk_now().isoformat()))
            conn.commit()
            n = lottery_count(cid)
            await update.message.reply_text(
                f"{pe('check')} <b>{esc(update.effective_user.full_name)}</b> купил билет за {LOTTERY_TICKET} LP! "
                f"Теперь участников: <b>{n}</b>, банк: <b>{n * LOTTERY_TICKET} LP</b> {pe('fire')}",
                parse_mode=ParseMode.HTML)
            return True
        return True

    if first in ("дуэль", "duel", "дуель"):
        cid = update.effective_chat.id
        if update.effective_chat.type not in ("group", "supergroup"):
            await update.message.reply_text(f"{pe('note')} Дуэли — только в чате клана.", parse_mode=ParseMode.HTML)
            return True
        sub = parts[1].lower() if len(parts) > 1 else ""
        me = update.effective_user
        # принять/отклонить
        if sub in ("принять", "accept", "+"):
            d = _duel_pending.get(cid)
            if not d or d["to"] != me.id:
                await update.message.reply_text(f"{pe('note')} Для тебя нет активного вызова на дуэль.", parse_mode=ParseMode.HTML)
                return True
            if (time.time() - d["ts"]) > DUEL_TIMEOUT_MIN * 60:
                _duel_pending.pop(cid, None)
                await update.message.reply_text(f"{pe('clock')} Вызов истёк. Пусть бросят заново.", parse_mode=ParseMode.HTML)
                return True
            bet = d["bet"]
            ensure_user(cid, me)
            ba = (get_user(cid, d["from"]) or {"lp": 0})["lp"]
            bb = (get_user(cid, me.id) or {"lp": 0})["lp"]
            if ba < bet or bb < bet:
                _duel_pending.pop(cid, None)
                await update.message.reply_text(
                    f"{pe('lp')} У кого-то не хватает {bet} LP на дуэль — отменено.", parse_mode=ParseMode.HTML)
                return True
            # повторная проверка анти-абуза на момент ПРИНЯТИЯ (КД могло истечь иначе)
            block = duel_block_reason(cid, d["from"], me.id)
            if block:
                _duel_pending.pop(cid, None)
                await update.message.reply_text(
                    f"{pe('note')} Дуэль не состоится: {block[1]}.", parse_mode=ParseMode.HTML)
                return True
            winner_id, winner_name = (d["from"], d["from_name"]) if random.random() < 0.5 else (me.id, me.full_name)
            loser_id = me.id if winner_id == d["from"] else d["from"]
            # комиссия «казны»: часть ставки сгорает — слив другу через поддавки теперь убыточен
            rake = max(1, round(bet * DUEL_RAKE_PCT / 100))
            payout = bet - rake
            add_lp(cid, winner_id, payout)
            add_lp(cid, loser_id, -bet)
            conn.execute("INSERT INTO duels (chat_id, a, b, bet, winner, ts) VALUES (?,?,?,?,?,?)",
                         (cid, d["from"], me.id, bet, winner_id, msk_now().isoformat()))
            conn.commit()
            _duel_pending.pop(cid, None)
            await update.message.reply_text(
                f"{pe('mega')} <b>ДУЭЛЬ!</b> Ставка {bet} LP\n"
                f"{esc(d['from_name'])} ⚔️ {esc(me.full_name)}\n\n"
                f"{pe('cup')} Победитель — <b>{esc(winner_name)}</b>! +{payout} LP "
                f"<i>(комиссия казны {rake} LP)</i>",
                parse_mode=ParseMode.HTML)
            # 5.8: квест «сыграй дуэль» — обоим, ачивка «Дуэлянт» — победителю
            for uid, nm in ((d["from"], d["from_name"]), (me.id, me.full_name)):
                await try_complete_quest(context, cid, uid, nm)
            await check_achievements(context, cid, winner_id, winner_name)
            return True
        if sub in ("отмена", "cancel", "-"):
            d = _duel_pending.get(cid)
            if d and me.id in (d["from"], d["to"]):
                _duel_pending.pop(cid, None)
                await update.message.reply_text(f"{pe('check')} Дуэль отменена.", parse_mode=ParseMode.HTML)
            else:
                await update.message.reply_text(f"{pe('note')} Нет активной дуэли с тобой.", parse_mode=ParseMode.HTML)
            return True
        # вызов: дуэль <ставка> в ответ на сообщение соперника
        target = await read_target(update, context, parts) if update.message.reply_to_message else None
        bet_arg = next((p for p in parts[1:] if p.lstrip("-").isdigit()), None)
        if not update.message.reply_to_message or not bet_arg:
            await update.message.reply_text(
                f"{pe('game')} <b>ДУЭЛЬ НА LP</b>\n{CLAN_LINE}\n"
                f"Ответь на сообщение соперника: <code>.duel {DUEL_MIN}–{DUEL_MAX}</code>\n"
                f"Он принимает: <code>.duel accept</code>. Победитель забирает ставку.\n"
                f"<i>50/50 · комиссия казны {DUEL_RAKE_PCT}% · не чаще {DUEL_DAILY_LIMIT}/день · "
                f"перерыв {DUEL_COOLDOWN_MIN // 60} ч между дуэлями.</i>",
                parse_mode=ParseMode.HTML)
            return True
        if target in (False, None) or target.is_bot:
            await update.message.reply_text(f"{pe('note')} Не понял соперника. Ответь на его сообщение.", parse_mode=ParseMode.HTML)
            return True
        if target.id == me.id:
            await update.message.reply_text(f"{pe('note')} С собой дуэль не выйдет 🙂", parse_mode=ParseMode.HTML)
            return True
        bet = int(bet_arg)
        if bet < DUEL_MIN or bet > DUEL_MAX:
            await update.message.reply_text(
                f"{pe('note')} Ставка должна быть от {DUEL_MIN} до {DUEL_MAX} LP.", parse_mode=ParseMode.HTML)
            return True
        if me.id in EXCLUDED_IDS or target.id in EXCLUDED_IDS:
            await update.message.reply_text("🛡️ Администраторы вне экономики не участвуют в дуэлях.", parse_mode=ParseMode.HTML)
            return True
        ensure_user(cid, me); ensure_user(cid, target)
        bal = (get_user(cid, me.id) or {"lp": 0})["lp"]
        if bal < bet:
            await update.message.reply_text(
                f"{pe('lp')} У тебя только {fmt_lp(bal)} LP — на ставку {bet} не хватает.", parse_mode=ParseMode.HTML)
            return True
        block = duel_block_reason(cid, me.id, target.id)
        if block:
            whose = {"you": "У тебя", "opp": "У соперника", "pair": "С этим соперником"}.get(block[0], "Сейчас")
            await update.message.reply_text(
                f"{pe('note')} {whose} {block[1]}.", parse_mode=ParseMode.HTML)
            return True
        _duel_pending[cid] = {"from": me.id, "from_name": me.full_name,
                              "to": target.id, "to_name": target.full_name, "bet": bet, "ts": time.time()}
        await update.message.reply_text(
            f"{pe('mega')} <b>{esc(me.full_name)}</b> вызывает <b>{esc(target.full_name)}</b> на дуэль!\n"
            f"{pe('lp')} Ставка: <b>{bet} LP</b>\n"
            f"{mention(target)}, принимаешь? <code>.duel accept</code> "
            f"(или <code>.duel cancel</code>). Время: {DUEL_TIMEOUT_MIN} мин.",
            parse_mode=ParseMode.HTML)
        return True

    if first in ("титул", "title"):
        cid = ALLOWED_CHAT_ID if update.effective_chat.type == "private" else update.effective_chat.id
        sub = parts[1].lower() if len(parts) > 1 else ""
        me = update.effective_user
        if sub in ("снять", "remove", "clear"):
            conn.execute("UPDATE users SET title_text=NULL, title_until=NULL WHERE chat_id=? AND user_id=?", (cid, me.id))
            conn.commit()
            await update.message.reply_text(f"{pe('check')} Титул снят.", parse_mode=ParseMode.HTML)
            return True
        # 5.9: витрина титулов — все доступные и прогресс
        if sub in ("список", "list", "витрина", "все", "all"):
            ensure_user(cid, me)
            total = quests_total(cid, me.id)
            qwins = quiz_wins_total(cid, me.id)
            urow = get_user(cid, me.id)
            lines = [f"{pe('cup')} <b>ВИТРИНА ТИТУЛОВ {CLAN_NAME}</b>\n{CLAN_LINE}"]
            lines.append(f"{pe('star')} Титулы за квесты <i>(выполнено: {total})</i>:")
            for need, title in QUEST_TITLES:
                lines.append(f"{pe('check')} <b>{title}</b> — открыт" if total >= need
                             else f"🔒 {title} — ещё {need - total} квестов")
            lines.append(f"\n{pe('game')} Титулы за викторину <i>(побед: {qwins})</i>:")
            for need, title in QUIZ_TITLES:
                lines.append(f"{pe('check')} <b>{title}</b> — открыт" if qwins >= need
                             else f"🔒 {title} — ещё {need - qwins} побед")
            cur_show = (urow["earned_title"] if urow and "earned_title" in urow.keys() else None)
            lines.append(f"\n{pe('person')} Твой титул сейчас: <b>{esc(cur_show) if cur_show else 'нет — выполни 3 квеста или 3 викторины'}</b>")
            lines.append(f"{pe('lp')} <b>Платный титул:</b> свой текст на {TITLE_TEMP_DAYS} дней за "
                         f"{TITLE_TEMP_COST} LP — <code>.title buy ТЕКСТ</code> (показывается «под именем», титул-роль остаётся).")
            await update.message.reply_text("\n".join(lines), parse_mode=ParseMode.HTML)
            return True
        # купить временный титул: .титул купить <текст>
        if sub in ("купить", "buy"):
            raw = text.split(maxsplit=2)
            wanted = clean_title(raw[2]) if len(raw) > 2 else ""
            if not wanted:
                await update.message.reply_text(
                    f"{pe('game')} <b>ВРЕМЕННЫЙ ТИТУЛ</b>\n{CLAN_LINE}\n"
                    f"Свой титул в профиле на {TITLE_TEMP_DAYS} дней за <b>{TITLE_TEMP_COST} LP</b>.\n"
                    f"Команда: <code>.title buy ТВОЙ ТЕКСТ</code> (до {TITLE_MAX_LEN} симв.)",
                    parse_mode=ParseMode.HTML)
                return True
            if me.id in EXCLUDED_IDS:
                await update.message.reply_text("🛡️ Администраторы вне экономики не покупают.", parse_mode=ParseMode.HTML)
                return True
            ensure_user(cid, me)
            bal = (get_user(cid, me.id) or {"lp": 0})["lp"]
            if bal < TITLE_TEMP_COST:
                await update.message.reply_text(
                    f"{pe('lp')} Не хватает LP (нужно {TITLE_TEMP_COST}, у тебя {fmt_lp(bal)}).", parse_mode=ParseMode.HTML)
                return True
            add_lp(cid, me.id, -TITLE_TEMP_COST)
            until = (msk_now() + timedelta(days=TITLE_TEMP_DAYS)).strftime("%Y-%m-%d")
            conn.execute("UPDATE users SET title_text=?, title_until=? WHERE chat_id=? AND user_id=?",
                         (wanted, until, cid, me.id))
            conn.commit()
            await update.message.reply_text(
                f"{pe('star')} <b>{esc(me.full_name)}</b> теперь носит титул «<b>{esc(wanted)}</b>» до {_fmt_day_human(until)}! "
                f"(−{TITLE_TEMP_COST} LP)", parse_mode=ParseMode.HTML)
            return True
        # выдать титул вручную (админ): .титул дать @кто текст  (бессрочно)
        if sub in ("дать", "give", "set"):
            if not await require(update, context, ADMIN):
                return True
            tgt = await read_target(update, context, parts)
            if tgt in (False, None):
                await update.message.reply_text(f"{pe('note')} Кому? Ответь на сообщение или укажи @ник, потом текст.", parse_mode=ParseMode.HTML)
                return True
            # текст титула: при ответе на сообщение — со 2-го слова (.титул дать ТЕКСТ),
            # при указании @ника аргументом — с 3-го (.титул дать @ник ТЕКСТ)
            if update.message.reply_to_message:
                raw = text.split(maxsplit=2)
                wanted = clean_title(raw[2]) if len(raw) > 2 else ""
            else:
                raw = text.split(maxsplit=3)
                wanted = clean_title(raw[3]) if len(raw) > 3 else ""
            if not wanted:
                await update.message.reply_text(f"{pe('note')} Укажи текст титула после имени.", parse_mode=ParseMode.HTML)
                return True
            ensure_user(cid, tgt)
            conn.execute("UPDATE users SET title_text=?, title_until=NULL WHERE chat_id=? AND user_id=?",
                         (wanted, cid, tgt.id))
            conn.commit()
            await update.message.reply_text(
                f"{pe('star')} <b>{esc(tgt.full_name)}</b> получает титул «<b>{esc(wanted)}</b>» (бессрочно).",
                parse_mode=ParseMode.HTML)
            return True
        # показать свой
        row = get_user(cid, me.id)
        cur = active_title(row) if row else ""
        await update.message.reply_text(
            (f"{pe('star')} Твой титул: «<b>{esc(cur)}</b>»" +
             (f" <i>(до {_fmt_day_human(row['title_until'])})</i>" if row and row["title_until"] else " <i>(бессрочно)</i>"))
            if cur else
            f"{pe('game')} У тебя нет титула. Купить на {TITLE_TEMP_DAYS} дней: <code>.title buy ТЕКСТ</code> ({TITLE_TEMP_COST} LP).",
            parse_mode=ParseMode.HTML)
        return True

    if first in ("ранг", "rank"):
        cid = ALLOWED_CHAT_ID if update.effective_chat.type == "private" else update.effective_chat.id
        sub = parts[1] if len(parts) > 1 else ""
        if sub in ("тест", "test"):
            if not await require(update, context, ADMIN):
                return True
            tag = next((p for p in parts[2:] if p.startswith("#")), None)
            if not tag:
                me = conn.execute("SELECT brawl_tag FROM users WHERE chat_id=? AND user_id=?",
                                  (cid, update.effective_user.id)).fetchone()
                tag = me["brawl_tag"] if me and me["brawl_tag"] else None
            if not tag:
                await update.message.reply_text(
                    f"{pe('note')} Укажи тег: <code>.rank test #ТЕГ</code> (или сначала привяжи свой: <code>.tag #ТЕГ</code>).",
                    parse_mode=ParseMode.HTML)
                return True
            status = await update.message.reply_text(
                f"{pe('clock')} Читаю журнал боёв <code>{esc(tag)}</code> — ищу ранговые матчи…",
                parse_mode=ParseMode.HTML)
            data = await brawl_battlelog(tag)
            if data is None:
                txt = f"{pe('cross')} Не удалось получить журнал боёв (API недоступен или тег неверный)."
            else:
                ent = extract_ranked_entries(data, tag)
                if not ent:
                    txt = (f"{pe('note')} В последних 25 боях <code>{esc(tag)}</code> <b>нет ранговых матчей</b>.\n"
                           f"Сыграй ранговую и повтори <code>.rank test</code> — журнал хранит только 25 последних боёв.")
                else:
                    rows_txt = "\n".join(
                        f"{_bs_dt(e['ts'])} · {esc(str(e['result']))} · значение <b>{e['value']}</b> → {rank_name(e['value'])}"
                        for e in ent[:6])
                    mx = max(e["value"] for e in ent)
                    txt = (f"{pe('stats')} <b>РАНГ ТЕСТ — сырые данные из журнала боёв</b>\n"
                           f"Тег: <code>{esc(tag)}</code> · ранговых боёв в журнале: <b>{len(ent)}</b>\n"
                           f"<blockquote expandable>{rows_txt}</blockquote>\n"
                           f"{pe('cup')} Максимум: <b>{mx}</b> → по карте это <b>{rank_name(mx)}</b>\n"
                           f"<i>Сверь с реальным рангом в игре и пришли скрин — если расходится, поправим карту. "
                           f"Карта рангов подтверждена боем 12.06.2026.</i>")
            try:
                await status.edit_text(txt, parse_mode=ParseMode.HTML)
            except Exception:
                await update.message.reply_text(txt, parse_mode=ParseMode.HTML)
            return True
        # .ранг — свой (или цель для модераторов)
        t = update.effective_user
        if (len(parts) > 1 or update.message.reply_to_message) and await eff_role(update, context) >= MODER:
            rt = await read_target(update, context, parts)
            if rt not in (False, None):
                t = rt
        ensure_user(cid, t)
        row = conn.execute("SELECT brawl_tag, rank_best, rank_seen FROM users WHERE chat_id=? AND user_id=?",
                           (cid, t.id)).fetchone()
        if not row or not row["brawl_tag"]:
            await update.message.reply_text(
                f"{pe('note')} Сначала привяжи тег: <code>.tag #ТЕГ</code> — и сыграй ранговые бои.",
                parse_mode=ParseMode.HTML)
            return True
        best = row["rank_best"]
        congrats = ""
        # живая проверка журнала боёв: новый максимум сразу сохранится, награды за ступени начислятся тут же
        data = await brawl_battlelog(row["brawl_tag"])
        ent = extract_ranked_entries(data, row["brawl_tag"]) if data else []
        if ent:
            mx = max(e["value"] for e in ent)
            best, awards = apply_rank_progress(cid, t.id, mx)
            if awards:
                congrats = "\n" + "\n".join(
                    f"{pe('star')} Впервые ступень <b>{tn}</b> — <b>+{lp} LP</b>!" for tn, lp in awards)
        if best is None:
            await update.message.reply_text(
                f"{pe('note')} <b>{esc(t.full_name)}</b>: ранговых боёв пока не видно. "
                f"Сыграй ранговую — бот заметит сам (сканирует журналы ежедневно) или сразу после боя набери <code>.rank</code>.\n"
                f"<i>Журнал хранит 25 последних боёв.</i>", parse_mode=ParseMode.HTML)
            return True
        seen = row["rank_seen"] or today_str()
        await update.message.reply_text(
            f"{pe('cup')} <b>РАНГ — {esc(t.full_name)}</b>\n"
            f"Лучший за сезон: <b>{rank_name(best)}</b> <i>(значение {best} · {_fmt_day_human(seen)})</i>{congrats}\n"
            f"<i>по журналу боёв · авто-скан ежедневно в 13:00</i>",
            parse_mode=ParseMode.HTML)
        return True
    if first in ("топранг", "rankedtop") or \
       (first in ("top", "топ") and len(parts) > 1 and parts[1] in ("ранг", "ранги", "rank", "ranked")):
        rows = conn.execute(
            "SELECT name, username, user_id, rank_best AS val FROM users WHERE chat_id=? AND rank_best IS NOT NULL "
            "AND COALESCE(gone,0)=0 AND user_id<>? ORDER BY val DESC, name LIMIT 10",
            (update.effective_chat.id, LEADER_ID)).fetchall()
        if not rows:
            await update.message.reply_text(
                f"{pe('game')} Пока ни у кого не замечен ранг. Привяжите теги (<code>.tag #ТЕГ</code>) и играйте ранговые — "
                f"бот сканирует журналы ежедневно.", parse_mode=ParseMode.HTML)
            return True
        medals = ["🥇", "🥈", "🥉"]
        lines = [f"{(medals[i] if i < 3 else str(i + 1) + '.')} {esc(disp_name(r))} — <b>{rank_name(r['val'])}</b>"
                 for i, r in enumerate(rows)]
        await update.message.reply_text(
            f"{pe('cup')} <b>ТОП {CLAN_NAME} — по рангам</b>\n<i>лучший за сезон · обновляется ежедневно в 13:00</i>\n\n"
            + "\n".join(lines), parse_mode=ParseMode.HTML)
        return True
    if first in ("нормы", "норма", "norms"):
        cid = ALLOWED_CHAT_ID if update.effective_chat.type == "private" else update.effective_chat.id
        sub = parts[1] if len(parts) > 1 else ""
        if sub in ("тест", "test", "прогон"):
            if not await require(update, context, ADMIN):
                return True
            status = await update.message.reply_text(
                f"{pe('clock')} Делаю сухой прогон проверки норм — опрашиваю кубки привязанных…",
                parse_mode=ParseMode.HTML)
            txt = await norm_dry_run(cid)
            try:
                await status.edit_text(txt, parse_mode=ParseMode.HTML)
            except Exception:
                await update.message.reply_text(txt, parse_mode=ParseMode.HTML)
            return True
        if sub in ("снимок", "snapshot"):
            if not await require(update, context, ADMIN):
                return True
            if meta_get("trophy_snapshot_week") == week_bounds()[0] and not any(
                    p in ("заново", "force") for p in parts[2:]):
                await update.message.reply_text(
                    f"{pe('warn')} Снимок этой недели <b>уже сделан</b> ({meta_get('trophy_snapshot_ts') or '—'} МСК). "
                    f"Повторный снимок <b>обнулит прирост кубков</b> у всех — поэтому я его не делаю.\n"
                    f"Если точно нужно: <code>.нормы снимок заново</code>.", parse_mode=ParseMode.HTML)
                return True
            status = await update.message.reply_text(
                f"{pe('clock')} Делаю снимок недели: отметки участников + кубки привязанных…",
                parse_mode=ParseMode.HTML)
            await run_norm_snapshot(context, cid, force=True)   # предупреждение и слово «заново» проверены выше
            n = conn.execute("SELECT COUNT(*) AS c FROM users WHERE chat_id=? AND trophy_base_week=?",
                             (cid, week_bounds()[0])).fetchone()["c"]
            done = (f"{pe('check')} Снимок недели готов. Кубки зафиксированы у <b>{n}</b> привязанных.\n"
                    f"{pe('warn')} <i>Точка отсчёта сброшена на СЕЙЧАС — недельный прирост у всех пошёл заново.</i>\n"
                    f"Теперь <code>.нормы тест</code> покажет полную картину.")
            try:
                await status.edit_text(done, parse_mode=ParseMode.HTML)
            except Exception:
                await update.message.reply_text(done, parse_mode=ParseMode.HTML)
            return True
        t = update.effective_user
        if (len(parts) > 1 or update.message.reply_to_message) and await eff_role(update, context) >= MODER:
            rt = await read_target(update, context, parts)
            if rt not in (False, None):
                t = rt
        ensure_user(cid, t)
        await update.message.reply_text(await norms_status_text(cid, t.id, t.full_name),
                                        parse_mode=ParseMode.HTML)
        return True
    if first in ("ошибки", "errors", "журналошибок"):
        if not await require(update, context, ADMIN):
            return True
        rows = errors_last(10)
        if not rows:
            await update.message.reply_text(f"{pe('check')} Журнал чист — ошибок не зафиксировано. Бот работает ровно.",
                                            parse_mode=ParseMode.HTML)
            return True
        lines = [f"<b>{r['ts'][11:16]} {r['ts'][8:10]}.{r['ts'][5:7]}</b> · {esc(r['place'])}: "
                 f"<code>{esc((r['text'] or '')[:160])}</code>" for r in rows]
        await update.message.reply_text(
            f"{pe('warn')} <b>Журнал ошибок бота</b>\n"
            f"{pe('clock')} Сегодня: <b>{errors_on(today_str())}</b> · показаны последние {len(rows)}\n"
            f"<blockquote expandable>" + "\n".join(lines) + "</blockquote>",
            parse_mode=ParseMode.HTML)
        return True
    if first in ("отчет", "отчёт", "report"):
        if not await require(update, context, ADMIN):
            return True
        ok = await send_daily_report(context, update.effective_chat.id, today_str(),
                                     recipient=update.effective_user.id)
        if ok:
            await update.message.reply_text(
                f"{pe('folder')} Отчёт по чату за сегодня отправил тебе в личку {pe('envelope')} "
                f"(полная таблица — в файле, открывается в Excel; плюс график активности).",
                parse_mode=ParseMode.HTML)
        else:
            await update.message.reply_text(
                f"{pe('catneutral')} За сегодня пока нечего показывать (никто не писал и ничего не происходило), "
                f"либо я не смог написать тебе в личку — открой со мной личку и попробуй снова.",
                parse_mode=ParseMode.HTML)
        return True
    if first in ("утро", "доброеутро", "morning"):
        if not await require(update, context, ADMIN):
            return True
        await _send_greeting(context, update.effective_chat.id,
                             build_morning_message(update.effective_chat.id), await _morning_gif())
        return True
    if first in ("ночь", "ночнойрежим", "night"):
        if not await require(update, context, ADMIN):
            return True
        await _send_greeting(context, update.effective_chat.id, build_night_message(), await _night_gif())
        return True
    if first in ("добавитьгиф", "addgif", "гиф+"):
        if not await require(update, context, ADMIN):
            return True
        kind = "morning" if (len(parts) > 1 and parts[1].lower() in ("утро", "morning", "m")) else \
               ("night" if (len(parts) > 1 and parts[1].lower() in ("ночь", "night", "n")) else None)
        if not kind:
            await update.message.reply_text(
                f"{pe('note')} Укажи, куда добавить: <code>.добавитьгиф утро</code> или <code>.добавитьгиф ночь</code> "
                f"— и обязательно <b>ответом на гифку</b>.", parse_mode=ParseMode.HTML)
            return True
        rep = update.message.reply_to_message
        anim = (rep.animation if rep else None) or (rep.document if rep else None)
        if not anim or not getattr(anim, "file_id", None):
            await update.message.reply_text(
                f"{pe('cross')} Не вижу гифку. Сделай так: пришли гифку в чат, потом <b>ответь на неё</b> "
                f"командой <code>.добавитьгиф {'утро' if kind=='morning' else 'ночь'}</code>.",
                parse_mode=ParseMode.HTML)
            return True
        gif_add(kind, anim.file_id)
        word = "утренних" if kind == "morning" else "ночных"
        await update.message.reply_text(
            f"{pe('check')} Гифка добавлена в копилку! Теперь {word} гифок: <b>{gif_count(kind)}</b>. "
            f"Бот будет брать случайную в приветствии {pe('catlove')}", parse_mode=ParseMode.HTML)
        return True
    if first in ("гифы", "gifs", "гифки"):
        if not await require(update, context, ADMIN):
            return True
        await update.message.reply_text(
            f"{pe('folder')} <b>Копилка гифок</b>\n"
            f"{pe('star')} Утро: <b>{gif_count('morning')}</b>\n"
            f"{pe('lock')} Ночь: <b>{gif_count('night')}</b>\n"
            f"<blockquote expandable>Добавить: пришли гифку и ответь на неё "
            f"<code>.добавитьгиф утро</code> (или <code>ночь</code>).\n"
            f"Очистить: <code>.очиститьгифы утро</code> / <code>ночь</code>.\n"
            f"Гифки хранятся внутри Telegram и не теряются.</blockquote>",
            parse_mode=ParseMode.HTML)
        return True
    if first in ("очиститьгифы", "cleargifs"):
        if not await require(update, context, ADMIN):
            return True
        kind = "morning" if (len(parts) > 1 and parts[1].lower() in ("утро", "morning", "m")) else \
               ("night" if (len(parts) > 1 and parts[1].lower() in ("ночь", "night", "n")) else None)
        if not kind:
            await update.message.reply_text(
                f"{pe('note')} Уточни: <code>.очиститьгифы утро</code> или <code>.очиститьгифы ночь</code>.",
                parse_mode=ParseMode.HTML)
            return True
        n = gif_count(kind)
        gif_clear(kind)
        await update.message.reply_text(
            f"{pe('check')} Копилка {'утренних' if kind=='morning' else 'ночных'} гифок очищена (было {n}).",
            parse_mode=ParseMode.HTML)
        return True
    if first in ("finish", "endgw") and active_giveaways.get(update.effective_chat.id):
        await end_giveaway(update, context); return True
    if first == "who" and t:
        await update.message.reply_text(
            f"👤 {esc(t.full_name)}\nID: <code>{t.id}</code>"
            + (f"\n@{esc(t.username)}" if t.username else ""), parse_mode=ParseMode.HTML)
        return True
    if first in ("rules",):
        await rules_cmd(update, context); return True
    if first in ("help", "commands"):
        await help_cmd(update, context); return True
    if first in ("id", "chatid"):
        tid = getattr(update.message, "message_thread_id", None)
        txt = f"ID чата: <code>{update.effective_chat.id}</code>"
        if tid:
            txt += f"\nID темы (топика): <code>{tid}</code>"
        await update.message.reply_text(txt, parse_mode=ParseMode.HTML)
        return True
    if first in ("emojiid", "emoji"):
        rep = update.message.reply_to_message
        ents = (rep.entities or []) + (rep.caption_entities or []) if rep else []
        ids = [e.custom_emoji_id for e in ents if getattr(e, "custom_emoji_id", None)]
        if not rep:
            await update.message.reply_text(
                "Чтобы узнать ID премиум-эмодзи: проще всего напиши боту в ЛИЧКУ сообщение с нужными "
                "анимированными эмодзи — он сразу пришлёт их ID. Либо ответь этой командой на сообщение с эмодзи.",
                parse_mode=ParseMode.HTML)
        elif not ids:
            await update.message.reply_text("В том сообщении нет премиум-эмодзи (нужны именно анимированные из набора, не обычные).")
        else:
            report = await emoji_id_report(context, ids)
            await update.message.reply_text(
                f"🆔 Премиум-эмодзи ({len(ids)} шт.) — рядом базовый смайлик:\n{report}\n\nСкопируй и пришли мне.",
                parse_mode=ParseMode.HTML)
        return True

    # ---- Мини-игры ----
    if first in ("dice",):
        await game_dice(update, context, "🎲"); return True
    if first in ("darts",):
        await game_dice(update, context, "🎯"); return True
    if first in ("basket", "basketball"):
        await game_dice(update, context, "🏀"); return True
    if first in ("football", "goal"):
        await game_dice(update, context, "⚽"); return True
    if first in ("bowling",):
        await game_dice(update, context, "🎳"); return True
    if first in ("casino", "slot"):
        await game_dice(update, context, "🎰"); return True
    if first in ("coin", "flip"):
        await update.message.reply_text(f"{pe('lp')} " + random.choice(["Орёл!", "Решка!"]), parse_mode=ParseMode.HTML); return True
    if first in ("8ball", "ball"):
        await game_8ball(update, context, " ".join(parts[1:])); return True
    if first in ("rps",):
        await game_rps(update, context, parts[1] if len(parts) > 1 else ""); return True
    if first in ("pick", "choose", "random"):
        await game_pick(update, context, " ".join(parts[1:])); return True
    if first in ("number", "guess"):
        await game_number(update, context, parts[1] if len(parts) > 1 else None); return True
    if first in ("duel",) and t:
        await game_duel(update, context, t); return True
    if first in ("roll",):
        await update.message.reply_text(f"🎲 Твоё число: {random.randint(1, 100)}"); return True

    # ---- AI ----
    if first in ("ai", "linkor", "ask"):
        await ai_ask(update, context, " ".join(parts[1:])); return True
    if first == "joke":
        await ai_fun(update, context, "joke"); return True
    if first == "fact":
        await ai_fun(update, context, "fact"); return True
    if first == "toast":
        await ai_fun(update, context, "toast"); return True
    if first in ("summary",):
        await ai_summary(update, context); return True

    if first in ("lp",):
        has_value = len(parts) > 1 and (parts[1][:1] in "+-=" or parts[1].lstrip("+-=").isdigit())
        if has_value:
            # АДМИН: начисление/установка по ответу или @юзеру
            if not await require(update, context, ADMIN):
                return True
            tgt = resolve_target(update, parts)
            if not tgt:
                await update.message.reply_text("Кому начислять? Ответь на сообщение участника или укажи @юзера. Пример: lp 200 (ответом).")
                return True
            arg = parts[1]
            ensure_user(update.effective_chat.id, tgt)
            if arg.startswith("="):
                try:
                    val = set_lp(update.effective_chat.id, tgt.id, int(arg[1:]))
                    await update.message.reply_text(f"{pe('lp')} LP <b>{esc(tgt.full_name)}</b> = <b>{fmt_lp(val)}</b>.", parse_mode=ParseMode.HTML)
                except ValueError:
                    await update.message.reply_text("Формат: lp =500")
            else:
                try:
                    n = int(arg)
                    if n == 0:
                        await update.message.reply_text(
                            f"{pe('note')} Ноль LP ничего не изменит 😄 Укажи число: <code>lp 200</code> или <code>lp -50</code>.",
                            parse_mode=ParseMode.HTML)
                        return True
                    val = add_lp(update.effective_chat.id, tgt.id, n)
                    await update.message.reply_text(
                        f"{pe('lp')} {'+' if n > 0 else ''}{n} LP → <b>{esc(tgt.full_name)}</b>. Баланс: <b>{fmt_lp(val)}</b>.",
                        parse_mode=ParseMode.HTML)
                except ValueError:
                    await update.message.reply_text("Формат: lp 200 / lp -50 / lp =500")
            return True
        # ЧТЕНИЕ: по умолчанию свой баланс; чужой — только по явному @юзеру (или ответ для модера+)
        rt = await read_target(update, context, parts)
        if rt is False:
            await update.message.reply_text("Не нашёл такого участника. Он должен был хотя бы раз написать в чат после запуска бота.")
        elif rt.id == update.effective_user.id:
            row = get_user(update.effective_chat.id, rt.id)
            await update.message.reply_text(f"{pe('lp')} У тебя <b>{fmt_lp(row['lp'] if row else 0)}</b> LP.", parse_mode=ParseMode.HTML)
        else:
            row = get_user(update.effective_chat.id, rt.id)
            await update.message.reply_text(f"{pe('lp')} У <b>{esc(rt.full_name)}</b>: <b>{fmt_lp(row['lp'] if row else 0)}</b> LP.", parse_mode=ParseMode.HTML)
        return True

    if text == "+" or first in ("+rep", "rep"):
        if t:
            await give_rep(update, context, t)
            return True

    if text == "-" or first in ("-rep", "-реп", "минусреп"):
        if t:
            await give_rep(update, context, t, delta=-1)
            return True

    if not t:
        return False

    # --- СЕКРЕТНЫЕ команды начисления LP за игровые нормы (только админ, не в help) ---
    is_norm_rank = first in ("нормаранг",) or (first == "норма" and len(parts) > 1 and parts[1] in ("ранг", "ранга"))
    is_norm_cups = first in ("нормакубки",) or (first == "норма" and len(parts) > 1 and parts[1] in ("кубки", "кубков"))
    if is_norm_rank or is_norm_cups:
        if await eff_role(update, context) < ADMIN:
            return False                      # не раскрываем команду обычным участникам
        amount = NORM_RANK_LP if is_norm_rank else NORM_TROPHY_LP
        what = "ранга (Легендарный)" if is_norm_rank else "кубков"
        ensure_user(update.effective_chat.id, t)
        given, bal = award_lp_capped(update.effective_chat.id, t.id, amount)
        try:
            await update.message.delete()
        except Exception:
            pass
        if given > 0:
            await context.bot.send_message(
                update.effective_chat.id,
                f"{pe('cup')} {mention(t)} выполнил норму {what} — <b>+{fmt_lp(given)} LP</b>!\n"
                f"{pe('lp')} Баланс: <b>{fmt_lp(bal)}</b> / {LP_MAX} LP", parse_mode=ParseMode.HTML)
        else:
            await context.bot.send_message(
                update.effective_chat.id,
                f"{pe('cup')} {mention(t)} — норма {what} зачтена, но недельный лимит LP уже достигнут.",
                parse_mode=ParseMode.HTML)
        return True

    if first == "отпуск":                              # секретная: пауза норм для участника
        if await eff_role(update, context) < ADMIN:
            return False
        chat_id = update.effective_chat.id
        ensure_user(chat_id, t)
        if any(p in ("стоп", "stop", "отмена") for p in parts[1:]):
            conn.execute("UPDATE users SET vacation_until=NULL WHERE chat_id=? AND user_id=?", (chat_id, t.id))
            conn.commit()
            await update.message.reply_text(
                f"{pe('check')} Отпуск для <b>{esc(t.full_name)}</b> отменён — нормы снова проверяются.",
                parse_mode=ParseMode.HTML)
            return True
        days = next((int(p) for p in parts[1:] if p.isdigit()), None)
        if not days or days < 1 or days > 30:
            await update.message.reply_text("Формат: отпуск @юзер 1–30 (дней). Отмена: отпуск @юзер стоп.")
            return True
        until = (msk_now().date() + timedelta(days=days)).isoformat()
        conn.execute("UPDATE users SET vacation_until=? WHERE chat_id=? AND user_id=?", (until, chat_id, t.id))
        conn.commit()
        await update.message.reply_text(
            f"{pe('check')} <b>{esc(t.full_name)}</b> в отпуске от норм на {days} {_days_word(days)} (до {until}). "
            f"Штрафы и проверки на паузе.", parse_mode=ParseMode.HTML)
        return True

    if first in ("warn",):
        if await require(update, context, MODER):
            await act_warn(update, context, t, rest)
        return True
    if first in ("unwarn", "-warn"):
        if await require(update, context, MODER):
            await act_unwarn(update, context, t, "" if rest == "без указания причины" else rest)
        return True
    if first in ("mute",):
        minutes, reason = 60, rest
        if len(parts) > 1:
            d = parse_duration(parts[1])
            if d:
                minutes = d
                reason = " ".join(parts[2:]) if len(parts) > 2 else "без указания причины"
        if await require(update, context, MODER):
            await act_mute(update, context, t, minutes, reason)
        return True
    if first in ("unmute",):
        if await require(update, context, MODER):
            await act_unmute(update, context, t, "" if rest == "без указания причины" else rest)
        return True
    if first in ("kick",):
        if await require(update, context, MODER):
            await act_kick(update, context, t)
        return True
    if first in ("ban",):
        if await require(update, context, ADMIN):
            await act_ban(update, context, t, rest)
        return True
    if first in ("unban",):
        if await require(update, context, ADMIN):
            await act_unban(update, context, t, "" if rest == "без указания причины" else rest)
        return True
    if first in ("clearwarns",):
        if await require(update, context, ADMIN):
            clear_warns(update.effective_chat.id, t.id)
            await update.message.reply_text(f"🧹 Варны <b>{esc(t.full_name)}</b> сброшены.", parse_mode=ParseMode.HTML)
        return True
    if first in ("role",):
        if not await require(update, context, LEADER):
            return True
        if len(parts) < 2 or parts[1] not in ROLE_WORDS:
            await update.message.reply_text("Укажи роль: участник / менеджер / модер / админ / лидер.")
            return True
        ensure_user(update.effective_chat.id, t)
        set_role(update.effective_chat.id, t.id, ROLE_WORDS[parts[1]])
        await update.message.reply_text(f"🏷 <b>{esc(t.full_name)}</b> теперь: <b>{ROLE_NAMES[ROLE_WORDS[parts[1]]]}</b>.", parse_mode=ParseMode.HTML)
        return True
    return False


# ============================================================
#                   AI (DeepSeek)
# ============================================================
_ai_cooldown = {}                 # user_id -> время последнего AI-запроса

# ===== 6.0 «ИМПЕРИЯ»: AI-дайджест чата (строгая приватность) =====
from collections import deque
_digest_buffer = deque(maxlen=400)   # последние тексты сообщений (только текст, БЕЗ имён/id, в памяти, не в БД)
DIGEST_MIN_MESSAGES = 25             # минимум сообщений, чтобы было что обобщать
DIGEST_COOLDOWN_MIN = 120            # пауза между дайджестами


def digest_feed(text):
    """6.0: кладёт текст сообщения в приватный буфер для дайджеста (без авторства). Короткие/команды пропускаем."""
    if not text:
        return
    t = text.strip()
    if len(t) < 3 or t.startswith((CMD_PREFIX, "/")):
        return
    _digest_buffer.append(t[:300])


async def build_ai_digest():
    """6.0: просит AI выдать ТОЛЬКО темы обсуждений за период. Строгая приватность:
    в модель уходит лишь обезличенный текст, а в промпте — запрет на имена и цитаты."""
    if len(_digest_buffer) < DIGEST_MIN_MESSAGES:
        return None, f"Пока маловато сообщений для дайджеста (нужно от {DIGEST_MIN_MESSAGES})."
    corpus = "\n".join(list(_digest_buffer)[-300:])
    system = (
        "Ты — аналитик чата игрового клана. Сделай КРАТКУЮ сводку ТЕМ обсуждений по сообщениям. "
        "СТРОГИЕ ПРАВИЛА ПРИВАТНОСТИ: НИКОГДА не указывай имена, ники, @упоминания. "
        "НИКОГДА не цитируй сообщения дословно. Не пересказывай личные данные. "
        "Только обобщённые ТЕМЫ и общее настроение. Формат: 4-7 коротких пунктов, каждый с эмодзи, "
        "по-русски, дружелюбно. В конце — одна строка про общее настроение чата."
    )
    user = f"Сообщения чата (обезличенные):\n{corpus}\n\nДай сводку тем (без имён и цитат):"
    out = await ai_complete(system, user, max_tokens=450, temperature=0.5)
    if not out:
        return None, "AI недоступен (проверь ключ/сеть)."
    # подчистка: на всякий случай режем @упоминания, если модель их вставила
    out = re.sub(r"@\w+", "участник", out)
    return out, None


_chat_buffer = {}                 # chat_id -> список последних сообщений [(имя, текст)]

# ===== 5.7 «КАЗНА»: состояние дуэлей в памяти =====
_duel_pending = {}   # chat_id -> {"from":id, "from_name":str, "to":id, "to_name":str, "bet":int, "ts":float}
_quiz_state = {}     # chat_id -> {"answer":int(1-based), "ts":float, "options":[...]}
_prank_running = set()   # 5.9: chat_id с активным пранк-таймером (защита от наложения)
PRANK_COUNTDOWN_SEC = 15  # длительность таймера фейк-пранков (легко поменять)

# 5.9: сценарии пранк-меню — (код, кнопка, текст-завязка, текст-разоблачение).
PRANK_SCENARIOS = {
    "ban": ("💣 Бан всех",
            f"{pe('ban')} <b>ВНИМАНИЕ!</b> Зафиксировано массовое нарушение правил клана.\n"
            f"Запускаю <b>АВТОБАН ВСЕХ участников</b>. Отмена невозможна. Бан через…"),
    "mute": ("🔇 Мут чата",
             f"{pe('mute')} <b>СИСТЕМА:</b> за флуд активируется <b>тотальный мут чата на 24 часа</b>.\n"
             f"Применяется ко всем без исключения. Активация через…"),
    "wipe": ("🗑 Сброс LP",
             f"{pe('warn')} <b>ВНИМАНИЕ:</b> техническая ошибка — запущен <b>сброс ВСЕХ балансов LP до нуля</b>.\n"
             f"Сброс начнётся через…"),
    "raid": ("🚨 Тревога рейда",
             f"{pe('warn')} <b>ТРЕВОГА!</b> Обнаружен рейд ботов. Чат <b>блокируется для защиты</b>.\n"
             f"Вход и сообщения будут закрыты через…"),
    "gift": ("🎁 Раздача LP",
             f"{pe('lp')} <b>СЕКРЕТНАЯ АКЦИЯ!</b> Всем участникам — <b>по 1000 LP</b> за активность!\n"
             f"Начисление через…"),
    "kick": ("👢 Чистка неактива",
             f"{pe('ban')} <b>АВТО-ЧИСТКА:</b> удаляю всех, кто писал мало за неделю.\n"
             f"Список сформирован, исключение через…"),
    "update": ("🔄 Сброс прогресса",
               f"{pe('warn')} <b>ОБНОВЛЕНИЕ БОТА:</b> требуется <b>полный сброс прогресса</b> — LP, ранги, серии "
               f"обнуляются у всех. Начинаю через…"),
    "winner": ("🏆 Фейк-победитель",
               f"{pe('cup')} <b>ИТОГИ ТАЙНОГО КОНКУРСА!</b> Определён победитель месяца — приз <b>3000 LP</b>!\n"
               f"Имя счастливчика будет объявлено через…"),
    "inspect": ("🕵️ Проверка аккаунтов",
                f"{pe('warn')} <b>ПРОВЕРКА:</b> запущено сканирование аккаунтов на читы и мультиаккаунты.\n"
                f"Подозрительные будут исключены. Результат через…"),
    "vip": ("👑 Тайный VIP",
            f"{pe('star')} <b>СИСТЕМА:</b> один случайный участник получит статус <b>VIP</b> и +5000 LP!\n"
            f"Розыгрыш VIP через…"),
    "leave": ("🚪 Бот уходит",
              f"{pe('warn')} <b>ПРОЩАНИЕ:</b> владелец отключает бота навсегда. Спасибо всем за игру 💙\n"
              f"Отключение через…"),
    "double": ("⚡ Удвоение LP",
               f"{pe('lp')} <b>АКЦИЯ x2!</b> Всем участникам <b>удваивается баланс LP</b> прямо сейчас!\n"
               f"Удвоение через…"),
}

# 5.8: банк вопросов викторины по Brawl Stars. answer — номер правильного варианта (с 1).
QUIZ_QUESTIONS = [
    {"q": "Сколько по умолчанию игроков в режиме «Награда за поимку» (Bounty)?", "options": ["2 на 2", "3 на 3", "5 на 5", "1 на 1"], "answer": 2},
    {"q": "Какой брawler первым появляется у нового игрока?", "options": ["Кольт", "Шелли", "Эль Примо", "Ник"], "answer": 2},
    {"q": "Как называется внутриигровая валюта за победы, открывающая ящики?", "options": ["Гемы", "Кубки", "Жетоны", "Монеты"], "answer": 3},
    {"q": "Сколько кубков нужно, чтобы брawler считался на максимуме ранговой шкалы трофеев (одна звезда силы открыта)?", "options": ["500", "750", "1000", "300"], "answer": 1},
    {"q": "Режим, где нужно собирать и удерживать 10 кристаллов?", "options": ["Захват кристаллов", "Броулбол", "Нокаут", "Осада"], "answer": 1},
    {"q": "Какой класс у Поко?", "options": ["Дамаг", "Поддержка", "Танк", "Контролёр"], "answer": 2},
    {"q": "Сколько секунд длится перезарядка суперспособности в среднем — она копится от чего?", "options": ["От времени", "От нанесённого урона", "Бесплатно сразу", "От движения"], "answer": 2},
    {"q": "Как называется главное соревновательное событие с билетами и наградами?", "options": ["Клубная лига", "Чемпионат мира", "Power League", "Турнир в чате"], "answer": 3},
    {"q": "Кто из этих брawler — легендарный (Legendary)?", "options": ["Спайк", "Кольт", "Джесси", "Нита"], "answer": 1},
    {"q": "Максимальный уровень прокачки брawler'а?", "options": ["9", "10", "11", "15"], "answer": 3},
]


def _owner_target(update, parts):
    """5.9: надёжно находит цель для команд владельца — по ОТВЕТУ, @нику или ID.
    В отличие от read_target не требует прав и работает даже если у цели нет username."""
    msg = update.message
    rr = real_reply(msg)
    if rr and rr.from_user:
        return rr.from_user
    chat_id = ALLOWED_CHAT_ID if update.effective_chat.type == "private" else update.effective_chat.id
    for p in parts[1:]:
        if p.startswith("@") and len(p) > 1:
            row = conn.execute("SELECT user_id, username, name FROM users WHERE chat_id=? AND lower(username)=?",
                               (chat_id, p[1:].lower())).fetchone()
            if row:
                return SimpleNamespace(id=row["user_id"], username=row["username"],
                                       full_name=row["name"] or f"@{row['username']}", is_bot=False)
            return None
        if p.lstrip("-").isdigit() and len(p) >= 5:   # похоже на user_id
            row = conn.execute("SELECT user_id, username, name FROM users WHERE chat_id=? AND user_id=?",
                               (chat_id, int(p))).fetchone()
            if row:
                return SimpleNamespace(id=row["user_id"], username=row["username"],
                                       full_name=row["name"] or str(row["user_id"]), is_bot=False)
            return SimpleNamespace(id=int(p), username=None, full_name=str(p), is_bot=False)  # ещё не в базе — всё равно цель
    return None


def _prank_get(row):
    """5.9: читает пранк-overlay профиля (фейковые значения для показа). {} если нет/выключен."""
    try:
        raw = row["prank"] if "prank" in row.keys() else None
        if not raw:
            return {}
        d = json.loads(raw)
        return d if isinstance(d, dict) else {}
    except Exception:
        return {}


# Поля, которые можно подменить пранком: ключ команды -> ключ overlay + тип
_PRANK_FIELDS = {
    "лп": ("lp", "int"), "lp": ("lp", "int"),
    "реп": ("rep", "int"), "rep": ("rep", "int"), "репутация": ("rep", "int"),
    "ник": ("name", "str"), "имя": ("name", "str"), "name": ("name", "str"),
    "ранг": ("rank", "str"), "rank": ("rank", "str"),
    "серия": ("streak", "int"), "стрик": ("streak", "int"), "streak": ("streak", "int"),
    "сообщения": ("msg", "int"), "сообщ": ("msg", "int"), "msg": ("msg", "int"),
    "титул": ("title", "str"), "title": ("title", "str"),
}


def active_title(row):
    """Декоративный титул под именем: купленный/временный (если не истёк). Заработанные за квесты/викторину
    титулы показываются в строке роли (5.9), а не здесь, чтобы не дублировалось."""
    try:
        t = row["title_text"] if "title_text" in row.keys() else None
        until = row["title_until"] if "title_until" in row.keys() else None
        if t and not (until and until < today_str()):
            return t
        return ""
    except Exception:
        return ""


def clean_title(s):
    """Чистит титул: режет длину, убирает HTML-спецсимволы и переносы."""
    t = re.sub(r"\s+", " ", (s or "").strip())
    t = t.replace("<", "").replace(">", "").replace("&", "")
    return t[:TITLE_MAX_LEN]


def lottery_count(chat_id):
    return conn.execute("SELECT COUNT(*) AS c FROM lottery WHERE chat_id=?", (chat_id,)).fetchone()["c"]


def lottery_bank(chat_id):
    return lottery_count(chat_id) * LOTTERY_TICKET


def _mins_since(ts_iso):
    """Сколько минут прошло с ISO-времени (или очень много, если не распарсить)."""
    try:
        return (msk_now() - datetime.fromisoformat(ts_iso)).total_seconds() / 60
    except Exception:
        return 10 ** 9


# ===== 5.8 «АРСЕНАЛ»: квесты дня =====
def quest_of_day(chat_id, user_id):
    """Стабильный квест дня для конкретного игрока (зависит от даты и id — у всех разные, но фиксированы на день)."""
    day = today_str()
    seed = int(hashlib.md5(f"{day}:{user_id}".encode()).hexdigest(), 16)
    return DAILY_QUESTS[seed % len(DAILY_QUESTS)]


def quest_progress(chat_id, user_id, code):
    """Текущий прогресс по квесту (число) из реальных событий ДНЯ."""
    day = today_str()
    if code.startswith("msg"):
        return stat_today(chat_id, user_id)
    if code == "rep":
        # респекты, полученные сегодня — считаем по rep_log, если есть, иначе 0/выполнено по факту реакции
        try:
            return conn.execute("SELECT COUNT(*) AS c FROM rep_log WHERE chat_id=? AND to_id=? AND day=?",
                                (chat_id, user_id, day)).fetchone()["c"]
        except Exception:
            return 0
    if code == "duel":
        return conn.execute("SELECT COUNT(*) AS c FROM duels WHERE chat_id=? AND (a=? OR b=?) AND ts LIKE ?",
                            (chat_id, user_id, user_id, day + "%")).fetchone()["c"]
    if code == "streak":
        return 1 if stat_today(chat_id, user_id) > 0 else 0
    return 0


def quest_claimed(chat_id, user_id):
    return conn.execute("SELECT code FROM quest_done WHERE chat_id=? AND user_id=? AND day=?",
                        (chat_id, user_id, today_str())).fetchone()


def quests_total(chat_id, user_id):
    return conn.execute("SELECT COUNT(*) AS c FROM quest_done WHERE chat_id=? AND user_id=?",
                        (chat_id, user_id)).fetchone()["c"]


def quiz_wins_total(chat_id, user_id):
    row = conn.execute("SELECT quiz_wins FROM users WHERE chat_id=? AND user_id=?",
                       (chat_id, user_id)).fetchone()
    return (row["quiz_wins"] if row and "quiz_wins" in row.keys() else 0) or 0


def best_earned_title(chat_id, user_id):
    """5.9: лучший престиж-титул игрока среди ОБЕИХ лестниц (квесты + викторина).
    Побеждает достижение с наибольшим порогом; возвращает текст титула или None."""
    quests = quests_total(chat_id, user_id)
    quizzes = quiz_wins_total(chat_id, user_id)
    cands = []   # (порог, титул)
    for need, title in QUEST_TITLES:
        if quests >= need:
            cands.append((need, title))
    for need, title in QUIZ_TITLES:
        if quizzes >= need:
            cands.append((need, title))
    if not cands:
        return None
    cands.sort(key=lambda x: x[0])
    return cands[-1][1]


def refresh_earned_title(chat_id, user_id):
    """Пересчитывает и сохраняет лучший титул. Возвращает (новый_титул|None если не изменился)."""
    best = best_earned_title(chat_id, user_id)
    cur = conn.execute("SELECT earned_title FROM users WHERE chat_id=? AND user_id=?",
                       (chat_id, user_id)).fetchone()
    cur_title = (cur["earned_title"] if cur else None) or None
    if best and best != cur_title:
        conn.execute("UPDATE users SET earned_title=? WHERE chat_id=? AND user_id=?", (best, chat_id, user_id))
        conn.commit()
        return best
    return None


def update_quest_title(chat_id, user_id):
    """5.9: пересчитывает титул (квесты+викторина). Возвращает (новый_титул|None, всего_квестов)."""
    new = refresh_earned_title(chat_id, user_id)
    return new, quests_total(chat_id, user_id)


async def try_complete_quest(context, chat_id, user_id, name):
    """Проверяет квест дня; засчитывает и, если открыт новый престиж-титул, поздравляет. Тихо, без спама.
    5.9: квесты больше НЕ дают LP — награда это престижные ТИТУЛЫ за количество выполненных."""
    if user_id in EXCLUDED_IDS or user_id == LEADER_ID:
        return
    if quest_claimed(chat_id, user_id):
        return
    code, desc, goal = quest_of_day(chat_id, user_id)
    base = code.rstrip("0123456789") or code  # 5.9 ФИКС: обрезаем цифру у ВСЕХ кодов (duel1→duel, rep1→rep), иначе квесты не засчитывались
    prog = quest_progress(chat_id, user_id, base)
    if prog < goal:
        return
    conn.execute("INSERT OR IGNORE INTO quest_done (chat_id, user_id, day, code) VALUES (?,?,?,?)",
                 (chat_id, user_id, today_str(), code))
    conn.commit()
    new_title, total = update_quest_title(chat_id, user_id)
    if ALLOWED_CHAT_ID:
        try:
            if new_title:
                await context.bot.send_message(
                    ALLOWED_CHAT_ID,
                    f"{pe('star')} <b>{esc(name)}</b> выполнил квест дня и получает титул <b>{new_title}</b>! "
                    f"(всего квестов: {total}) {pe('fire')}", parse_mode=ParseMode.HTML)
            else:
                await context.bot.send_message(
                    ALLOWED_CHAT_ID,
                    f"{pe('check')} <b>{esc(name)}</b> выполнил квест дня «{desc}»! Всего квестов: <b>{total}</b> {pe('fire')}",
                    parse_mode=ParseMode.HTML)
        except Exception as e:
            log_error("квест-анонс", e)


# ===== 5.8 «АРСЕНАЛ»: ачивки-значки =====
def has_achievement(chat_id, user_id, code):
    return conn.execute("SELECT 1 FROM achievements WHERE chat_id=? AND user_id=? AND code=?",
                        (chat_id, user_id, code)).fetchone() is not None


def user_achievements(chat_id, user_id):
    rows = conn.execute("SELECT code FROM achievements WHERE chat_id=? AND user_id=?",
                        (chat_id, user_id)).fetchall()
    return [r["code"] for r in rows]


async def check_achievements(context, chat_id, user_id, name):
    """Проверяет и выдаёт новые ачивки игроку. Возвращает список названий новых значков."""
    row = get_user(chat_id, user_id)
    if not row:
        return []
    earned = []
    streak = row["streak"] or 0
    streak_best = (row["streak_best"] if "streak_best" in row.keys() else 0) or 0
    peak_streak = max(streak, streak_best)
    total = stat_total(chat_id, user_id)
    rep = row["reputation"] or 0
    rb = row["rank_best"] if "rank_best" in row.keys() else None
    duel_wins = conn.execute("SELECT COUNT(*) AS c FROM duels WHERE chat_id=? AND winner=?",
                            (chat_id, user_id)).fetchone()["c"]
    checks = {
        "week_streak": peak_streak >= 7,
        "month_streak": peak_streak >= 30,
        "chatty": total >= 5000,
        "veteran": total >= 20000,
        "legend_rank": rb is not None and rb >= LEGEND_VALUE_MIN,
        "respected": rep >= 50,
        "duelist": duel_wins >= 10,
    }
    for code, ok in checks.items():
        if ok and not has_achievement(chat_id, user_id, code):
            conn.execute("INSERT OR IGNORE INTO achievements (chat_id, user_id, code, ts) VALUES (?,?,?,?)",
                         (chat_id, user_id, code, today_str()))
            a = ACHIEVEMENTS_BY_CODE[code]
            earned.append(f"{a[1]} {a[2]}")
    if earned:
        conn.commit()
        if ALLOWED_CHAT_ID:
            try:
                await context.bot.send_message(
                    ALLOWED_CHAT_ID,
                    f"{pe('star')} <b>{esc(name)}</b> получает значок: " + ", ".join(earned) + "!",
                    parse_mode=ParseMode.HTML)
            except Exception as e:
                log_error("ачивка-анонс", e)
    return earned


def grant_achievement(chat_id, user_id, code):
    """Прямая выдача ачивки по событию (лотерея/дуэли). Без анонса."""
    if has_achievement(chat_id, user_id, code):
        return False
    conn.execute("INSERT OR IGNORE INTO achievements (chat_id, user_id, code, ts) VALUES (?,?,?,?)",
                 (chat_id, user_id, code, today_str()))
    conn.commit()
    return True


def duel_block_reason(chat_id, a, b):
    """5.7: причина, по которой дуэль между a и b сейчас нельзя (или None если можно).
    Защита от слива баланса другу через поддавки. Владелец (5.9) — без ограничений."""
    if is_owner(a) or is_owner(b):
        return None
    today = today_str()
    # дневной лимит на каждого
    for uid in (a, b):
        cnt = conn.execute(
            "SELECT COUNT(*) AS c FROM duels WHERE chat_id=? AND (a=? OR b=?) AND ts LIKE ?",
            (chat_id, uid, uid, today + "%")).fetchone()["c"]
        if cnt >= DUEL_DAILY_LIMIT:
            return ("you" if uid == a else "opp"), f"дневной лимит дуэлей исчерпан ({DUEL_DAILY_LIMIT}/день)"
    # личный КД (последняя дуэль любого из них)
    for uid in (a, b):
        last = conn.execute(
            "SELECT ts FROM duels WHERE chat_id=? AND (a=? OR b=?) ORDER BY ts DESC LIMIT 1",
            (chat_id, uid, uid)).fetchone()
        if last:
            left = DUEL_COOLDOWN_MIN - _mins_since(last["ts"])
            if left > 0:
                return ("you" if uid == a else "opp"), f"перерыв между дуэлями ещё {int(left) + 1} мин"
    # КД на пару (анти-фарм «гоняем баланс туда-сюда»)
    lastpair = conn.execute(
        "SELECT ts FROM duels WHERE chat_id=? AND ((a=? AND b=?) OR (a=? AND b=?)) ORDER BY ts DESC LIMIT 1",
        (chat_id, a, b, b, a)).fetchone()
    if lastpair:
        left = DUEL_PAIR_COOLDOWN_MIN - _mins_since(lastpair["ts"])
        if left > 0:
            return "pair", f"с этим соперником можно снова через {int(left // 60) + 1} ч"
    return None
_last_sender = {}                 # chat_id -> [user_id, сколько подряд] — для анти-спама
_ai_memory = {}                   # (chat_id, user_id) -> последние реплики диалога с помощником
_last_ai_error = ""               # текст последней ошибки AI (для показа в чате)

CODEX_SUMMARY = (
    "Правила клана LINKOR: запрещены оскорбления, унижения, оскорбление семьи, "
    "троллинг и провокации, буллинг, расизм и дискриминация, сексизм, угрозы, "
    "флуд, спам и реклама, злоупотребление КАПСом, политика и религия, "
    "вредоносный/мошеннический контент."
)


async def ai_complete(system, user, max_tokens=500, temperature=0.7, history=None):
    """Запрос к AI (OpenAI-совместимый). None при ошибке/без ключа. Причину пишет в консоль (рус.).
    history — список предыдущих реплик [{role, content}] для «памяти» диалога."""
    global _last_ai_error
    _last_ai_error = ""
    if not AI_API_KEY:
        _last_ai_error = "no_key"
        print("[AI] Ключ не задан (AI_API_KEY пустой).", flush=True)
        return None
    url = f"{AI_BASE_URL}/chat/completions"
    headers = {"Authorization": f"Bearer {AI_API_KEY}", "Content-Type": "application/json"}
    messages = [{"role": "system", "content": system}]
    if history:
        messages.extend(history)
    messages.append({"role": "user", "content": user})
    payload = {"model": AI_MODEL, "max_tokens": max_tokens, "temperature": temperature,
               "messages": messages}
    for attempt in range(2):                       # одна авто-повтор при лимите
        try:
            async with httpx.AsyncClient(timeout=40) as client:
                r = await client.post(url, headers=headers, json=payload)
            if r.status_code == 200:
                return r.json()["choices"][0]["message"]["content"].strip()
            if r.status_code == 429:
                _last_ai_error = "rate_limit"
                print("[AI] Лимит запросов Cerebras (429): слишком часто/много запросов. "
                      "Подожди немного или снизь частоту обращений.", flush=True)
                if attempt == 0:
                    await asyncio.sleep(2)
                    continue
                return None
            _last_ai_error = "http"
            print(f"[AI] Ошибка HTTP {r.status_code}: {r.text[:300]}", flush=True)
            return None
        except httpx.ConnectError as e:
            _last_ai_error = "connect"
            print(f"[AI] Не удалось подключиться к {AI_BASE_URL} "
                  f"(возможно блокировка по региону — нужен зарубежный IP/VPS): {e}", flush=True)
            return None
        except httpx.TimeoutException:
            _last_ai_error = "timeout"
            print(f"[AI] Таймаут запроса к {AI_BASE_URL} (медленно или блокировка по региону).", flush=True)
            return None
        except Exception as e:
            _last_ai_error = "other"
            print(f"[AI] Непредвиденная ошибка: {type(e).__name__}: {e}", flush=True)
            return None
    return None


def ai_cooldown_left(user_id):
    last = _ai_cooldown.get(user_id, 0)
    left = AI_COOLDOWN_SEC - (time.time() - last)
    return max(0, int(left))


async def ai_ask(update, context, question):
    if not AI_API_KEY:
        return await update.message.reply_text(f"{pe('robot')} AI пока не подключён (нужен ключ Cerebras в настройках).",
                                                parse_mode=ParseMode.HTML)
    if not question.strip():
        return await update.message.reply_text("Напиши вопрос после слова: Linkor какой бравлер лучший против танков?")
    left = ai_cooldown_left(update.effective_user.id)
    if left > 0 and not is_owner(update.effective_user.id):   # 5.9: владелец без КД
        return await update.message.reply_text(f"{pe('clock')} Подожди {left} сек перед следующим вопросом к AI.",
                                                parse_mode=ParseMode.HTML)
    _ai_cooldown[update.effective_user.id] = time.time()
    await context.bot.send_chat_action(update.effective_chat.id, "typing")
    system = (
        f"Ты — официальный AI-ассистент игрового клана {CLAN_NAME} по игре Brawl Stars. "
        f"Общайся вежливо, грамотно и по делу, в сдержанно-официальном, но доброжелательном тоне. "
        f"Давай точные, полезные и структурированные ответы на русском языке. "
        f"Не используй markdown-звёздочки. Если вопрос не по теме клана или игры — всё равно помоги корректно и кратко."
    )
    key = (update.effective_chat.id, update.effective_user.id)
    history = _ai_memory.get(key, [])
    answer = await ai_complete(system, question, history=history)
    if not answer:
        # AI не ответил — не штрафуем полным кулдауном, разрешаем повтор через AI_RETRY_SEC
        _ai_cooldown[update.effective_user.id] = time.time() - max(0, AI_COOLDOWN_SEC - AI_RETRY_SEC)
    if answer:
        # запоминаем последние ~3 обмена репликами (память сбрасывается при перезапуске бота)
        mem = history + [{"role": "user", "content": question[:500]},
                         {"role": "assistant", "content": answer[:800]}]
        _ai_memory[key] = mem[-6:]
        await update.message.reply_text(premiumize(esc(answer)), parse_mode=ParseMode.HTML)
    elif _last_ai_error == "rate_limit":
        await update.message.reply_text(f"{pe('robot')} Сейчас слишком много запросов, подожди минутку и спроси снова 🙏",
                                        parse_mode=ParseMode.HTML)
    elif _last_ai_error in ("connect", "timeout"):
        await update.message.reply_text(f"{pe('robot')} Не получается связаться с сервером, попробуй чуть позже.",
                                        parse_mode=ParseMode.HTML)
    else:
        await update.message.reply_text(f"{pe('robot')} Не получилось ответить, попробуй позже.",
                                        parse_mode=ParseMode.HTML)


async def ai_fun(update, context, kind):
    if not AI_API_KEY:
        return await update.message.reply_text(f"{pe('robot')} AI пока не подключён (нужен ключ Cerebras в настройках).",
                                                parse_mode=ParseMode.HTML)
    left = ai_cooldown_left(update.effective_user.id)
    if left > 0:
        return await update.message.reply_text(f"{pe('clock')} Подожди {left} сек.", parse_mode=ParseMode.HTML)
    _ai_cooldown[update.effective_user.id] = time.time()
    await context.bot.send_chat_action(update.effective_chat.id, "typing")

    if kind == "fact":
        topic = random.choice([
            "Brawl Stars", "видеоигры и киберспорт", "космос", "животных", "историю",
            "технологии и интернет", "человеческое тело", "географию и страны", "еду",
            "океан и природу", "науку", "спорт", "кино и музыку", "изобретения",
        ])
        user_prompt = (f"Расскажи ОДИН короткий, неожиданный и малоизвестный факт про {topic}. "
                       f"Не банальный — такой, чтобы реально удивил. Только сам факт, 1–2 предложения, без вступлений.")
        sys_prompt = "Ты — эрудит, который удивляет необычными фактами. Каждый раз — новый, не повторяйся. По-русски, без markdown."
    elif kind == "toast":
        user_prompt = f"Произнеси короткий заряжающий тост для бойцов клана {CLAN_NAME} (Brawl Stars). 1–2 предложения."
        sys_prompt = "Ты — харизматичный ведущий клан-чата. По-русски, без markdown."
    else:  # joke
        angle = random.choice([
            "про рандомных тиммейтов", "про вайпы и проигрыши", "про донат и пропуски",
            "про конкретного бравлера", "про кубковый ад", "про токсичных игроков",
            "про неудачные супер-удары", "про режимы игры",
        ])
        user_prompt = f"Расскажи короткую смешную шутку {angle} в Brawl Stars. Дерзко, по-пацански, 1–3 предложения."
        sys_prompt = "Ты — остроумный комик игрового чата. Шутки свежие, не повторяйся. По-русски, без markdown."

    answer = await ai_complete(sys_prompt, user_prompt, max_tokens=180, temperature=1.05)
    if not answer:
        # AI не ответил — разрешаем повтор через AI_RETRY_SEC, а не через полный кулдаун
        _ai_cooldown[update.effective_user.id] = time.time() - max(0, AI_COOLDOWN_SEC - AI_RETRY_SEC)
    await update.message.reply_text(premiumize(esc(answer)) if answer else f"{pe('robot')} Попробуй позже.",
                                    parse_mode=ParseMode.HTML)


async def ai_summary(update, context):
    if not await require(update, context, MODER):
        return
    if not AI_API_KEY:
        return await update.message.reply_text(f"{pe('robot')} AI пока не подключён (нужен ключ Cerebras).",
                                                parse_mode=ParseMode.HTML)
    buf = _chat_buffer.get(update.effective_chat.id, [])
    if len(buf) < 5:
        return await update.message.reply_text("Маловато сообщений для сводки.")
    # на бесплатном тарифе Cerebras контекст 8K — берём последние ~30 сообщений и обрезаем
    transcript = "\n".join(f"{n}: {tx}" for n, tx in buf[-30:])
    transcript = transcript[-6000:]
    await context.bot.send_chat_action(update.effective_chat.id, "typing")
    sys_prompt = (
        "Ты — аналитик чата игрового клана LINKOR. Делаешь ёмкую, структурированную и полезную сводку "
        "обсуждения по-русски. Только то, что реально было в сообщениях — ничего не выдумывай. "
        "Без markdown-звёздочек, пиши живым языком."
    )
    user_prompt = (
        "Сделай сводку последних сообщений чата СТРОГО в таком формате, каждый блок с новой строки:\n\n"
        "🔥 Главное — 2–4 коротких пункта о ключевых темах (каждый с новой строки, начинай с «• »)\n"
        "💬 Атмосфера — одна короткая фраза о настроении в чате\n"
        "⭐ Активнее всех — 2–3 имени, если заметно по сообщениям (иначе пропусти этот блок)\n"
        "📌 Итоги — если были договорённости/решения, кратко; иначе напиши «без конкретных решений»\n\n"
        f"Вот сообщения:\n{transcript}"
    )
    answer = await ai_complete(sys_prompt, user_prompt, max_tokens=500, temperature=0.6)
    if not answer:
        return await update.message.reply_text(f"{pe('robot')} Не вышло собрать сводку, попробуй позже.",
                                                parse_mode=ParseMode.HTML)
    body = esc(answer)
    # заменяем обычные значки из AI-вывода на премиум-эмодзи
    for plain, nm in (("🔥", "fire"), ("💬", "msg"), ("⭐", "star"), ("📌", "pin"),
                      ("🏆", "cup"), ("📊", "stats")):
        body = body.replace(plain, pe(nm))
    await update.message.reply_text(
        f"{pe('note')} <b>СВОДКА ОБСУЖДЕНИЯ — {CLAN_NAME}</b>\n{CLAN_LINE}\n\n"
        f"<blockquote>{body}</blockquote>", parse_mode=ParseMode.HTML)


async def ai_moderate(update, context):
    """AI-распознавание нарушений в речи. Возвращает True, если приняты меры."""
    if not (AI_MODERATION and AI_API_KEY):
        return False
    msg = update.message
    user = update.effective_user
    if len(msg.text) < 4:
        return False
    system = (
        "Ты — модератор русскоязычного игрового чата клана, где РАЗРЕШЕНЫ мат, грубые и "
        "крепкие словечки. За обычную брань, шутки и самовыражение наказывать НЕЛЬЗЯ. "
        "Нарушением считается ТОЛЬКО одно из двух:\n"
        "1) Прямое оскорбление, унижение или переход на личности В АДРЕС КОНКРЕТНОГО человека "
        "(например адресная травля, унижение конкретного участника).\n"
        "2) Намеренное разжигание конфликта, провокации, подстрекательство к ссоре.\n"
        "Мат сам по себе, крепкие выражения и эмоции, НЕ направленные на унижение конкретного "
        "человека, — это НЕ нарушение (violation=false). "
        "Если сомневаешься — считай, что нарушения НЕТ. "
        "Ответь СТРОГО в JSON без пояснений: "
        '{"violation": true/false, "rule": "краткая причина", "severity": "low|med|high"}.')
    raw = await ai_complete(system, msg.text, max_tokens=120, temperature=0.0)
    if not raw:
        return False
    raw = raw.replace("```json", "").replace("```", "").strip()
    try:
        verdict = json.loads(raw)
    except Exception:
        return False
    if not verdict.get("violation") or verdict.get("severity") not in ("med", "high"):
        return False
    rule = str(verdict.get("rule", "нарушение правил"))[:120]
    if AI_MOD_AUTOPUNISH:
        # та же лестница: выговор → мут → варн → исключение
        await apply_discipline(update, context, user, f"{rule} (AI)", MUTE_HOURS_INSULT, delete_msg=True)
    else:
        await msg.reply_text(f"{pe('robot')} Кажется, это нарушает правила ({esc(rule)}). Будь, пожалуйста, аккуратнее.",
                             parse_mode=ParseMode.HTML)
    return True


# ============================================================
#                   МИНИ-ИГРЫ
# ============================================================
EIGHTBALL = [
    "Бесспорно ✅", "Мне кажется — да", "Никаких сомнений", "Можешь быть уверен",
    "Скорее всего", "Хорошие перспективы", "Знаки говорят — да",
    "Пока неясно, попробуй снова 🤔", "Спроси позже", "Лучше не рассказывать сейчас",
    "Даже не думай ❌", "Мой ответ — нет", "Очень сомнительно", "Не рассчитывай на это",
]
RPS = {"камень": 0, "ножницы": 1, "бумага": 2}
RPS_EMOJI = ["🪨 Камень", "✂️ Ножницы", "📄 Бумага"]


async def game_dice(update, context, emoji):
    await update.message.reply_dice(emoji=emoji)


async def game_8ball(update, context, question):
    if not question:
        return await update.message.reply_text("Задай вопрос: шар будет ли сегодня ивент?")
    await update.message.reply_text("🎱 " + random.choice(EIGHTBALL))


async def game_rps(update, context, choice):
    if choice not in RPS:
        return await update.message.reply_text("Выбери: кнб камень / кнб ножницы / кнб бумага")
    me = random.randint(0, 2)
    you = RPS[choice]
    if me == you:
        res = "Ничья 🤝"
    elif (you - me) % 3 == 2:        # ты бьёшь то, что выбрал бот
        res = "Ты победил! 🎉"
    else:
        res = "Я победил 😎"
    await update.message.reply_text(f"Ты: {RPS_EMOJI[you]}\nЯ: {RPS_EMOJI[me]}\n\n{res}")


async def game_pick(update, context, items_text):
    items = [x.strip() for x in re.split(r"[,/]| или ", items_text) if x.strip()]
    if len(items) < 2:
        return await update.message.reply_text("Дай варианты через запятую: выбери пицца, суши, бургер")
    await update.message.reply_text(f"🎯 Мой выбор: <b>{esc(random.choice(items))}</b>", parse_mode=ParseMode.HTML)


async def game_duel(update, context, t):
    me_name = update.effective_user.full_name
    winner = random.choice([me_name, t.full_name])
    await update.message.reply_text(
        f"⚔️ <b>Дуэль!</b>\n{esc(me_name)} 🆚 {esc(t.full_name)}\n\n🏆 Победитель: <b>{esc(winner)}</b>",
        parse_mode=ParseMode.HTML)


async def game_number(update, context, guess_text):
    secret = random.randint(1, 10)
    try:
        g = int(guess_text)
    except (ValueError, TypeError):
        return await update.message.reply_text("Угадай число 1–10: число 7")
    await update.message.reply_text(f"🎲 Загадано {secret}. " + ("Угадал! 🎉" if g == secret else "Мимо!"))


# ============================================================
#                   АВТОМОДЕРАЦИЯ
# ============================================================
_flood = {}


async def apply_discipline(update, context, user, reason, mute_hours, delete_msg=False):
    """Прогрессивное наказание по кодексу: выговор → мут → варн → (3 варна) исключение."""
    chat_id = update.effective_chat.id
    ensure_user(chat_id, user)
    row = get_user(chat_id, user.id)
    stage = row["disc_stage"] if row else 0
    # сброс стадии после 7 дней без нарушений
    if row and row["disc_last"]:
        try:
            if msk_now() - datetime.fromisoformat(row["disc_last"]) > timedelta(days=DISCIPLINE_DECAY_DAYS):
                stage = 0
        except Exception:
            pass
    if delete_msg:
        try:
            await update.message.delete()
        except Exception:
            pass

    if stage == 0:
        # 1) устный выговор
        conn.execute("UPDATE users SET disc_stage=1, disc_last=? WHERE chat_id=? AND user_id=?",
                     (msk_now().isoformat(), chat_id, user.id))
        conn.commit()
        await context.bot.send_message(
            chat_id, f"✋ <b>{esc(user.full_name)}</b>, устное предупреждение: {esc(reason)}.\n"
                     f"При повторном нарушении — мут.", parse_mode=ParseMode.HTML)
    elif stage == 1:
        # 2) мут
        await do_mute(context, chat_id, user.id, mute_hours * 60)
        conn.execute("UPDATE users SET disc_stage=2, disc_last=? WHERE chat_id=? AND user_id=?",
                     (msk_now().isoformat(), chat_id, user.id))
        conn.commit()
        await context.bot.send_message(
            chat_id, f"🔇 <b>{esc(user.full_name)}</b> — мут на {mute_hours} ч за повторное нарушение: {esc(reason)}.\n"
                     f"Следующее нарушение — варн.", parse_mode=ParseMode.HTML)
    else:
        # 3) варн (+ списание LP); 3 варна — исключение
        n = add_warn(chat_id, user.id)
        bal = add_lp(chat_id, user.id, -WARN_LP_PENALTY)
        conn.execute("UPDATE users SET disc_last=? WHERE chat_id=? AND user_id=?",
                     (msk_now().isoformat(), chat_id, user.id))
        conn.commit()
        if n >= MAX_WARNS:
            clear_warns(chat_id, user.id)
            conn.execute("UPDATE users SET disc_stage=0 WHERE chat_id=? AND user_id=?", (chat_id, user.id))
            conn.commit()
            await do_exclude(context, chat_id, user.id)
            await context.bot.send_message(
                chat_id, f"⛔ <b>{esc(user.full_name)}</b> исключён из чата (3 варна, кодекс 4.3). Причина: {esc(reason)}.",
                parse_mode=ParseMode.HTML)
        else:
            await context.bot.send_message(
                chat_id, f"⚠ <b>{esc(user.full_name)}</b> — варн {n}/{MAX_WARNS} за: {esc(reason)}.\n"
                         f"🔻 Списано {WARN_LP_PENALTY} LP (баланс: {fmt_lp(bal)}).", parse_mode=ParseMode.HTML)


async def auto_moderate(update, context):
    msg = update.message
    user = update.effective_user
    chat_id = update.effective_chat.id
    if await eff_role(update, context) >= MODER:     # админы/модеры — полный иммунитет
        return
    if NO_MODERATION_TOPIC_ID and getattr(msg, "message_thread_id", None) == NO_MODERATION_TOPIC_ID:
        return                                        # топик "cherdak linkor" — без наказаний
    text = msg.text

    # 1) Мат/оскорбления из списка (мгновенно, бесплатно, ловит обход)
    if has_banned(text):
        await apply_discipline(update, context, user, "оскорбления / мат (кодекс 1.1)",
                               MUTE_HOURS_INSULT, delete_msg=True)
        return

    # 2) КАПС
    letters = [c for c in text if c.isalpha()]
    if len(letters) >= CAPS_MIN_LEN and sum(c.isupper() for c in letters) / len(letters) >= CAPS_RATIO:
        await apply_discipline(update, context, user, "злоупотребление КАПСом (кодекс 1.10)", MUTE_HOURS_CAPS)
        return

    # 3) Флуд
    key = (chat_id, user.id)
    now = time.time()
    times = [tt for tt in _flood.get(key, []) if now - tt < FLOOD_WINDOW]
    times.append(now)
    _flood[key] = times
    if len(times) >= FLOOD_LIMIT:
        _flood[key] = []
        await apply_discipline(update, context, user, "флуд (кодекс 1.8)", MUTE_HOURS_FLOOD)
        return

    # 4) AI-распознавание оскорблений в речи (если включено и есть ключ)
    await ai_moderate(update, context)


# ============================================================
#               ОХРАНА ЧАТА (только свой чат)
# ============================================================
async def guard(update, context):
    chat = update.effective_chat
    if not chat:
        raise ApplicationHandlerStop
    if chat.type == "private":
        return                                # личку обрабатывает private_router (обжалования, помощь, конкурсы)
    if ALLOWED_CHAT_ID and chat.id != ALLOWED_CHAT_ID:
        try:
            await context.bot.leave_chat(chat.id)
        except Exception:
            pass
        raise ApplicationHandlerStop


# ============================================================
#               ОБРАБОТЧИКИ СООБЩЕНИЙ
# ============================================================
async def maybe_finalize_season(context, chat_id):
    """Если начался новый месяц — подводит итоги прошлого сезона: топ-3 в зал славы + LP + анонс.
    Без планировщика: срабатывает при первом сообщении в новом месяце."""
    if not ALLOWED_CHAT_ID or chat_id != ALLOWED_CHAT_ID:
        return
    cur = current_season()
    last = meta_get("season_cur")
    if last is None:
        meta_set("season_cur", cur)                 # первый запуск — просто запоминаем сезон
        return
    if last == cur:
        return                                       # сезон не сменился
    # месяц сменился — подводим итоги прошлого сезона `last`
    if not meta_get(f"season_done_{last}"):
        top = [r for r in season_top(chat_id, last, 3) if r["val"] > 0]
        if top:
            medals = [pe("gold"), pe("silver"), pe("bronze")]
            now_iso = msk_now().isoformat()
            lines = []
            for i, r in enumerate(top):
                rank = i + 1
                bonus = SEASON_REWARDS.get(rank, 0)
                conn.execute(
                    "INSERT INTO hall_of_fame (chat_id, season, rank, user_id, name, score, ts) VALUES (?,?,?,?,?,?,?)",
                    (chat_id, last, rank, r["user_id"], r["name"], int(r["val"]), now_iso))
                if r["user_id"] not in EXCLUDED_IDS and r["user_id"] != LEADER_ID:
                    if bonus:
                        add_lp(chat_id, r["user_id"], bonus)
                    # 6.0 Сезоны 2.0: вручаем престиж-титул чемпиона/призёра на ~месяц
                    stitle = SEASON_TITLES.get(rank)
                    if stitle:
                        until = (msk_now() + timedelta(days=SEASON_CHAMPION_TITLE_DAYS)).strftime("%Y-%m-%d")
                        conn.execute("UPDATE users SET title_text=?, title_until=? WHERE chat_id=? AND user_id=?",
                                     (stitle, until, chat_id, r["user_id"]))
                lines.append(f"{medals[i]} <b>{esc(r['name'])}</b> — {r['val']} сообщ. (+{bonus} LP · {SEASON_TITLES.get(i+1,'')})")
            conn.commit()
            try:
                await context.bot.send_message(
                    chat_id,
                    f"{pe('cup')} <b>СЕЗОН «{season_title(last)}» ЗАВЕРШЁН — {CLAN_NAME}</b>\n{CLAN_LINE}\n\n"
                    f"{pe('crown')} <b>Победители сезона:</b>\n" + "\n".join(lines) + "\n\n"
                    f"{pe('star')} Они занесены в зал славы (<code>hof</code>) и получили <b>титул-регалию в профиль</b> на месяц! Новый сезон «{season_title(cur)}» начался — "
                    f"топ обнулён, у всех равные шансы. Погнали! {pe('fire')}",
                    parse_mode=ParseMode.HTML)
            except Exception:
                pass
        meta_set(f"season_done_{last}", "1")
    meta_set("season_cur", cur)


async def norms_status_text(chat_id, user_id, name):
    """5.5: личный прогресс по нормам недели — для команды «нормы»."""
    row = conn.execute("SELECT * FROM users WHERE chat_id=? AND user_id=?", (chat_id, user_id)).fetchone()
    if not row:
        return f"{pe('cross')} Не нашёл такого участника в системе."
    monday, sunday = week_bounds()
    started = today_str() >= NORMS_START_DATE
    msgs = stat_week(chat_id, user_id)
    left = max(0, WEEKLY_MSG_NORM - msgs)
    lines = [f"{pe('scroll')} <b>НОРМЫ НЕДЕЛИ — {esc(name)}</b>",
             f"<i>Неделя {_fmt_day_human(monday)} – {_fmt_day_human(sunday)} · проверка в воскресенье ~21:00 МСК</i>"]
    if not started:
        lines.append(f"{pe('clock')} <b>Нормы стартуют {_fmt_day_human(NORMS_START_DATE)}.</b> Ниже — тренировочный прогресс.")
    lines.append(f"{pe('msg')} Сообщения: <b>{msgs}</b> из {WEEKLY_MSG_NORM} "
                 + (f"{pe('check')} норма закрыта!" if msgs >= WEEKLY_MSG_NORM
                    else f"· осталось <b>{left}</b>"))
    _days_ru = ["пн", "вт", "ср", "чт", "пт", "сб", "вс"]
    _mon_d = datetime.fromisoformat(monday).date()
    per = []
    for k in range(7):
        c = conn.execute("SELECT COALESCE(SUM(cnt),0) FROM stats WHERE chat_id=? AND user_id=? AND day=?",
                         (chat_id, user_id, (_mon_d + timedelta(days=k)).isoformat())).fetchone()[0]
        per.append(f"{_days_ru[k]} {c}")
    lines.append(f"<i>по дням: {' · '.join(per)}</i>")
    if row["brawl_tag"]:
        if row["trophy_base_week"] == monday and row["trophy_base"] is not None:
            gain, cur = None, None
            try:
                data = await brawl_fetch(row["brawl_tag"])
                if data:
                    cur = int(data.get("trophies", 0))
                    gain = cur - int(row["trophy_base"])
            except Exception as e:
                log_error("нормы (кубки)", e)
            if gain is None:
                lines.append(f"{pe('cup')} Кубки: <i>не удалось получить данные — попробуй чуть позже</i>")
            else:
                cup_left = max(0, WEEKLY_TROPHY_NORM - gain)
                lines.append(f"{pe('cup')} Кубки за неделю: <b>{'+' if gain >= 0 else ''}{gain}</b> из +{WEEKLY_TROPHY_NORM} "
                             + (f"{pe('check')} неделя зачтена!"
                                if gain >= WEEKLY_TROPHY_NORM else f"· осталось <b>{cup_left}</b>"))
                lines.append(f"<i>снимок {meta_get('trophy_snapshot_ts') or '—'}: было {row['trophy_base']} → сейчас {cur}</i>")
                # 6.0: сколько недель закрыто в этом месяце (кубки + сообщения) → будущая выплата
                mkey = today_str()[:7]
                _wkrow = conn.execute("SELECT COALESCE(SUM(cups_ok),0) AS c, COALESCE(SUM(msg_ok),0) AS m FROM norm_weeks WHERE chat_id=? AND user_id=? AND week LIKE ?",
                                      (chat_id, user_id, mkey + "%")).fetchone()
                wk_ok = _wkrow["c"]; wk_msg = _wkrow["m"]
                payout = wk_ok * NORM_MONTH_PER_WEEK + wk_msg * NORM_MSG_WEEK_LP
                lines.append(f"{pe('lp')} В этом месяце: кубки ×{wk_ok}, сообщения ×{wk_msg} → к выплате <b>{payout} LP</b> "
                             f"<i>(1-го числа · кубки {NORM_MONTH_PER_WEEK}/нед, сообщ {NORM_MSG_WEEK_LP}/нед)</i>")
        else:
            lines.append(f"{pe('cup')} Кубки: <i>точки отсчёта этой недели ещё нет — появится сама (понедельник или дневной скан в 13:00), либо привяжи тег командой <code>.tag</code></i>")
    else:
        lines.append(f"{pe('cup')} Кубки: <i>тег не привязан</i> — <code>.tag #ТЕГ</code>")
    if on_vacation(row):
        lines.append(f"{pe('star')} Отпуск до <b>{row['vacation_until']}</b> — нормы на паузе.")
    if started and soft_start_active():
        soft_end = (datetime.fromisoformat(NORMS_START_DATE).date()
                    + timedelta(weeks=SOFT_START_WEEKS)).strftime("%d.%m")
        lines.append(f"{pe('shield')} Мягкий старт до {soft_end}: нормы считаются, штрафов нет.")
    nw = count_norm_warns(chat_id, user_id)
    if nw:
        lines.append(f"{pe('warn')} Норм-варны: <b>{nw}</b> из {NORM_WARN_LIMIT}.")
    if not started:
        lines.append(f"<blockquote expandable>{pe('note')} <b>Как будет с {_fmt_day_human(NORMS_START_DATE)}:</b>\n"
                     f"сообщения {WEEKLY_MSG_NORM}/нед; кубки +{WEEKLY_TROPHY_NORM}/нед (зачёт недели → +{NORM_MONTH_PER_WEEK} LP, выплата 1-го числа месяца); "
                     f"провал нормы → −{NORM_FAIL_PENALTY} LP и норм-варн ({NORM_WARN_LIMIT} за месяц = исключение); "
                     f"первые {SOFT_START_WEEKS} недели — мягкий старт без штрафов.</blockquote>")
    return "\n".join(lines)


async def norm_dry_run(chat_id):
    """5.5: сухой прогон воскресной проверки — кто прошёл бы, кто нет. БЕЗ штрафов, наград и варнов.
    Логику условий держать в синхроне с run_norm_check."""
    monday = week_bounds()[0]
    rows = conn.execute(
        "SELECT user_id, name, username, brawl_tag, trophy_base, trophy_base_week, vacation_until, norm_anchor_week "
        "FROM users WHERE chat_id=? AND COALESCE(gone,0)=0", (chat_id,)).fetchall()
    anchored = any(r["norm_anchor_week"] == monday for r in rows)
    ok, fail, skip = [], [], []
    for r in rows:
        uid = r["user_id"]
        if uid in EXCLUDED_IDS or uid == LEADER_ID:
            continue
        if on_vacation(r):
            skip.append(f"{esc(disp_name(r))} — отпуск")
            continue
        if anchored and r["norm_anchor_week"] != monday:
            skip.append(f"{esc(disp_name(r))} — вступил среди недели")
            continue
        probs, oks = [], []
        msgs = stat_week(chat_id, uid)
        (oks if msgs >= WEEKLY_MSG_NORM else probs).append(f"сообщ. {msgs}/{WEEKLY_MSG_NORM}")
        if r["brawl_tag"] and r["trophy_base_week"] == monday and r["trophy_base"] is not None:
            try:
                data = await brawl_fetch(r["brawl_tag"])
            except Exception:
                data = None
            if data:
                gain = int(data.get("trophies", 0)) - int(r["trophy_base"])
                (oks if gain >= WEEKLY_TROPHY_NORM else probs).append(
                    f"кубки {'+' if gain >= 0 else ''}{gain}/+{WEEKLY_TROPHY_NORM}")
            else:
                oks.append("кубки: API недоступен, пропуск")
        line = f"{esc(disp_name(r))} — " + ", ".join(probs or oks)
        if probs and oks:
            line += f" · ок: {', '.join(oks)}"
        (fail if probs else ok).append(line)
    head = (f"{pe('scales')} <b>СУХОЙ ПРОГОН НОРМ — без штрафов</b>\n"
            f"<i>Неделя с {_fmt_day_human(monday)} · так выглядела бы воскресная проверка</i>\n"
            f"{pe('cup')} <i>Кубки считаются с момента снимка ({meta_get('trophy_snapshot_ts') or 'снимка ещё не было'}).</i>")
    if not anchored:
        head += f"\n{pe('note')} <i>Отметок начала недели нет (до старта норм) — проверяю всех по текущей неделе.</i>"
    elif soft_start_active() and today_str() >= NORMS_START_DATE:
        head += f"\n{pe('shield')} <i>Идёт мягкий старт — штрафов не будет и в боевой проверке.</i>"
    out = [head]
    if ok:
        out.append(f"{pe('check')} <b>Прошли бы ({len(ok)}):</b>\n<blockquote expandable>" + "\n".join(ok[:60]) + "</blockquote>")
    if fail:
        out.append(f"{pe('cross')} <b>Провалили бы ({len(fail)}):</b>\n<blockquote expandable>" + "\n".join(fail[:60]) + "</blockquote>")
    if skip:
        out.append(f"{pe('clock')} <b>Вне проверки ({len(skip)}):</b>\n<blockquote expandable>" + "\n".join(skip[:60]) + "</blockquote>")
    if not (ok or fail):
        out.append("<i>Проверять некого — в базе нет подходящих участников.</i>")
    return "\n\n".join(out)


NORM_REMIND_HOUR = 12   # 5.5: суббота, час напоминания не закрывшим норму сообщений (МСК)


RANK_SCAN_HOUR = 13     # 5.6: час ежедневного сканирования рангов по журналам боёв (МСК)


async def job_rank_scan(context):
    """5.6: ежедневный скан привязанных — свежие кубки/ник для всех, лучший ранг и авто-награды за ступени.
    Благодаря этому «обновлять тег» вручную больше не нужно никому."""
    if not ALLOWED_CHAT_ID:
        return
    day = today_str()
    if meta_get("rank_scan_day") == day:
        return
    meta_set("rank_scan_day", day)
    mon_w = week_bounds()[0]
    rows = conn.execute(
        "SELECT user_id, name, username, brawl_tag FROM users WHERE chat_id=? AND COALESCE(gone,0)=0 "
        "AND brawl_tag IS NOT NULL AND brawl_tag<>''", (ALLOWED_CHAT_ID,)).fetchall()
    achievements, ranked_cnt = [], 0
    for r in rows:
        # 1) свежие кубки и игровой ник
        pdata = await brawl_fetch(r["brawl_tag"])
        if pdata:
            tro = int(pdata.get("trophies", 0))
            conn.execute("UPDATE users SET brawl_trophies=?, brawl_name=? WHERE chat_id=? AND user_id=?",
                         (tro, pdata.get("name"), ALLOWED_CHAT_ID, r["user_id"]))
            # если точки отсчёта этой недели нет — ставим её сейчас (существующую НЕ трогаем)
            conn.execute("UPDATE users SET trophy_base=?, trophy_base_week=? WHERE chat_id=? AND user_id=? "
                         "AND (trophy_base_week IS NULL OR trophy_base_week<>?)",
                         (tro, mon_w, ALLOWED_CHAT_ID, r["user_id"], mon_w))
        # 2) ранги по журналу боёв + награды за новые ступени
        data = await brawl_battlelog(r["brawl_tag"])
        ent = extract_ranked_entries(data, r["brawl_tag"]) if data else []
        if ent:
            mx = max(e["value"] for e in ent)
            _best, awards = apply_rank_progress(ALLOWED_CHAT_ID, r["user_id"], mx)
            for tn, lp in awards:
                achievements.append(f"{esc(disp_name(r))} — впервые <b>{tn}</b>! <b>+{lp} LP</b>")
            ranked_cnt += 1
        await asyncio.sleep(0.4)   # бережём API
    conn.commit()
    if achievements:
        try:
            await context.bot.send_message(
                ALLOWED_CHAT_ID,
                f"{pe('cup')} <b>РАНГОВЫЕ ДОСТИЖЕНИЯ</b>\n{CLAN_LINE}\n" + "\n".join(achievements[:20]),
                parse_mode=ParseMode.HTML)
        except Exception as e:
            log_error("анонс рангов", e)
    print(f"[РАНГ] скан: игроков {len(rows)}, с ранговыми боями {ranked_cnt}, наград {len(achievements)}", flush=True)


async def job_norm_reminder(context):
    """5.5: суббота 12:00 — напоминаем в чате тем, кто ещё не закрыл норму сообщений."""
    if not ALLOWED_CHAT_ID or today_str() < NORMS_START_DATE:
        return
    if msk_now().weekday() != 5:        # 5 = суббота
        return
    monday, _sun = week_bounds()
    key = f"norm_remind_{monday}"
    if meta_get(key):
        return
    meta_set(key, "1")
    rows = conn.execute(
        "SELECT user_id, name, username, vacation_until, norm_anchor_week FROM users "
        "WHERE chat_id=? AND COALESCE(gone,0)=0", (ALLOWED_CHAT_ID,)).fetchall()
    lag = []
    for r in rows:
        if r["user_id"] in EXCLUDED_IDS or r["user_id"] == LEADER_ID or on_vacation(r):
            continue
        if r["norm_anchor_week"] != monday:
            continue
        msgs = stat_week(ALLOWED_CHAT_ID, r["user_id"])
        if msgs < WEEKLY_MSG_NORM:
            ping = f"@{r['username']}" if r["username"] else esc(disp_name(r))
            lag.append(f"{ping} ({msgs}/{WEEKLY_MSG_NORM})")
    if not lag:
        return
    shown = ", ".join(lag[:25]) + (f" и ещё {len(lag) - 25}" if len(lag) > 25 else "")
    soft_note = (f"\n{pe('shield')} <i>Идёт мягкий старт — штрафов не будет, но темп держим.</i>"
                 if soft_start_active() else "")
    try:
        await context.bot.send_message(
            ALLOWED_CHAT_ID,
            f"{pe('mega')} <b>СУББОТА — последний рывок недели!</b>\n"
            f"Норма сообщений ({WEEKLY_MSG_NORM}/нед) пока не закрыта у: {shown}.\n"
            f"{pe('clock')} Проверка — завтра вечером. Свой прогресс: <code>.нормы</code>{soft_note}",
            parse_mode=ParseMode.HTML)
    except Exception as e:
        log_error("суббота-напоминание", e)


async def run_norm_snapshot(context, chat_id, force=False):
    """Начало недели: точка отсчёта норм — помечает участников и снимает кубки у привязанных.
    5.6: ЗАЩИЩЁН. Повторный снимок той же недели сбросил бы прирост кубков, поэтому без force пропускается."""
    monday = week_bounds()[0]
    if not force and meta_get("trophy_snapshot_week") == monday:
        print("[НОРМЫ] снимок этой недели уже есть — пропускаю (нужно слово «заново»)", flush=True)
        return False
    # отметка присутствия на начало недели — для всех, кроме админов вне экономики (нужно для нормы активности)
    excl = tuple(EXCLUDED_IDS) or (0,)
    ph = ",".join("?" * len(excl))
    conn.execute(f"UPDATE users SET norm_anchor_week=? WHERE chat_id=? AND COALESCE(gone,0)=0 AND user_id NOT IN ({ph})",
                 (monday, chat_id, *excl))
    conn.commit()
    # снимок кубков — только у привязанных (и не в отпуске)
    rows = conn.execute(
        "SELECT user_id, brawl_tag, vacation_until FROM users WHERE chat_id=? AND COALESCE(gone,0)=0 "
        "AND brawl_tag IS NOT NULL AND brawl_tag<>''",
        (chat_id,)).fetchall()
    for r in rows:
        if on_vacation(r):
            continue
        try:
            data = await brawl_fetch(r["brawl_tag"])
        except Exception:
            data = None
        if not data:
            continue
        tro = int(data.get("trophies", 0))
        conn.execute("UPDATE users SET trophy_base=?, trophy_base_week=?, brawl_trophies=?, brawl_name=? WHERE chat_id=? AND user_id=?",
                     (tro, monday, tro, data.get("name"), chat_id, r["user_id"]))
    conn.commit()
    meta_set("trophy_snapshot_week", monday)                      # 5.6: защита от повторного обнуления
    meta_set("trophy_snapshot_ts", msk_now().strftime("%d.%m %H:%M"))
    return True


async def run_norm_check(context, chat_id, announce=True):
    """Проверка норм за неделю: кубки (у привязанных) и активность (у всех). Штраф −LP за каждую
    проваленную норму + максимум один норм-варн в неделю; 3 норм-варна → исключение. Мягкий старт — без штрафов."""
    monday = week_bounds()[0]
    soft = soft_start_active()
    rows = conn.execute(
        "SELECT user_id, name, username, brawl_tag, trophy_base, trophy_base_week, vacation_until, norm_anchor_week "
        "FROM users WHERE chat_id=? AND COALESCE(gone,0)=0", (chat_id,)).fetchall()
    cup_done, cup_failed = [], []          # (имя, прирост[, варн])
    act_done, act_failed = [], []          # (имя, сообщений)
    excluded = []
    for r in rows:
        uid = r["user_id"]
        nm = disp_name(r)
        if uid in EXCLUDED_IDS or uid == LEADER_ID or on_vacation(r):
            continue
        if r["norm_anchor_week"] != monday:
            continue                                       # вступил в середине недели / не был на старте — пропуск
        failed_any = False
        cups_ok_flag, msg_ok_flag = None, 0
        # --- НОРМА КУБКОВ (только у привязанных со снимком этой недели) ---
        if r["brawl_tag"] and r["trophy_base_week"] == monday and r["trophy_base"] is not None:
            try:
                data = await brawl_fetch(r["brawl_tag"])
            except Exception:
                data = None
            if data:                                       # если API недоступен — кубки в этот раз не проверяем
                gain = int(data.get("trophies", 0)) - int(r["trophy_base"])
                if gain >= WEEKLY_TROPHY_NORM:
                    cups_ok_flag = 1                        # 5.8: зачёт недели — LP начислим в конце месяца
                    cup_done.append((nm, gain))
                elif soft:
                    cups_ok_flag = 0
                    cup_failed.append((nm, gain, 0))
                else:
                    cups_ok_flag = 0
                    add_lp(chat_id, uid, -NORM_FAIL_PENALTY)
                    failed_any = True
                    cup_failed.append((nm, gain, None))
        # --- НОРМА АКТИВНОСТИ (у всех присутствовавших на старте недели) ---
        msgs = stat_week(chat_id, uid)
        if msgs >= WEEKLY_MSG_NORM:
            msg_ok_flag = 1
            act_done.append((nm, msgs))
        elif soft:
            act_failed.append((nm, msgs))
        else:
            add_lp(chat_id, uid, -NORM_FAIL_PENALTY)
            failed_any = True
            act_failed.append((nm, msgs))
        # 5.8: фиксируем результат недели для месячной выплаты (кубки начисляются раз в месяц)
        conn.execute(
            "INSERT INTO norm_weeks (chat_id, user_id, week, cups_ok, msg_ok) VALUES (?,?,?,?,?) "
            "ON CONFLICT(chat_id, user_id, week) DO UPDATE SET cups_ok=excluded.cups_ok, msg_ok=excluded.msg_ok",
            (chat_id, uid, monday, 1 if cups_ok_flag else 0, msg_ok_flag))
        # --- НОРМ-ВАРН: максимум один в неделю, если провалена хотя бы одна норма ---
        if failed_any and not soft:
            nw = add_norm_warn(chat_id, uid)
            if nw >= NORM_WARN_LIMIT:
                try:
                    await do_exclude(context, chat_id, uid)
                except Exception:
                    pass
                conn.execute("DELETE FROM norm_warns WHERE chat_id=? AND user_id=?", (chat_id, uid))
                conn.commit()
                excluded.append(nm)
    conn.commit()   # 5.8: сохраняем зачёты недели
    if announce and (cup_done or cup_failed or act_done or act_failed or excluded):
        P = [f"{pe('cup')} <b>ИТОГИ НЕДЕЛИ — НОРМЫ ({CLAN_NAME})</b>\n{CLAN_LINE}"]
        if soft:
            P.append(f"{pe('bulb')} <i>Мягкий старт: штрафы и норм-варны пока не применяются.</i>")
        P.append(f"{pe('lp')} <i>Награда за кубки копится и выдаётся big-проверкой 1-го числа месяца "
                 f"(+{NORM_MONTH_PER_WEEK} LP за каждую закрытую неделю).</i>")
        # кубки
        P.append(f"\n{pe('cup')} <b>Кубки</b> (норма +{WEEKLY_TROPHY_NORM}/нед):")
        if cup_done:
            P.append(f"{pe('check')} Зачтено за неделю: " + ", ".join(f"{esc(n)} (+{g})" for n, g in cup_done))
        if cup_failed:
            P.append(f"{pe('cross')} Не выполнили" + ("" if soft else f" (−{NORM_FAIL_PENALTY} LP)") + ": " +
                     ", ".join(f"{esc(n)} (+{g})" for n, g, _w in cup_failed))
        if not cup_done and not cup_failed:
            P.append("<i>— некого проверять (нет снимков кубков)</i>")
        # активность
        P.append(f"\n{pe('msg')} <b>Активность</b> (норма {WEEKLY_MSG_NORM} сообщ./нед):")
        if act_done:
            P.append(f"{pe('check')} Выполнили: <b>{len(act_done)}</b>")
        if act_failed:
            P.append(f"{pe('cross')} Не выполнили" + ("" if soft else f" (−{NORM_FAIL_PENALTY} LP)") + ": " +
                     ", ".join(f"{esc(n)} ({m})" for n, m in act_failed[:15]) + (f" и ещё {len(act_failed)-15}" if len(act_failed) > 15 else ""))
        if excluded:
            P.append(f"\n{pe('ban')} <b>Исключены</b> (3 норм-варна за месяц): " + ", ".join(esc(n) for n in excluded))
        try:
            await context.bot.send_message(chat_id, "\n".join(P), parse_mode=ParseMode.HTML)
        except Exception:
            pass
    return len(cup_done) + len(act_done), len(cup_failed) + len(act_failed), len(excluded)


async def maybe_citadel_bonus(context, chat_id):
    """Разовое начисление всем участникам +CITADEL_BONUS_LP за запуск «Цитадели». Срабатывает один раз."""
    if not ALLOWED_CHAT_ID or chat_id != ALLOWED_CHAT_ID:
        return
    if meta_get("citadel_bonus_done"):
        return
    meta_set("citadel_bonus_done", "1")                # ставим флаг сразу — чтобы не повторилось
    rows = conn.execute("SELECT user_id FROM users WHERE chat_id=?", (chat_id,)).fetchall()
    n = 0
    for r in rows:
        if r["user_id"] in EXCLUDED_IDS or r["user_id"] == LEADER_ID:
            continue
        add_lp(chat_id, r["user_id"], CITADEL_BONUS_LP)
        n += 1
    try:
        await context.bot.send_message(
            chat_id,
            f"{pe('crown')} <b>Спасибо за терпение!</b>\n{CLAN_LINE}\n"
            f"В честь завершения большого обновления <b>«Цитадель»</b> и за ваше терпение во время долгих "
            f"техработ — каждому участнику <b>+{CITADEL_BONUS_LP} LP</b>! {pe('lp')}\n"
            f"Начислено <b>{n}</b> участникам. Дальше — только лучше! {pe('fire')}",
            parse_mode=ParseMode.HTML)
    except Exception:
        pass


def _fmt_day_human(day):
    """YYYY-MM-DD -> ДД.ММ.ГГГГ."""
    try:
        return datetime.strptime(day, "%Y-%m-%d").strftime("%d.%m.%Y")
    except Exception:
        return day


def _hhmm(ts):
    """Из ISO-времени достаёт ЧЧ:ММ."""
    try:
        return ts[11:16] if ts and len(ts) >= 16 else (ts or "")
    except Exception:
        return ""


def build_daily_report_csv(chat_id, day):
    """CSV-отчёт за день: активность участников, модерация, заказы, итоги.
    Возвращает (csv_text, summary) или (None, None), если за день вообще ничего не происходило."""
    rows = conn.execute(
        "SELECT u.user_id, COALESCE(u.name,'') AS name, COALESCE(u.username,'') AS uname, "
        "COALESCE(u.lp,0) AS lp, COALESCE(u.reputation,0) AS rep, "
        "COALESCE((SELECT cnt FROM stats s WHERE s.chat_id=u.chat_id AND s.user_id=u.user_id AND s.day=?),0) AS msgs, "
        "COALESCE((SELECT SUM(delta) FROM lp_log l WHERE l.chat_id=u.chat_id AND l.user_id=u.user_id AND l.day=?),0) AS lpd "
        "FROM users u WHERE u.chat_id=? ORDER BY msgs DESC, lp DESC",
        (day, day, chat_id)).fetchall()
    active = [r for r in rows if r["msgs"] > 0 or r["lpd"] != 0]
    mod = conn.execute(
        "SELECT ts, COALESCE(admin_name,'') AS adm, COALESCE(target_name,'') AS tgt, "
        "COALESCE(action,'') AS act, COALESCE(reason,'') AS rsn FROM modlog "
        "WHERE chat_id=? AND ts LIKE ? ORDER BY ts", (chat_id, day + "%")).fetchall()
    orders = conn.execute(
        "SELECT ts, COALESCE(name,'') AS name, COALESCE(item,'') AS item, "
        "COALESCE(price,0) AS price, COALESCE(status,'') AS st FROM orders "
        "WHERE chat_id=? AND ts LIKE ? ORDER BY ts", (chat_id, day + "%")).fetchall()
    if not active and not mod and not orders:
        return None, None
    total_msgs = sum(r["msgs"] for r in active)
    total_lp = sum(r["lpd"] for r in active)

    import csv as _csv, io as _io
    buf = _io.StringIO()
    buf.write("\ufeff")   # BOM — чтобы Excel показал кириллицу корректно
    w = _csv.writer(buf, delimiter=";")
    w.writerow([f"Отчёт за {_fmt_day_human(day)}", CLAN_NAME])
    w.writerow([])
    w.writerow(["УЧАСТНИКИ — активность за день"])
    w.writerow(["Имя", "Ник", "Сообщений", "LP за день", "Баланс LP", "Репутация"])
    for r in active:
        uname = ("@" + r["uname"]) if r["uname"] else ""
        lpd = f"+{r['lpd']}" if r["lpd"] > 0 else str(r["lpd"])
        w.writerow([r["name"], uname, r["msgs"], lpd, r["lp"], r["rep"]])
    w.writerow([])
    w.writerow(["МОДЕРАЦИЯ ЗА ДЕНЬ"])
    if mod:
        w.writerow(["Время", "Админ", "Кому", "Действие", "Причина"])
        for m in mod:
            w.writerow([_hhmm(m["ts"]), m["adm"], m["tgt"], m["act"], m["rsn"]])
    else:
        w.writerow(["нарушений и наказаний не было"])
    w.writerow([])
    w.writerow(["ЗАКАЗЫ В МАГАЗИНЕ"])
    if orders:
        w.writerow(["Время", "Кто", "Товар", "Цена LP", "Статус"])
        for o in orders:
            w.writerow([_hhmm(o["ts"]), o["name"], o["item"], o["price"], o["st"]])
    else:
        w.writerow(["заказов не было"])
    w.writerow([])
    w.writerow(["ИТОГО ЗА ДЕНЬ"])
    w.writerow(["Всего сообщений", total_msgs])
    w.writerow(["Активных участников", len(active)])
    issued, burned = lp_day_flows(chat_id, day)
    w.writerow(["Выпущено LP", issued])
    w.writerow(["Сожжено LP", burned])
    w.writerow(["Изменение LP суммарно", (f"+{total_lp}" if total_lp > 0 else str(total_lp))])
    w.writerow(["Ошибок бота", errors_on(day)])
    w.writerow(["Действий модерации", len(mod)])
    w.writerow(["Заказов", len(orders)])
    summary = dict(msgs=total_msgs, active=len(active), lp=total_lp, mod=len(mod), orders=len(orders),
                   top=(active[0]["name"] if active else "—"), top_msgs=(active[0]["msgs"] if active else 0))
    return buf.getvalue(), summary


def build_daily_report_xlsx(chat_id, day, charts=None):
    """Единый официальный Excel-отчёт за день: сводка, активность, диаграммы и журнал.
    charts — список (title, png_bytes): диаграммы встраиваются КАРТИНКАМИ (видны на любом
    устройстве). Возвращает (bytes, summary) или (None, None). Требует openpyxl — если его нет,
    вернёт None, и сработает запасной CSV-вариант."""
    try:
        import io as _io
        from openpyxl import Workbook
        from openpyxl.styles import Font, PatternFill, Alignment, Border, Side
    except Exception:
        return None, None
    rows = conn.execute(
        "SELECT u.user_id, COALESCE(u.name,'') AS name, COALESCE(u.username,'') AS uname, "
        "COALESCE(u.lp,0) AS lp, COALESCE(u.reputation,0) AS rep, "
        "COALESCE((SELECT cnt FROM stats s WHERE s.chat_id=u.chat_id AND s.user_id=u.user_id AND s.day=?),0) AS msgs, "
        "COALESCE((SELECT SUM(delta) FROM lp_log l WHERE l.chat_id=u.chat_id AND l.user_id=u.user_id AND l.day=?),0) AS lpd "
        "FROM users u WHERE u.chat_id=? ORDER BY msgs DESC, lp DESC",
        (day, day, chat_id)).fetchall()
    active = [r for r in rows if r["msgs"] > 0 or r["lpd"] != 0]
    mod = conn.execute(
        "SELECT ts, COALESCE(admin_name,'') AS adm, COALESCE(target_name,'') AS tgt, "
        "COALESCE(action,'') AS act, COALESCE(reason,'') AS rsn FROM modlog "
        "WHERE chat_id=? AND ts LIKE ? ORDER BY ts", (chat_id, day + "%")).fetchall()
    orders = conn.execute(
        "SELECT ts, COALESCE(name,'') AS name, COALESCE(item,'') AS item, "
        "COALESCE(price,0) AS price, COALESCE(status,'') AS st FROM orders "
        "WHERE chat_id=? AND ts LIKE ? ORDER BY ts", (chat_id, day + "%")).fetchall()
    if not active and not mod and not orders:
        return None, None
    total_msgs = sum(r["msgs"] for r in active)
    total_lp = round(sum(r["lpd"] for r in active), 2)

    NAVY, BLUE, LIGHT, RED = "1F3864", "2E5496", "D9E1F2", "C00000"
    white_bold = Font(bold=True, color="FFFFFF")
    thin = Side(style="thin", color="BFBFBF")
    box = Border(left=thin, right=thin, top=thin, bottom=thin)
    center = Alignment(horizontal="center", vertical="center")
    left = Alignment(horizontal="left", vertical="center")

    def hdr(cell, text, fill=BLUE):
        cell.value = text
        cell.font = white_bold
        cell.fill = PatternFill("solid", fgColor=fill)
        cell.alignment = center
        cell.border = box

    wb = Workbook()
    ws = wb.active
    ws.title = "Отчёт"
    ws.sheet_view.showGridLines = False
    for i, wdt in enumerate([30, 22, 14, 15, 14, 13], 1):
        ws.column_dimensions[chr(64 + i)].width = wdt
    ws.merge_cells("A1:F1")
    c = ws["A1"]
    c.value = f"{CLAN_NAME} — ОТЧЁТ ПО ЧАТУ"
    c.font = Font(bold=True, size=18, color="FFFFFF")
    c.fill = PatternFill("solid", fgColor=NAVY)
    c.alignment = center
    ws.row_dimensions[1].height = 34
    ws.merge_cells("A2:F2")
    c = ws["A2"]
    c.value = f"за {_fmt_day_human(day)}   •   КОНФИДЕНЦИАЛЬНО — только для администрации"
    c.font = Font(bold=True, italic=True, color=RED)
    c.alignment = center
    ws.row_dimensions[2].height = 20
    ws["A4"].value = "СВОДКА ЗА ДЕНЬ"
    ws["A4"].font = Font(bold=True, size=13, color=NAVY)
    issued, burned = lp_day_flows(chat_id, day)
    pace = max(0.0, issued - burned) * 30 / 850
    summ = [("Сообщений за день", total_msgs, None), ("Активных участников", len(active), None),
            ("Выпущено LP за день", issued, "0.00"),
            ("Сожжено LP за день", burned, "0.00"),
            ("Изменение LP за день", total_lp, "+0.00;-0.00;0"),
            ("Темп экономики", f"≈ {pace:.1f} пасса/мес при таком темпе", None),
            ("Действий модерации", len(mod), None), ("Заказов в магазине", len(orders), None),
            ("Ошибок бота за день", errors_on(day), None),
            ("Самый активный", active[0]["name"] if active else "—", None)]
    for i, (k, v, fmt) in enumerate(summ):
        ws.cell(5 + i, 1, k).font = Font(bold=True)
        cell = ws.cell(5 + i, 2, v)
        if fmt:
            cell.number_format = fmt
    tr = 5 + len(summ) + 1
    ws.cell(tr, 1).value = "АКТИВНОСТЬ УЧАСТНИКОВ"
    ws.cell(tr, 1).font = Font(bold=True, size=13, color=NAVY)
    tr += 1
    for j, h in enumerate(["Имя", "Ник", "Сообщений", "LP за день", "Баланс LP", "Репутация"], 1):
        hdr(ws.cell(tr, j), h)
    ws.row_dimensions[tr].height = 18
    for i, r in enumerate(active):
        rr = tr + 1 + i
        uname = ("@" + r["uname"]) if r["uname"] else ""
        vals = [r["name"], uname, r["msgs"], round(r["lpd"], 2), round(r["lp"], 2), r["rep"]]
        for j, v in enumerate(vals, 1):
            cell = ws.cell(rr, j, v)
            cell.border = box
            cell.alignment = left if j <= 2 else center
            if i % 2 == 0:
                cell.fill = PatternFill("solid", fgColor=LIGHT)
        ws.cell(rr, 4).number_format = "+0.00;-0.00;0"
        ws.cell(rr, 5).number_format = "0.##"
    table_end = tr + len(active)

    # ── Диаграммы: картинками (видны везде), иначе — родные диаграммы Excel ──
    embedded = False
    if charts:
        try:
            import PIL  # noqa: F401  — openpyxl рисует картинки через Pillow
            from openpyxl.drawing.image import Image as XLImage
            arow = table_end + 3
            ws.cell(arow, 1).value = "ДИАГРАММЫ"
            ws.cell(arow, 1).font = Font(bold=True, size=13, color=NAVY)
            arow += 1
            for title, png in charts:
                if not png:
                    continue
                ws.cell(arow, 1).value = title
                ws.cell(arow, 1).font = Font(bold=True, color=NAVY)
                im = XLImage(_io.BytesIO(png))
                if im.width:
                    k = 600.0 / im.width
                    im.width = int(im.width * k)
                    im.height = int(im.height * k)
                ws.add_image(im, f"A{arow + 1}")
                arow += int(im.height / 19) + 3
            embedded = True
        except Exception as e:
            print(f"[REPORT] не удалось встроить графики-картинки: {e}", flush=True)
            embedded = False
    if not embedded:
        from openpyxl.chart import BarChart, PieChart, Reference
        wd = wb.create_sheet("Графики")
        wd.sheet_view.showGridLines = False
        wd.column_dimensions["A"].width = 16
        wd.column_dimensions["B"].width = 14
        try:
            base = datetime.fromisoformat(day).date()
        except Exception:
            base = msk_now().date()
        week = []
        for i in range(6, -1, -1):
            d = (base - timedelta(days=i)).isoformat()
            cc = conn.execute("SELECT COALESCE(SUM(cnt),0) AS c FROM stats WHERE chat_id=? AND day=?",
                              (chat_id, d)).fetchone()["c"]
            week.append((datetime.fromisoformat(d).strftime("%d.%m"), int(cc)))
        wd.cell(2, 1, "День"); wd.cell(2, 2, "Сообщений")
        for i, (lbl, val) in enumerate(week):
            wd.cell(3 + i, 1, lbl); wd.cell(3 + i, 2, val)
        bar = BarChart(); bar.type = "col"; bar.title = "Активность за 7 дней"; bar.legend = None
        bar.add_data(Reference(wd, min_col=2, min_row=2, max_row=2 + len(week)), titles_from_data=True)
        bar.set_categories(Reference(wd, min_col=1, min_row=3, max_row=2 + len(week)))
        bar.height = 8; bar.width = 18
        wd.add_chart(bar, "D2")
        sr = 3 + len(week) + 2
        top = [r for r in active if r["msgs"] > 0][:6]
        shares = [(r["name"], r["msgs"]) for r in top]
        others = total_msgs - sum(v for _, v in shares)
        if others > 0:
            shares.append(("Остальные", others))
        if shares:
            wd.cell(sr, 1, "Участник"); wd.cell(sr, 2, "Сообщений")
            for i, (nm, vv) in enumerate(shares):
                wd.cell(sr + 1 + i, 1, nm); wd.cell(sr + 1 + i, 2, vv)
            pie = PieChart(); pie.title = "Доли сообщений за день"
            pie.add_data(Reference(wd, min_col=2, min_row=sr, max_row=sr + len(shares)), titles_from_data=True)
            pie.set_categories(Reference(wd, min_col=1, min_row=sr + 1, max_row=sr + len(shares)))
            pie.height = 8; pie.width = 18
            wd.add_chart(pie, "D" + str(sr))

    wj = wb.create_sheet("Журнал")
    wj.sheet_view.showGridLines = False
    for col, wdt in (("A", 10), ("B", 24), ("C", 24), ("D", 18), ("E", 34)):
        wj.column_dimensions[col].width = wdt
    wj["A1"].value = "МОДЕРАЦИЯ ЗА ДЕНЬ"
    wj["A1"].font = Font(bold=True, size=13, color=NAVY)
    if mod:
        for j, h in enumerate(["Время", "Админ", "Кому", "Действие", "Причина"], 1):
            hdr(wj.cell(2, j), h)
        for i, m in enumerate(mod):
            rr = 3 + i
            for j, v in enumerate([_hhmm(m["ts"]), m["adm"], m["tgt"], m["act"], m["rsn"]], 1):
                wj.cell(rr, j, v).border = box
        orow = 3 + len(mod) + 2
    else:
        wj.cell(2, 1, "нарушений и наказаний не было")
        orow = 4
    wj.cell(orow, 1).value = "ЗАКАЗЫ В МАГАЗИНЕ"
    wj.cell(orow, 1).font = Font(bold=True, size=13, color=NAVY)
    if orders:
        for j, h in enumerate(["Время", "Кто", "Товар", "Цена LP", "Статус"], 1):
            hdr(wj.cell(orow + 1, j), h)
        for i, o in enumerate(orders):
            rr = orow + 2 + i
            for j, v in enumerate([_hhmm(o["ts"]), o["name"], o["item"], o["price"], o["st"]], 1):
                wj.cell(rr, j, v).border = box
    else:
        wj.cell(orow + 1, 1, "заказов не было")

    bio = _io.BytesIO()
    wb.save(bio)
    summary = dict(msgs=total_msgs, active=len(active), lp=total_lp, mod=len(mod), orders=len(orders),
                   top=(active[0]["name"] if active else "—"), top_msgs=(active[0]["msgs"] if active else 0))
    return bio.getvalue(), summary


async def send_daily_report(context, chat_id, day, recipient=None):
    """Шлёт официальный отчёт за день в личку (по умолчанию владельцу).
    Основной формат — красивый Excel с диаграммами; если openpyxl нет — запасной CSV + графики."""
    recipient = recipient or LEADER_ID
    try:
        import io as _io
        # Готовим диаграммы картинками — они встроятся в Excel и видны на любом устройстве.
        charts = []
        cfg_a, _ = _activity_chart_config(chat_id, 7)
        png_a = await _quickchart_png(cfg_a, 760, 380)
        if png_a:
            charts.append(("Активность за 7 дней", png_a))
        cfg_s, _ = _share_chart_config(chat_id, day)
        if cfg_s:
            png_s = await _quickchart_png(cfg_s, 760, 400)
            if png_s:
                charts.append(("Доли сообщений за день", png_s))
        xlsx, s = build_daily_report_xlsx(chat_id, day, charts=charts)
        if xlsx:
            data = _io.BytesIO(xlsx)
            data.name = f"linkor_report_{day}.xlsx"
            cap = (f"{pe('folder')} <b>ОФИЦИАЛЬНЫЙ ОТЧЁТ — {CLAN_NAME}</b>\n"
                   f"за {_fmt_day_human(day)} · {pe('lock')} только для администрации\n"
                   f"<blockquote>{pe('msg')} Сообщений: <b>{s['msgs']}</b> · активных: <b>{s['active']}</b>\n"
                   f"{pe('lp')} LP за день: <b>{('+' if s['lp'] > 0 else '')}{s['lp']}</b>\n"
                   f"{pe('shield')} Модерация: <b>{s['mod']}</b> · заказов: <b>{s['orders']}</b>\n"
                   f"{pe('cup')} Самый активный: <b>{esc(str(s['top']))}</b> ({s['top_msgs']} сообщ.)</blockquote>\n"
                   f"<i>Внутри: сводка, активность, графики (активность + доли) и журнал. Открывается в Excel.</i>")
            await context.bot.send_document(recipient, document=data, filename=data.name,
                                            caption=cap, parse_mode=ParseMode.HTML)
            return True
        # Запасной вариант: openpyxl недоступен — отправляем CSV и графики отдельными сообщениями.
        csv_text, s = build_daily_report_csv(chat_id, day)
        if not csv_text:
            return False
        data = _io.BytesIO(csv_text.encode("utf-8"))
        data.name = f"linkor_report_{day}.csv"
        cap = (f"{pe('folder')} <b>Отчёт по чату за {_fmt_day_human(day)} — {CLAN_NAME}</b>\n"
               f"{pe('lock')} только для администрации\n"
               f"<blockquote>{pe('msg')} Сообщений: <b>{s['msgs']}</b> · активных: <b>{s['active']}</b>\n"
               f"{pe('lp')} LP за день: <b>{('+' if s['lp'] > 0 else '')}{s['lp']}</b>\n"
               f"{pe('shield')} Действий модерации: <b>{s['mod']}</b> · заказов: <b>{s['orders']}</b>\n"
               f"{pe('cup')} Самый активный: <b>{esc(str(s['top']))}</b> ({s['top_msgs']} сообщ.)</blockquote>\n"
               f"<i>Полная таблица — в файле (Excel). Графики — следующими сообщениями.</i>")
        await context.bot.send_document(recipient, document=data, filename=data.name,
                                        caption=cap, parse_mode=ParseMode.HTML)
        await send_activity_chart(context, chat_id, recipient)
        await send_share_chart(context, chat_id, recipient, day=day)
        return True
    except Exception as e:
        print(f"[REPORT] не удалось отправить отчёт за {day}: {e}", flush=True)
        return False


def _activity_chart_config(chat_id, days=7):
    """Строит конфиг QuickChart для графика активности за N дней. Возвращает (config, caption)."""
    base = msk_now().date()
    labels, values = [], []
    for i in range(days - 1, -1, -1):
        d = (base - timedelta(days=i)).isoformat()
        c = conn.execute("SELECT COALESCE(SUM(cnt),0) AS c FROM stats WHERE chat_id=? AND day=?",
                         (chat_id, d)).fetchone()["c"]
        labels.append(datetime.fromisoformat(d).strftime("%d.%m"))
        values.append(int(c))
    colors = ["#4e79a7"] * len(values)
    if colors:
        colors[-1] = "#e15759"   # сегодня — выделяем красным
    config = {
        "type": "bar",
        "data": {"labels": labels,
                 "datasets": [{"label": "Сообщений", "data": values, "backgroundColor": colors}]},
        "options": {
            "title": {"display": True, "text": f"Активность {CLAN_NAME} — сообщений в день", "fontSize": 16},
            "legend": {"display": False},
            "scales": {"yAxes": [{"ticks": {"beginAtZero": True}}]},
        },
    }
    today_c = values[-1] if values else 0
    yest_c = values[-2] if len(values) >= 2 else 0
    if yest_c > 0:
        pct = round((today_c - yest_c) / yest_c * 100)
        trend = f"{pe('growth')} +{pct}% к вчера" if today_c >= yest_c else f"📉 {pct}% к вчера"
    else:
        trend = "за вчера данных нет"
    cap = (f"{pe('growth')} <b>Активность {CLAN_NAME} за {days} дн.</b>\n"
           f"Сегодня: <b>{today_c}</b> сообщ. · вчера: <b>{yest_c}</b> · {trend}\n"
           f"<i>Следи за трендом: проседает актив — пора расшевелить клан.</i>")
    return config, cap


def _share_chart_config(chat_id, day=None, top_n=6):
    """Строит конфиг QuickChart для круговой диаграммы долей. Возвращает (config, caption) или (None, None)."""
    day = day or today_str()
    rows = conn.execute(
        "SELECT COALESCE(u.name,'?') AS name, s.cnt AS cnt FROM stats s "
        "JOIN users u ON u.chat_id=s.chat_id AND u.user_id=s.user_id "
        "WHERE s.chat_id=? AND s.day=? AND s.cnt>0 ORDER BY s.cnt DESC",
        (chat_id, day)).fetchall()
    total = sum(r["cnt"] for r in rows)
    if total <= 0:
        return None, None
    top = rows[:top_n]
    labels = [r["name"] for r in top]
    values = [r["cnt"] for r in top]
    others = total - sum(values)
    if others > 0:
        labels.append("Остальные")
        values.append(others)
    palette = ["#4e79a7", "#f28e2b", "#e15759", "#76b7b2", "#59a14f",
               "#edc948", "#b07aa1", "#9c755f", "#bab0ac"]
    colors = [palette[i % len(palette)] for i in range(len(values))]
    config = {
        "type": "doughnut",
        "data": {"labels": labels, "datasets": [{"data": values, "backgroundColor": colors}]},
        "options": {
            "title": {"display": True, "text": f"Доли сообщений за {_fmt_day_human(day)} — {CLAN_NAME}",
                      "fontSize": 15},
            "legend": {"position": "right"},
        },
    }
    top1_pct = round(values[0] / total * 100)
    topn_pct = round(sum(r["cnt"] for r in top) / total * 100)
    cap = (f"{pe('stats')} <b>Доли сообщений за {_fmt_day_human(day)}</b>\n"
           f"Топ-1 — <b>{esc(labels[0])}</b> ({top1_pct}%) · топ-{len(top)} дают <b>{topn_pct}%</b> всего чата.\n"
           f"<i>Если пара человек тянет почти всё — стоит вовлечь остальных.</i>")
    return config, cap


async def _quickchart_png(config, width=640, height=360):
    """Запрашивает у QuickChart картинку (PNG) по конфигу. Возвращает bytes или None."""
    try:
        async with httpx.AsyncClient(timeout=15) as client:
            r = await client.post("https://quickchart.io/chart",
                                  json={"width": width, "height": height, "format": "png",
                                        "backgroundColor": "white", "chart": config})
        if r.status_code == 200 and r.content:
            return r.content
        print(f"[CHART] QuickChart вернул {r.status_code}", flush=True)
    except Exception as e:
        print(f"[CHART] QuickChart ошибка: {e}", flush=True)
    return None


async def send_activity_chart(context, chat_id, recipient, days=7):
    """Отправляет график активности картинкой (запасной путь, когда нет Excel)."""
    config, cap = _activity_chart_config(chat_id, days)
    png = await _quickchart_png(config, 640, 360)
    if not png:
        return False
    import io as _io
    img = _io.BytesIO(png); img.name = "activity.png"
    await context.bot.send_photo(recipient, photo=img, caption=cap, parse_mode=ParseMode.HTML)
    return True


async def send_share_chart(context, chat_id, recipient, day=None, top_n=6):
    """Отправляет диаграмму долей картинкой (запасной путь, когда нет Excel)."""
    config, cap = _share_chart_config(chat_id, day, top_n)
    if not config:
        return False
    png = await _quickchart_png(config, 640, 380)
    if not png:
        return False
    import io as _io
    img = _io.BytesIO(png); img.name = "shares.png"
    await context.bot.send_photo(recipient, photo=img, caption=cap, parse_mode=ParseMode.HTML)
    return True


async def _safe_daily_report(context, chat_id, day):
    try:
        await send_daily_report(context, chat_id, day)
    except Exception as e:
        log_error("отчёт (страховка)", e)


async def job_daily_report(context):
    """5.4: официальный отчёт владельцу уходит точно в 23:59 МСК по расписанию."""
    if not ALLOWED_CHAT_ID:
        return
    day = today_str()
    if meta_get("report_done_for") == day:
        return
    meta_set("report_done_for", day)
    await _safe_daily_report(context, ALLOWED_CHAT_ID, day)


async def maybe_daily_report(context, chat_id):
    """Страховка (5.4): если бот был выключен в 23:59 — отчёт за ВЧЕРА уйдёт
    с первым сообщением нового дня. Основной путь — job_daily_report по расписанию."""
    if not ALLOWED_CHAT_ID or chat_id != ALLOWED_CHAT_ID:
        return
    yesterday = (msk_now() - timedelta(days=1)).strftime("%Y-%m-%d")
    if meta_get("report_done_for") in (yesterday, today_str()):
        return
    meta_set("report_done_for", yesterday)   # ставим сразу, чтобы отчёт не задвоился
    asyncio.create_task(_safe_daily_report(context, chat_id, yesterday))


async def run_monthly_norms(context, chat_id, month_key):
    """5.8 БОЛЬШАЯ МЕСЯЧНАЯ ПРОВЕРКА: за прошедший месяц считаем, сколько недель каждый закрыл норму
    кубков, и выдаём LP пропорционально (NORM_MONTH_PER_WEEK за неделю). Прокрастинаторы, отыгравшие
    лишь последнюю неделю, получают только за неё — схитрить нельзя. Недели определяются по их понедельнику."""
    rows = conn.execute(
        "SELECT user_id, SUM(cups_ok) AS cw, SUM(msg_ok) AS mw, COUNT(*) AS wk FROM norm_weeks "
        "WHERE chat_id=? AND week LIKE ? GROUP BY user_id HAVING cw > 0 OR mw > 0",
        (chat_id, month_key + "%")).fetchall()
    paid = []
    for r in rows:
        uid = r["user_id"]
        if uid in EXCLUDED_IDS or uid == LEADER_ID:
            continue
        weeks_cups = int(r["cw"] or 0)
        weeks_msg = int(r["mw"] or 0)
        amount = weeks_cups * NORM_MONTH_PER_WEEK + weeks_msg * NORM_MSG_WEEK_LP   # 6.0: + норма сообщений
        if amount <= 0:
            continue
        add_lp(chat_id, uid, amount)
        urow = get_user(chat_id, uid)
        nm = disp_name(urow) if urow else str(uid)
        paid.append((nm, weeks_cups, weeks_msg, amount))
    paid.sort(key=lambda x: -x[3])
    # красивый месячный отчёт
    ym = month_key.split("-")
    month_names = ["", "январь", "февраль", "март", "апрель", "май", "июнь",
                   "июль", "август", "сентябрь", "октябрь", "ноябрь", "декабрь"]
    mname = month_names[int(ym[1])] if len(ym) == 2 and ym[1].isdigit() else month_key
    if paid:
        lines = [f"{pe('cup')} <b>БОЛЬШАЯ ПРОВЕРКА НОРМ — ИТОГИ МЕСЯЦА ({mname.upper()})</b>\n{CLAN_LINE}",
                 f"{pe('lp')} Кубки: <b>{NORM_MONTH_PER_WEEK} LP</b>/нед · Сообщения: <b>{NORM_MSG_WEEK_LP} LP</b>/нед (за каждую закрытую неделю).\n"]
        for nm, wc, wm, amt in paid[:30]:
            detail = []
            if wc:
                detail.append(f"кубки×{wc}")
            if wm:
                detail.append(f"сообщ×{wm}")
            lines.append(f"{pe('check')} <b>{esc(nm)}</b> — {', '.join(detail)} → <b>+{amt} LP</b>")
        total = sum(a for _n, _wc, _wm, a in paid)
        lines.append(f"\n{pe('star')} Всего выдано: <b>{total} LP</b> · отличная дисциплина, {CLAN_NAME}!")
        try:
            await context.bot.send_message(chat_id, "\n".join(lines), parse_mode=ParseMode.HTML)
        except Exception as e:
            log_error("месячные нормы (анонс)", e)
    # подчищаем отыгранный месяц, чтобы таблица не росла
    conn.execute("DELETE FROM norm_weeks WHERE chat_id=? AND week LIKE ?", (chat_id, month_key + "%"))
    conn.commit()
    return len(paid)


async def maybe_norm_cycle(context, chat_id):
    """Авто-снимок по понедельникам и авто-проверка по воскресеньям (фоном, без планировщика)."""
    if not ALLOWED_CHAT_ID or chat_id != ALLOWED_CHAT_ID:
        return
    if today_str() < NORMS_START_DATE:                 # нормы стартуют только с даты старта
        return
    now = msk_now()
    monday, sunday = week_bounds()
    if now.weekday() == 0 and not meta_get(f"snapshot_{monday}"):     # понедельник
        meta_set(f"snapshot_{monday}", "1")
        asyncio.create_task(run_norm_snapshot(context, chat_id))
    if now.weekday() == 6 and now.hour >= 21 and not meta_get(f"normcheck_{sunday}"):  # вс ≥21:00
        meta_set(f"normcheck_{sunday}", "1")
        asyncio.create_task(run_norm_check(context, chat_id))
    # 5.8: большая месячная проверка — 1-го числа за ПРОШЛЫЙ месяц (после того как все недели зачтены)
    if now.day == 1:
        prev_month = (now.replace(day=1) - timedelta(days=1)).strftime("%Y-%m")
        if not meta_get(f"monthnorms_{prev_month}"):
            meta_set(f"monthnorms_{prev_month}", "1")
            asyncio.create_task(run_monthly_norms(context, chat_id, prev_month))


async def send_weekly_report(context, chat_id):
    """Строит и отправляет отчёт за текущую неделю (без проверок времени — для авто и ручного вызова)."""
    mon, sun = week_bounds()
    excl = tuple(EXCLUDED_IDS) or (0,)
    ph = ",".join("?" * len(excl))
    rows = conn.execute(
        f"SELECT u.name, u.user_id, COALESCE(SUM(s.cnt),0) AS val FROM users u "
        f"LEFT JOIN stats s ON s.chat_id=u.chat_id AND s.user_id=u.user_id AND s.day BETWEEN ? AND ? "
        f"WHERE u.chat_id=? AND u.user_id NOT IN ({ph}) GROUP BY u.user_id ORDER BY val DESC",
        (mon, sun, chat_id, *excl)).fetchall()
    if not rows:
        return False
    medals = {0: pe("gold"), 1: pe("silver"), 2: pe("bronze")}
    top = [r for r in rows if r["val"] > 0][:5]
    top_lines = [f"<b>{i+1}.</b> {medals.get(i, pe('star'))} {esc(r['name'])} — <b>{r['val']}</b>"
                 for i, r in enumerate(top)]
    met = [r for r in rows if r["val"] >= WEEKLY_MSG_NORM]
    below = [r for r in rows if 0 < r["val"] < WEEKLY_MSG_NORM]
    below_names = ", ".join(esc(r["name"]) for r in below[:12])
    if len(below) > 12:
        below_names += f" и ещё {len(below) - 12}"
    text = (f"{pe('calendar')} <b>ИТОГИ НЕДЕЛИ — {CLAN_NAME}</b> <i>(Пн–Вс)</i>\n{CLAN_LINE}\n\n"
            f"{pe('cup')} <b>Самые активные:</b>\n"
            + ("\n".join(top_lines) if top_lines else "— тишина в чате —") + "\n\n")
    if today_str() >= NORMS_START_DATE:                 # норму активности показываем только с даты старта
        text += f"{pe('check')} Норму {WEEKLY_MSG_NORM}+ выполнили: <b>{len(met)}</b>\n"
        if below:
            text += f"{pe('warn')} Не дотянули норму: {below_names}\n"
    text += f"\n{pe('fire')} Новая неделя — новые цели. Погнали!"
    try:
        await context.bot.send_message(chat_id, text, parse_mode=ParseMode.HTML)
        return True
    except Exception:
        return False


async def maybe_weekly_report(context, chat_id):
    """Воскресный авто-отчёт клана (вс ≥20:00 МСК), один раз в неделю. Без планировщика."""
    if not ALLOWED_CHAT_ID or chat_id != ALLOWED_CHAT_ID:
        return
    now = msk_now()
    if now.weekday() != 6 or now.hour < 20:        # 6 = воскресенье
        return
    _, sun = week_bounds()
    key = f"weekreport_{sun}"
    if conn.execute("SELECT 1 FROM meta WHERE key=?", (key,)).fetchone():
        return
    conn.execute("INSERT OR REPLACE INTO meta (key, value) VALUES (?, '1')", (key,))
    conn.commit()
    await send_weekly_report(context, chat_id)


async def counter(update, context):
    if not update.effective_user or update.effective_user.is_bot:
        return
    if not update.effective_chat or update.effective_chat.type == "private":
        return
    chat_id = update.effective_chat.id
    user = update.effective_user
    txt_msg = (update.message.text or update.message.caption or "") if update.message else ""
    is_cmd = bool(txt_msg) and (txt_msg.startswith(CMD_PREFIX) or txt_msg.startswith("/"))   # 5.4: команды — не активность
    try:
        ensure_user(chat_id, user)
        if not is_cmd:
            stat_add(chat_id, user.id)
    except Exception as e:
        log_error("счётчик", e)   # 5.6: если у кого-то «теряются» сообщения — увидим в .ошибки
        return
    # буфер последних сообщений для AI-сводки
    if update.message and update.message.text and not is_cmd:
        buf = _chat_buffer.setdefault(chat_id, [])
        buf.append((user.full_name, update.message.text[:300]))
        if len(buf) > 60:
            del buf[:len(buf) - 60]
    # ЭКОНОМИКА: LP капают за каждое сообщение (молча), но:
    #  • не более чем за MSG_DAILY_CAP сообщений в день (дневной антиспам-потолок);
    #  • если человек пишет подряд сам с собой (никто не отвечает) дольше SELF_SPAM_LIMIT — LP не идут (анти-фарм).
    try:
        ls = _last_sender.get(chat_id)
        if ls and ls[0] == user.id:
            ls[1] += 1
        else:
            _last_sender[chat_id] = [user.id, 1]
            ls = _last_sender[chat_id]
        if not is_cmd and stat_today(chat_id, user.id) <= MSG_DAILY_CAP and ls[1] <= SELF_SPAM_LIMIT:
            add_lp(chat_id, user.id, round(LP_PER_MESSAGE * event_multiplier(), 3))   # 6.0: x2/x3 во время ивента
    except Exception:
        pass
    # 6.0: приватный фид для AI-дайджеста (только текст, без авторства)
    try:
        if not is_cmd and update.message.text:
            digest_feed(update.message.text)
    except Exception:
        pass
    # Ежедневный стрик (чек-ин при первом сообщении за день)
    try:
        res = None if is_cmd else touch_streak(chat_id, user)
        if res:
            streak, bonus = res
            if bonus > 0:   # достигнута веха серии — поздравляем в чате
                await update.message.reply_text(
                    f"{pe('fire')} <b>{esc(user.full_name)}</b> — серия активности <b>{streak} "
                    f"{_days_word(streak)} подряд</b>! {pe('lp')} +{bonus} LP", parse_mode=ParseMode.HTML)
    except Exception:
        pass
    # 5.8: квест дня (на сообщения/серию) — проверяем тихо при каждом сообщении
    try:
        if not is_cmd:
            await try_complete_quest(context, chat_id, user.id, user.full_name)
    except Exception as e:
        log_error("квест-проверка", e)
    # Бонус за «Цитадель» — только вручную командой bonusall. Воскресный авто-отчёт убран
    # (дублировал команду top). Итоги сезона при смене месяца (фоном при сообщениях)
    try:
        await maybe_finalize_season(context, chat_id)
    except Exception:
        pass
    # Норм-цикл: снимок кубков (Пн) и проверка нормы (Вс) — фоном
    try:
        await maybe_norm_cycle(context, chat_id)
    except Exception:
        pass
    # Ежедневный отчёт владельцу за прошедший день — фоном
    try:
        await maybe_daily_report(context, chat_id)
    except Exception:
        pass
    # Поздравить первого, кто написал сегодня утром — фоном
    try:
        await maybe_first_writer(update, context, chat_id, user)
    except Exception:
        pass
    # Тег-гейт: новичок без привязанного тега получает предупреждения (3 дня → исключение)
    try:
        await check_tag_gate(update, context, user)
    except Exception:
        pass


async def text_router(update, context):
    msg = update.message
    if not msg or not msg.text:
        return
    raw = msg.text.strip()
    # 5.4: реакции-на-ответ — БЕЗ точки. Ответь «+» / «респект» (или «-» / «дизреспект») — изменится репутация.
    if msg.reply_to_message and update.effective_chat and update.effective_chat.type in ("group", "supergroup"):
        lr = raw.lower()
        if lr in REACTION_PLUS or lr in REACTION_MINUS:
            t = msg.reply_to_message.from_user
            if t and not t.is_bot:
                await give_rep(update, context, t, 1 if lr in REACTION_PLUS else -1)
                return
    # В чате команды срабатывают ТОЛЬКО с префиксом-точкой (.help, .profile, .Linkor ...).
    # Сообщения без точки — обычная речь: командой не считаются (но в статистике/LP учитываются отдельно).
    if CMD_PREFIX and raw.startswith(CMD_PREFIX):
        text = raw[len(CMD_PREFIX):].strip()
        low = text.lower()
        parts = low.split()
        if parts and await handle_triggers(update, context, text, low, parts):
            return
    await auto_moderate(update, context)


async def record_member(update, context):
    """Реестр участников по служебным событиям. Активные — фиксируются (для сбора/статистики),
    вышедшие/исключённые — помечаются (5.4): исчезают из живых топов, история сохраняется."""
    try:
        cm = update.chat_member or update.my_chat_member
        if not cm or not cm.chat or cm.chat.id != ALLOWED_CHAT_ID:
            return
        member = cm.new_chat_member.user if cm.new_chat_member else None
        if not member or member.is_bot:
            return
        status = cm.new_chat_member.status
        if status in ACTIVE_MEMBER_STATUSES:
            ensure_user(ALLOWED_CHAT_ID, member)
        elif AUTO_PURGE_ON_LEAVE and member.id not in (
                {LEADER_ID} | EXCLUDED_IDS | CONTEST_CREATORS | STORE_ADMINS):
            # вышел / исключён / забанен — помечаем: из живых топов исчезает, история и варны остаются
            mark_left(ALLOWED_CHAT_ID, member.id)
            print(f"[AUTO-CLEAN] участник {member.id} вышел из чата — помечен как ушедший.", flush=True)
    except Exception as e:
        log_error("реестр участников", e)


async def welcome(update, context):
    for member in update.message.new_chat_members:
        if member.is_bot:
            continue
        ensure_user(update.effective_chat.id, member)
        # новичок попадает под тег-гейт (обязан привязать тег); старичков это не трогает
        conn.execute("UPDATE users SET tag_gate=1 WHERE chat_id=? AND user_id=?",
                     (update.effective_chat.id, member.id))
        conn.commit()
        # анти-рейд: при наплыве входов новичок ограничивается и обычное приветствие пропускаем
        if await check_raid(context, update.effective_chat.id, member):
            continue
        kb = InlineKeyboardMarkup([[InlineKeyboardButton("✅ Открыть бота и принять соглашение", url=faq_deeplink(context))]])
        await update.message.reply_text(
            f"{pe('catlove')} <b>Добро пожаловать в клан {CLAN_NAME}!</b>\n{CLAN_LINE}\n"
            f"Рады видеть тебя, {mention(member)} {pe('hug')}\n\n"
            f"{pe('excl')} <b>Два обязательных шага:</b>\n"
            f"<blockquote>1. Прими соглашение — кнопка ниже (открой бота в ЛС).\n"
            f"2. Привяжи игровой тег: <code>.tag #ТВОЙ_ТЕГ</code> (на это есть 3 дня, иначе авто-исключение).</blockquote>\n"
            f"<blockquote>{pe('scroll')} <b>rules</b> — кодекс · {pe('excl')} <b>norms</b> — нормы кубков\n"
            f"{pe('person')} <b>profile</b> · {pe('shop')} <b>shop</b> · {pe('robot')} <b>help</b></blockquote>\n"
            f"Общайся в чате — за сообщения капают {pe('lp')} LP. С 22 июня действует норма кубков (+1450/нед). "
            f"Удачи в боях! {pe('fire')}",
            parse_mode=ParseMode.HTML, reply_markup=kb)


# ============================================================
#               СЛЕШ-КОМАНДЫ
# ============================================================
async def start_cmd(update, context):
    user = update.effective_user
    if user:
        ensure_user(ALLOWED_CHAT_ID, user)
    # Кто ещё НЕ принял — показываем соглашение с кнопками (заход по ссылке start=agree тоже сюда).
    if user and not has_agreed(user.id):
        return await send_faq_agreement(update, context)
    # Кто уже принял (или владелец/админ) — обычное приветствие, кнопки повторно НЕ показываем.
    await update.message.reply_text(
        f"{pe('crown')} <b>Официальный бот клана {CLAN_NAME}</b>\n"
        f"Модерация · статистика · LP · конкурсы · игры · AI\n\n"
        f"{pe('check')} Соглашение уже принято — бот тебе полностью доступен.\n"
        f"В личке: <code>.profile</code> · <code>.appeal</code> · <code>.faq</code> · <code>.help</code>", parse_mode=ParseMode.HTML)


async def id_cmd(update, context):
    tid = getattr(update.message, "message_thread_id", None)
    txt = f"ID чата: <code>{update.effective_chat.id}</code>"
    if tid:
        txt += f"\nID темы (топика): <code>{tid}</code>"
    await update.message.reply_text(txt, parse_mode=ParseMode.HTML)


# 5.4: старые текстовые справки удалены — действует кнопочное меню help_menu_keyboard

def help_menu_keyboard(owner_id, staff=False):
    """Кнопочное меню справки. owner_id — кто открыл (жать может только он).
    staff=True добавляет кнопку «Админ-команды» (обычным участникам она не видна)."""
    u = owner_id
    rows = [
        [InlineKeyboardButton("👤 Профиль и статистика", callback_data=f"help:profile:{u}")],
        [InlineKeyboardButton("🛒 Клан", callback_data=f"help:clan:{u}"),
         InlineKeyboardButton("🎮 Игры", callback_data=f"help:games:{u}")],
        [InlineKeyboardButton("🤖 AI", callback_data=f"help:ai:{u}"),
         InlineKeyboardButton("⚖️ Обжалование", callback_data=f"help:appeal:{u}")],
    ]
    if staff:
        rows.append([InlineKeyboardButton("🛡 Админ-команды", callback_data=f"help:admin:{u}")])
    return InlineKeyboardMarkup(rows)


def _help_intro():
    return (f"{pe('person')} <b>СПРАВКА — БОТ КЛАНА {CLAN_NAME}</b>\n{CLAN_LINE}\n"
            f"<i>Все команды пишутся с точкой в начале: <code>.help</code>, <code>.profile</code> …</i>\n"
            f"<i>Выбери раздел кнопкой — команды откроются прямо здесь.</i>\n"
            f"{pe('lp')} LP капают за каждое сообщение · статистика по МСК")


def _help_section(key):
    """Текст одного раздела справки (в сворачиваемой цитате). Команды — с точкой-префиксом."""
    if key == "profile":
        return (f"{pe('person')} <b>Профиль и статистика</b>\n<blockquote expandable>"
                "• <code>.profile</code> — твой профиль (или <code>.profile @юзер</code> / ответом — чужой)\n"
                "• <code>.lp</code> — твой баланс LP\n"
                "• <code>.+</code> (ответом) — дать репутацию · <code>.-</code> — снять\n"
                "• <code>.tag #ТВОЙТЕГ</code> — привязать профиль Brawl Stars\n"
                "• <code>.top active</code> · <code>.top lp</code> · <code>.top rep</code> · <code>.top trophies</code> (+ <code>day</code>/<code>week</code>)\n"
                "• <code>.streak</code> — серия дней подряд\n"
                "• <code>.norms</code> — твой прогресс по нормам недели (сообщения, кубки, сроки)\n"
                "• <code>.rank</code> — твой ранг · <code>.top rank</code> — топ клана по рангам\n"
                "• <code>.lottery</code> — купить билет и выиграть банк · <code>.duel</code> (ответом) — сразиться на LP\n"
                "• <code>.title buy ТЕКСТ</code> — личный титул в профиле · <code>.title list</code> — витрина титулов\n"
                "• <code>.quest</code> — задание дня (титулы!) · <code>.achievements</code> — твои значки · <code>.quiz</code> — вопрос с банком LP\n"
                "• <code>.event</code> — активные ивенты клана (x2/x3 LP!)\n"
                "• <code>.card</code> — твоя карточка профиля картинкой 🖼️\n"
                "• <code>.season</code> — топ сезона · <code>.hof</code> — зал славы</blockquote>")
    if key == "clan":
        return (f"{pe('shop')} <b>Клан</b>\n<blockquote expandable>"
                "• <code>.shop</code> — магазин и как получать LP\n"
                "• <code>.norms</code> — обязательные нормы клана\n"
                "• <code>.rules</code> — кодекс клана\n"
                "• <code>.docs</code> — о боте и история обновлений\n"
                "• <code>.faq</code> — соглашение и FAQ (в личке боту)</blockquote>")
    if key == "games":
        return (f"{pe('game')} <b>Игры</b>\n<blockquote expandable>"
                "<code>.dice</code> · <code>.darts</code> · <code>.basket</code> · <code>.football</code> · <code>.bowling</code> · "
                "<code>.casino</code> · <code>.coin</code> · <code>.8ball &lt;вопрос&gt;</code> · <code>.rps камень</code> · "
                "<code>.pick a, b, c</code> · <code>.number 7</code> · <code>.duel</code> · <code>.roll</code></blockquote>")
    if key == "ai":
        return (f"{pe('robot')} <b>AI-помощник</b>\n<blockquote expandable>"
                "• <code>.Linkor &lt;вопрос&gt;</code> — спросить AI про Brawl Stars или что угодно\n"
                "• <code>.joke</code> — шутка · <code>.fact</code> — факт · <code>.toast</code> — тост\n"
                "<i>Без точки слово «linkor» в обычной речи AI не вызывает.</i></blockquote>")
    if key == "appeal":
        return (f"{pe('scales')} <b>Обжалование наказания</b>\n<blockquote expandable>"
                "Получил наказание и считаешь его несправедливым? Напиши боту "
                "<b>в личку</b> команду <code>.appeal</code> — заявка с твоим объяснением уйдёт администрации, "
                "и они решат вопрос.</blockquote>")
    if key == "admin":
        return (f"{pe('shield')} <b>Админ-команды</b> <i>(работают только у администрации)</i>\n<blockquote expandable>"
                "<b>Модерация</b> (ответом/<code>@юзер</code>): <code>.warn</code> · <code>.unwarn</code> · "
                "<code>.clearwarns</code> · <code>.mute 12ч причина</code> · <code>.unmute</code> · <code>.kick</code> · "
                "<code>.ban</code> · <code>.unban</code>\n"
                "<i>длительность: <code>30м</code> · <code>12ч</code> · <code>7д</code></i>\n\n"
                "<b>Управление:</b> <code>.role модер</code> · <code>.lp 200</code>/<code>.lp -50</code>/<code>.lp =500</code> · "
                "<code>.who</code> · <code>.summary</code> · <code>.announce текст</code> · <code>.summon текст</code> · "
                "<code>.конкурс</code> (в личке)\n\n"
                "<b>Чистка топов:</b> <code>.чистка</code> — авто-удалить всех, кто вышел из чата · "
                "<code>.удалить</code> (ответом/@/ID) — убрать одного из системы · "
                "<code>.отвязать</code> (ответом/@/ID) — снять чужой игровой тег\n\n"
                "<b>Владелец</b> (в чате и в личке): <code>.modlog</code> · <code>.analytics</code> · <code>.backup</code> · "
                "<code>.bonusall 150</code> · <code>.checknorms</code> · <code>.snapshotnorms</code> · "
                "<code>.отчет</code> (официальный Excel-отчёт по чату за день: сводка, активность, графики, журнал — в личку)\n"
                "<code>.ошибки</code> (журнал ошибок бота: что и когда сбоило)\n"
                "<code>.нормы тест</code> / <code>.нормы снимок</code> — сухой прогон проверки норм и ручной снимок недели (обкатка до боевого старта)\n"
                "<code>.rank test [#ТЕГ]</code> — сырые значения рангов из журнала боёв (сверка карты рангов)\n"
                "<code>.lottery draw</code> — провести розыгрыш лотереи · <code>.title give @кто ТЕКСТ</code> — выдать титул\n"
                "<code>.задать реп/лп/кубки/ранг @кто N</code> — задать точное значение · <code>.пранк @кто поле значение</code> — фейк-профиль (только владелец)\n"
                "<code>.утро</code> / <code>.ночь</code> — показать приветствие сейчас (для проверки; обычно бот шлёт сам в 8:00 и 22:00 МСК)\n"
                "<code>.добавитьгиф утро/ночь</code> (ответом на гифку) · <code>.гифы</code> · <code>.очиститьгифы утро/ночь</code> — копилка гифок для приветствий\n\n"
                "<b>Опрос:</b> <code>.poll Вопрос? | вар1 | вар2</code>\n"
                "<b>Нормы</b> (с 22 июня): авто по Пн/Вс · <code>.отпуск @юзер 1–30</code>\n"
                "<b>Награды</b> (ответом): <code>.норма ранг</code> · <code>.норма кубки</code></blockquote>")
    return _help_intro()


async def help_cmd(update, context, include_admin=False):
    staff = include_admin or await can_use_admin(context, update.effective_user.id)
    kb = help_menu_keyboard(update.effective_user.id, staff)
    try:
        await update.message.reply_text(_help_intro(), parse_mode=ParseMode.HTML, reply_markup=kb)
    except Exception as e:
        print(f"[HELP] ошибка отправки справки: {e}", flush=True)
        try:
            await update.message.reply_text(f"Справка {CLAN_NAME}. Выбери раздел кнопкой.", reply_markup=kb)
        except Exception:
            pass


async def rules_cmd(update, context):
    await update.message.reply_text(
        f"━━━━━━━━━━━━━━━━\n"
        f"{pe('scroll')} <b>КОДЕКС КЛАНА {CLAN_NAME}</b>\n"
        f"━━━━━━━━━━━━━━━━\n\n"
        "<b>I. Культура общения</b>\nБез оскорблений, троллинга, буллинга, угроз, расизма, "
        "сексизма, спама и злоупотребления КАПСом.\n\n"
        "<b>II. Дисциплина</b>\nУчастие в событиях, выполнение норм активности, без саботажа.\n\n"
        "<b>III. Иерархия</b>\nЛидер → Администрация → Модераторы → Менеджеры → Участники.\n\n"
        "<b>IV. Наказания</b>\nВыговор → мут → варн → исключение. 3 варна = исключение.\n\n"
        "<i>Полный кодекс — у администрации клана.</i>",
        parse_mode=ParseMode.HTML)


async def profile_cmd(update, context):
    await show_profile(update, context, target_user(update) or update.effective_user)


async def roll_cmd(update, context):
    await update.message.reply_text(f"🎲 Твоё число: {random.randint(1,100)}")


async def coin_cmd(update, context):
    await update.message.reply_text(f"{pe('lp')} " + random.choice(["Орёл!", "Решка!"]), parse_mode=ParseMode.HTML)


async def dice_cmd(update, context):
    await update.message.reply_dice()


async def guess_cmd(update, context):
    secret = random.randint(1, 10)
    if not context.args:
        return await update.message.reply_text("Напиши /guess <число от 1 до 10>")
    try:
        g = int(context.args[0])
    except ValueError:
        return await update.message.reply_text("Это не число 🙂")
    await update.message.reply_text(f"🎉 Угадал! Было {secret}." if g == secret else f"Мимо! Было {secret}.")


# ============================================================
#                       ЗАПУСК
# ============================================================
async def error_handler(update, context):
    """Ловит ошибки в обработчиках, чтобы бот не падал от сетевых сбоев."""
    err = context.error
    log_error("обработчик", err)   # 5.4: фиксируем в журнале — см. .ошибки
    try:
        from telegram.error import NetworkError, TimedOut, RetryAfter, Conflict
    except Exception:
        NetworkError = TimedOut = RetryAfter = Conflict = ()
    if Conflict and isinstance(err, Conflict):
        print("=" * 60, flush=True)
        print("[КОНФЛИКТ] Telegram сообщает: бота опрашивает ДРУГОЙ экземпляр!", flush=True)
        print("Значит ОДНОВРЕМЕННО запущено 2+ копии бота (часто старый контейнер на хостинге).", flush=True)
        print("Из-за этого баланс/топы прыгают. Оставь РОВНО ОДНУ копию — удали лишние приложения.", flush=True)
        print("=" * 60, flush=True)
    elif isinstance(err, (NetworkError, TimedOut)):
        print(f"[СЕТЬ] Временная проблема связи с Telegram ({err}). "
              f"Проверь интернет/DNS на машине с ботом. Бот продолжает работу.", flush=True)
    elif isinstance(err, RetryAfter):
        print(f"[ЛИМИТ] Telegram просит подождать {getattr(err, 'retry_after', '?')} сек. Бот продолжает работу.", flush=True)
    else:
        print(f"[ОШИБКА] {type(err).__name__}: {err}", flush=True)


async def show_modlog(update, context, parts):
    """Журнал последних действий модерации (для владельца; работает везде)."""
    chat_id = ALLOWED_CHAT_ID
    rows = conn.execute(
        "SELECT admin_name, target_name, action, reason, ts FROM modlog WHERE chat_id=? ORDER BY id DESC LIMIT 15",
        (chat_id,)).fetchall()
    if not rows:
        return await update.message.reply_text(
            f"{pe('folder')} <b>ЖУРНАЛ МОДЕРАЦИИ {CLAN_NAME}</b>\n{CLAN_LINE}\n<i>Пока пусто — наказаний не было.</i>",
            parse_mode=ParseMode.HTML)
    act_emoji = {"бан": "🚫", "ban": "🚫", "мут": "🔇", "mute": "🔇", "варн": "⚠️", "warn": "⚠️",
                 "кик": "👢", "kick": "👢", "разбан": "✅", "размут": "🔊", "снят": "✅"}
    lines = [f"{pe('folder')} <b>ЖУРНАЛ МОДЕРАЦИИ {CLAN_NAME}</b>\n{CLAN_LINE}\n<i>Последние {len(rows)} действий:</i>\n"]
    for r in rows:
        d = (r["ts"] or "")[:16].replace("T", " ")
        ic = next((v for k, v in act_emoji.items() if k in (r["action"] or "").lower()), "•")
        rsn = f"\n   <i>причина: {esc(r['reason'])}</i>" if r["reason"] else ""
        lines.append(f"{ic} <b>{esc(r['target_name'])}</b> — {esc(r['action'])}\n"
                     f"   <code>{d}</code> · модератор: {esc(r['admin_name'])}{rsn}")
    await update.message.reply_text("\n".join(lines), parse_mode=ParseMode.HTML)


async def make_poll(update, context, text):
    """Создаёт нативный опрос Telegram. Формат: poll Вопрос? | вариант1 | вариант2 [| ...]"""
    raw = text.split(maxsplit=1)
    if len(raw) < 2:
        return await update.message.reply_text(
            "Формат: <code>poll Вопрос? | вариант 1 | вариант 2</code>\nБез вариантов будет «Да / Нет».",
            parse_mode=ParseMode.HTML)
    chunks = [p.strip() for p in raw[1].split("|") if p.strip()]
    question = chunks[0]
    options = chunks[1:] if len(chunks) >= 3 else ["Да", "Нет"]
    options = options[:10]
    try:
        await context.bot.send_poll(update.effective_chat.id, question[:300], options, is_anonymous=False)
    except Exception:
        await update.message.reply_text("Не получилось создать опрос. Проверь формат и права бота.")


async def clan_analytics(update, context):
    """Сводная аналитика по клану (для владельца, в личке)."""
    chat_id = ALLOWED_CHAT_ID
    today = today_str()
    mon, sun = week_bounds()
    def one(q, a=()):
        return conn.execute(q, a).fetchone()[0]
    members = one("SELECT COUNT(*) FROM users WHERE chat_id=?", (chat_id,))
    act_today = one("SELECT COUNT(DISTINCT user_id) FROM stats WHERE chat_id=? AND day=?", (chat_id, today))
    act_week = one("SELECT COUNT(DISTINCT user_id) FROM stats WHERE chat_id=? AND day BETWEEN ? AND ?", (chat_id, mon, sun))
    m_today = one("SELECT COALESCE(SUM(cnt),0) FROM stats WHERE chat_id=? AND day=?", (chat_id, today))
    m_week = one("SELECT COALESCE(SUM(cnt),0) FROM stats WHERE chat_id=? AND day BETWEEN ? AND ?", (chat_id, mon, sun))
    m_total = one("SELECT COALESCE(SUM(cnt),0) FROM stats WHERE chat_id=?", (chat_id,))
    tagged = one("SELECT COUNT(*) FROM users WHERE chat_id=? AND brawl_tag IS NOT NULL", (chat_id,))
    total_lp = one("SELECT COALESCE(SUM(lp),0) FROM users WHERE chat_id=?", (chat_id,))
    top = [r for r in season_top(chat_id, current_season(), 3) if r["val"] > 0]
    medals = [pe("gold"), pe("silver"), pe("bronze")]
    top_lines = "\n".join(f"{medals[i]} {esc(r['name'])} — {r['val']}" for i, r in enumerate(top)) or "—"
    await update.message.reply_text(
        f"{pe('stats')} <b>АНАЛИТИКА {CLAN_NAME}</b>\n{CLAN_LINE}\n"
        f"<blockquote>{pe('person')} Участников в базе: <b>{members}</b>\n"
        f"{pe('game')} С привязанным тегом: <b>{tagged}</b>\n"
        f"{pe('fire')} Активны сегодня: <b>{act_today}</b> · за неделю: <b>{act_week}</b></blockquote>\n"
        f"<blockquote>{pe('msg')} Сообщений сегодня: <b>{fmt_num(m_today)}</b>\n"
        f"{pe('calendar')} За неделю: <b>{fmt_num(m_week)}</b>\n"
        f"{pe('stats')} Всего: <b>{fmt_num(m_total)}</b></blockquote>\n"
        f"<blockquote>{pe('lp')} LP в обороте: <b>{fmt_num(total_lp)}</b></blockquote>\n"
        f"{pe('growth')} <b>Топ активных за месяц:</b>\n{top_lines}",
        parse_mode=ParseMode.HTML)


async def _do_backup(context, note=""):
    """6.0: ядро бэкапа — отправляет файл базы владельцу в ЛС. Используется командой и авто-джобой."""
    try:
        conn.execute("PRAGMA wal_checkpoint(FULL)")
        conn.commit()
    except Exception:
        pass
    with open(DB_FILE, "rb") as f:
        await context.bot.send_document(
            LEADER_ID, document=f, filename=f"linkor_backup_{today_str()}.db",
            caption=f"{pe('folder')} <b>Бэкап базы {CLAN_NAME}</b>\nДата: {today_str()}{note}",
            parse_mode=ParseMode.HTML)


async def job_auto_backup(context):
    """6.0: авто-бэкап раз в неделю (понедельник 05:00). Тихо шлёт базу владельцу в ЛС."""
    if msk_now().weekday() != 0:   # 0 = понедельник
        return
    wk = week_bounds()[0]
    if meta_get("autobackup_week") == wk:
        return
    meta_set("autobackup_week", wk)
    try:
        await _do_backup(context, note="\n<i>Авто-бэкап (еженедельный)</i>")
        print("[БЭКАП] авто-бэкап отправлен", flush=True)
    except Exception as e:
        log_error("авто-бэкап", e)


def build_weekly_report(chat_id):
    """6.0: компактный недельный отчёт владельцу — актив, прирост, экономика."""
    mon, sun = week_bounds()
    rows = conn.execute(
        "SELECT user_id, name, username FROM users WHERE chat_id=? AND COALESCE(gone,0)=0", (chat_id,)).fetchall()
    # топ активности за неделю
    act = []
    total_msgs = 0
    for r in rows:
        if r["user_id"] in EXCLUDED_IDS or r["user_id"] == LEADER_ID:
            continue
        w = stat_week(chat_id, r["user_id"])
        total_msgs += w
        if w > 0:
            act.append((disp_name(r), w))
    act.sort(key=lambda x: -x[1])
    active_cnt = len(act)
    # экономика за неделю (приход/расход LP по дневным потокам)
    earn = spend = 0
    d = mon
    while d <= sun and d <= today_str():
        try:
            e, s = lp_day_flows(chat_id, d)
            earn += e
            spend += s
        except Exception:
            pass
        d = (datetime.fromisoformat(d) + timedelta(days=1)).strftime("%Y-%m-%d") if False else \
            (datetime.strptime(d, "%Y-%m-%d") + timedelta(days=1)).strftime("%Y-%m-%d")
    lines = [f"{pe('stats')} <b>НЕДЕЛЬНЫЙ ОТЧЁТ {CLAN_NAME}</b>",
             f"{CLAN_LINE}",
             f"Период: {_fmt_day_human(mon)} — {_fmt_day_human(sun)}\n",
             f"{pe('person')} Активных участников: <b>{active_cnt}</b>",
             f"{pe('msg')} Сообщений за неделю: <b>{total_msgs}</b>"]
    if act:
        lines.append(f"\n{pe('cup')} <b>Топ активности:</b>")
        for i, (nm, w) in enumerate(act[:10], 1):
            medal = {1: "🥇", 2: "🥈", 3: "🥉"}.get(i, f"{i}.")
            lines.append(f"{medal} {esc(nm)} — <b>{w}</b>")
    lines.append(f"\n{pe('lp')} <b>Экономика недели:</b> приход +{int(earn)} LP · расход −{int(spend)} LP")
    name, mult = event_active()
    if name:
        lines.append(f"{pe('mega')} Активен ивент: {name} (x{mult:g})")
    return "\n".join(lines)


async def job_weekly_report(context):
    """6.0: недельный отчёт владельцу — воскресенье 22:30."""
    if not ALLOWED_CHAT_ID:
        return
    if msk_now().weekday() != 6:   # 6 = воскресенье
        return
    wk = week_bounds()[0]
    if meta_get("weekly_report_week") == wk:
        return
    meta_set("weekly_report_week", wk)
    try:
        await context.bot.send_message(LEADER_ID, build_weekly_report(ALLOWED_CHAT_ID), parse_mode=ParseMode.HTML)
        print("[ОТЧЁТ] недельный отчёт отправлен", flush=True)
    except Exception as e:
        log_error("недельный отчёт", e)


def build_monthly_report(chat_id, month_key):
    """6.0: большой месячный отчёт владельцу — итоги месяца: актив, рост, экономика, нормы, чемпионы."""
    rows = conn.execute(
        "SELECT user_id, name, username, lp FROM users WHERE chat_id=? AND COALESCE(gone,0)=0", (chat_id,)).fetchall()
    act = []
    total_msgs = 0
    for r in rows:
        if r["user_id"] in EXCLUDED_IDS or r["user_id"] == LEADER_ID:
            continue
        m = conn.execute("SELECT COALESCE(SUM(cnt),0) AS c FROM stats WHERE chat_id=? AND user_id=? AND day LIKE ?",
                         (chat_id, r["user_id"], month_key + "%")).fetchone()["c"]
        total_msgs += m
        if m > 0:
            act.append((disp_name(r), m))
    act.sort(key=lambda x: -x[1])
    active_cnt = len(act)
    # новые участники за месяц (по дате вступления joined)
    try:
        newcomers = conn.execute(
            "SELECT COUNT(*) AS c FROM users WHERE chat_id=? AND COALESCE(gone,0)=0 AND joined LIKE ?",
            (chat_id, month_key + "%")).fetchone()["c"]
    except Exception:
        newcomers = None
    # экономика за месяц
    earn = spend = 0
    d = month_key + "-01"
    last_day = today_str()
    while d <= last_day and d.startswith(month_key):
        try:
            e, s = lp_day_flows(chat_id, d)
            earn += e
            spend += s
        except Exception:
            pass
        d = (datetime.strptime(d, "%Y-%m-%d") + timedelta(days=1)).strftime("%Y-%m-%d")
    # нормы: сколько недель закрыто по кубкам за месяц (из norm_weeks)
    try:
        cups_weeks = conn.execute(
            "SELECT COALESCE(SUM(cups_ok),0) AS c FROM norm_weeks WHERE chat_id=? AND week LIKE ?",
            (chat_id, month_key + "%")).fetchone()["c"]
    except Exception:
        cups_weeks = 0
    # чемпионы прошлого сезона (из зала славы)
    champs = conn.execute(
        "SELECT rank, name, score FROM hall_of_fame WHERE chat_id=? AND season=? ORDER BY rank ASC LIMIT 3",
        (chat_id, month_key)).fetchall()

    lines = [f"{pe('stats')} <b>МЕСЯЧНЫЙ ОТЧЁТ {CLAN_NAME} — {season_title(month_key)}</b>",
             f"{CLAN_LINE}",
             f"{pe('person')} Активных участников за месяц: <b>{active_cnt}</b>",
             f"{pe('msg')} Всего сообщений: <b>{total_msgs}</b>"]
    if newcomers is not None:
        lines.append(f"{pe('star')} Новичков пришло: <b>{newcomers}</b>")
    lines.append(f"{pe('cup')} Норм по кубкам закрыто (недель суммарно): <b>{cups_weeks}</b>")
    lines.append(f"\n{pe('lp')} <b>Экономика месяца:</b> приход +{int(earn)} LP · расход −{int(spend)} LP")
    if act:
        lines.append(f"\n{pe('crown')} <b>Топ активности месяца:</b>")
        for i, (nm, m) in enumerate(act[:10], 1):
            medal = {1: "🥇", 2: "🥈", 3: "🥉"}.get(i, f"{i}.")
            lines.append(f"{medal} {esc(nm)} — <b>{m}</b>")
    if champs:
        lines.append(f"\n{pe('cup')} <b>Чемпионы сезона (зал славы):</b>")
        for c in champs:
            md = {1: "🥇", 2: "🥈", 3: "🥉"}.get(c["rank"], "•")
            lines.append(f"{md} {esc(c['name'])} — {c['score']} сообщ.")
    return "\n".join(lines)


async def job_monthly_report(context):
    """6.0: большой месячный отчёт владельцу — 1-го числа за прошлый месяц."""
    if not ALLOWED_CHAT_ID:
        return
    now = msk_now()
    if now.day != 1:
        return
    prev = (now.replace(day=1) - timedelta(days=1)).strftime("%Y-%m")
    if meta_get(f"monthly_report_{prev}"):
        return
    meta_set(f"monthly_report_{prev}", "1")
    try:
        await context.bot.send_message(LEADER_ID, build_monthly_report(ALLOWED_CHAT_ID, prev), parse_mode=ParseMode.HTML)
        print("[ОТЧЁТ] месячный отчёт отправлен", flush=True)
    except Exception as e:
        log_error("месячный отчёт", e)


async def send_backup(update, context):
    """Отправляет файл базы владельцу в ЛС (резервная копия перед деплоем)."""
    if update.effective_user.id != LEADER_ID:
        return await update.message.reply_text("Бэкап базы может делать только владелец клана.")
    try:
        await _do_backup(context)
        await update.message.reply_text(
            f"{pe('check')} <b>Бэкап готов.</b> Файл базы отправлен тебе в личку.", parse_mode=ParseMode.HTML)
    except Exception as e:
        await update.message.reply_text(f"{pe('cross')} Не удалось отправить бэкап: {esc(str(e))}", parse_mode=ParseMode.HTML)


async def on_unknown_callback(update, context):
    """Подстраховка: отвечает на любой колбэк, не пойманный другими обработчиками (чтобы кнопка не зависала)."""
    cq = update.callback_query
    try:
        print(f"[CB] НЕ ПОЙМАН колбэк: {cq.data!r} от {cq.from_user.id}", flush=True)
        await cq.answer()
    except Exception:
        pass


# ─────────────────────── Авто-приветствия (утро / ночь) ───────────────────────
# Что можно настроить: время по Москве и ссылки на гифки (оставь пустыми — будет только текст).
GREETINGS_ENABLED = True
MORNING_HOUR = 8        # час утреннего приветствия (МСК)
NIGHT_HOUR = 22         # час ночного режима (МСК)
FIRST_WRITER_UNTIL = 12  # 5.5: «первый писавший» отмечается ТОЛЬКО в окне утра (8:00–12:00 МСК)
MORNING_GIF = ""        # ссылка на гифку для утра (например, с котятами). Пусто — без гифки.
NIGHT_GIF = ""          # ссылка на гифку для ночи. Пусто — без гифки.
AUTO_CAT_GIFS = True    # запасной источник утренних котиков (The Cat API), если ключ Giphy не задан.
CAT_API_KEY = os.environ.get("CAT_API_KEY", "")       # необязательный ключ thecatapi.com.
GIPHY_API_KEY = os.environ.get("GIPHY_API_KEY", "")   # ключ Giphy — ЗАДАЁТСЯ В ПЕРЕМЕННЫХ ОКРУЖЕНИЯ на BotHost (как токен).
NIGHT_AUTO_GIFS = True  # ночью бот сам берёт спокойную тематическую гифку (через Giphy).
MORNING_GIF_TAG = "funny cat"  # тема утренней гифки в Giphy (весёлые/играющие котики).
NIGHT_GIF_TAG = "sleepy cat"   # тема ночной гифки в Giphy (например: sleepy cat, good night, calm night).

# Наборы фраз: бот берёт по дню (каждый день другая, со временем повторяются — без шаблонности).
_MORNING_LINES = [
    "Новый день — новые кубки. Погнали 💪",
    "Заряжаемся на победы и хорошее настроение ⚡",
    "Пусть сегодня залетают только лёгкие катки 🎮",
    "Отличный день, чтобы пообщаться и накидать трофеев 🏆",
    "Врываемся в новый день, бойцы! 🚀",
    "Пусть день будет добрым, а катки — победными 🔥",
    "Сделаем этот день огненным 😎",
]
_NIGHT_LINES = [
    "Час тишины и отдыха — самое время сбавить обороты 😴",
    "Чат потихоньку засыпает. Сладких снов 🌌",
    "Отдыхаем и набираемся сил перед новыми катками 💤",
    "Спокойствие и уют — добрых снов всему клану 🌙",
    "Тихий час клана: отдыхаем до утра ✨",
]
_MORNING_WEEKDAY = {
    0: ["Понедельник — врываемся в новую неделю! 🚀", "Новая неделя — новые победы, погнали 💪"],
    1: ["Вторник в деле — набираем обороты ⚡", "Вторник: погнали фармить кубки 🏆"],
    2: ["Серединка недели — держим темп 🔥", "Среда — самое время размяться в катках 🎮"],
    3: ["Четверг — финишная прямая близко 💪", "Четверг: ещё чуть-чуть до выходных ⚡"],
    4: ["Пятница! Выходные на горизонте 🎉", "Пятница — врубаем хорошее настроение 😎"],
    5: ["Суббота — отдыхаем и катаем в удовольствие 🎮", "Выходной режим: больше катки, больше веселья 🔥"],
    6: ["Воскресенье — заряжаемся перед новой неделей ☀️", "Спокойное воскресенье — катаем в кайф 😌"],
}
_RU_WEEKDAYS = ["понедельник", "вторник", "среда", "четверг", "пятница", "суббота", "воскресенье"]
_RU_MONTHS = ["", "января", "февраля", "марта", "апреля", "мая", "июня",
              "июля", "августа", "сентября", "октября", "ноября", "декабря"]

# Что уже сделано сегодня (чтобы не повторяться в течение суток).
# 5.4: состояние «первого писавшего» переехало в БД (meta) — переживает перезапуски, см. maybe_first_writer


def _ru_date(dt):
    return f"{_RU_WEEKDAYS[dt.weekday()]}, {dt.day} {_RU_MONTHS[dt.month]}"


def _daily_pick(pool, salt=0):
    if not pool:
        return ""
    return pool[(msk_now().date().toordinal() + salt) % len(pool)]


def _morning_digest(chat_id):
    """Короткий итог вчерашнего дня для утреннего приветствия (берётся из данных бота)."""
    y = (msk_now().date() - timedelta(days=1)).isoformat()
    bits = []
    row = conn.execute(
        "SELECT COALESCE(u.name,'?') AS name, s.cnt AS cnt FROM stats s "
        "JOIN users u ON u.chat_id=s.chat_id AND u.user_id=s.user_id "
        "WHERE s.chat_id=? AND s.day=? AND s.cnt>0 ORDER BY s.cnt DESC LIMIT 1",
        (chat_id, y)).fetchone()
    if row:
        bits.append(f"вчера в топе — <b>{esc(row['name'])}</b> ({row['cnt']} сообщ.)")
    lp = conn.execute(
        "SELECT COALESCE(u.name,'?') AS name, SUM(l.delta) AS d FROM lp_log l "
        "JOIN users u ON u.chat_id=l.chat_id AND u.user_id=l.user_id "
        "WHERE l.chat_id=? AND l.day=? GROUP BY l.user_id HAVING d>0 ORDER BY d DESC LIMIT 1",
        (chat_id, y)).fetchone()
    if lp and (not row or lp["name"] != row["name"]):
        bits.append(f"больше всех LP заработал <b>{esc(lp['name'])}</b>")
    return bits


def build_morning_message(chat_id):
    now = msk_now()
    line = _daily_pick(_MORNING_WEEKDAY.get(now.weekday(), _MORNING_LINES))
    text = (f"{pe('star')} <b>ДОБРОЕ УТРО, {CLAN_NAME}!</b>\n"
            f"<blockquote>Сегодня {_ru_date(now)}. {line}")
    digest = _morning_digest(chat_id)
    if digest:
        text += f"\n{pe('stats')} " + "; ".join(digest) + "."
    text += "</blockquote>"
    return text


def build_night_message():
    return (f"{pe('lock')} <b>{CLAN_NAME} уходит в ночной режим</b>\n"
            f"<blockquote>{_daily_pick(_NIGHT_LINES, salt=3)}\nДо утра, бойцы.</blockquote>")


def gif_add(kind, file_id):
    conn.execute("INSERT INTO greeting_gifs(kind, file_id, ts) VALUES(?,?,?)",
                 (kind, file_id, msk_now().isoformat()))
    conn.commit()


def gif_count(kind):
    return conn.execute("SELECT COUNT(*) AS c FROM greeting_gifs WHERE kind=?", (kind,)).fetchone()["c"]


def gif_clear(kind):
    conn.execute("DELETE FROM greeting_gifs WHERE kind=?", (kind,))
    conn.commit()


def gif_random(kind):
    """Случайная гифка из копилки данного типа (или '' если копилка пуста)."""
    row = conn.execute("SELECT file_id FROM greeting_gifs WHERE kind=? ORDER BY RANDOM() LIMIT 1",
                       (kind,)).fetchone()
    return row["file_id"] if row else ""


async def _fetch_cat_gif():
    """Берёт случайную гифку с котиком через бесплатный The Cat API (без ключа тоже работает)."""
    try:
        headers = {"x-api-key": CAT_API_KEY} if CAT_API_KEY else {}
        async with httpx.AsyncClient(timeout=12) as c:
            r = await c.get("https://api.thecatapi.com/v1/images/search?mime_types=gif&limit=1",
                            headers=headers)
        if r.status_code == 200:
            data = r.json()
            if isinstance(data, list) and data and data[0].get("url"):
                return data[0]["url"]
    except Exception as e:
        log_error("гифка Cat API", e)
    return ""


async def _fetch_giphy_gif(tag):
    """Случайная гифка по теме через Giphy (нужен ключ). Берём оригинал — это качественная версия."""
    if not GIPHY_API_KEY:
        return ""
    try:
        import urllib.parse
        q = urllib.parse.quote(tag or "cat")
        url = f"https://api.giphy.com/v1/gifs/random?api_key={GIPHY_API_KEY}&tag={q}&rating=g"
        async with httpx.AsyncClient(timeout=12) as c:
            r = await c.get(url)
        if r.status_code == 200:
            d = (r.json() or {}).get("data") or {}
            imgs = d.get("images") or {}
            # 5.4: сперва лёгкий mp4 (Telegram показывает его как гифку и он всегда «долетает»,
            # в отличие от тяжёлых оригиналов >20 МБ), потом уменьшенный gif, потом оригинал.
            u = ((imgs.get("original") or {}).get("mp4")
                 or (imgs.get("downsized_large") or {}).get("url")
                 or (imgs.get("original") or {}).get("url")
                 or d.get("image_original_url"))
            if u:
                return u
        else:
            print(f"[GREET] Giphy вернул {r.status_code}", flush=True)
    except Exception as e:
        log_error("гифка Giphy", e)
    return ""


async def _morning_gif():
    """Утренняя гифка. Приоритет: твоя копилка (лучшее качество) → Giphy → котосервис → ссылка."""
    g = gif_random("morning")
    if g:
        return g
    g = await _fetch_giphy_gif(MORNING_GIF_TAG)
    if g:
        return g
    if AUTO_CAT_GIFS:
        g = await _fetch_cat_gif()
        if g:
            return g
    return MORNING_GIF


async def _night_gif():
    """Ночная гифка. Приоритет: твоя копилка → Giphy (спокойная тема) → ссылка."""
    g = gif_random("night")
    if g:
        return g
    if NIGHT_AUTO_GIFS:
        g = await _fetch_giphy_gif(NIGHT_GIF_TAG)
        if g:
            return g
    return NIGHT_GIF


async def _send_greeting(context, chat_id, text, gif):
    """Шлёт приветствие: с гифкой (если задана ссылка), иначе просто текст.
    Если гифка не отправилась (битая ссылка) — пробуем хотя бы текст."""
    try:
        if gif:
            await context.bot.send_animation(chat_id, animation=gif, caption=text, parse_mode=ParseMode.HTML)
        else:
            await context.bot.send_message(chat_id, text=text, parse_mode=ParseMode.HTML)
        return True
    except Exception as e:
        log_error("приветствие", e)
        if gif:
            try:
                await context.bot.send_message(chat_id, text=text, parse_mode=ParseMode.HTML)
                return True
            except Exception as e2:
                log_error("приветствие (текст)", e2)
        return False


async def job_morning_greeting(context):
    if not GREETINGS_ENABLED:
        return
    await _send_greeting(context, ALLOWED_CHAT_ID, build_morning_message(ALLOWED_CHAT_ID), await _morning_gif())


async def job_night_greeting(context):
    if not GREETINGS_ENABLED:
        return
    await _send_greeting(context, ALLOWED_CHAT_ID, build_night_message(), await _night_gif())


async def maybe_first_writer(update, context, chat_id, user):
    """Поздравляет первого, кто написал сегодня после утреннего часа. Срабатывает раз в сутки."""
    if not GREETINGS_ENABLED or chat_id != ALLOWED_CHAT_ID or not user:
        return
    now = msk_now()
    if not (MORNING_HOUR <= now.hour < FIRST_WRITER_UNTIL):
        return   # 5.5: только утреннее окно — после деплоя днём или ночью отметка не срабатывает
    today = now.date().isoformat()
    if meta_get("first_writer_day") == today:
        return
    meta_set("first_writer_day", today)   # 5.4: в БД — переживает перезапуск бота, дублей не будет
    try:
        await update.message.reply_text(
            f"{pe('star')} Первым сегодня проснулся <b>{esc(user.full_name)}</b> — красава! "
            f"{pe('fire')} С тебя заряд активности на весь день 💪",
            parse_mode=ParseMode.HTML)
    except Exception:
        pass


async def on_startup(app):
    """Автопроверка: после запуска бот пишет владельцу в личку — какая версия и когда поднялась.
    Если сообщение пришло — значит на сервере уже работает свежий код (а не старая сборка)."""
    boot_id = app.bot_data.get("boot_id", "—")
    try:
        when = msk_now().strftime("%d.%m.%Y · %H:%M")
        await app.bot.send_message(
            chat_id=LEADER_ID,
            text=(f"{pe('check')} <b>LINKOR BOSS v{BOT_VERSION} «{PATCH_NAME}» запущен!</b>\n"
                  f"{pe('clock')} Старт: <b>{when}</b> (МСК) · сборка <code>{boot_id}</code>\n"
                  f"<blockquote expandable>Это сообщение приходит при КАЖДОМ запуске. Пришло — значит "
                  f"на сервере уже свежий код.\n"
                  f"Что нового в этой версии — команда <code>.docs</code>.</blockquote>"),
            parse_mode=ParseMode.HTML)
    except Exception as e:
        print(f"[STARTUP] не смог уведомить владельца ({LEADER_ID}): {e} — "
              f"возможно, владелец ещё не открывал личку с ботом.", flush=True)
    # Планировщик авто-приветствий (нужна библиотека job-queue).
    jq = getattr(app, "job_queue", None)
    if jq and GREETINGS_ENABLED:
        try:
            from datetime import time as _dtime
            jq.run_daily(job_morning_greeting, time=_dtime(MORNING_HOUR, 0, tzinfo=MSK), name="morning_greeting")
            jq.run_daily(job_night_greeting, time=_dtime(NIGHT_HOUR, 0, tzinfo=MSK), name="night_greeting")
            print(f"[GREET] приветствия запланированы: {MORNING_HOUR}:00 и {NIGHT_HOUR}:00 МСК", flush=True)
        except Exception as e:
            print(f"[GREET] не удалось запланировать приветствия: {e}", flush=True)
    elif GREETINGS_ENABLED:
        print("[GREET] планировщик недоступен — добавь 'python-telegram-bot[job-queue]' в requirements.txt, "
              "иначе авто-приветствия по времени не работают.", flush=True)
    if jq:
        try:
            from datetime import time as _dtime
            jq.run_daily(job_daily_report, time=_dtime(23, 59, tzinfo=MSK), name="daily_report")
            jq.run_daily(job_norm_reminder, time=_dtime(NORM_REMIND_HOUR, 0, tzinfo=MSK), name="norm_reminder")
            jq.run_daily(job_rank_scan, time=_dtime(RANK_SCAN_HOUR, 0, tzinfo=MSK), name="rank_scan")
            jq.run_daily(job_auto_backup, time=_dtime(5, 0, tzinfo=MSK), name="auto_backup")          # 6.0: пн авто-бэкап
            jq.run_daily(job_weekly_report, time=_dtime(22, 30, tzinfo=MSK), name="weekly_report")    # 6.0: вс недельный отчёт
            jq.run_daily(job_monthly_report, time=_dtime(9, 0, tzinfo=MSK), name="monthly_report")    # 6.0: 1-го месячный отчёт
            print("[ОТЧЁТ] отчёт — 23:59; нормы-напоминание — Сб 12:00; скан рангов — 13:00 (МСК)", flush=True)
        except Exception as e:
            log_error("планировщик отчёта", e)


def main():
    init_db()
    boot_id = random.randint(1000, 9999)
    app = Application.builder().token(TOKEN).post_init(on_startup).build()
    app.bot_data["boot_id"] = boot_id
    app.add_error_handler(error_handler)

    app.add_handler(MessageHandler(filters.ALL, guard), group=-1)
    app.add_handler(MessageHandler(filters.ALL & ~filters.StatusUpdate.ALL, counter), group=1)

    app.add_handler(CommandHandler("start", start_cmd))
    app.add_handler(CommandHandler("id", id_cmd))
    # Слэш-дубли команд убраны намеренно: все команды работают только с точкой (.help, .profile, .rules, .roll …).
    app.add_handler(MessageHandler(filters.StatusUpdate.NEW_CHAT_MEMBERS, welcome))
    app.add_handler(ChatMemberHandler(record_member, ChatMemberHandler.CHAT_MEMBER))
    app.add_handler(CallbackQueryHandler(giveaway_join, pattern="^gw_join$"))
    app.add_handler(CallbackQueryHandler(on_action_callback, pattern="^(menu|buy|order|appeal|faq|help|store):"))
    app.add_handler(CallbackQueryHandler(on_docs_callback, pattern="^docs:"))
    app.add_handler(CallbackQueryHandler(on_prankmenu_callback, pattern="^pm:"))
    app.add_handler(CallbackQueryHandler(on_dailytop_callback, pattern="^td:"))
    app.add_handler(CallbackQueryHandler(on_unknown_callback))   # подстраховка: любой не пойманный колбэк
    app.add_handler(MessageHandler(filters.ChatType.PRIVATE & ~filters.COMMAND, private_router))
    app.add_handler(MessageHandler(filters.ChatType.GROUPS & filters.TEXT & ~filters.COMMAND, text_router))

    print(f"Бот клана LINKOR запущен (boot-id {boot_id}). Ctrl+C для остановки.", flush=True)
    print(f"Если в логах два разных boot-id или ошибка [КОНФЛИКТ] — запущено несколько копий бота!", flush=True)
    app.run_polling(allowed_updates=Update.ALL_TYPES, drop_pending_updates=True)


if __name__ == "__main__":
    main()
